import RulerNode from"../../RulerNode.js";export default function demoRulerNode(layoutBounds){const rulerLength=500;const majorTickWidth=50;const majorTickLabels=[];const numberOfTicks=Math.floor(rulerLength/majorTickWidth)+1;for(let i=0;i<numberOfTicks;i++){majorTickLabels[i]=`${i*majorTickWidth}`}return new RulerNode(rulerLength,.15*rulerLength,majorTickWidth,majorTickLabels,"m",{insetsWidth:25,minorTicksPerMajorTick:4,center:layoutBounds.center})}