import Vector2 from"../../dot/js/Vector2.js";import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import sceneryPhet from"./sceneryPhet.js";export default class ArrowShape extends Shape{constructor(tailX,tailY,tipX,tipY,providedOptions){super();if(tipX!==tailX||tipY!==tailY){const points=ArrowShape.getArrowShapePoints(tailX,tailY,tipX,tipY,[],providedOptions);this.moveTo(points[0].x,points[0].y);const tail=_.tail(points);_.each(tail,point=>this.lineTo(point.x,point.y));this.close()}}static getArrowShapePoints(tailX,tailY,tipX,tipY,shapePoints,providedOptions){const options=optionize()({tailWidth:5,headWidth:10,headHeight:10,fractionalHeadHeight:.5,doubleHead:false,isHeadDynamic:false,scaleTailToo:false},providedOptions);if(!shapePoints){shapePoints=[]}if(tipX===tailX&&tipY===tailY){shapePoints.length=0}else{const vector=new Vector2(tipX-tailX,tipY-tailY);const length=vector.magnitude;let headWidth=options.headWidth;let headHeight=options.headHeight;let tailWidth=options.tailWidth;if(options.isHeadDynamic){const maxHeadHeight=options.fractionalHeadHeight*length;if(options.headHeight>maxHeadHeight){headHeight=maxHeadHeight;if(options.scaleTailToo){headWidth=options.headWidth*headHeight/options.headHeight;tailWidth=options.tailWidth*headHeight/options.headHeight}}}else{headHeight=Math.min(options.headHeight,options.doubleHead?.35*length:.99*length)}let index=0;const xHatUnit=vector.normalized();const yHatUnit=xHatUnit.rotated(Math.PI/2);const addPoint=function(xHat,yHat){const x=xHatUnit.x*xHat+yHatUnit.x*yHat+tailX;const y=xHatUnit.y*xHat+yHatUnit.y*yHat+tailY;if(shapePoints[index]){shapePoints[index].x=x;shapePoints[index].y=y}else{shapePoints.push(new Vector2(x,y))}index++};if(options.doubleHead){addPoint(0,0);addPoint(headHeight,headWidth/2);addPoint(headHeight,tailWidth/2)}else{addPoint(0,tailWidth/2)}addPoint(length-headHeight,tailWidth/2);addPoint(length-headHeight,headWidth/2);addPoint(length,0);addPoint(length-headHeight,-headWidth/2);addPoint(length-headHeight,-tailWidth/2);if(options.doubleHead){addPoint(headHeight,-tailWidth/2);addPoint(headHeight,-headWidth/2)}else{addPoint(0,-tailWidth/2)}if(index<shapePoints.length){shapePoints.length=index}}return shapePoints}}sceneryPhet.register("ArrowShape",ArrowShape);