import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";import sceneryPhet from"./sceneryPhet.js";import{roundSymmetric}from"../../dot/js/util/roundSymmetric.js";import{linear}from"../../dot/js/util/linear.js";import{clamp}from"../../dot/js/util/clamp.js";const RAYS_START_ANGLE=3*Math.PI/4;const RAYS_ARC_ANGLE=3*Math.PI/2;export default class LightRaysNode extends Path{constructor(bulbRadius,provideOptions){assert&&assert(bulbRadius>0);const options=optionize()({minRays:8,maxRays:60,minRayLength:0,maxRayLength:200,longRayLineWidth:1.5,mediumRayLineWidth:1,shortRayLineWidth:.5,stroke:"yellow"},provideOptions);super(null);this.bulbRadius=bulbRadius;this.minRays=options.minRays;this.maxRays=options.maxRays;this.minRayLength=options.minRayLength;this.maxRayLength=options.maxRayLength;this.mediumRayLineWidth=options.mediumRayLineWidth;this.longRayLineWidth=options.longRayLineWidth;this.shortRayLineWidth=options.shortRayLineWidth;this.setBrightness(0);this.mutate(options)}setBrightness(brightness){assert&&assert(brightness>=0&&brightness<=1);const numberOfRays=brightness===0?0:this.minRays+roundSymmetric(brightness*(this.maxRays-this.minRays));const rayLength=this.minRayLength+brightness*(this.maxRayLength-this.minRayLength);let angle=RAYS_START_ANGLE;const deltaAngle=RAYS_ARC_ANGLE/(numberOfRays-1);const lineWidth=linear(.3*this.maxRayLength,.6*this.maxRayLength,this.shortRayLineWidth,this.longRayLineWidth,rayLength);this.lineWidth=clamp(lineWidth,this.shortRayLineWidth,this.longRayLineWidth);const shape=new Shape;for(let i=0,x1,x2,y1,y2,cosAngle,sinAngle;i<this.maxRays;i++){if(i<numberOfRays){cosAngle=Math.cos(angle);sinAngle=Math.sin(angle);x1=cosAngle*this.bulbRadius;y1=sinAngle*this.bulbRadius;x2=cosAngle*(this.bulbRadius+rayLength);y2=sinAngle*(this.bulbRadius+rayLength);shape.moveTo(x1,y1).lineTo(x2,y2);angle+=deltaAngle}}this.setVisible(numberOfRays>0);if(numberOfRays===0){shape.circle(0,0,this.bulbRadius)}this.setShape(shape)}}sceneryPhet.register("LightRaysNode",LightRaysNode);