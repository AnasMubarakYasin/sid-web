import Vector2 from"../../dot/js/Vector2.js";import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import sceneryPhet from"./sceneryPhet.js";export default class CurvedArrowShape extends Shape{constructor(radius,startAngle,endAngle,providedOptions){const options=optionize()({doubleHead:false,headWidth:10,headHeight:10,tailWidth:5,anticlockwise:false},providedOptions);super();let baseInnerX;let baseInnerY;let baseOuterX;let baseOuterY;if(options.doubleHead){baseInnerX=Math.cos(startAngle)*(radius-options.headWidth/2);baseInnerY=Math.sin(startAngle)*(radius-options.headWidth/2);baseOuterX=Math.cos(startAngle)*(radius+options.headWidth/2);baseOuterY=Math.sin(startAngle)*(radius+options.headWidth/2);const startTip=computePerpendicularPoint(baseOuterX,baseOuterY,baseInnerX,baseInnerY,options.headHeight);this.moveTo(baseInnerX,baseInnerY).lineTo(startTip.x,startTip.y).lineTo(baseOuterX,baseOuterY)}this.arc(0,0,radius+options.tailWidth/2,startAngle,endAngle,options.anticlockwise);baseInnerX=Math.cos(endAngle)*(radius-options.headWidth/2);baseInnerY=Math.sin(endAngle)*(radius-options.headWidth/2);baseOuterX=Math.cos(endAngle)*(radius+options.headWidth/2);baseOuterY=Math.sin(endAngle)*(radius+options.headWidth/2);const endTip=computePerpendicularPoint(baseInnerX,baseInnerY,baseOuterX,baseOuterY,options.headHeight);this.lineTo(baseOuterX,baseOuterY).lineTo(endTip.x,endTip.y).lineTo(baseInnerX,baseInnerY);this.arc(0,0,radius-options.tailWidth/2,endAngle,startAngle,!options.anticlockwise);this.lineTo(Math.cos(startAngle)*radius,Math.sin(startAngle)*radius+1e-5);this.close()}}const computePerpendicularPoint=function(x1,y1,x2,y2,distance){const dx=x1-x2;const dy=y1-y2;const r=Math.sqrt(dx*dx+dy*dy);const x=(x1+x2)/2+distance*dy/r;const y=(y1+y2)/2-distance*dx/r;return new Vector2(x,y)};sceneryPhet.register("CurvedArrowShape",CurvedArrowShape);