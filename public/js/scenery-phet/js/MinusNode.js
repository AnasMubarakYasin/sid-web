import Dimension2 from"../../dot/js/Dimension2.js";import optionize from"../../phet-core/js/optionize.js";import Rectangle from"../../scenery/js/nodes/Rectangle.js";import sceneryPhet from"./sceneryPhet.js";const DEFAULT_SIZE=new Dimension2(20,5);let MinusNode=class MinusNode extends Rectangle{constructor(providedOptions){const options=optionize()({size:DEFAULT_SIZE,fill:"black"},providedOptions);assert&&assert(options.size.width>=options.size.height);super(0,0,options.size.width,options.size.height,options)}};sceneryPhet.register("MinusNode",MinusNode);export default MinusNode;