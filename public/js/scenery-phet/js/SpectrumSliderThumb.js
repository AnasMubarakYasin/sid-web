import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";import Rectangle from"../../scenery/js/nodes/Rectangle.js";import sceneryPhet from"./sceneryPhet.js";import SpectrumNode from"./SpectrumNode.js";export default class SpectrumSliderThumb extends Path{constructor(property,providedOptions){const options=optionize()({width:35,height:45,valueToColor:SpectrumNode.DEFAULT_VALUE_TO_COLOR,cursorHeight:30,cursorWidth:3,windowCursorOptions:{bottom:0,centerX:0,stroke:"black",cornerRadius:2},fill:"black",stroke:"black",lineWidth:1},providedOptions);const width=options.width;const height=options.height;const radiusScale=.15;const radius=width<height?radiusScale*width:radiusScale*height;const hypotenuse=Math.sqrt(Math.pow(.5*width,2)+Math.pow(.3*height,2));const angle=Math.acos(width*.5/hypotenuse);const heightOffset=radius*Math.sin(angle);const handleShape=new Shape().moveTo(.5*width,.3*height+heightOffset).lineTo(.5*width,height-radius).arc(.5*width-radius,height-radius,radius,0,Math.PI/2).lineTo(-.5*width+radius,height).arc(-.5*width+radius,height-radius,radius,Math.PI/2,Math.PI).lineTo(-.5*width,.3*height+heightOffset).arc(-.5*width+radius,.3*height+heightOffset,radius,Math.PI,Math.PI+angle);const sideArcPoint=handleShape.getLastPoint();assert&&assert(sideArcPoint);handleShape.lineTo(0,0).lineTo(-sideArcPoint.x,sideArcPoint.y).arc(.5*width-radius,.3*height+heightOffset,radius,-angle,0).close();super(handleShape,options);this.windowCursor=new Rectangle(0,0,options.cursorWidth,options.cursorHeight,options.windowCursorOptions);this.addChild(this.windowCursor);const listener=value=>this.setFill(options.valueToColor(value));property.link(listener);this.disposeSpectrumSliderThumb=()=>property.unlink(listener)}setCenterY(centerY){return super.setY(centerY+this.windowCursor.height/2)}dispose(){this.disposeSpectrumSliderThumb();super.dispose()}}sceneryPhet.register("SpectrumSliderThumb",SpectrumSliderThumb);