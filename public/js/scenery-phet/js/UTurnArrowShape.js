import Shape from"../../kite/js/Shape.js";import sceneryPhet from"./sceneryPhet.js";export default class UTurnArrowShape extends Shape{constructor(size){super();const strokeWidth=size*.3;const strokeOffset=strokeWidth/2;const mainWidth=size*.6;const mainHeight=size;const headWidth=size*.5;const headHeight=size*.75;const halfHeadWidth=headWidth/2;const halfHeadHeight=headHeight/2;this.moveTo(halfHeadWidth,-strokeOffset);this.lineTo(mainWidth,-strokeOffset);this.arc(mainWidth,mainHeight/2,mainHeight/2+strokeOffset,-Math.PI/2,Math.PI/2,false);this.lineTo(0,mainHeight+strokeOffset);this.lineTo(0,mainHeight-strokeOffset);this.lineTo(mainWidth,mainHeight-strokeOffset);this.arc(mainWidth,mainHeight/2,mainHeight/2-strokeOffset,Math.PI/2,-Math.PI/2,true);this.lineTo(halfHeadWidth,strokeOffset);this.lineTo(halfHeadWidth,halfHeadHeight);this.lineTo(-halfHeadWidth,0);this.lineTo(halfHeadWidth,-halfHeadHeight);this.close()}}sceneryPhet.register("UTurnArrowShape",UTurnArrowShape);