import Vector2 from"../../dot/js/Vector2.js";import Shape from"../../kite/js/Shape.js";import InstanceRegistry from"../../phet-core/js/documentation/InstanceRegistry.js";import optionize from"../../phet-core/js/optionize.js";import Node from"../../scenery/js/nodes/Node.js";import Path from"../../scenery/js/nodes/Path.js";import sceneryPhet from"./sceneryPhet.js";export default class LineArrowNode extends Node{constructor(tailX,tailY,tipX,tipY,providedOptions){const options=optionize()({stroke:"black",lineJoin:"miter",lineCap:"butt",headHeight:10,headWidth:10,headLineWidth:1,tailLineWidth:1,tailLineDash:[]},providedOptions);super();this.headWidth=options.headWidth;this.headHeight=options.headHeight;this.tailNode=new Path(null,{stroke:options.stroke,lineJoin:options.lineJoin,lineCap:options.lineCap,lineWidth:options.tailLineWidth,lineDash:options.tailLineDash});this.headNode=new Path(null,{stroke:options.stroke,lineJoin:options.lineJoin,lineCap:options.lineCap,lineWidth:options.headLineWidth});this.setTailAndTip(tailX,tailY,tipX,tipY);options.children=[this.tailNode,this.headNode];this.mutate(options);assert&&window.phet?.chipper?.queryParameters?.binder&&InstanceRegistry.registerDataURL("scenery-phet","LineArrowNode",this)}setTailAndTip(tailX,tailY,tipX,tipY){this.tailNode.shape=Shape.lineSegment(tailX,tailY,tipX,tipY);const vector=new Vector2(tipX-tailX,tipY-tailY);const xHatUnit=vector.normalized();const yHatUnit=xHatUnit.rotated(Math.PI/2);const length=vector.magnitude;const getPoint=function(xHat,yHat){const x=xHatUnit.x*xHat+yHatUnit.x*yHat+tailX;const y=xHatUnit.y*xHat+yHatUnit.y*yHat+tailY;return new Vector2(x,y)};const headHeight=Math.min(this.headHeight,.99*length);this.headNode.shape=new Shape().moveToPoint(getPoint(length-headHeight,this.headWidth/2)).lineToPoint(getPoint(length,0)).lineToPoint(getPoint(length-headHeight,-this.headWidth/2))}}sceneryPhet.register("LineArrowNode",LineArrowNode);