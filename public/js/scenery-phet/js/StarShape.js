import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import sceneryPhet from"./sceneryPhet.js";export default class StarShape extends Shape{constructor(providedOptions){const options=optionize()({outerRadius:15,innerRadius:7.5,numberStarPoints:5},providedOptions);super();const numSegments=2*options.numberStarPoints;_.times(numSegments,i=>{const angle=i/numSegments*Math.PI*2-Math.PI/2;const radius=i%2===0?options.outerRadius:options.innerRadius;this.lineTo(radius*Math.cos(angle),radius*Math.sin(angle))});this.close();this.makeImmutable()}}sceneryPhet.register("StarShape",StarShape);