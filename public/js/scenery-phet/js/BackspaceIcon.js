import Dimension2 from"../../dot/js/Dimension2.js";import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";import sceneryPhet from"./sceneryPhet.js";export default class BackspaceIcon extends Path{constructor(providedOptions){const options=optionize()({size:new Dimension2(15,10),stroke:"black",lineWidth:1,lineJoin:"round",lineCap:"square"},providedOptions);const iconShape=new Shape;const tipWidth=options.size.width/3;iconShape.moveTo(0,tipWidth).lineTo(tipWidth,0).lineTo(options.size.width,0).lineTo(options.size.width,options.size.height).lineTo(tipWidth,options.size.height).close();const left=.47*options.size.width;const right=.73*options.size.width;const top=.3*options.size.height;const bottom=.7*options.size.height;iconShape.moveTo(left,top).lineTo(right,bottom).moveTo(right,top).lineTo(left,bottom);super(iconShape,options)}}sceneryPhet.register("BackspaceIcon",BackspaceIcon);