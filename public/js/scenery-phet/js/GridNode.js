import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";import sceneryPhet from"./sceneryPhet.js";export default class GridNode extends Path{constructor(transformProperty,spacing,center,numberOfGridLines,providedOptions){const options=optionize()({stroke:"gray"},providedOptions);super(null,options);transformProperty.link(transform=>{const shape=new Shape;const x1=-numberOfGridLines*spacing+center.x;const x2=numberOfGridLines*spacing+center.x;const y1=-numberOfGridLines*spacing+center.y;const y2=numberOfGridLines*spacing+center.y;for(let i=-numberOfGridLines;i<=numberOfGridLines;i++){const x=i*spacing+center.x;const y=i*spacing+center.y;shape.moveTo(x1,y).lineTo(x2,y);shape.moveTo(x,y1).lineTo(x,y2)}this.shape=transform.modelToViewShape(shape)})}}sceneryPhet.register("GridNode",GridNode);