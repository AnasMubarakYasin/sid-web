import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";import sceneryPhet from"./sceneryPhet.js";export default class GridIcon extends Path{constructor(providedOptions){const options=optionize()({size:30,numberOfRows:4,stroke:"rgb( 100, 100, 100 )",lineWidth:1},providedOptions);assert&&assert(options.size>0,`invalid size: ${options.size}`);assert&&assert(Number.isInteger(options.numberOfRows)&&options.numberOfRows>2,`invalid numberOfRows: ${options.numberOfRows}`);const shape=new Shape;for(let row=1;row<options.numberOfRows;row++){const y=row/options.numberOfRows*options.size;shape.moveTo(0,y);shape.lineTo(options.size,y)}const numberOfColumns=options.numberOfRows;for(let column=1;column<numberOfColumns;column++){const x=column/numberOfColumns*options.size;shape.moveTo(x,0);shape.lineTo(x,options.size)}super(shape,options)}}sceneryPhet.register("GridIcon",GridIcon);