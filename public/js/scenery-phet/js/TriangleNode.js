import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";import sceneryPhet from"./sceneryPhet.js";export default class TriangleNode extends Path{constructor(providedOptions){const options=optionize()({pointDirection:"up",triangleWidth:15,triangleHeight:13,stroke:"black",lineWidth:1,cursor:"pointer"},providedOptions);const triangleShape=new Shape().moveTo(options.triangleWidth/2,0).lineTo(options.triangleWidth,options.triangleHeight).lineTo(0,options.triangleHeight).close();super(triangleShape,options);this.rotation=options.pointDirection==="up"?0:options.pointDirection==="right"?Math.PI/2:options.pointDirection==="down"?Math.PI:-Math.PI/2}}sceneryPhet.register("TriangleNode",TriangleNode);