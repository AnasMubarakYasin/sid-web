import deprecationWarning from"../../phet-core/js/deprecationWarning.js";const GriddleUtils={getValuesInRangeWithAnchor(min,max,delta=1,anchor=0){assert&&deprecationWarning("Please use bamboo");const nMin=Math.floor((min-anchor)/delta);const nMax=Math.ceil((max-anchor)/delta);const results=[];for(let n=nMin;n<=nMax;n++){const result=anchor+n*delta;if(result>=min&&result<=max){results.push(result)}}return results}};export default GriddleUtils;