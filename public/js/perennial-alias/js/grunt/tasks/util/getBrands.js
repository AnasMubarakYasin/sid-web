import assert from"assert";import fs from"fs";import{buildLocal}from"../../../common/buildLocal.js";import getOption from"./getOption.js";const getBrands=repo=>{assert(!getOption("brand"),"Use --brands={{BRANDS}} instead of brand");const localPackageObject=JSON.parse(fs.readFileSync(`../${repo}/package.json`,"utf-8"));const supportedBrands=localPackageObject.phet?.supportedBrands||[];let brands;if(getOption("brands")){if(getOption("brands")==="*"){brands=supportedBrands}else{brands=getOption("brands").split(",")}}else if(buildLocal.brands){assert(Array.isArray(buildLocal.brands),"If brands exists in build-local.json, it should be an array");brands=buildLocal.brands.filter(brand=>supportedBrands.includes(brand))}else{brands=["adapted-from-phet"]}if(!localPackageObject.phet?.buildStandalone){brands.forEach(brand=>assert(supportedBrands.includes(brand),`Unsupported brand: ${brand}`));assert(brands.length>0,`must have one or more brands, but found none for ${repo}`)}return brands};export default getBrands;