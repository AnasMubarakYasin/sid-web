import path from"path";import process from"process";import affirm from"../../../browser-and-node/affirm.js";import dirname from"../../../common/dirname.js";import getOption from"./getOption.js";const __dirname=dirname(import.meta.url);const gitRoot=path.resolve(`${__dirname}/../../../../../`);export default function getRepo(){const repoOption=getOption("repo");if(repoOption){return repoOption}else{const relative=path.relative(gitRoot,process.cwd());affirm(relative!==""&&!relative.startsWith(".."),`cannot find repo name, expected cwd to be inside of git root. gitRoot: ${gitRoot}, cwd: ${process.cwd()}`);const term=relative.split(path.sep)[0];return term}}