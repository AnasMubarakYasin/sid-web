import winston from"winston";import{getActiveRepos}from"../../common/repos/getActiveRepos.js";import fs from"fs";import os from"os";async function generateData(){const activeRepos=getActiveRepos();function writeList(name,packageFilter){const repos=activeRepos.filter(repo=>{if(!fs.existsSync(`../${repo}`)){return false}let packageObject;try{packageObject=JSON.parse(fs.readFileSync(`../${repo}/package.json`,"utf8"))}catch(e){return false}return packageObject.phet&&packageFilter(packageObject.phet)});winston.info(`Writing to data/${name}`);fs.writeFileSync(`data/${name}`,repos.join(os.EOL)+os.EOL)}writeList("interactive-description",phet=>phet.simFeatures&&phet.simFeatures.supportsInteractiveDescription);writeList("voicing",phet=>phet.simFeatures&&phet.simFeatures.supportsVoicing);writeList("active-runnables",phet=>phet.runnable);writeList("active-sims",phet=>phet.simulation);writeList("unit-tests",phet=>phet.generatedUnitTests);writeList("phet-io",phet=>phet.runnable&&phet.supportedBrands&&phet.supportedBrands.includes("phet-io"));writeList("phet-io-api-stable",phet=>{return phet.runnable&&phet.supportedBrands&&phet.supportedBrands.includes("phet-io")&&phet["phet-io"]&&phet["phet-io"].compareDesignedAPIChanges})}(async()=>generateData())();