import assert from"assert";import{assertIsValidDependencyName}from"../../common/assertIsValidDependencyName.js";import getOption from"./util/getOption.js";import{Checkout}from"../../common/Checkout.js";(async()=>{const sim=getOption("sim");const name=getOption("name");const message=getOption("message");assert(sim,"Requires specifying a repository with --sim={{SIM}}");assert(name,"Requires specifying a name with --name={{NAME}}");assert(!name.includes("-")&&!name.includes("."),"Branch should not contain dashes or periods");assertIsValidDependencyName(sim);await Checkout.createOneOffCheckout(sim,name,message)})();