import assert from"assert";import{assertIsValidDependencyName}from"../../common/assertIsValidDependencyName.js";import{checkoutRelease}from"../../common/checkoutRelease.js";import getOption from"./util/getOption.js";(async()=>{const repo=getOption("repo");assert(repo,"Requires specifying a repository with --repo={{REPOSITORY}}");assertIsValidDependencyName(repo);await checkoutRelease(repo)})();