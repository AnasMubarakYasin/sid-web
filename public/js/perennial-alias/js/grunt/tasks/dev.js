import assert from"assert";import{assertIsValidDependencyName}from"../../common/assertIsValidDependencyName.js";import{dev}from"../../common/deployment/dev.js";import getOption from"./util/getOption.js";(async()=>{const repo=getOption("repo");assert(repo,"Requires specifying a repository with --repo={{REPOSITORY}}");assertIsValidDependencyName(repo);await dev(repo,{noninteractive:!!getOption("noninteractive"),message:getOption("message")});process.exit(0)})();