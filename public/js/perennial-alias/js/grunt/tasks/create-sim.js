import assert from"assert";import{assertIsValidDependencyName}from"../../common/assertIsValidDependencyName.js";import{createSim}from"../../common/createSim.js";import getOption from"./util/getOption.js";(async()=>{const sim=getOption("sim");const author=getOption("author");const title=getOption("title");const clean=getOption("clean");assert(sim,"Requires specifying a sim name with --sim={{REPO}}");assert(getOption("author"),"Requires specifying a author with --author={{AUTHOR}}");assertIsValidDependencyName(sim);await createSim(sim,author,{title:title,clean:clean})})();