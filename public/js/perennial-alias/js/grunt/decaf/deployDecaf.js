const assert=require("assert");const SimVersion=require("../../browser-and-node/SimVersion").default;const buildLocal=require("../../common/buildLocal");const devDirectoryExists=require("../../common/devDirectoryExists");const devScp=require("../../common/devScp");const devSsh=require("../../common/devSsh");const getBranch=require("../../common/git/getBranch");const getBranchSHAMap=require("../../common/getBranchSHAMap");const gitIsClean=require("../../common/git/gitIsClean");const gitRevParse=require("../../common/git/gitRevParse");const loadJSON=require("../../common/loadJSON");const vpnCheck=require("../../common/vpnCheck");const grunt=require("grunt");const fs=require("fs");const BUILD_LOCAL_FILENAME=`${process.env.HOME}/.phet/build-local.json`;module.exports=async function(project,dev,production){const buildLocalJSON=JSON.parse(fs.readFileSync(BUILD_LOCAL_FILENAME,{encoding:"utf-8"}));const gitRoot=buildLocalJSON.gitRoot;const trunkPath=buildLocalJSON.decafTrunkPath;assert&&assert(gitRoot!==undefined,"buildLocal.gitRoot is undefined");assert&&assert(trunkPath!==undefined,"buildLocal.decafTrunkPath is undefined");const stringFiles=fs.readdirSync(`${trunkPath}/simulations-java/simulations/${project}/data/${project}/localization`);const locales=stringFiles.filter(stringFile=>stringFile.indexOf("_")>=0).map(file=>file.substring(file.indexOf("_")+1,file.lastIndexOf(".")));console.log(locales.join("\n"));const javaProperties=fs.readFileSync(`${trunkPath}/simulations-java/simulations/${project}/${project}-build.properties`,"utf-8");const flavorLines=javaProperties.split("\n").filter(line=>line.startsWith("project.flavor"));const flavors=flavorLines.length>0?flavorLines.map(line=>line.split(".")[2]):[`${project}`];console.log(flavors.join("\n"));if(!await vpnCheck()){throw new Error("VPN or being on campus is required for this build. Ensure VPN is enabled, or that you have access to phet-server2.int.colorado.edu")}const currentBranch=await getBranch();if(currentBranch!=="main"){throw new Error(`deployment should be on the branch main, not: ${currentBranch?currentBranch:"(detached head)"}`)}const packageFileRelative=`projects/${project}/package.json`;const packageFile=`../decaf/${packageFileRelative}`;const packageObject=await loadJSON(packageFile);const version=SimVersion.parse(packageObject.version);const isClean=await gitIsClean("decaf");if(!isClean){throw new Error(`Unclean status in ${project}, cannot deploy`)}const currentSHA=await gitRevParse("decaf","HEAD");const latestSHA=(await getBranchSHAMap("decaf")).main;if(currentSHA!==latestSHA){throw new Error(`Out of date with remote, please push or pull repo. Current SHA: ${currentSHA}, latest SHA: ${latestSHA}`)}const versionString=version.toString();if(dev){const simPath=buildLocal.decafDeployPath+project;const versionPath=`${simPath}/${versionString}`;const simPathExists=await devDirectoryExists(simPath);const versionPathExists=await devDirectoryExists(versionPath);if(versionPathExists){throw new Error(`Directory ${versionPath} already exists.  If you intend to replace the content then remove the directory manually from ${buildLocal.devDeployServer}.`)}if(!simPathExists){await devSsh(`mkdir -p "${simPath}" && echo "IndexOrderDefault Descending Date
" > "${simPath}/.htaccess"`)}await devSsh(`mkdir -p "${versionPath}"`);console.log(`../decaf/projects/${project}`);console.log(`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/${project}_all.jar`,`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/${project}_all.jar.js`,`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/${project}.html`,`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/splash.gif`,`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/style.css`,`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/dependencies.json`,`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/locales.txt`,`${versionPath}/`);await devScp(`../decaf/projects/${project}/build/simulations.txt`,`${versionPath}/`);const versionURL=`https://phet-dev.colorado.edu/decaf/${project}/${versionString}`;console.log("DEPLOYED");if(!fs.existsSync(`${gitRoot}/decaf/build/log.txt`)){fs.mkdirSync(`${gitRoot}/decaf/build`)}flavors.forEach(flavor=>{const url=`${versionURL}/${project}.html?simulation=${flavor}`;grunt.log.writeln(url);fs.appendFileSync(`${gitRoot}/decaf/build/log.txt`,`${url}
`)});if(flavors.length===0){const URL=`${versionURL}/${project}.html`;grunt.log.writeln(URL);fs.appendFileSync(`${gitRoot}/decaf/build/log.txt`,`${URL}
`)}}console.log("FLAVORS");console.log(flavors.join(", "));console.log("LOCALES");console.log(locales.join(", "));if(production){const productionServerURL=buildLocal.productionServerURL||"https://phet.colorado.edu";const template=`cd /data/web/static/phetsims/sims/cheerpj/
sudo -u phet-admin mkdir -p ${project}
cd ${project}
sudo -u phet-admin scp -r bayes.colorado.edu:/data/web/htdocs/dev/decaf/${project}/${version} .

sudo chmod g+w *
printf "RewriteEngine on\\nRewriteBase /sims/cheerpj/${project}/\\nRewriteRule ^latest(.*) ${version}\\$1\\nHeader set Access-Control-Allow-Origin \\"*\\"\\n" > .htaccess

cd ${version}
sudo chmod g+w *

token=$(grep serverToken ~/.phet/build-local.json | sed -r 's/ *"serverToken": "(.*)",/\\1/') && \\
curl -u "token:$\{token}" '${productionServerURL}/services/deploy-cheerpj?project=${project}&version=${version}&locales=${locales.join(",")}&simulations=${flavors.join(",")}'
`;console.log("SERVER SCRIPT TO PROMOTE DEV VERSION TO PRODUCTION VERSION");console.log(template)}};