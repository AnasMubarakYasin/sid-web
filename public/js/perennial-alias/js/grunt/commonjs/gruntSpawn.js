const child_process=require("child_process");const isWindows=/^win/.test(process.platform);module.exports=function spawn(grunt,command,args,cwd,preHook){const done=grunt.task.current.async();const argsString=args.map(arg=>`"${arg}"`).join(" ");const spawned=child_process.spawn(command,args,{cwd:cwd,shell:isWindows,env:Object.create(process.env)});preHook&&preHook(argsString);spawned.stderr.on("data",data=>process.stderr.write(data.toString()));spawned.stdout.on("data",data=>process.stdout.write(data.toString()));process.stdin.pipe(spawned.stdin);spawned.on("close",code=>{done();process.exit(code)})};