const fs=require("fs");module.exports=file=>{const source=fs.readFileSync(file,{encoding:"utf-8"});if(source.includes("/**")&&source.includes("*/")){const myDoc=source.substring(source.indexOf("/**")+"/**".length,source.indexOf("*/"));const lines=myDoc.split("\n");const docLines=lines.map(line=>line.trim().replace(/^\*\s*/,"")).filter(line=>line!=="*"&&!line.startsWith("@author"));while(docLines.length>0&&docLines[0].trim().length===0){docLines.shift()}while(docLines.length>0&&docLines[docLines.length-1].trim().length===0){docLines.pop()}return docLines.join("\n")}else{return"No documentation found"}};