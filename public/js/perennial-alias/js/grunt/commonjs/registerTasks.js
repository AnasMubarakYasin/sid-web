const fs=require("fs");const getDocumentationForTask=require("./getDocumentationForTask");const path=require("path");const gruntSpawn=require("./gruntSpawn");const assert=require("assert");const _=require("lodash");const isWindows=process.platform.startsWith("win");const runnable=isWindows?"tsx.cmd":"tsx";const tsxCommand=`${path.join(__dirname,`../../../node_modules/.bin/${runnable}`)}`;function getArgsToForward(args=process.argv){for(let i=0;i<args.length;i++){if(args[i].includes("grunt")||/\bpm2\b/.test(args[i])){const nextArg=args[i+1];const isNextArgTheTask=!nextArg||!nextArg.startsWith("-");return args.slice(i+(isNextArgTheTask?2:1))}}assert(false,`unexpected grunt task arguments that didn't launch with "grunt": [${args.join(" ")}]`);return[]}function execTask(grunt,taskFilename){return()=>{const args=getArgsToForward();gruntSpawn(grunt,tsxCommand,[taskFilename,...args],process.cwd())}}const supportedTaskFileExtensions=[".js",".ts",".cjs"];module.exports=(grunt,dir)=>{assert(fs.existsSync(dir),`dir does not exist: ${dir}`);fs.readdirSync(dir).forEach(file=>{if(_.some(supportedTaskFileExtensions,extension=>file.endsWith(extension))){const taskName=file.substring(0,file.lastIndexOf("."));const numberOfFiles=supportedTaskFileExtensions.map(extension=>fs.existsSync(path.join(dir,`${taskName}${extension}`))).filter(_.identity);if(numberOfFiles.length>1){throw new Error(`Both TypeScript and JavaScript versions of the task ${taskName} exist. Please remove one of them.`)}else{const absolutePath=path.join(dir,file);grunt.registerTask(taskName,getDocumentationForTask(absolutePath),execTask(grunt,absolutePath))}}})};module.exports.getArgsToForward=getArgsToForward;