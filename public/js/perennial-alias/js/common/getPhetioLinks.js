import _ from"lodash";import simPhetioMetadata from"./simPhetioMetadata.js";import{Checkout}from"./Checkout.js";const getPhetioLinks=async()=>{const allSimsData=await simPhetioMetadata({active:true,latest:true});const sortedAndReversed=_.sortBy(allSimsData,simData=>`${simData.name}${getVersion(simData)}`).reverse();const oneVersionPerSimList=_.uniqBy(sortedAndReversed,simData=>simData.name).reverse();const phetioLinks=[];for(const simData of oneVersionPerSimList){const useTopLevelIndex=await usesTopLevelIndex(simData.name,getBranch(simData));phetioLinks.push(`https://phet-io.colorado.edu/sims/${simData.name}/${getVersion(simData)}/${useTopLevelIndex?"":"wrappers/index/"}`)}return phetioLinks};export default getPhetioLinks;async function usesTopLevelIndex(sim,branchVersion){return(await Checkout.getLightweightReleaseBranchCheckout(sim,branchVersion)).hasTopLevelStudioIndex()}const getVersion=simData=>`${simData.versionMajor}.${simData.versionMinor}`;const getBranch=simData=>{let branch=`${simData.versionMajor}.${simData.versionMinor}`;if(simData.versionSuffix.length){branch+=`-${simData.versionSuffix}`}return branch};