import{buildLocal}from"./buildLocal.js";import Octokit from"@octokit/rest";import _ from"lodash";import winston from"winston";export const githubCreateIssue=async(repo,title,options)=>{winston.info(`Creating issue for ${repo}`);const octokit=new Octokit({auth:buildLocal.phetDevGitHubAccessToken});await octokit.issues.create(_.extend({owner:"phetsims",repo:repo,title:title},options))};