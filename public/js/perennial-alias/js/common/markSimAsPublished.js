import fs from"fs";import{gitAdd}from"./git/gitAdd.js";import{gitCommit}from"./git/gitCommit.js";import{gitPush}from"./git/gitPush.js";export const markSimAsPublished=async sim=>{const packageObject=JSON.parse(fs.readFileSync(`../${sim}/package.json`,"utf8"));if(!packageObject.phet.published){packageObject.phet.published=true;fs.writeFileSync(`../${sim}/package.json`,JSON.stringify(packageObject,null,2));await gitAdd(`${sim}/package.json`);await gitCommit("Marking simulation as published");await gitPush("main")}};