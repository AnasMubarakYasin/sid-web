import readline from"readline";import winston from"winston";const MAGENTA="\x1b[35m";const RESET="\x1b[0m";export const prompt=prompt=>{return new Promise((resolve,reject)=>{winston.debug(`prompting the user with ${prompt}`);const rl=readline.createInterface({input:process.stdin,output:process.stdout});rl.question(MAGENTA+prompt+RESET,answer=>{rl.close();winston.debug(`received answer: ${answer}`);resolve(answer)})})};