import winston from"winston";import{gitImmutableExecute}from"./gitMutex.js";export const hasRemoteBranch=async branch=>{winston.debug(`checking for remote branch ${branch}`);const stdout=await gitImmutableExecute(["ls-remote","--heads","https://github.com/phetsims/totality.git",branch],"..");if(stdout.trim().length===0){return false}else if(stdout.includes(`refs/heads/${branch}`)){return true}else{throw new Error(`Failure trying to check for a remote branch ${branch}`)}};