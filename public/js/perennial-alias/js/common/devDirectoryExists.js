import{devSsh}from"./devSsh.js";export const devDirectoryExists=async directory=>{return devSsh(`[[ -d "${directory}" ]] && echo exists || echo not`).then(stdout=>{if(stdout.trim()==="exists"){return true}else if(stdout.trim()==="not"){return false}else{throw new Error(`Problem determining whether a dev directory exists: ${directory}`)}}).catch(reason=>{if(reason.stderr.includes("Connection timed out")){throw new Error("Cannot reach the dev server.  Check that you have an internet connection and that you are either on campus or on the VPN.")}throw new Error(reason)})};