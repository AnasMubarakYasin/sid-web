import fs from"fs";import winston from"winston";export const copyFile=(sourceFilename,destinationFilename)=>{return new Promise((resolve,reject)=>{winston.info(`Copying ${sourceFilename} to ${destinationFilename}`);const readStream=fs.createReadStream(sourceFilename);const writeStream=fs.createWriteStream(destinationFilename);readStream.pipe(writeStream);readStream.on("end",()=>resolve());readStream.on("error",err=>reject(new Error(`${err}`)));writeStream.on("error",err=>reject(new Error(`${err}`)))})};