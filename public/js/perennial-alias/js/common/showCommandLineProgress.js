const _=require("lodash");module.exports=function showCommandLineProgress(progress,newline,options){options=_.assignIn({progressBarLength:40},options);const dots=".".repeat(Math.round(progress*options.progressBarLength));const empty=" ".repeat(Math.round((1-progress)*options.progressBarLength));const newlineString=newline?"\n":"";process.stdout.write(`\r[${dots}${empty}] ${(progress*100).toFixed(2)}%${newlineString}`)};