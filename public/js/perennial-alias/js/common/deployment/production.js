import SimVersion from"../../browser-and-node/SimVersion.js";import{Checkout}from"../Checkout.js";import{gitIsClean}from"../git/gitIsClean.js";import{hasRemoteBranch}from"../git/hasRemoteBranch.js";import{vpnCheck}from"../vpnCheck.js";import{booleanPrompt}from"../booleanPrompt.js";import simMetadata from"../simMetadata.js";import assert from"assert";import winston from"winston";import{buildServerRequest}from"../buildServerRequest.js";import{buildLocal}from"../buildLocal.js";import{getBranch}from"../git/getBranch.js";let ProductionDeployError=class ProductionDeployError extends Error{constructor(message){super(`Cancelling Production deployment: ${message}`);this.name="ProductionDeployError"}};export const production=async(sim,branchVersion,options)=>{const noninteractive=options?.noninteractive??false;const message=options?.message;const skipBuild=options?.skipBuild??false;const redeploy=options?.redeploy??false;SimVersion.ensureReleaseBranch(branchVersion);if(!Checkout.isBranchNameMainlike(await getBranch())){throw new ProductionDeployError("Primary checkout must be on main in order to deploy")}winston.info("checking vpn");if(!await vpnCheck()){throw new ProductionDeployError("VPN or being on campus is required for this build. Ensure VPN is enabled, or that you have access to phet-server2.int.colorado.edu")}winston.info("checking clean main checkout");if(!await gitIsClean()){throw new ProductionDeployError("Unclean status on main checkout, cannot create release branch")}winston.info("checking for remote branch");if(!await hasRemoteBranch(Checkout.getReleaseBranchName(sim,branchVersion))){throw new ProductionDeployError(`Cannot find release branch ${Checkout.getReleaseBranchName(sim,branchVersion)} for ${sim}`)}winston.info("loading checkout");const releaseBranch=await Checkout.getReleaseBranch(sim,branchVersion);const checkout=releaseBranch.checkout;if(!(await Checkout.getMainCheckout()).existsRelative(`${sim}/assets/${sim}-screenshot.png`)&&releaseBranch.brands.includes("phet")){throw new ProductionDeployError(`Missing screenshot file (${sim}/assets/${sim}-screenshot.png)`)}if(!await booleanPrompt("Are QA credits up-to-date?",noninteractive)){throw new ProductionDeployError("QA credits not up-to-date")}if(!await booleanPrompt("Have all maintenance patches that need spot checks been tested? (An issue would be created in the sim repo)",noninteractive)){throw new ProductionDeployError("Maintenance patches are not tested")}winston.info("querying sim metadata");const isFirstVersion=!(await simMetadata({simulation:sim})).projects;if(isFirstVersion){if(!await booleanPrompt("Is the main checklist complete (e.g. are screenshots added to assets, etc.)",noninteractive)){throw new ProductionDeployError("Main checklist not complete")}}redeploy&&assert(noninteractive,"redeploy can only be specified with noninteractive:true");const published=await releaseBranch.isPublished();winston.info("updating worktree");await checkout.updateWorktree();winston.info("checking clean release branch checkout");if(!await checkout.isClean()){throw new ProductionDeployError(`Unclean status in ${checkout.branch}, cannot deploy`)}const previousVersion=await releaseBranch.getSimVersion();let version;let versionChanged;if(previousVersion.testType===null){if(!redeploy&&(noninteractive||!await booleanPrompt(`The last deployment was a production deployment (${previousVersion.toString()}) and an RC version is required between production versions. Would you like to redeploy ${previousVersion.toString()} (y) or cancel this process and revert to main (N)`,false))){throw new ProductionDeployError("It appears that the last deployment was for production")}version=previousVersion;versionChanged=false}else if(previousVersion.testType==="rc"){version=new SimVersion(previousVersion.major,previousVersion.minor,previousVersion.maintenance);versionChanged=true}else{throw new ProductionDeployError(`The version number cannot be incremented safely: ${previousVersion}`)}const versionString=version.toString();if(!await booleanPrompt(`DEPLOY ${sim} ${versionString} (brands: ${releaseBranch.brands.join(",")}) to PRODUCTION`,noninteractive)){throw new ProductionDeployError('"DEPLOY" user request')}if(versionChanged){winston.info("setting sim version on release branch");await releaseBranch.setSimVersion(version,message)}if(published){winston.info("updating production README on release branch");await releaseBranch.updateProductionREADME()}if(!skipBuild){winston.info("building... (may take a while)");winston.info(await releaseBranch.build({minify:!noninteractive}))}if(!skipBuild&&!await booleanPrompt(`Please test the built version of ${sim}.
Is it ready to deploy`,noninteractive)){await releaseBranch.setSimVersion(previousVersion,`Reverting deploy version for: ${message}`);throw new ProductionDeployError(`Built sim test failed, reverted back to ${previousVersion}`)}winston.info("sending build-server request");await buildServerRequest(sim,version,branchVersion,releaseBranch.getBuildServerBrands(),await checkout.getSHA(),{locales:"*",servers:["dev","production"]});if(releaseBranch.brands.includes("phet")){winston.info(`Deployed: https://phet.colorado.edu/sims/html/${sim}/latest/${sim}_all.html`)}if(releaseBranch.brands.includes("phet-io")){winston.info(`Deployed: https://phet-io.colorado.edu/sims/${sim}/${versionString}/`)}winston.info("Please wait for the build-server to complete the deployment, and then test!");winston.info(`To view the current build status, visit ${buildLocal.productionServerURL}/deploy-status`);if(isFirstVersion&&releaseBranch.brands.includes("phet")){winston.info("After testing, let the simulation lead know it has been deployed, so they can edit metadata on the website");if(published){winston.info("Updating main README");await (await Checkout.getMainRunnableBranch(sim)).updateProductionREADME()}}if(releaseBranch.brands.includes("phet-io")){const phetioLogText="PhET-iO deploys involve a couple of extra steps after production. Create an issue in the "+'phet-io repo using the "New or Republished PhET-iO Simulation Publication" issue template '+'to make sure these are accomplished. Assign yourself for "developer" steps.';winston.info(phetioLogText)}return version};