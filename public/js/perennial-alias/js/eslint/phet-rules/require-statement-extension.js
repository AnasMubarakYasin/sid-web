/**
 * @fileoverview Rule to check that a require statement extension is not provided for .js files.
 * @author Sam Reid (PhET Interactive Simulations)
 * @author Michael Kauzmann (PhET Interactive Simulations)
 * @copyright 2024 University of Colorado Boulder
 */const path=require("path");module.exports={meta:{type:"suggestion",docs:{description:"Ensure require statements have same extension usages",category:"Best Practices",recommended:false},fixable:"code"},create:function(context){const filename=context.getFilename();return{CallExpression(node){if(node.callee?.name==="require"&&node.arguments.length===1&&node.arguments[0].type==="Literal"&&typeof node.arguments[0].value==="string"){const pathString=node.arguments[0].value;let moduleName;if(pathString.startsWith(".")){moduleName=path.basename(pathString);const extName=path.extname(moduleName);const hasExtension=extName.length>0;if(hasExtension){moduleName=moduleName.slice(0,-extName.length)}const enforceNoJsSuffix=filename.endsWith(".js")&&!pathString.includes("node_modules");if(enforceNoJsSuffix&&hasExtension&&extName===".js"){context.report({node:node.arguments[0],message:`Require path '${pathString}' should never include'.js' extension. (It is harder to convert commonJS modules to TypeScript)`,fix:function(fixer){return fixer.replaceText(node.arguments[0],`'${pathString.replace(".js","")}'`)}})}}}}}}};