module.exports={meta:{type:"problem",docs:{description:"Require a file extension on relative import paths",category:"Best Practices",recommended:false},fixable:"code",schema:[]},create:function(context){const handler=node=>{const source=node.source?.value;if(source?.startsWith(".")){const hasExtension=/\.[^./\\]+$/.test(source);if(!hasExtension){context.report({node:node,message:"Relative import paths must include a file extension.",fix:function(fixer){const importSource=node.source;const raw=importSource.raw;const quote=raw[0];const newValue=source+".js";const newImportPath=`${quote}${newValue}${quote}`;return fixer.replaceText(importSource,newImportPath)}})}}};return{ImportDeclaration(node){handler(node)},ImportExpression(node){handler(node)}}}};