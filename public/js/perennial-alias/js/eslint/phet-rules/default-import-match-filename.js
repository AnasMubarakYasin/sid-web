const path=require("path");const _=require("lodash");function removeExtension(filename){return path.basename(filename,path.extname(filename))}function matchesCamelCase(localName,filenameWithoutExtension){return localName===_.camelCase(filenameWithoutExtension)}function isCompatible(localName,filename){const fileWithoutExtension=removeExtension(filename);if(localName!==fileWithoutExtension&&localName.toLowerCase()===fileWithoutExtension.toLowerCase()){return false}return localName===fileWithoutExtension||matchesCamelCase(localName,fileWithoutExtension)||localName==="_"}function isBarePackageImport(filePath){return filePath!=="."&&filePath!==".."&&!filePath.includes("/")&&!filePath.startsWith("@")||/@[^/]+\/[^/]+$/.test(filePath)}function isAncestorRelativePath(filePath){return filePath.length>0&&!filePath.startsWith("/")&&filePath.split(/[/\\]/).every(segment=>segment===".."||segment==="."||segment==="")}function getPackageJsonName(packageJsonPath){try{return require(packageJsonPath).name||undefined}catch(e){return undefined}}function getNameFromPackageJsonOrDirname(filePath,context){const directoryName=path.join(context.getFilename(),filePath,"..");const packageJsonPath=path.join(directoryName,"package.json");const packageJsonName=getPackageJsonName(packageJsonPath);return packageJsonName||path.basename(directoryName)}function getFilename(filePath,context){if(isBarePackageImport(filePath)){return undefined}const basename=path.basename(filePath);const isDir=/^index$|^index\./.test(basename);const processedPath=isDir?path.dirname(filePath):filePath;if(isAncestorRelativePath(processedPath)){return getNameFromPackageJsonOrDirname(processedPath,context)}return path.basename(processedPath)+(isDir?"/":"")}module.exports={create:function(context){return{ImportDeclaration(node){const defaultImportSpecifier=node.specifiers.find(({type})=>type==="ImportDefaultSpecifier");const defaultImportName=defaultImportSpecifier&&defaultImportSpecifier.local.name;if(!defaultImportName){return}const filename=getFilename(node.source.value,context);if(!filename){return}if(!isCompatible(defaultImportName,filename)){context.report({node:defaultImportSpecifier,message:`Default import name does not match filename "${filename}".`})}}}}};