import winston from"winston";import simMetadata from"../common/simMetadata.js";import getOption from"../grunt/tasks/util/getOption.js";winston.default.transports.console.level="error";(async()=>{const data=await simMetadata({type:"html"});console.log(data.projects.map(project=>{let result=project.name.slice(project.name.indexOf("/")+1);if(getOption("versions")){result+=` ${project.version.major}.${project.version.minor}.${project.version.dev}`}return result}).join("\n"))})();