import{Node,Project,ts}from"ts-morph";const ARRAY_METHODS=new Set(["map","filter","forEach","find","some","every","reduce","reduceRight","includes","indexOf","lastIndexOf","flat","flatMap","sort","reverse","slice","splice","concat","join","push","pop","shift","unshift","length","entries","keys","values","at","fill","copyWithin","findIndex"]);const VALUE_TYPES=new Set(["Range","Bounds2","Bounds3","Dimension2","Dimension3","Vector2","Vector3","Vector4","Matrix3"]);const NARROWING_CANDIDATE_MAX_MEMBERS=5;function getBaseTypeName(typeText){return typeText.replace(/import\("[^"]*"\)\./g,"").replace(/<.*>$/s,"")}function isSkippedType(typeText,type){if(type.isUnion()){return type.getUnionTypes().every(t=>t.isNull()||t.isUndefined()||isSkippedType(getBaseTypeName(t.getText()),t))}if(type.isNumber()||type.isString()||type.isBoolean()||type.isStringLiteral()||type.isNumberLiteral()||type.isBooleanLiteral()){return true}const baseName=getBaseTypeName(typeText);if(baseName==="Tandem"){return true}if(/^(Property|TReadOnlyProperty|TProperty|ReadOnlyProperty|PhetioProperty|TinyProperty|NumberProperty|BooleanProperty|StringProperty|StringUnionProperty|DerivedProperty|DynamicProperty|EnumerationProperty|MappedProperty)$/.test(baseName)){return true}if(/^(Readonly)?(Map|Set)$/.test(baseName)||/^Weak(Map|Set)$/.test(baseName)){return true}if(VALUE_TYPES.has(baseName)){return true}return false}function isArrayType(typeText,type){return type.isArray()||typeText.endsWith("[]")}function countPublicMembers(type){return type.getProperties().filter(symbol=>{const declarations=symbol.getDeclarations();return!declarations.some(declaration=>{const flags=ts.getCombinedModifierFlags(declaration.compilerNode);return(flags&ts.ModifierFlags.NonPublicAccessibilityModifier)!==0})}).length}function getNarrowedAliasName(typeNode){if(!typeNode){return null}if(Node.isTypeLiteral(typeNode)){return"<inline type literal>"}if(Node.isTypeReference(typeNode)){const declarations=typeNode.getTypeName().getSymbol()?.getDeclarations()||[];for(const declaration of declarations){if(Node.isTypeAliasDeclaration(declaration)){const aliasTypeNode=declaration.getTypeNode();if(aliasTypeNode&&Node.isTypeLiteral(aliasTypeNode)){return declaration.getName()}}}}return null}function getSameFileCallable(callNode,sourceFile){if(!Node.isCallExpression(callNode)&&!Node.isNewExpression(callNode)){return null}const declarations=callNode.getExpression().getSymbol()?.getDeclarations()||[];for(const declaration of declarations){if(declaration.getSourceFile()!==sourceFile){continue}if((Node.isFunctionDeclaration(declaration)||Node.isMethodDeclaration(declaration))&&declaration.getBody()){return declaration}if(Node.isClassDeclaration(declaration)){const childConstructor=declaration.getConstructors()[0];if(childConstructor&&childConstructor.getBody()){return childConstructor}}}return null}function collectParameterUsage(body,paramName,sourceFile,visited,result){body.forEachDescendant(node=>{if(Node.isPropertyAccessExpression(node)){const expression=node.getExpression();if(Node.isIdentifier(expression)&&expression.getText()===paramName){result.accessed.add(node.getName())}}if(Node.isCallExpression(node)||Node.isNewExpression(node)){const args=node.getArguments();for(const arg of args){if(Node.isIdentifier(arg)&&arg.getText()===paramName){const calleeName=node.getExpression().getText();const sameFileCallable=getSameFileCallable(node,sourceFile);const targetParam=sameFileCallable&&sameFileCallable.getParameters()[args.indexOf(arg)];const targetBody=sameFileCallable&&sameFileCallable.getBody();if(sameFileCallable&&targetParam&&targetBody){const visitedKey=`${sameFileCallable.getPos()}:${targetParam.getName()}`;if(!visited.has(visitedKey)){visited.add(visitedKey);result.followedHelpers.add(calleeName);collectParameterUsage(targetBody,targetParam.getName(),sourceFile,visited,result)}}else{result.passedToExternal.add(calleeName)}}}}})}async function reportConstructorCoupling(repoPath){const project=new Project({tsConfigFilePath:`${repoPath}/tsconfig.json`});const sourceFiles=project.getSourceFiles(`${repoPath}/js/**/*.ts`);console.log(`Found ${sourceFiles.length} source file(s) under ${repoPath}/js/**/*.ts
`);const reports=[];const alreadyNarrowed=[];const conduits=[];for(const sourceFile of sourceFiles){const classes=sourceFile.getClasses();for(const classDeclaration of classes){const constructor=classDeclaration.getConstructors()[0];if(!constructor){continue}const className=classDeclaration.getName()||"<Unnamed>";const params=constructor.getParameters();const paramReports=[];for(const param of params){const paramName=param.getName();if(paramName==="options"||paramName==="providedOptions"){continue}const type=param.getType();const paramType=type.getText(param);if(isSkippedType(paramType,type)){continue}const body=constructor.getBody();if(!body){continue}const usage={accessed:new Set,passedToExternal:new Set,followedHelpers:new Set};collectParameterUsage(body,paramName,sourceFile,new Set,usage);if(isArrayType(paramType,type)){for(const prop of usage.accessed){if(ARRAY_METHODS.has(prop)){usage.accessed.delete(prop)}}}if(usage.accessed.size===0&&usage.passedToExternal.size===0){continue}const aliasName=getNarrowedAliasName(param.getTypeNode());if(aliasName){alreadyNarrowed.push({className:className,filePath:sourceFile.getFilePath(),paramName:paramName,paramType:aliasName,detail:`uses ${usage.accessed.size} of ${countPublicMembers(type)} declared members`});continue}if(usage.passedToExternal.size>0){const accessNote=usage.accessed.size>0?`, plus ${usage.accessed.size} member(s) accessed directly`:"";conduits.push({className:className,filePath:sourceFile.getFilePath(),paramName:paramName,paramType:paramType,detail:`passed whole to: ${[...usage.passedToExternal].sort().join(", ")}${accessNote}`});continue}const totalPublicMembers=countPublicMembers(type);const isModelOrSceneClass=type.isClass()&&/(Model|Scene)$/.test(getBaseTypeName(paramType));const isNarrowingCandidate=isModelOrSceneClass&&usage.accessed.size<NARROWING_CANDIDATE_MAX_MEMBERS&&totalPublicMembers>=usage.accessed.size*2;paramReports.push({paramName:paramName,paramType:paramType,properties:[...usage.accessed].sort(),followedHelpers:[...usage.followedHelpers].sort(),totalPublicMembers:totalPublicMembers,isNarrowingCandidate:isNarrowingCandidate})}if(paramReports.length>0){reports.push({className:className,filePath:sourceFile.getFilePath(),params:paramReports})}}}reports.sort((a,b)=>{const totalA=a.params.reduce((sum,p)=>sum+p.properties.length,0);const totalB=b.params.reduce((sum,p)=>sum+p.properties.length,0);return totalA-totalB});const candidates=reports.filter(r=>r.params.some(p=>p.isNarrowingCandidate));const others=reports.filter(r=>!r.params.some(p=>p.isNarrowingCandidate));const printReport=report=>{console.log(`${report.className} (${report.filePath})`);for(const param of report.params){const ratio=param.totalPublicMembers>0?Math.round(100*param.properties.length/param.totalPublicMembers):100;const flag=param.isNarrowingCandidate?" ⚠ NARROWING CANDIDATE":"";console.log(`  ${param.paramName}: ${param.paramType} — uses ${param.properties.length} of ${param.totalPublicMembers} public members (${ratio}%)${flag}`);for(const prop of param.properties){console.log(`    .${prop}`)}for(const helper of param.followedHelpers){console.log(`    (followed same-file helper: ${helper})`)}}console.log()};console.log("=== Constructor Coupling Report ===\n");console.log(`--- Narrowing candidates: *Model/*Scene class with <${NARROWING_CANDIDATE_MAX_MEMBERS} members used (${candidates.length}) ---
`);candidates.forEach(printReport);console.log(`--- Other findings (${others.length}) ---
`);others.forEach(printReport);console.log(`--- Already narrowed: structural type alias parameters (${alreadyNarrowed.length}) ---
`);for(const entry of alreadyNarrowed){console.log(`  ${entry.className}.${entry.paramName}: ${entry.paramType} — ${entry.detail}`)}console.log();console.log(`--- Skipped conduits: parameter passed whole to externally-declared callables (${conduits.length}) ---
`);for(const entry of conduits){console.log(`  ${entry.className}.${entry.paramName}: ${entry.paramType} — ${entry.detail}`)}console.log();const totalParams=reports.reduce((sum,r)=>sum+r.params.length,0);const totalCandidateParams=reports.reduce((sum,r)=>sum+r.params.filter(p=>p.isNarrowingCandidate).length,0);console.log(`Summary: ${totalCandidateParams} narrowing candidate(s), ${totalParams} finding(s) total, `+`${alreadyNarrowed.length} already narrowed, ${conduits.length} conduit(s) skipped.`)}if(process.argv.includes("--help")){console.log(`
\x1b[1mUsage (run from the totality monorepo root):\x1b[0m
  \x1b[36mbash perennial-alias/bin/sage run perennial-alias/js/scripts/reportConstructorCoupling.ts <repo-directory>\x1b[0m

\x1b[1mParameters:\x1b[0m
  \x1b[33m<repo-directory>\x1b[0m - The repo directory name (relative to the totality root) whose
                       TypeScript files should be analyzed. Must contain a 'tsconfig.json'.

\x1b[1mOptions:\x1b[0m
  \x1b[32m--help\x1b[0m                  - Displays this help message and exits.

\x1b[1mExample:\x1b[0m
  \x1b[36mbash perennial-alias/bin/sage run perennial-alias/js/scripts/reportConstructorCoupling.ts quantum-wave-interference\x1b[0m

\x1b[1mNote:\x1b[0m
- Only constructor parameters are analyzed. Parameters named 'options' or 'providedOptions'
  are excluded.
- Parameters whose declared type is already minimal (Property types, Map/Set, Range/Bounds2/etc.,
  Tandem, primitives, literal unions) are skipped.
- Parameters typed as a structural type alias are classified as "already narrowed".
- Parameters passed whole to externally-declared callables are classified as conduits and skipped;
  same-file helper calls are followed transitively instead.
- Findings report accessed-count vs. total public member count; concrete *Model/*Scene class
  parameters with <5 members used are flagged as narrowing candidates.
- This is a read-only reporting tool — no files are modified.
  `);process.exit(0)}if(process.argv.length<3){console.error("Error: Please provide the path to the repository directory. Check --help for instructions.");process.exit(1)}const repoPath=process.argv[2];reportConstructorCoupling(repoPath).then(()=>console.log("\nFinished.")).catch(error=>{console.error("An error occurred:",error);process.exit(1)});