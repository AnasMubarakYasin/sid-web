import Octokit from"@octokit/rest";import fs from"fs";import _ from"lodash";import process from"process";import{buildLocal}from"../common/buildLocal.js";import{createDirectory}from"../common/createDirectory.js";import{getRepoList}from"../common/repos/getRepoList.js";import lint from"../eslint/lint.js";import{getOption}from"../grunt/tasks/util/getOption.js";const CHIPPER_DIST_ESLINT="../chipper/dist/eslint/";const TODOsFilename=`${CHIPPER_DIST_ESLINT}/issuesFromTODOs.txt`;const repoList=getOption("repoList")||"active-repos";(async()=>{process.env.saveTODOIssues="true";if(!fs.existsSync(CHIPPER_DIST_ESLINT)){await createDirectory(CHIPPER_DIST_ESLINT)}fs.writeFileSync(TODOsFilename,"");console.log("grunt lint --all started");try{await lint(getRepoList(repoList).filter(repo=>repo!=="perennial"&&repo!=="aqua"),{clean:true})}catch(e){console.error("Error running lint --all:\n\n",e);process.exit()}console.log("grunt lint --all finished");const TODOIssues=fs.readFileSync(TODOsFilename).toString().trim().split("\n");const uniqueTODOIssues=_.uniq(TODOIssues);console.log(uniqueTODOIssues.length,"issues to check");const octokit=new Octokit({auth:buildLocal.phetDevGitHubAccessToken});let reopenedCount=0;for(let i=0;i<uniqueTODOIssues.length;i++){const issueURL=uniqueTODOIssues[i];const repoMatch=issueURL.match(/phetsims\/([\w-]+)\/issues/);const issueNumberMatch=issueURL.match(/phetsims\/[\w-]+\/issues\/(\d+)/);if(!repoMatch||!issueNumberMatch){console.error("unexpected issue URL:",issueURL);continue}const repo=repoMatch[1];const issueNumber=issueNumberMatch[1];try{const response=await octokit.request("GET /repos/{owner}/{repo}/issues/{issue_number}",{owner:"phetsims",repo:repo,issue_number:issueNumber});if(response.data.state==="closed"){console.log(`TODO linking to closed issue: ${issueURL}`);await octokit.request("PATCH /repos/{owner}/{repo}/issues/{issue_number}",{owner:"phetsims",repo:repo,issue_number:issueNumber,state:"open"});await octokit.request("POST /repos/{owner}/{repo}/issues/{issue_number}/comments",{owner:"phetsims",repo:repo,issue_number:issueNumber,body:"Reopening because there is a TODO marked for this issue."});reopenedCount++}}catch(e){console.error("Issue does not exist",`${repo}#${issueNumber}`,e)}}console.log(`Finished. Reopened ${reopenedCount}/${uniqueTODOIssues.length} issues`)})();