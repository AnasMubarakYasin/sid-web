const{exec}=require("child_process");exec("git rev-list main",(error,stdout,stderr)=>{if(error){console.error(`exec error: ${error}`);return}if(stderr.length===0&&stdout.length!==0){const lines=stdout.trim().split(/\n/).reverse();console.log("sha	date	LOC	TODO	REVIEW");const visit=function(index){exec(`git checkout ${lines[index]}`,(error,stdout,stderr)=>{exec('grep -ro "TODO" ./js/ | wc -l',(error,stdout,stderr)=>{const todoCount=stdout.trim();exec('grep -ro "REVIEW" ./js/ | wc -l',(error,stdout,stderr)=>{const reviewCount=stdout.trim();exec("git log -1 --format=format:'%ai'",(error,stdout,stderr)=>{const date=stdout.trim();exec("( find ./js/ -name '*.js' -print0 | xargs -0 cat ) | wc -l",(error,stdout,stderr)=>{const lineCount=stdout.trim();console.log(`${lines[index]}	${date}	${lineCount}	${todoCount}	${reviewCount}`);if(index<lines.length-1){visit(index+1)}else{exec("git checkout main",(error,stdout,stderr)=>{})}})})})})})};visit(0)}});