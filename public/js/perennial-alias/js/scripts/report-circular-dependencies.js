import fs from"fs";import _ from"lodash";import execute from"../common/execute.js";import{getRepoList}from"../common/repos/getRepoList.js";import{isOptionKeyProvided}from"../grunt/tasks/util/getOption.js";(async()=>{const isCommonOnly=isOptionKeyProvided("common");const includeSherpa=isOptionKeyProvided("include-sherpa");const commonRepos=getRepoList("active-common-sim-repos");const simRepos=getRepoList("active-sims");const runnableRepos=getRepoList("active-runnables");const potentialRepos=_.uniq([...commonRepos,...isCommonOnly?[]:[...simRepos,...runnableRepos]]).sort();const repos=potentialRepos.filter(repo=>{return fs.existsSync(`../${repo}/js`)});const npxCommand=process.platform.startsWith("win")?"npx.cmd":"npx";const madgeArguments=["madge","--warning","--circular",...includeSherpa?[]:["--exclude","^sherpa/"],...repos.map(repo=>`../${repo}/js`)];const result=await execute(npxCommand,madgeArguments,".",{errors:"resolve"});const regExp=/Found \d+ circular dependen/;const success=!regExp.test(result.stderr);if(!success){const endIndex=result.stdout.indexOf("✖ Skipped ");const trimmed=result.stdout.slice(0,endIndex);console.log(trimmed);console.log(result.stderr)}process.exit(result.code)})();