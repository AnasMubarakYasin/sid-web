const fs=require("fs");(async()=>{const args=process.argv.slice(2);const filename=args[0];const modifier=args[1];const report=fs.readFileSync(filename,"utf8").trim();const lines=report.split("\n").map(sim=>sim.trim());let currentFile=null;lines.forEach(line=>{if(line.endsWith(".ts")&&(line.includes("/")||line.includes("\\"))){currentFile=line}else if(line.includes("error")&&line.endsWith("@typescript-eslint/explicit-member-accessibility")){const substring=line.substring(0,line.indexOf("error"));const terms=substring.trim().split(":");const lineNumber=Number(terms[0]);const column=Number(terms[1]);console.log(currentFile,lineNumber,column);const file=fs.readFileSync(currentFile,"utf8");const lines=file.split("\n");lines[lineNumber-1]=lines[lineNumber-1].substring(0,column-1)+modifier+" "+lines[lineNumber-1].substring(column-1);console.log(lines[lineNumber-1]);fs.writeFileSync(currentFile,lines.join("\n"))}})})();