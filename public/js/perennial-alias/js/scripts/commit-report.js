const execute=require("../common/execute").default;const fs=require("fs");(async()=>{const args=process.argv.slice(2);const username=args[0];const months=["Jan","Feb","March","April","May","June","July","Aug","Sept","Oct","Nov","Dec"];if(!username){console.log("username must be supplied as first command-line argument")}else{const outputtedLines=[];const d=new Date(Date.now());const day=d.getDate();const month=d.getMonth();const year=d.getFullYear();console.log(`${username==="samreid"?"Sam Reid - ":""}${months[month]} ${day}, ${year}`);console.log();console.log("Highlights");console.log("");console.log("Pose Hours: ");const contents=fs.readFileSync("perennial/data/active-repos","utf8").trim();const repos=contents.split("\n").map(sim=>sim.trim());const gitArgs=["--no-pager","log","--all","--remotes","--since=7.days",'--pretty=format:"%an %ad %s"',"--date=relative"];const a=repos.map(repo=>execute("git",gitArgs,`${repo}`,{errors:"resolve"}));const out=await Promise.all(a);for(let i=0;i<a.length;i++){let outputtedRepo=false;const repo=repos[i];const o=out[i];if(o.stderr.trim().length>0){console.log(o.stderr.trim())}const stdout=o.stdout.trim();if(stdout.length>0||o.stderr.trim().length>0){const lines=stdout.split("\n");lines.forEach(line=>{if(line.startsWith('"')&&line.endsWith('"')){line=line.substring(1,line.length-1)}if(line.startsWith(username)){line=line.substring(username.length).trim();const tokens=line.split(" ");const number=Number(tokens[0]);const time=tokens[1];if(time==="days"&&number<=7){line=line.substring("n days ago ".length)}if(time==="hours"&&number<=9){line=line.substring("n hours ago ".length)}if(time==="hours"&&number>=10&&number<=99){line=line.substring("nn hours ago ".length)}if(!outputtedLines.find(x=>x===line)&&!line.startsWith("Merge branch 'main' of")){if(!outputtedRepo){console.log();console.log(repo);outputtedRepo=true}console.log(line);outputtedLines.push(line)}}})}}}})();