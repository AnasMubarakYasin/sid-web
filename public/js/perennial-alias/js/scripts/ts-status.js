const _=require("lodash");const fs=require("fs");const repos=["axon","brand","chipper","dot","joist","kite","mobius","nitroglycerin","perennial","phet-core","phet-io","phetcommon","phetmarks","scenery","scenery-phet","shred","studio","sun","tambo","tandem","tappi","twixt","utterance-queue","vegas"];const jsHeader="JS";const tsHeader="TS";const tsExpectErrorHeader='"@ts-expect-error"';const completeHeader="% Complete";const tableData={};const doesNotCountJS=["-overrides.js"];const percent=(numerator,denominator)=>{return Math.floor(numerator/denominator*100)};const countLines=path=>{const text=fs.readFileSync(path,"utf8");const textLines=text.trim().split(/\r?\n/);return textLines.length};const countWord=(path,word)=>{const occurrence=[];const text=fs.readFileSync(path,"utf8");const textLines=text.trim().split(/\r?\n/);textLines.forEach(line=>{if(line.includes(word)){occurrence.push(word)}});return occurrence.length};const captureData=(path,tableData)=>{let tsCount=0;let jsCount=0;let tsExpectErrorCount=0;const entries=fs.readdirSync(path);entries.forEach(file=>{const newPath=`${path}/${file}`;if(fs.statSync(newPath).isDirectory()){captureData(newPath,tableData)}else if(file.match(/\.js$/)){if(!_.some(doesNotCountJS,string=>file.includes(string))){jsCount+=countLines(newPath)}}else if(file.match(/\.ts$/)){tsCount+=countLines(newPath);tsExpectErrorCount+=countWord(newPath,"@ts-expect-error")}});tableData[jsHeader]+=jsCount;tableData[tsHeader]+=tsCount;tableData[tsExpectErrorHeader]+=tsExpectErrorCount};repos.forEach(repo=>{tableData[repo]={[jsHeader]:0,[tsHeader]:0,[completeHeader]:0,[tsExpectErrorHeader]:0};const repoData=tableData[repo];captureData(`${__dirname}/../../../${repo}/js`,repoData);repoData[completeHeader]=percent(repoData.TS,repoData.TS+repoData.JS)});const rows=Object.values(tableData);const totalJS=_.sumBy(rows,jsHeader);const totalTS=_.sumBy(rows,tsHeader);const totalTSExpectError=_.sumBy(rows,tsExpectErrorHeader);const summary=`
 --------- SUMMARY ----------
 Total ${tsExpectErrorHeader}: ${totalTSExpectError}
 Total ${jsHeader}: ${totalJS}
 Total ${tsHeader}: ${totalTS}
 ${completeHeader}: ${percent(totalTS,totalTS+totalJS)}%
 `;console.log(summary);console.table(tableData);