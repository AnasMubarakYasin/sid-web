const protectGithubBranches=require("../common/protectGithubBranches");const fs=require("fs");const contents=fs.readFileSync("perennial/data/active-repos","utf8").trim();const activeRepos=contents.split("\n").map(sim=>sim.trim());if(activeRepos.includes("perennial-alias")){activeRepos.splice(activeRepos.indexOf("perennial-alias"),1)}(async()=>{await protectGithubBranches.protectBranches(activeRepos)})();