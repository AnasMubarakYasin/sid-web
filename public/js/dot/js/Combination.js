import dot from"./dot.js";let Combination=class Combination{constructor(inclusions){this.inclusions=inclusions}size(){return this.inclusions.length}includes(index){return this.inclusions[index]}apply(array){return array.filter((element,index)=>this.inclusions[index])}inverted(){return new Combination(this.inclusions.map(inclusion=>!inclusion))}getIncludedIndices(){return _.range(0,this.size()).filter(i=>this.inclusions[i])}toString(){return`C[${this.inclusions.map(i=>i?"1":"0").join("")}]`}equals(combination){return this.inclusions.length===combination.inclusions.length&&_.isEqual(this.inclusions,combination.inclusions)}static empty(size){return new Combination(_.range(0,size).map(()=>false))}static full(size){return new Combination(_.range(0,size).map(()=>true))}static combinations(size){const combinations=[];const stack=[];(function recurse(index){if(index===size){combinations.push(new Combination(stack.slice()))}else{stack.push(false);recurse(index+1);stack.pop();stack.push(true);recurse(index+1);stack.pop()}})(0);return combinations}static forEachCombination(array,callback){const stack=[];(function recurse(index){if(index===array.length){callback(stack)}else{recurse(index+1);stack.push(array[index]);recurse(index+1);stack.pop()}})(0)}static combinationsOf(array){const results=[];Combination.forEachCombination(array,result=>{results.push(result.slice())});return results}};dot.register("Combination",Combination);export default Combination;