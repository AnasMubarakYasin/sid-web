import dot from"../dot.js";let generate=false;let z0=0;let z1=0;const TWO_PI=2*Math.PI;export function boxMullerTransform(mu,sigma,random){generate=!generate;if(!generate){return z1*sigma+mu}let u1;let u2;do{u1=random.nextDouble();u2=random.nextDouble()}while(u1<=Number.MIN_VALUE)z0=Math.sqrt(-2*Math.log(u1))*Math.cos(TWO_PI*u2);z1=Math.sqrt(-2*Math.log(u1))*Math.sin(TWO_PI*u2);return z0*sigma+mu}dot.register("boxMullerTransform",boxMullerTransform);