import dot from"../dot.js";import Big from"../../../sherpa/lib/big-6.2.1.js";export function toFixed(value,decimalPlaces){assert&&assert(Number.isInteger(decimalPlaces),`decimal places must be an integer: ${decimalPlaces}`);if(isNaN(value)){return"NaN"}else if(value===Number.POSITIVE_INFINITY){return"Infinity"}else if(value===Number.NEGATIVE_INFINITY){return"-Infinity"}const result=new Big(value).toFixed(decimalPlaces);if(result.startsWith("-0.")&&Number.parseFloat(result)===0){return"0"+result.slice(2)}else{return result}}dot.register("toFixed",toFixed);