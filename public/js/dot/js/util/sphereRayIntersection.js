import dot from"../dot.js";import Vector3 from"../Vector3.js";export function sphereRayIntersection(radius,ray,epsilon){epsilon=epsilon===undefined?1e-5:epsilon;const center=new Vector3(0,0,0);const rayDir=ray.direction;const pos=ray.position;const centerToRay=pos.minus(center);const tmp=rayDir.dot(centerToRay);const centerToRayDistSq=centerToRay.magnitudeSquared;const det=4*tmp*tmp-4*(centerToRayDistSq-radius*radius);if(det<epsilon){return null}const base=rayDir.dot(center)-rayDir.dot(pos);const sqt=Math.sqrt(det)/2;const ta=base-sqt;const tb=base+sqt;if(tb<epsilon){return null}const hitPositionB=ray.pointAtDistance(tb);const normalB=hitPositionB.minus(center).normalized();if(ta<epsilon){return{distance:tb,hitPoint:hitPositionB,normal:normalB.negated(),fromOutside:false}}else{const hitPositionA=ray.pointAtDistance(ta);const normalA=hitPositionA.minus(center).normalized();return{distance:ta,hitPoint:hitPositionA,normal:normalA,fromOutside:true}}}dot.register("sphereRayIntersection",sphereRayIntersection);