import dot from"../dot.js";export default function factorial(n){assert&&assert(Number.isInteger(n)&&n>=0,`n must be a non-negative integer: ${n}`);let f=1;for(let i=2;i<=n;i++){f*=i}return f}dot.register("factorial",factorial);