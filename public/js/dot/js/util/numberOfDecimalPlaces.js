import dot from"../dot.js";export function numberOfDecimalPlaces(value){assert&&assert(typeof value==="number"&&isFinite(value),`value must be a finite number ${value}`);if(Math.floor(value)===value){return 0}else{const string=value.toString();if(string.includes("e")){const split=string.split("e");const mantissa=split[0];const exponent=Number(split[1]);const mantissaDecimalPlaces=mantissa.includes(".")?mantissa.split(".")[1].length:0;return Math.max(mantissaDecimalPlaces-exponent,0)}else{return string.split(".")[1].length}}}dot.register("numberOfDecimalPlaces",numberOfDecimalPlaces);