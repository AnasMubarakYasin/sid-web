import dot from"../dot.js";import Vector2 from"../Vector2.js";export function distToSegmentSquared(point,a,b){const segmentSquaredLength=a.distanceSquared(b);if(segmentSquaredLength===0){return point.distanceSquared(a)}const t=((point.x-a.x)*(b.x-a.x)+(point.y-a.y)*(b.y-a.y))/segmentSquaredLength;let distanceSquared;if(t<0){distanceSquared=point.distanceSquared(a)}else if(t>1){distanceSquared=point.distanceSquared(b)}else{distanceSquared=point.distanceSquared(new Vector2(a.x+t*(b.x-a.x),a.y+t*(b.y-a.y)))}return distanceSquared}dot.register("distToSegmentSquared",distToSegmentSquared);