import dot from"../dot.js";import Vector2 from"../Vector2.js";export function centroidOfPolygon(vertices){const centroid=new Vector2(0,0);let area=0;vertices.forEach((v0,i)=>{const v1=vertices[(i+1)%vertices.length];const doubleShoelace=v0.x*v1.y-v1.x*v0.y;area+=doubleShoelace/2;centroid.addXY((v0.x+v1.x)*doubleShoelace,(v0.y+v1.y)*doubleShoelace)});centroid.divideScalar(6*area);return centroid}dot.register("centroidOfPolygon",centroidOfPolygon);