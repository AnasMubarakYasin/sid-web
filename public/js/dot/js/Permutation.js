import dot from"./dot.js";import{rangeInclusive}from"./util/rangeInclusive.js";let Permutation=class Permutation{constructor(indices){this.indices=indices}size(){return this.indices.length}apply(arrayOrInt){if(typeof arrayOrInt==="number"){return this.indices[arrayOrInt]}else{if(arrayOrInt.length!==this.size()){throw new Error(`Permutation length ${this.size()} not equal to list length ${arrayOrInt.length}`)}const result=new Array(arrayOrInt.length);for(let i=0;i<arrayOrInt.length;i++){result[i]=arrayOrInt[this.indices[i]]}return result}}inverted(){const newPermutation=new Array(this.size());for(let i=0;i<this.size();i++){newPermutation[this.indices[i]]=i}return new Permutation(newPermutation)}withIndicesPermuted(indices){const result=[];Permutation.forEachPermutation(indices,integers=>{const oldIndices=this.indices;const newPermutation=oldIndices.slice(0);for(let i=0;i<indices.length;i++){newPermutation[indices[i]]=oldIndices[integers[i]]}result.push(new Permutation(newPermutation))});return result}toString(){return`P[${this.indices.join(", ")}]`}equals(permutation){return this.indices.length===permutation.indices.length&&_.isEqual(this.indices,permutation.indices)}static identity(size){assert&&assert(size>=0);const indices=new Array(size);for(let i=0;i<size;i++){indices[i]=i}return new Permutation(indices)}static permutations(size){const result=[];Permutation.forEachPermutation(rangeInclusive(0,size-1),integers=>{result.push(new Permutation(integers.slice()))});return result}static forEachPermutation(array,callback){recursiveForEachPermutation(array,[],callback)}static permutationsOf(array){const results=[];Permutation.forEachPermutation(array,result=>{results.push(result.slice())});return results}};dot.register("Permutation",Permutation);function recursiveForEachPermutation(array,prefix,callback){if(array.length===0){callback(prefix)}else{for(let i=0;i<array.length;i++){const element=array[i];const nextArray=array.slice(0);nextArray.splice(i,1);const nextPrefix=prefix.slice(0);nextPrefix.push(element);recursiveForEachPermutation(nextArray,nextPrefix,callback)}}}export default Permutation;