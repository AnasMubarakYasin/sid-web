import Matrix3,{m3,Matrix3Type}from"./Matrix3.js";import Vector2 from"./Vector2.js";QUnit.module("Matrix3");function approximateEquals(assert,a,b,msg){assert.ok(Math.abs(a-b)<1e-8,`${msg} expected: ${b}, result: ${a}`)}const epsilon=1e-5;function approximateEqual(assert,a,b,msg){assert.ok(Math.abs(a-b)<epsilon,`${msg} expected: ${b}, got: ${a}`)}function approximateMatrixEqual(assert,a,b,msg){assert.ok(a.equalsEpsilon(b,epsilon),`${msg} expected: ${b.toString()}, got: ${a.toString()}`)}function A(){return m3(.216673,-.455249,-.0897734,-.261922,-.208968,-.0790977,-.0689069,-.620147,.275399)}function B(){return m3(.366511,-.872824,.490591,.0543773,.610759,.961396,.880586,.991026,-.358927)}function C(){return m3(.521806,.523286,-.275077,.270099,.135544,.614202,0,0,1)}function V2(){return new Vector2(.739498,.827537)}QUnit.test("Affine detection",assert=>{assert.equal(A().type,Matrix3Type.OTHER);assert.equal(B().type,Matrix3Type.OTHER);assert.equal(C().type,Matrix3Type.AFFINE)});QUnit.test("Row-major",assert=>{const m=m3(1,2,3,4,5,6,7,8,9);assert.equal(m.m00(),1,"m00");assert.equal(m.m01(),2,"m01");assert.equal(m.m02(),3,"m02");assert.equal(m.m10(),4,"m10");assert.equal(m.m11(),5,"m11");assert.equal(m.m12(),6,"m12");assert.equal(m.m20(),7,"m20");assert.equal(m.m21(),8,"m21");assert.equal(m.m22(),9,"m22")});QUnit.test("Column-major",assert=>{const m=m3();m.columnMajor(1,4,7,2,5,8,3,6,9);assert.equal(m.m00(),1,"m00");assert.equal(m.m01(),2,"m01");assert.equal(m.m02(),3,"m02");assert.equal(m.m10(),4,"m10");assert.equal(m.m11(),5,"m11");assert.equal(m.m12(),6,"m12");assert.equal(m.m20(),7,"m20");assert.equal(m.m21(),8,"m21");assert.equal(m.m22(),9,"m22")});QUnit.test("Determinant",assert=>{approximateEqual(assert,A().getDeterminant(),-.0717069,"A");approximateEqual(assert,B().getDeterminant(),-1.42292,"B")});QUnit.test("Rotation",assert=>{const angle=Math.PI/6+.2543;approximateEqual(assert,Matrix3.rotation2(angle).getRotation(),angle)});QUnit.test("plus / add",assert=>{const a=A();const b=B();const result=m3(.583184,-1.32807,.400818,-.207545,.401791,.882298,.81168,.370878,-.0835274);approximateMatrixEqual(assert,a.plus(b),result,"plus");approximateMatrixEqual(assert,a,A(),"verifying immutability");approximateMatrixEqual(assert,a.add(b),result,"add");approximateMatrixEqual(assert,a,result,"verifying mutability")});QUnit.test("minus / subtract",assert=>{const a=A();const b=B();const result=m3(-.149837,.417574,-.580365,-.3163,-.819726,-1.04049,-.949493,-1.61117,.634326);approximateMatrixEqual(assert,a.minus(b),result,"minus");approximateMatrixEqual(assert,a,A(),"verifying immutability");approximateMatrixEqual(assert,a.subtract(b),result,"subtract");approximateMatrixEqual(assert,a,result,"verifying mutability")});QUnit.test("transposed / transpose",assert=>{const a=A();const result=m3(.216673,-.261922,-.0689069,-.455249,-.208968,-.620147,-.0897734,-.0790977,.275399);approximateMatrixEqual(assert,a.transposed(),result,"transposed");approximateMatrixEqual(assert,a,A(),"verifying immutability");approximateMatrixEqual(assert,a.transpose(),result,"transpose");approximateMatrixEqual(assert,a,result,"verifying mutability")});QUnit.test("negated / negate",assert=>{const a=A();const result=m3(-.216673,.455249,.0897734,.261922,.208968,.0790977,.0689069,.620147,-.275399);approximateMatrixEqual(assert,a.negated(),result,"negated");approximateMatrixEqual(assert,a,A(),"verifying immutability");approximateMatrixEqual(assert,a.negate(),result,"negate");approximateMatrixEqual(assert,a,result,"verifying mutability")});QUnit.test("inverted / invert",assert=>{const a=A();const result=m3(1.48663,-2.52483,-.240555,-1.08195,-.745893,-.566919,-2.06439,-2.31134,2.29431);approximateMatrixEqual(assert,a.inverted(),result,"inverted");approximateMatrixEqual(assert,a,A(),"verifying immutability");approximateMatrixEqual(assert,a.invert(),result,"invert");approximateMatrixEqual(assert,a,result,"verifying mutability")});QUnit.test("timesMatrix / multiplyMatrix",assert=>{const a=A();const b=B();const result=m3(-.0243954,-.556133,-.299155,-.177013,.0225954,-.301007,.183536,-.0456892,-.72886);approximateMatrixEqual(assert,a.timesMatrix(b),result,"timesMatrix");approximateMatrixEqual(assert,a,A(),"verifying immutability");approximateMatrixEqual(assert,a.multiplyMatrix(b),result,"multiplyMatrix");approximateMatrixEqual(assert,a,result,"verifying mutability")});QUnit.test("timesVector2 / multiplyVector2",assert=>{const c=C();const v=V2();const result=new Vector2(.543836,.926107);approximateMatrixEqual(assert,c.timesVector2(v),result,"timesVector2");approximateMatrixEqual(assert,v,V2(),"verifying immutability");approximateMatrixEqual(assert,c.multiplyVector2(v),result,"multiplyVector2");approximateMatrixEqual(assert,v,result,"verifying mutability")});QUnit.test("timesTransposeVector2 / multiplyTransposeVector2",assert=>{const c=C();const v=V2();c.timesTransposeVector2(v);approximateMatrixEqual(assert,v,V2(),"verifying immutability");approximateMatrixEqual(assert,c.multiplyTransposeVector2(v),c.timesTransposeVector2(V2()),"multiplyTransposeVector2");approximateMatrixEqual(assert,v,c.timesTransposeVector2(V2()),"verifying mutability")});QUnit.test("timesRelativeVector2 / multiplyRelativeVector2",assert=>{const c=C();const v=V2();c.timesRelativeVector2(v);approximateMatrixEqual(assert,v,V2(),"verifying immutability");approximateMatrixEqual(assert,c.multiplyRelativeVector2(v),c.timesRelativeVector2(V2()),"multiplyRelativeVector2");approximateMatrixEqual(assert,v,c.timesRelativeVector2(V2()),"verifying mutability")});QUnit.test("Inverse / Multiplication tests",assert=>{approximateMatrixEqual(assert,Matrix3.IDENTITY.inverted(),Matrix3.IDENTITY,"I * I = I");approximateMatrixEqual(assert,Matrix3.IDENTITY.timesMatrix(A()),A(),"I * A = A");approximateMatrixEqual(assert,A().timesMatrix(Matrix3.IDENTITY),A(),"A * I = A");const translation=Matrix3.translation(2,-5);const rotation=Matrix3.rotation2(Math.PI/6);const scale=Matrix3.scale(2,3);approximateMatrixEqual(assert,translation.timesMatrix(translation.inverted()),Matrix3.IDENTITY,"translation inverse check");approximateMatrixEqual(assert,rotation.timesMatrix(rotation.inverted()),Matrix3.IDENTITY,"rotation inverse check");approximateMatrixEqual(assert,scale.timesMatrix(scale.inverted()),Matrix3.IDENTITY,"scale inverse check");approximateMatrixEqual(assert,A().timesMatrix(A().inverted()),Matrix3.IDENTITY,"A inverse check");approximateMatrixEqual(assert,B().timesMatrix(B().inverted()),Matrix3.IDENTITY,"B inverse check");approximateMatrixEqual(assert,C().timesMatrix(C().inverted()),Matrix3.IDENTITY,"C inverse check")});QUnit.test("Matrix Scaling Tests",assert=>{const scale=Matrix3.scale(-2,3);const scaleVector=scale.getScaleVector();approximateEqual(assert,scaleVector.x,2,"Scale X should be -2");approximateEqual(assert,scaleVector.y,3,"Scale Y should be 3");const beforeScale=scale.timesMatrix(Matrix3.rotation2(Math.PI/2));approximateEqual(assert,beforeScale.getScaleVector().x,3,"before pi/2 rotation x");approximateEqual(assert,beforeScale.getScaleVector().y,2,"before pi/2 rotation y");const afterScale=Matrix3.rotation2(Math.PI/2).timesMatrix(scale);approximateEqual(assert,afterScale.getScaleVector().x,2,"after pi/2 rotation x");approximateEqual(assert,afterScale.getScaleVector().y,3,"after pi/2 rotation y");const rotation=Matrix3.rotation2(.35264);approximateEqual(assert,rotation.getScaleVector().x,1,"rotation x");approximateEqual(assert,rotation.getScaleVector().y,1,"rotation x")});QUnit.test("Matrix scaling()",assert=>{const rotation=Matrix3.rotation2(Math.PI/4);const translation=Matrix3.translation(20,30);const scale2=Matrix3.scaling(2);const scale2x3y=Matrix3.scaling(2,3);assert.equal(scale2.getScaleVector().x,2,"normal x scale");assert.equal(scale2.getScaleVector().y,2,"normal y scale");assert.equal(scale2x3y.getScaleVector().x,2,"normal x scale");assert.equal(scale2x3y.getScaleVector().y,3,"normal y scale");const combination=rotation.timesMatrix(scale2).timesMatrix(translation);approximateEquals(assert,combination.getScaleVector().x,2,"rotated x scale");approximateEquals(assert,combination.getScaleVector().y,2,"rotated x scale")});