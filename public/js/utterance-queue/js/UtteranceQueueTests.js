import stepTimer from"../../axon/js/stepTimer.js";import{QueryStringMachine}from"../../query-string-machine/js/QueryStringMachineModule.js";import voicingManager from"../../scenery/js/accessibility/voicing/voicingManager.js";import DisplayGlobals from"../../scenery/js/display/DisplayGlobals.js";import responseCollector from"./responseCollector.js";import SpeechSynthesisAnnouncer from"./SpeechSynthesisAnnouncer.js";import Utterance from"./Utterance.js";import UtteranceQueue from"./UtteranceQueue.js";import UtteranceQueueTestUtils from"./UtteranceQueueTestUtils.js";const queryParameters=QueryStringMachine.getAll({manualInput:{type:"flag"}});const VOICING_UTTERANCE_INTERVAL=125;const TIMING_BUFFER=VOICING_UTTERANCE_INTERVAL+50;const DEFAULT_VOICE_TIMEOUT=3e3;const testVoicingManager=new voicingManager.constructor;const testVoicingUtteranceQueue=new UtteranceQueue(testVoicingManager);const setDefaultVoice=async()=>{let resolved=false;return new Promise(resolve=>{const setIt=()=>{if(!resolved){testVoicingManager.voiceProperty.value=testVoicingManager.voicesProperty.value[0]||null;clearTimeout(timeout);resolved=true;resolve()}};const timeout=setTimeout(()=>{setIt()},DEFAULT_VOICE_TIMEOUT);if(testVoicingManager.voicesProperty.value.length>0){setIt()}else{testVoicingManager.voicesProperty.lazyLink(()=>{setIt()})}})};testVoicingManager.initialize(DisplayGlobals.userGestureEmitter);testVoicingManager.enabledProperty.value=true;function timeout(ms){return new Promise(resolve=>setTimeout(resolve,ms))}let alerts=[];const noCancelOptions={cancelSelf:false,cancelOther:false};const timeUtterance=utterance=>{return new Promise(resolve=>{const startTime=Date.now();testVoicingUtteranceQueue.addToBack(utterance);testVoicingManager.announcementCompleteEmitter.addListener(function toRemove(completeUtterance){if(completeUtterance===utterance){resolve(Date.now()-startTime);testVoicingManager.announcementCompleteEmitter.removeListener(toRemove)}})})};const getSpeakingUtterance=()=>{return testVoicingManager["speakingSpeechSynthesisUtteranceWrapper"]?testVoicingManager["speakingSpeechSynthesisUtteranceWrapper"].utterance:null};const firstUtterance=new Utterance({alert:"This is the first utterance",alertDelay:0,announcerOptions:noCancelOptions});const secondUtterance=new Utterance({alert:"This is the second utterance",alertDelay:0,announcerOptions:noCancelOptions});const thirdUtterance=new Utterance({alert:"This is the third utterance",alertDelay:0,announcerOptions:noCancelOptions});let timeForFirstUtterance;let timeForSecondUtterance;let timeForThirdUtterance;let intervalID;QUnit.module("UtteranceQueue",{before:async()=>{const timerInterval=1/60;let previousTime=Date.now();intervalID=window.setInterval(()=>{const currentTime=Date.now();const elapsedTime=currentTime-previousTime;stepTimer.emit(elapsedTime/1e3);previousTime=currentTime},timerInterval*1e3);testVoicingManager.announcementCompleteEmitter.addListener(utterance=>{alerts.unshift(utterance)});if(queryParameters.manualInput){await timeout(3e3);timeForFirstUtterance=await timeUtterance(firstUtterance);timeForSecondUtterance=await timeUtterance(secondUtterance);timeForThirdUtterance=await timeUtterance(thirdUtterance);if(timeForFirstUtterance<1200||timeForSecondUtterance<1200||timeForThirdUtterance<1200){console.log(`timeForFirstUtterance: ${timeForFirstUtterance}, timeForThirdUtterance: ${timeForSecondUtterance}, timeForThirdUtterane: ${timeForThirdUtterance}`);throw new Error("time for Utterances is too short, did you click in the window before the first test started?")}}alerts=[];await setDefaultVoice()},beforeEach:async()=>{testVoicingUtteranceQueue.cancel();firstUtterance.priorityProperty.value=1;secondUtterance.priorityProperty.value=1;thirdUtterance.priorityProperty.value=1;await UtteranceQueueTestUtils.beforeEachTimingWorkarounds();responseCollector.reset();alerts=[]},after(){clearInterval(intervalID)}});QUnit.test("Welcome to UtteranceQueueTests!",async assert=>{assert.ok(true,"UtteranceQueue tests take time, run with ?manualInput and click in the window before the first test")});QUnit.test("prioritize utterances on add to back",async assert=>{const utterance1=new Utterance({alert:"1",priority:5});const utterance2=new Utterance({alert:"2",priority:1});const utterance3=new Utterance({alert:"3",priority:1});const utterance4=new Utterance({alert:"4",priority:1,announcerOptions:{cancelOther:false}});const utterance5=new Utterance({alert:"5",priority:1,announcerOptions:{cancelOther:false}});const speechSynthesisAnnouncer=new SpeechSynthesisAnnouncer;speechSynthesisAnnouncer.hasSpoken=true;const utteranceQueue=new UtteranceQueue(speechSynthesisAnnouncer);assert.ok(utteranceQueue["queue"].length===0,"nothing man");utteranceQueue.addToBack(utterance1);assert.ok(utteranceQueue["queue"].length===1,"one add to back");utteranceQueue.addToBack(utterance2);assert.ok(utteranceQueue["queue"].length===2,"one add to back");utteranceQueue.addToBack(utterance3);assert.ok(utteranceQueue["queue"].length===2,"one add to back");assert.ok(utteranceQueue["queue"][0].utterance===utterance1,"one add to back");assert.ok(utteranceQueue["queue"][1].utterance===utterance3,"utterance3 removed utterance1 because cancelOther:true");utteranceQueue.addToBack(utterance4);assert.ok(utteranceQueue["queue"].length===3,"one add to back");assert.ok(utteranceQueue["queue"][0].utterance===utterance1,"one add to back");assert.ok(utteranceQueue["queue"][1].utterance===utterance3,"utterance3 removed utterance1 because cancelOther:true");assert.ok(utteranceQueue["queue"][2].utterance===utterance4,"utterance4 does not removed utterance3 because cancelOther:true");utteranceQueue.addToBack(utterance5);assert.ok(utteranceQueue["queue"].length===4,"one add to back");assert.ok(utteranceQueue["queue"][0].utterance===utterance1,"one add to back");assert.ok(utteranceQueue["queue"][1].utterance===utterance3,"utterance3 removed utterance1 because cancelOther:true");assert.ok(utteranceQueue["queue"][2].utterance===utterance4,"utterance4 does not removed utterance3 because cancelOther:true");assert.ok(utteranceQueue["queue"][3].utterance===utterance5,"utterance4 does not removed utterance3 because cancelOther:true");utterance5.priorityProperty["setPropertyValue"](3);utterance4.priorityProperty.value=2;assert.ok(utteranceQueue["queue"].length===2,"one add to back");assert.ok(utteranceQueue["queue"][0].utterance===utterance1,"one add to back");assert.ok(utteranceQueue["queue"][1].utterance===utterance5,"utterance5 kicked utterance4 outta the park.")});QUnit.test("interruptible=false resists equal priority cancellation",assert=>{const utterance1=new Utterance({alert:"1",priority:1,interruptible:false});const utterance2=new Utterance({alert:"2",priority:1});const announcer=new SpeechSynthesisAnnouncer;announcer.hasSpoken=true;const utteranceQueue=new UtteranceQueue(announcer);utteranceQueue.addToBack(utterance1);utteranceQueue.addToBack(utterance2);assert.ok(utteranceQueue["queue"].length===2,"equal priority should not cancel non-interruptible utterance");assert.ok(utteranceQueue["queue"][0].utterance===utterance1,"non-interruptible utterance remains in queue")});QUnit.test("interruptible=false resists lower priority cancellation",assert=>{const utterance1=new Utterance({alert:"1",priority:2,interruptible:false});const utterance2=new Utterance({alert:"2",priority:1});const announcer=new SpeechSynthesisAnnouncer;announcer.hasSpoken=true;const utteranceQueue=new UtteranceQueue(announcer);utteranceQueue.addToBack(utterance1);utteranceQueue.addToBack(utterance2);assert.ok(utteranceQueue["queue"].length===2,"lower priority should not cancel non-interruptible utterance");assert.ok(utteranceQueue["queue"][0].utterance===utterance1,"non-interruptible utterance remains in queue")});QUnit.test("interruptible=false allows higher priority cancellation",assert=>{const utterance1=new Utterance({alert:"1",priority:1,interruptible:false});const utterance2=new Utterance({alert:"2",priority:2});const announcer=new SpeechSynthesisAnnouncer;announcer.hasSpoken=true;const utteranceQueue=new UtteranceQueue(announcer);utteranceQueue.addToBack(utterance1);utteranceQueue.addToBack(utterance2);assert.ok(utteranceQueue["queue"].length===1,"higher priority cancels non-interruptible utterance");assert.ok(utteranceQueue["queue"][0].utterance===utterance2,"higher priority utterance remains")});QUnit.test("interruptible=true keeps default equal priority cancellation",assert=>{const utterance1=new Utterance({alert:"1",priority:1});const utterance2=new Utterance({alert:"2",priority:1});const announcer=new SpeechSynthesisAnnouncer;announcer.hasSpoken=true;const utteranceQueue=new UtteranceQueue(announcer);utteranceQueue.addToBack(utterance1);utteranceQueue.addToBack(utterance2);assert.ok(utteranceQueue["queue"].length===1,"equal priority cancels with default interruptible behavior");assert.ok(utteranceQueue["queue"][0].utterance===utterance2,"newest utterance remains")});QUnit.test("interruptible=true allows replacement of multiple queued utterances",assert=>{const utterance1=new Utterance({alert:"1",priority:1});const utterance2=new Utterance({alert:"2",priority:1});const utterance3=new Utterance({alert:"3",priority:1});const announcer=new SpeechSynthesisAnnouncer;announcer.hasSpoken=true;const utteranceQueue=new UtteranceQueue(announcer);utteranceQueue.addToBack(utterance1);utteranceQueue.addToBack(utterance2);utteranceQueue.addToBack(utterance3);assert.ok(utteranceQueue["queue"].length===1,"newest utterance replaces earlier interruptible utterances");assert.ok(utteranceQueue["queue"][0].utterance===utterance3,"newest utterance remains")});if(testVoicingManager.voicesProperty.value>0){QUnit.test("utterance.announcerOptions.voice",async assert=>{const done=assert.async();testVoicingManager.voiceProperty.value=null;const voice=testVoicingManager.voicesProperty.value[0];const utterance=new Utterance({alert:"one",announcerOptions:{voice:voice}});testVoicingManager.endSpeakingEmitter.addListener(function myListener(){const x=testVoicingManager["speakingSpeechSynthesisUtteranceWrapper"];assert.ok(x,"we should have one");assert.ok(x.speechSynthesisUtterance.voice===voice,"voice should match the provided utterance's");testVoicingManager.endSpeakingEmitter.removeListener(myListener);done()});testVoicingManager.speakIgnoringEnabled(utterance);testVoicingManager.voiceProperty.value=voice})}if(queryParameters.manualInput){QUnit.test("Basic UtteranceQueue test",async assert=>{testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);await timeout(timeForFirstUtterance+timeForSecondUtterance+timeForThirdUtterance+TIMING_BUFFER*3);assert.ok(alerts.length===3,"Three basic Utterances went through the queue")});QUnit.test("cancelUtterance tests",async assert=>{testVoicingUtteranceQueue.addToBack(firstUtterance);await timeout(timeForFirstUtterance/2);testVoicingUtteranceQueue.cancelUtterance(firstUtterance);testVoicingUtteranceQueue.addToBack(firstUtterance);assert.ok(alerts[0]===firstUtterance,"firstUtterance was cancelled");assert.ok(testVoicingUtteranceQueue["queue"].length===1,"There is one Utterance in the queue")});QUnit.test("PriorityProperty interruption",async assert=>{testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);assert.ok(testVoicingUtteranceQueue["queue"].length===3,"All three utterances in the queue");thirdUtterance.priorityProperty.value=2;assert.ok(testVoicingUtteranceQueue["queue"].length===1,"Only the one Utterance remains");assert.ok(testVoicingUtteranceQueue["queue"][0].utterance===thirdUtterance,"Only the third Utterance remains")});QUnit.test("Announced Utterance can also be in queue and interruption during announcement",async assert=>{testVoicingUtteranceQueue.addToBack(firstUtterance);await timeout(timeForFirstUtterance/2);testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);await timeout(timeForFirstUtterance);firstUtterance.priorityProperty.value=0;await timeout(timeForSecondUtterance/2);assert.ok(getSpeakingUtterance()===secondUtterance,"Utterance being announced still observes priorityProperty");assert.ok(testVoicingUtteranceQueue["queue"].length===0,"queue empty after interruption and sending secondUtterance to Announcer")});QUnit.test("Higher priority removes earlier Utterances from queue",async assert=>{testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);assert.ok(testVoicingUtteranceQueue["queue"].length===3,"All three utterances in the queue");secondUtterance.priorityProperty.value=2;await timeout(timeForSecondUtterance/2);assert.ok(getSpeakingUtterance()===secondUtterance,"The secondUtterance interrupted the firstUtterance because it is higher priority.");await timeout(timeForSecondUtterance/2+timeForThirdUtterance/2);assert.ok(alerts[0]===secondUtterance,"secondUtterance spoken in full");assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance spoken after secondUtterance finished")});QUnit.test("Utterance removed because of self priority reduction before another is added to queue",async assert=>{firstUtterance.priorityProperty.value=10;testVoicingUtteranceQueue.addToBack(firstUtterance);firstUtterance.priorityProperty.value=0;testVoicingUtteranceQueue.addToBack(thirdUtterance);await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance spoken because firstUtterance.priorityProperty was reduced before adding thirdUtterance to the queue")});QUnit.test("Utterance removed because of self priority reduction after another is added to queue",async assert=>{firstUtterance.priorityProperty.value=10;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);firstUtterance.priorityProperty.value=0;await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance spoken because firstUtterance.priorityProperty was reduced after adding thirdUtterance to the queue")});QUnit.test("Utterance interruption because self priority reduced while being announced",async assert=>{firstUtterance.priorityProperty.value=10;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===firstUtterance);firstUtterance.priorityProperty.value=0;await timeout(timeForFirstUtterance/4);assert.ok(alerts[0]===firstUtterance,"firstUtterance was interrupted because its priority was reduced while it was being announced");await timeout(timeForThirdUtterance/2);assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance being announced after interrupting firstUtterance")});QUnit.test("Utterance interruption during annoumcement because another in the queue made higher priority",async assert=>{firstUtterance.priorityProperty.value=0;thirdUtterance.priorityProperty.value=0;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===firstUtterance,"firstUtterance being announced");thirdUtterance.priorityProperty.value=3;await timeout(timeForFirstUtterance/4);assert.ok(alerts[0]===firstUtterance,"firstUtterance was interrupted because an Utterance in the queue was made higher priority");await timeout(timeForThirdUtterance/2);assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance being announced after interrupting firstUtterance")});QUnit.test("Utterance NOT interrupted because self priority still relatively higher",async assert=>{firstUtterance.priorityProperty.value=10;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);await timeout(timeForFirstUtterance/2);firstUtterance.priorityProperty.value=5;await timeout(timeForFirstUtterance/10);assert.ok(alerts.length===0,"firstUtterance was not interrupted because priority was set to a value higher than next utterance in queue");await timeout(timeForThirdUtterance/2+timeForFirstUtterance/2);assert.ok(alerts[0]===firstUtterance,"firstUtterance finished being announced");assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance being announced after waiting for firstUtterance")});QUnit.test("announceImmediately",async assert=>{testVoicingUtteranceQueue.announceImmediately(firstUtterance);assert.ok(testVoicingUtteranceQueue["queue"].length===0,"announceImmediately should be synchronous with voicingManager for an empty queue");await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===firstUtterance,"first utterance spoken immediately")});QUnit.test("announceImmediately reduces duplicate Utterances in queue",async assert=>{testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);testVoicingUtteranceQueue.addToBack(thirdUtterance);testVoicingUtteranceQueue.announceImmediately(firstUtterance);await timeout(timeForFirstUtterance/2);assert.ok(testVoicingUtteranceQueue["queue"].length===2,"announcing firstUtterance immediately should remove the duplicate firstUtterance in the queue");assert.ok(getSpeakingUtterance()===firstUtterance,"first utterance is being spoken after announceImmediately")});QUnit.test("announceImmediately does nothing when Utterance has low priority relative to queued Utterances",async assert=>{testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);firstUtterance.priorityProperty.value=2;thirdUtterance.priorityProperty.value=1;testVoicingUtteranceQueue.announceImmediately(thirdUtterance);assert.ok(testVoicingUtteranceQueue["queue"].length===2,"only first and second utterances in the queue");assert.ok(!testVoicingUtteranceQueue.hasUtterance(thirdUtterance),"thirdUtterance not in queue after announceImmediately");await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===firstUtterance);assert.ok(alerts[0]!==thirdUtterance,"thirdUtterance was not spoken with announceImmediately")});QUnit.test("anounceImmediatelety does nothing when Utterance has low priority relative to announcing Utterance",async assert=>{firstUtterance.priorityProperty.value=1;thirdUtterance.priorityProperty.value=1;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);firstUtterance.priorityProperty.value=2;thirdUtterance.priorityProperty.value=1;await timeout(timeForFirstUtterance/2);testVoicingUtteranceQueue.announceImmediately(thirdUtterance);await timeout(timeForFirstUtterance/4);assert.ok(getSpeakingUtterance()!==thirdUtterance,"announceImmediately should not interrupt a higher priority utterance");assert.ok(!testVoicingUtteranceQueue.hasUtterance(thirdUtterance),"lower priority thirdUtterance should be dropped from the queue")});QUnit.test("Utterance spoken with announceImmediately should be interrupted if priority is reduced",async assert=>{firstUtterance.priorityProperty.value=2;thirdUtterance.priorityProperty.value=2;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);testVoicingUtteranceQueue.announceImmediately(thirdUtterance);await timeout(timeForThirdUtterance/2);assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance is announced immediately");thirdUtterance.priorityProperty.value=1;await timeout(timeForThirdUtterance/4);assert.ok(alerts[0]===thirdUtterance,"third utterance was interrupted by reducing its priority");await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===firstUtterance,"moved on to next utterance in queue")});QUnit.test("Utterance spoken by announceImmediately is interrupted by raising priority of queued utterance",async assert=>{firstUtterance.priorityProperty.value=1;thirdUtterance.priorityProperty.value=1;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);testVoicingUtteranceQueue.announceImmediately(thirdUtterance);await timeout(timeForThirdUtterance/2);assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance is announced immediately");firstUtterance.priorityProperty.value=2;await timeout(timeForThirdUtterance/4);assert.ok(alerts[0]===thirdUtterance,"third utterance was interrupted by the next Utterance increasing priority");await timeout(timeForFirstUtterance/2);assert.ok(getSpeakingUtterance()===firstUtterance,"moved on to higher priority utterance in queue")});QUnit.test("announceImmediately interrupts another Utterance if new Utterance is high priority",async assert=>{firstUtterance.priorityProperty.value=1;thirdUtterance.priorityProperty.value=2;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);await timeout(timeForFirstUtterance/2);testVoicingUtteranceQueue.announceImmediately(thirdUtterance);await timeout(timeForFirstUtterance/4);assert.ok(alerts[0]===firstUtterance,"firstUtterance interrupted because it had lower priority");await timeout(timeForThirdUtterance/2);assert.ok(getSpeakingUtterance()===thirdUtterance,"thirdUtterance spoken immediately")});QUnit.test("announceImmediately will not interrupt Utterance of equal or lesser priority ",async assert=>{firstUtterance.priorityProperty.value=1;thirdUtterance.priorityProperty.value=1;testVoicingUtteranceQueue.addToBack(firstUtterance);testVoicingUtteranceQueue.addToBack(secondUtterance);await timeout(timeForFirstUtterance/2);testVoicingUtteranceQueue.announceImmediately(thirdUtterance);await timeout(timeForFirstUtterance/4);assert.ok(getSpeakingUtterance()===firstUtterance,"firstUtterance not interrupted, it has equal priority");assert.ok(testVoicingUtteranceQueue["queue"][0].utterance===secondUtterance,"thirdUtterance was added to the front of the queue");assert.ok(testVoicingUtteranceQueue["queue"].length===1,"thirdUtterance was not added to queue and will never be announced");await timeout(timeForFirstUtterance/4+timeForThirdUtterance/2);assert.ok(alerts[0]===firstUtterance,"firstUtterance spoken in full")})}