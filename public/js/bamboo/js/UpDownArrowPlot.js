import Vector2 from"../../dot/js/Vector2.js";import optionize from"../../phet-core/js/optionize.js";import ArrowNode from"../../scenery-phet/js/ArrowNode.js";import Node from"../../scenery/js/nodes/Node.js";import{PAINTABLE_DEFAULT_OPTIONS}from"../../scenery/js/nodes/Paintable.js";const DEFAULT_ARROW_PAINTABLE_OPTIONS={fill:"black"};let UpDownArrowPlot=class UpDownArrowPlot extends Node{constructor(chartTransform,dataSet,providedOptions){const options=optionize()({arrowNodeOptions:{},pointToPaintableFields:point=>DEFAULT_ARROW_PAINTABLE_OPTIONS},providedOptions);super(options);assert&&assert(Object.keys(options.arrowNodeOptions).filter(key=>Object.keys(PAINTABLE_DEFAULT_OPTIONS).includes(key)).length===0,"arrowNodeOptions should not include Paintable options, use pointToPaintableFields instead");this.chartTransform=chartTransform;this.dataSet=dataSet;this.pointToPaintableFields=options.pointToPaintableFields;this.arrowNodeOptions=options.arrowNodeOptions;this.arrowNodes=[];this.setDataSet(dataSet);const changedListener=()=>this.update();chartTransform.changedEmitter.addListener(changedListener);this.disposeUpDownArrowPLot=()=>chartTransform.changedEmitter.removeListener(changedListener)}setDataSet(dataSet){this.dataSet=dataSet;this.update()}update(){while(this.arrowNodes.length<this.dataSet.length){const arrowNode=new ArrowNode(0,0,0,0,this.arrowNodeOptions);this.arrowNodes.push(arrowNode);this.addChild(arrowNode)}while(this.arrowNodes.length>this.dataSet.length){const arrowNode=this.arrowNodes.pop();this.removeChild(arrowNode)}for(let i=0;i<this.arrowNodes.length;i++){const dataPoint=this.dataSet[i];const tail=this.chartTransform.modelToViewPosition(new Vector2(dataPoint.x,0));const tip=this.chartTransform.modelToViewPosition(dataPoint);this.arrowNodes[i].setTailAndTip(tail.x,tail.y,tip.x,tip.y);const providedOptions=this.pointToPaintableFields(dataPoint);assert&&assert(Object.keys(providedOptions).filter(key=>!Object.keys(PAINTABLE_DEFAULT_OPTIONS).includes(key)).length===0,"options contain keys that could be dangerous for mutate");this.arrowNodes[i].mutate(providedOptions)}}dispose(){this.disposeUpDownArrowPLot();super.dispose()}};export default UpDownArrowPlot;