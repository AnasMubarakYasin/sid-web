import optionize,{combineOptions}from"../../phet-core/js/optionize.js";import Orientation from"../../phet-core/js/Orientation.js";import ArrowNode from"../../scenery-phet/js/ArrowNode.js";import FlowBox from"../../scenery/js/layout/nodes/FlowBox.js";import Line from"../../scenery/js/nodes/Line.js";import Node from"../../scenery/js/nodes/Node.js";const notificationThreshold=1e-13;const ARROW_NODE_OPTIONS={doubleHead:true,headHeight:4.5,headWidth:5,tailWidth:1.5,stroke:null};let SpanNode=class SpanNode extends FlowBox{constructor(chartTransform,axisOrientation,delta,labelNode,providedOptions){assert&&assert(axisOrientation!==Orientation.VERTICAL,"Orientation.VERTICAL is not yet supported");const options=optionize()({color:"black",outerLineLength:6,spacing:0},providedOptions);super();this.chartTransform=chartTransform;this.axisOrientation=axisOrientation;this.delta=delta;this.labelNode=labelNode;this.color=options.color;this.outerLineLength=options.outerLineLength;this.viewWidth=0;this.arrowNodeOptions=combineOptions({fill:options.color},ARROW_NODE_OPTIONS,options.arrowNodeOptions);this.update();this.mutate(options);const changedListener=()=>this.update();chartTransform.changedEmitter.addListener(changedListener);this.disposeSpanNode=()=>chartTransform.changedEmitter.removeListener(changedListener)}setDelta(delta){if(delta!==this.delta){this.delta=delta;this.update()}}update(){const viewWidth=this.chartTransform.modelToViewDelta(this.axisOrientation,this.delta);if(Math.abs(viewWidth-this.viewWidth)>notificationThreshold){this.viewWidth=viewWidth;const createBar=centerX=>new Line(0,0,0,this.outerLineLength,{stroke:this.color,centerX:centerX});const leftBar=createBar(0);const rightBar=createBar(viewWidth);const arrowNode=new ArrowNode(leftBar.right,leftBar.centerY,rightBar.left,rightBar.centerY,this.arrowNodeOptions);const arrowWithBars=new Node({children:[leftBar,rightBar,arrowNode]});this.labelNode.maxWidth=arrowWithBars.width;this.children=[arrowWithBars,this.labelNode]}}dispose(){this.disposeSpanNode();super.dispose()}};export default SpanNode;