import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";let ScatterPlot=class ScatterPlot extends Path{constructor(chartTransform,dataSet,providedOptions){const options=optionize()({radius:2,fill:"black"},providedOptions);super(null,options);this.chartTransform=chartTransform;this.dataSet=dataSet;this.radius=options.radius;this.update();const changedListener=()=>this.update();chartTransform.changedEmitter.addListener(changedListener);this.disposeScatterPlot=()=>chartTransform.changedEmitter.removeListener(changedListener)}setDataSet(dataSet){this.dataSet=dataSet;this.update()}update(){const shape=new Shape;const length=this.dataSet.length;for(let i=0;i<length;i++){const dataPoint=this.dataSet[i];if(dataPoint.isFinite()){const viewPoint=this.chartTransform.modelToViewPosition(dataPoint);shape.moveTo(viewPoint.x+this.radius,viewPoint.y);shape.circle(viewPoint.x,viewPoint.y,this.radius)}}this.shape=shape.makeImmutable()}dispose(){this.disposeScatterPlot();super.dispose()}};export default ScatterPlot;