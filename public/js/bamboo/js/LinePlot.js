import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import Path from"../../scenery/js/nodes/Path.js";let LinePlot=class LinePlot extends Path{constructor(chartTransform,dataSet,providedOptions){const options=optionize()({stroke:"black"},providedOptions);super(null,options);this.chartTransform=chartTransform;this.dataSet=dataSet;this.update();const changedListener=()=>this.update();chartTransform.changedEmitter.addListener(changedListener);this.disposeLinePlot=()=>chartTransform.changedEmitter.removeListener(changedListener)}setDataSet(dataSet){this.dataSet=dataSet;this.update()}update(){const shape=new Shape;let moveToNextPoint=true;for(let i=0;i<this.dataSet.length;i++){const dataPoint=this.dataSet[i];assert&&assert(dataPoint===null||dataPoint.isFinite(),"data points must be finite Vector2 or null");if(dataPoint){const viewPoint=this.chartTransform.modelToViewPosition(dataPoint);if(moveToNextPoint){shape.moveToPoint(viewPoint);moveToNextPoint=false}else{shape.lineToPoint(viewPoint)}}else{moveToNextPoint=true}}this.shape=shape.makeImmutable()}dispose(){this.disposeLinePlot();super.dispose()}};export default LinePlot;