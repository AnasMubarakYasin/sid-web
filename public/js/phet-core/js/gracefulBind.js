import affirm from"../../perennial-alias/js/browser-and-node/affirm.js";import phetCore from"./phetCore.js";import _ from"../../sherpa/js/lodash.js";const gracefulBind=path=>{affirm(path.split(".").length>1,"path must have multiple parts");affirm(path.trim()===path,"path must be trimmed");const terms=path.split(".");const method=terms.pop();const object=_.get(window,terms);return object?object[method].bind(object):null};phetCore.register("gracefulBind",gracefulBind);export default gracefulBind;