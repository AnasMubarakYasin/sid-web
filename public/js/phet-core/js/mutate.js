import affirm from"../../perennial-alias/js/browser-and-node/affirm.js";import _ from"../../sherpa/js/lodash.js";import phetCore from"./phetCore.js";function mutate(target,orderedKeys,options){affirm(target);affirm(Array.isArray(orderedKeys));if(!options){return}affirm(Object.getPrototypeOf(options)===Object.prototype,"Extra prototype on options object is a code smell");_.each(orderedKeys,key=>{affirm(!options.hasOwnProperty(key)||options[key]!==undefined,`Undefined not allowed for key: ${key}`);if(options[key]!==undefined){target[key]=options[key]}})}phetCore.register("mutate",mutate);export default mutate;