import affirm from"../../perennial-alias/js/browser-and-node/affirm.js";import phetCore from"./phetCore.js";function interleave(arr,generator){affirm(Array.isArray(arr));const result=[];const finalLength=arr.length*2-1;for(let i=0;i<finalLength;i++){if(i%2===0){result.push(arr[i/2])}else{result.push(generator((i-1)/2))}}return result}phetCore.register("interleave",interleave);export default interleave;