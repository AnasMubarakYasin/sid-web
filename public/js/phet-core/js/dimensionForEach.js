import phetCore from"./phetCore.js";function dimensionForEach(dimension,array,forEach){const indices=[];function recur(dim,arr){return arr.forEach((element,index)=>{indices.push(index);if(dim===1){forEach(element,...indices)}else{recur(dim-1,element)}indices.pop()})}return recur(dimension,array)}phetCore.register("dimensionForEach",dimensionForEach);export default dimensionForEach;