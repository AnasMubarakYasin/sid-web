import affirm from"../../perennial-alias/js/browser-and-node/affirm.js";import phetCore from"./phetCore.js";function partition(array,predicate){affirm(Array.isArray(array));const satisfied=[];const unsatisfied=[];const length=array.length;for(let i=0;i<length;i++){if(predicate(array[i])){satisfied.push(array[i])}else{unsatisfied.push(array[i])}}return[satisfied,unsatisfied]}phetCore.register("partition",partition);export default partition;