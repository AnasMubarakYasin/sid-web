import LayoutCell from"../../layout/constraints/LayoutCell.js";import LayoutConstraint from"../../layout/constraints/LayoutConstraint.js";import Node from"../../nodes/Node.js";import scenery from"../../scenery.js";export default class RelaxedManualConstraint extends LayoutConstraint{constructor(ancestorNode,nodes,layoutCallback){assert&&assert(Array.isArray(nodes)&&_.every(nodes,node=>node instanceof Node));super(ancestorNode);this.lock();this.nodes=nodes;this.cells=nodes.map(node=>new LayoutCell(this,node,null));this.layoutCallback=layoutCallback;this.nodes.forEach(node=>this.addNode(node,false));this.unlock();this.updateLayout()}layout(){super.layout();assert&&assert(_.every(this.nodes,node=>!node.isDisposed));const proxies=this.cells.map(cell=>cell.isConnected()?cell.proxy:null);this.layoutCallback.apply(null,proxies);this.finishedLayoutEmitter.emit()}dispose(){this.cells.forEach(cell=>cell.dispose());super.dispose()}static create(ancestorNode,nodes,layoutCallback){return new RelaxedManualConstraint(ancestorNode,nodes,layoutCallback)}}scenery.register("RelaxedManualConstraint",RelaxedManualConstraint);