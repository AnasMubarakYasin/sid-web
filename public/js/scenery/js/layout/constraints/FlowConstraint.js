import Bounds2 from"../../../../dot/js/Bounds2.js";import arrayRemove from"../../../../phet-core/js/arrayRemove.js";import mutate from"../../../../phet-core/js/mutate.js";import Orientation from"../../../../phet-core/js/Orientation.js";import{FLOW_CONFIGURABLE_OPTION_KEYS}from"../../layout/constraints/FlowConfigurable.js";import FlowConfigurable from"../../layout/constraints/FlowConfigurable.js";import FlowLine from"../../layout/constraints/FlowLine.js";import LayoutAlign from"../../layout/LayoutAlign.js";import LayoutJustification from"../../layout/LayoutJustification.js";import{default as NodeLayoutConstraint}from"../../layout/constraints/NodeLayoutConstraint.js";import scenery from"../../scenery.js";const FLOW_CONSTRAINT_OPTION_KEYS=[...FLOW_CONFIGURABLE_OPTION_KEYS,"spacing","lineSpacing","justify","justifyLines","wrap","excludeInvisible"];export default class FlowConstraint extends FlowConfigurable(NodeLayoutConstraint){constructor(ancestorNode,providedOptions){super(ancestorNode,providedOptions),this.cells=[],this._justify=LayoutJustification.SPACE_BETWEEN,this._justifyLines=null,this._wrap=false,this._spacing=0,this._lineSpacing=0,this.displayedCells=[];this.setConfigToBaseDefault();this.mutateConfigurable(providedOptions);mutate(this,FLOW_CONSTRAINT_OPTION_KEYS,providedOptions);this.changedEmitter.addListener(this._updateLayoutListener);this.orientationChangedEmitter.addListener(()=>this.cells.forEach(cell=>{cell.orientation=this.orientation}))}updateSeparatorVisibility(){let firstVisibleNonSeparatorIndex=0;for(;firstVisibleNonSeparatorIndex<this.cells.length;firstVisibleNonSeparatorIndex++){const cell=this.cells[firstVisibleNonSeparatorIndex];if(cell._isSeparator){cell.node.visible=false}else if(cell.node.visible&&cell.node.boundsProperty.value.isValid()){break}}let hasVisibleNonSeparator=false;for(let i=this.cells.length-1;i>firstVisibleNonSeparatorIndex;i--){const cell=this.cells[i];if(cell._isSeparator){cell.node.visible=hasVisibleNonSeparator;hasVisibleNonSeparator=false}else if(cell.node.visible&&cell.node.boundsProperty.value.isValid()){hasVisibleNonSeparator=true}}}layout(){super.layout();const orientation=this._orientation;const oppositeOrientation=this._orientation.opposite;this.updateSeparatorVisibility();const cells=this.filterLayoutCells(this.cells);this.displayedCells=cells;if(!cells.length){this.layoutBoundsProperty.value=Bounds2.NOTHING;this.minimumWidthProperty.value=null;this.minimumHeightProperty.value=null;return}let preferredSize=this.getPreferredProperty(orientation).value;const preferredOppositeSize=this.getPreferredProperty(oppositeOrientation).value;const maxMinimumCellSize=Math.max(...cells.map(cell=>cell.getMinimumSize(orientation)||0));if(maxMinimumCellSize>(preferredSize||Number.POSITIVE_INFINITY)){preferredSize=maxMinimumCellSize}const lines=[];if(this.wrap){let currentLineCells=[];let availableSpace=preferredSize||Number.POSITIVE_INFINITY;while(cells.length){const cell=cells.shift();const cellSpace=cell.getMinimumSize(orientation);if(currentLineCells.length===0){currentLineCells.push(cell);availableSpace-=cellSpace}else if(this.spacing+cellSpace<=availableSpace+1e-7){currentLineCells.push(cell);availableSpace-=this.spacing+cellSpace}else{lines.push(FlowLine.pool.create(orientation,currentLineCells));availableSpace=preferredSize||Number.POSITIVE_INFINITY;currentLineCells=[cell];availableSpace-=cellSpace}}if(currentLineCells.length){lines.push(FlowLine.pool.create(orientation,currentLineCells))}}else{lines.push(FlowLine.pool.create(orientation,cells))}lines.forEach(line=>{line.cells.forEach(cell=>{line.min=Math.max(line.min,cell.getMinimumSize(oppositeOrientation));line.max=Math.min(line.max,cell.getMaximumSize(oppositeOrientation));if(cell.effectiveAlign===LayoutAlign.ORIGIN){const originBounds=cell.getOriginBounds();line.minOrigin=Math.min(originBounds[oppositeOrientation.minCoordinate],line.minOrigin);line.maxOrigin=Math.max(originBounds[oppositeOrientation.maxCoordinate],line.maxOrigin)}});if(isFinite(line.minOrigin)&&isFinite(line.maxOrigin)){line.size=Math.max(line.min,line.maxOrigin-line.minOrigin)}else{line.size=line.min}});const minimumCurrentSize=Math.max(...lines.map(line=>line.getMinimumSize(this.spacing)));const minimumCurrentOppositeSize=_.sum(lines.map(line=>line.size))+(lines.length-1)*this.lineSpacing;const minimumAllowableSize=this.wrap?maxMinimumCellSize:minimumCurrentSize;const size=Math.max(minimumCurrentSize,preferredSize||0);const oppositeSize=Math.max(minimumCurrentOppositeSize,preferredOppositeSize||0);const originPrimary=this.layoutOriginProperty.value[orientation.coordinate];const originSecondary=this.layoutOriginProperty.value[orientation.opposite.coordinate];lines.forEach(line=>{const minimumContent=_.sum(line.cells.map(cell=>cell.getMinimumSize(orientation)));const spacingAmount=this.spacing*(line.cells.length-1);let spaceRemaining=size-minimumContent-spacingAmount;line.cells.forEach(cell=>{cell.size=cell.getMinimumSize(orientation)});let growableCells;while(spaceRemaining>1e-7&&(growableCells=line.cells.filter(cell=>{return cell.effectiveGrow!==0&&cell.size<cell.getMaximumSize(orientation)-1e-7})).length){const totalGrow=_.sum(growableCells.map(cell=>cell.effectiveGrow));const amountToGrow=Math.min(Math.min(...growableCells.map(cell=>(cell.getMaximumSize(orientation)-cell.size)/cell.effectiveGrow)),spaceRemaining/totalGrow);assert&&assert(amountToGrow>1e-11);growableCells.forEach(cell=>{cell.size+=amountToGrow*cell.effectiveGrow});spaceRemaining-=amountToGrow*totalGrow}const primarySpacingFunction=this._justify.spacingFunctionFactory(spaceRemaining,line.cells.length);let position=originPrimary;line.cells.forEach((cell,index)=>{position+=primarySpacingFunction(index);if(index>0){position+=this.spacing}cell.reposition(orientation,cell.size,position,true,0,cell.effectiveCellAlign);position+=cell.size;assert&&assert(this.spacing>=0||cell.size>=-this.spacing-1e-7,"Negative spacing more than a cell's size causes issues with layout")})});const oppositeSpaceRemaining=oppositeSize-minimumCurrentOppositeSize;const initialOppositePosition=(lines[0].hasOrigin()?lines[0].minOrigin:0)+originSecondary;let oppositePosition=initialOppositePosition;if(this._justifyLines===null){lines.forEach(line=>{line.size+=oppositeSpaceRemaining/lines.length});lines.forEach(line=>{line.position=oppositePosition;oppositePosition+=line.size+this.lineSpacing})}else{const spacingFunction=this._justifyLines.spacingFunctionFactory(oppositeSpaceRemaining,lines.length);lines.forEach((line,index)=>{oppositePosition+=spacingFunction(index);line.position=oppositePosition;oppositePosition+=line.size+this.lineSpacing})}lines.forEach(line=>line.cells.forEach(cell=>{cell.reposition(oppositeOrientation,line.size,line.position,cell.effectiveStretch,-line.minOrigin,cell.effectiveAlign)}));const minCoordinate=originPrimary;const maxCoordinate=originPrimary+size;const minOppositeCoordinate=initialOppositePosition;const maxOppositeCoordinate=initialOppositePosition+oppositeSize;this.layoutBoundsProperty.value=Bounds2.oriented(orientation,minCoordinate,minOppositeCoordinate,maxCoordinate,maxOppositeCoordinate);this.minimumWidthProperty.value=orientation===Orientation.HORIZONTAL?minimumAllowableSize:minimumCurrentOppositeSize;this.minimumHeightProperty.value=orientation===Orientation.HORIZONTAL?minimumCurrentOppositeSize:minimumAllowableSize;this.finishedLayoutEmitter.emit();lines.forEach(line=>line.clean())}get justify(){const result=LayoutJustification.internalToJustify(this._orientation,this._justify);assert&&assert(LayoutJustification.getAllowedJustificationValues(this._orientation).includes(result));return result}set justify(value){assert&&assert(LayoutJustification.getAllowedJustificationValues(this._orientation).includes(value),`justify ${value} not supported, with the orientation ${this._orientation}, the valid values are ${LayoutJustification.getAllowedJustificationValues(this._orientation)}`);const mappedValue=LayoutJustification.justifyToInternal(this._orientation,value);if(this._justify!==mappedValue){this._justify=mappedValue;this.updateLayoutAutomatically()}}get justifyLines(){if(this._justifyLines===null){return null}else{const result=LayoutJustification.internalToJustify(this._orientation,this._justifyLines);assert&&assert(LayoutJustification.getAllowedJustificationValues(this._orientation).includes(result));return result}}set justifyLines(value){assert&&assert(value===null||LayoutJustification.getAllowedJustificationValues(this._orientation.opposite).includes(value),`justify ${value} not supported, with the orientation ${this._orientation.opposite}, the valid values are ${LayoutJustification.getAllowedJustificationValues(this._orientation.opposite)} or null`);const mappedValue=value===null?null:LayoutJustification.justifyToInternal(this._orientation.opposite,value);assert&&assert(mappedValue===null||mappedValue instanceof LayoutJustification);if(this._justifyLines!==mappedValue){this._justifyLines=mappedValue;this.updateLayoutAutomatically()}}get wrap(){return this._wrap}set wrap(value){if(this._wrap!==value){this._wrap=value;this.updateLayoutAutomatically()}}get spacing(){return this._spacing}set spacing(value){assert&&assert(isFinite(value));if(this._spacing!==value){this._spacing=value;this.updateLayoutAutomatically()}}get lineSpacing(){return this._lineSpacing}set lineSpacing(value){assert&&assert(isFinite(value));if(this._lineSpacing!==value){this._lineSpacing=value;this.updateLayoutAutomatically()}}insertCell(index,cell){assert&&assert(index>=0);assert&&assert(index<=this.cells.length);assert&&assert(!_.includes(this.cells,cell));cell.orientation=this.orientation;this.cells.splice(index,0,cell);this.addNode(cell.node);cell.changedEmitter.addListener(this._updateLayoutListener);this.updateLayoutAutomatically()}removeCell(cell){assert&&assert(_.includes(this.cells,cell));arrayRemove(this.cells,cell);this.removeNode(cell.node);cell.changedEmitter.removeListener(this._updateLayoutListener);this.updateLayoutAutomatically()}getCellQuantity(){return this.cells.length}reorderCells(cells,minChangeIndex,maxChangeIndex){this.cells.splice(minChangeIndex,maxChangeIndex-minChangeIndex+1,...cells);this.updateLayoutAutomatically()}getPreferredProperty(orientation){return orientation===Orientation.HORIZONTAL?this.preferredWidthProperty:this.preferredHeightProperty}dispose(){this.lock();this.cells.forEach(cell=>this.removeCell(cell));this.displayedCells=[];super.dispose();this.unlock()}static create(ancestorNode,options){return new FlowConstraint(ancestorNode,options)}}scenery.register("FlowConstraint",FlowConstraint);export{FLOW_CONSTRAINT_OPTION_KEYS};