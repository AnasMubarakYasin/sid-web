import Pool from"../../../../phet-core/js/Pool.js";import LayoutLine from"../../layout/constraints/LayoutLine.js";import scenery from"../../scenery.js";export default class GridLine extends LayoutLine{constructor(index,cells,grow){super();this.initialize(index,cells,grow)}initialize(index,cells,grow){this.index=index;this.cells=cells;this.grow=grow;this.initializeLayoutLine();return this}freeToPool(){GridLine.pool.freeToPool(this)}clean(){this.cells.length=0;this.freeToPool()}static{this.pool=new Pool(GridLine,{defaultArguments:[0,[],0]})}}scenery.register("GridLine",GridLine);