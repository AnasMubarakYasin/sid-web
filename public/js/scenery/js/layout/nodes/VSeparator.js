import HeightSizable from"../../layout/HeightSizable.js";import scenery from"../../scenery.js";import Separator from"../../layout/nodes/Separator.js";export default class VSeparator extends HeightSizable(Separator){constructor(options){super();this.localPreferredHeightProperty.link(height=>{if(height!==null){this.y2=height}});this.mutate(options)}}scenery.register("VSeparator",VSeparator);