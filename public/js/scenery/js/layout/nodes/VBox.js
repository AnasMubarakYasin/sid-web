import optionize from"../../../../phet-core/js/optionize.js";import FlowBox from"../../layout/nodes/FlowBox.js";import scenery from"../../scenery.js";import VSeparator from"../../layout/nodes/VSeparator.js";export default class VBox extends FlowBox{constructor(providedOptions){assert&&assert(!providedOptions||!providedOptions.orientation,"VBox sets orientation");super(optionize()({orientation:"vertical"},providedOptions))}onFlowBoxChildInserted(node,index){assert&&assert(!(node instanceof VSeparator),"VSeparator should not be used in an VBox. Use HSeparator instead");super.onFlowBoxChildInserted(node,index)}mutate(options){return super.mutate(options)}}scenery.register("VBox",VBox);