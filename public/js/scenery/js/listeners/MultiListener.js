import Property from"../../../axon/js/Property.js";import Matrix from"../../../dot/js/Matrix.js";import Matrix3 from"../../../dot/js/Matrix3.js";import{SingularValueDecomposition}from"../../../dot/js/Matrix.js";import Vector2 from"../../../dot/js/Vector2.js";import arrayRemove from"../../../phet-core/js/arrayRemove.js";import optionize from"../../../phet-core/js/optionize.js";import Tandem from"../../../tandem/js/Tandem.js";import{Intent}from"../input/Pointer.js";import Mouse from"../input/Mouse.js";import MultiListenerPress from"../listeners/MultiListenerPress.js";import scenery from"../scenery.js";const MOVE_INTERRUPT_MAGNITUDE=25;let MultiListener=class MultiListener{constructor(targetNode,providedOptions){const options=optionize()({mouseButton:0,pressCursor:"pointer",allowScale:true,allowRotation:true,allowMultitouchInterruption:false,allowMoveInterruption:true,minScale:1,maxScale:4,tandem:Tandem.REQUIRED},providedOptions);this._targetNode=targetNode;this._minScale=options.minScale;this._maxScale=options.maxScale;this._mouseButton=options.mouseButton;this._pressCursor=options.pressCursor;this._allowScale=options.allowScale;this._allowRotation=options.allowRotation;this._allowMultitouchInterruption=options.allowMultitouchInterruption;this._allowMoveInterruption=options.allowMoveInterruption;this._presses=[];this._backgroundPresses=[];this.matrixProperty=new Property(targetNode.matrix.copy(),{phetioValueType:Matrix3.Matrix3IO,tandem:options.tandem.createTandem("matrixProperty"),phetioReadOnly:true});this.matrixProperty.link(matrix=>{this._targetNode.matrix=matrix});this._interrupted=false;this._pressListener={move:event=>{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener pointer move");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();const press=this.findPress(event.pointer);assert&&assert(press,"Press should be found for move event");this.movePress(press);sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()},up:event=>{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener pointer up");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();const press=this.findPress(event.pointer);assert&&assert(press,"Press should be found for up event");this.removePress(press);sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()},cancel:event=>{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener pointer cancel");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();const press=this.findPress(event.pointer);assert&&assert(press,"Press should be found for cancel event");press.interrupted=true;this.removePress(press);sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()},interrupt:()=>{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener pointer interrupt");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();this.interrupt();sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}};this._backgroundListener={up:event=>{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener background up");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();if(!this._interrupted){const backgroundPress=this.findBackgroundPress(event.pointer);assert&&assert(backgroundPress,"Background press should be found for up event");this.removeBackgroundPress(backgroundPress)}sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()},move:event=>{const candidateBackgroundPresses=this._backgroundPresses.filter(press=>{return!press.pointer.hasIntent(Intent.DRAG)&&press.initialPoint.distance(press.pointer.point)>MOVE_INTERRUPT_MAGNITUDE});if(this.getCurrentScale()!==1||candidateBackgroundPresses.length>=2){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener attached, interrupting for press");candidateBackgroundPresses.forEach(press=>{this.removeBackgroundPress(press);this.interruptOtherListeners(press.pointer);this.addPress(press)})}},cancel:event=>{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener background cancel");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();if(!this._interrupted){const backgroundPress=this.findBackgroundPress(event.pointer);assert&&assert(backgroundPress,"Background press should be found for cancel event");this.removeBackgroundPress(backgroundPress)}sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()},interrupt:()=>{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener background interrupt");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();this.interrupt();sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}}}findPress(pointer){for(let i=0;i<this._presses.length;i++){if(this._presses[i].pointer===pointer){return this._presses[i]}}return null}findBackgroundPress(pointer){for(let i=0;i<this._backgroundPresses.length;i++){if(this._backgroundPresses[i].pointer===pointer){return this._backgroundPresses[i]}}return null}hasPress(press){return _.some(this._presses.concat(this._backgroundPresses),existingPress=>{return existingPress.pointer===press.pointer})}interruptOtherListeners(pointer){const listeners=pointer.getListeners();for(let i=0;i<listeners.length;i++){const listener=listeners[i];if(listener!==this._backgroundListener){listener.interrupt&&listener.interrupt()}}}down(event){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener down");if(event.pointer instanceof Mouse&&event.domEvent instanceof MouseEvent&&event.domEvent.button!==this._mouseButton){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener abort: wrong mouse button");return}this._interrupted=false;let pressTrail;if(!_.includes(event.trail.nodes,this._targetNode)){pressTrail=this._targetNode.getUniqueTrailTo(event.target)}else{pressTrail=event.trail.subtrailTo(this._targetNode,false)}assert&&assert(_.includes(pressTrail.nodes,this._targetNode),"targetNode must be in the Trail for Press");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();const press=new MultiListenerPress(event.pointer,pressTrail);if(!this._allowMoveInterruption&&!this._allowMultitouchInterruption){if(!event.pointer.isAttached()){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener unattached, using press");this.addPress(press)}}else{sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener attached, adding background press");this.addBackgroundPress(press)}sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}addPress(press){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener addPress");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();if(!this.hasPress(press)){this._presses.push(press);press.pointer.cursor=this._pressCursor;press.pointer.addInputListener(this._pressListener,true);this.recomputeLocals();this.reposition()}sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}movePress(press){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener movePress");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();this.reposition();sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}removePress(press){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener removePress");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();press.pointer.removeInputListener(this._pressListener);press.pointer.cursor=null;arrayRemove(this._presses,press);this.recomputeLocals();this.reposition();sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}addBackgroundPress(press){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener addBackgroundPress");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();if(!this.hasPress(press)){this._backgroundPresses.push(press);press.pointer.addInputListener(this._backgroundListener,false)}sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}removeBackgroundPress(press){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener removeBackgroundPress");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();press.pointer.removeInputListener(this._backgroundListener);arrayRemove(this._backgroundPresses,press);sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}reposition(){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener reposition");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();this.matrixProperty.set(this.computeMatrix());sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}recomputeLocals(){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener recomputeLocals");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();for(let i=0;i<this._presses.length;i++){this._presses[i].recomputeLocalPoint()}sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}interrupt(){sceneryLog&&sceneryLog.InputListener&&sceneryLog.InputListener("MultiListener interrupt");sceneryLog&&sceneryLog.InputListener&&sceneryLog.push();while(this._presses.length){this.removePress(this._presses[this._presses.length-1])}while(this._backgroundPresses.length){this.removeBackgroundPress(this._backgroundPresses[this._backgroundPresses.length-1])}this._interrupted=true;sceneryLog&&sceneryLog.InputListener&&sceneryLog.pop()}computeMatrix(){if(this._presses.length===0){return this._targetNode.getMatrix()}else if(this._presses.length===1){return this.computeSinglePressMatrix()}else if(this._allowScale&&this._allowRotation){return this.computeTranslationRotationScaleMatrix()}else if(this._allowScale){return this.computeTranslationScaleMatrix()}else if(this._allowRotation){return this.computeTranslationRotationMatrix()}else{return this.computeTranslationMatrix()}}computeSinglePressMatrix(){const singleTargetPoint=this._presses[0].targetPoint;const localPoint=this._presses[0].localPoint;assert&&assert(localPoint,"localPoint is not defined on the Press?");const singleMappedPoint=this._targetNode.localToParentPoint(localPoint);const delta=singleTargetPoint.minus(singleMappedPoint);return Matrix3.translationFromVector(delta).timesMatrix(this._targetNode.getMatrix())}computeTranslationMatrix(){const sum=new Vector2(0,0);for(let i=0;i<this._presses.length;i++){sum.add(this._presses[i].targetPoint);const localPoint=this._presses[i].localPoint;assert&&assert(localPoint,"localPoint is not defined on the Press?");sum.subtract(localPoint)}return Matrix3.translationFromVector(sum.dividedScalar(this._presses.length))}computeTranslationScaleMatrix(){const localPoints=this._presses.map(press=>{assert&&assert(press.localPoint,"localPoint is not defined on the Press?");return press.localPoint});const targetPoints=this._presses.map(press=>press.targetPoint);const localCentroid=new Vector2(0,0);const targetCentroid=new Vector2(0,0);localPoints.forEach(localPoint=>{localCentroid.add(localPoint)});targetPoints.forEach(targetPoint=>{targetCentroid.add(targetPoint)});localCentroid.divideScalar(this._presses.length);targetCentroid.divideScalar(this._presses.length);let localSquaredDistance=0;let targetSquaredDistance=0;localPoints.forEach(localPoint=>{localSquaredDistance+=localPoint.distanceSquared(localCentroid)});targetPoints.forEach(targetPoint=>{targetSquaredDistance+=targetPoint.distanceSquared(targetCentroid)});let scale=this.getCurrentScale();if(targetSquaredDistance!==0){scale=this.limitScale(Math.sqrt(targetSquaredDistance/localSquaredDistance))}const translateToTarget=Matrix3.translation(targetCentroid.x,targetCentroid.y);const translateFromLocal=Matrix3.translation(-localCentroid.x,-localCentroid.y);return translateToTarget.timesMatrix(Matrix3.scaling(scale)).timesMatrix(translateFromLocal)}limitScale(scale){let correctedScale=Math.max(scale,this._minScale);correctedScale=Math.min(correctedScale,this._maxScale);return correctedScale}computeTranslationRotationMatrix(){let i;const localMatrix=new Matrix(2,this._presses.length);const targetMatrix=new Matrix(2,this._presses.length);const localCentroid=new Vector2(0,0);const targetCentroid=new Vector2(0,0);for(i=0;i<this._presses.length;i++){const localPoint=this._presses[i].localPoint;const targetPoint=this._presses[i].targetPoint;localCentroid.add(localPoint);targetCentroid.add(targetPoint);localMatrix.set(0,i,localPoint.x);localMatrix.set(1,i,localPoint.y);targetMatrix.set(0,i,targetPoint.x);targetMatrix.set(1,i,targetPoint.y)}localCentroid.divideScalar(this._presses.length);targetCentroid.divideScalar(this._presses.length);for(i=0;i<this._presses.length;i++){localMatrix.set(0,i,localMatrix.get(0,i)-localCentroid.x);localMatrix.set(1,i,localMatrix.get(1,i)-localCentroid.y);targetMatrix.set(0,i,targetMatrix.get(0,i)-targetCentroid.x);targetMatrix.set(1,i,targetMatrix.get(1,i)-targetCentroid.y)}const covarianceMatrix=localMatrix.times(targetMatrix.transpose());const svd=new SingularValueDecomposition(covarianceMatrix);let rotation=svd.getV().times(svd.getU().transpose());if(rotation.det()<0){rotation=svd.getV().times(Matrix.diagonalMatrix([1,-1])).times(svd.getU().transpose())}const rotation3=new Matrix3().rowMajor(rotation.get(0,0),rotation.get(0,1),0,rotation.get(1,0),rotation.get(1,1),0,0,0,1);const translation=targetCentroid.minus(rotation3.timesVector2(localCentroid));rotation3.set02(translation.x);rotation3.set12(translation.y);return rotation3}computeTranslationRotationScaleMatrix(){let i;const localMatrix=new Matrix(this._presses.length*2,4);for(i=0;i<this._presses.length;i++){const localPoint=this._presses[i].localPoint;localMatrix.set(2*i+0,0,localPoint.x);localMatrix.set(2*i+0,1,localPoint.y);localMatrix.set(2*i+0,2,1);localMatrix.set(2*i+1,0,localPoint.y);localMatrix.set(2*i+1,1,-localPoint.x);localMatrix.set(2*i+1,3,1)}const targetMatrix=new Matrix(this._presses.length*2,1);for(i=0;i<this._presses.length;i++){const targetPoint=this._presses[i].targetPoint;targetMatrix.set(2*i+0,0,targetPoint.x);targetMatrix.set(2*i+1,0,targetPoint.y)}const coefficientMatrix=SingularValueDecomposition.pseudoinverse(localMatrix).times(targetMatrix);const m11=coefficientMatrix.get(0,0);const m12=coefficientMatrix.get(1,0);const m13=coefficientMatrix.get(2,0);const m23=coefficientMatrix.get(3,0);return new Matrix3().rowMajor(m11,m12,m13,-m12,m11,m23,0,0,1)}getCurrentScale(){return this._targetNode.getScaleVector().x}resetTransform(){this._targetNode.resetTransform();this.matrixProperty.set(this._targetNode.matrix.copy())}};scenery.register("MultiListener",MultiListener);export default MultiListener;