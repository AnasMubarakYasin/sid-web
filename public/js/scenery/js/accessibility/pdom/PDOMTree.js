import arrayDifference from"../../../../phet-core/js/arrayDifference.js";import PartialPDOMTrail from"../../accessibility/pdom/PartialPDOMTrail.js";import PDOMInstance from"../../accessibility/pdom/PDOMInstance.js";import BrowserEvents from"../../input/BrowserEvents.js";import scenery from"../../scenery.js";import Trail from"../../util/Trail.js";import{getPDOMFocusedNode}from"../pdomFocusProperty.js";export default class PDOMTree{static addChild(parent,child){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`addChild parent:n#${parent._id}, child:n#${child._id}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();assert&&assert(!child._rendererSummary.hasNoPDOM());const focusedNode=PDOMTree.beforeOp();if(!child._pdomParent){PDOMTree.addTree(parent,child)}PDOMTree.afterOp(focusedNode);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static removeChild(parent,child){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`removeChild parent:n#${parent._id}, child:n#${child._id}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();assert&&assert(!child._rendererSummary.hasNoPDOM());const focusedNode=PDOMTree.beforeOp();if(!child._pdomParent){PDOMTree.removeTree(parent,child)}PDOMTree.afterOp(focusedNode);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static childrenOrderChange(node){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`childrenOrderChange node:n#${node._id}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();assert&&assert(!node._rendererSummary.hasNoPDOM());const focusedNode=PDOMTree.beforeOp();PDOMTree.reorder(node);PDOMTree.afterOp(focusedNode);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static pdomOrderChange(node,oldOrder,newOrder){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`pdomOrderChange n#${node._id}: ${PDOMTree.debugOrder(oldOrder)},${PDOMTree.debugOrder(newOrder)}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();const focusedNode=PDOMTree.beforeOp();const removedItems=[];const addedItems=[];arrayDifference(oldOrder||[],newOrder||[],removedItems,addedItems);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`removed: ${PDOMTree.debugOrder(removedItems)}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`added: ${PDOMTree.debugOrder(addedItems)}`);let i;let j;if(assert){for(i=0;i<removedItems.length;i++){const item=removedItems[i];assert(item===null||item._pdomParent===node,"Node should have had a pdomOrder")}for(i=0;i<addedItems.length;i++){const item=addedItems[i];assert(item===null||item._pdomParent===null,"Node is already specified in a pdomOrder")}}const pdomTrails=PDOMTree.findPDOMTrails(node);for(i=0;i<removedItems.length;i++){const removedItemToRemove=removedItems[i];if(removedItemToRemove){PDOMTree.removeTree(node,removedItemToRemove,pdomTrails);removedItemToRemove._pdomParent=null;removedItemToRemove.pdomParentChangedEmitter.emit()}}for(i=0;i<addedItems.length;i++){const addedItemToRemove=addedItems[i];if(addedItemToRemove){const removedParents=addedItemToRemove._parents;for(j=0;j<removedParents.length;j++){PDOMTree.removeTree(removedParents[j],addedItemToRemove)}addedItemToRemove._pdomParent=node;addedItemToRemove.pdomParentChangedEmitter.emit()}}for(i=0;i<removedItems.length;i++){const removedItemToAdd=removedItems[i];if(removedItemToAdd){const addedParents=removedItemToAdd._parents;for(j=0;j<addedParents.length;j++){PDOMTree.addTree(addedParents[j],removedItemToAdd)}}}for(i=0;i<addedItems.length;i++){const addedItemToAdd=addedItems[i];addedItemToAdd&&PDOMTree.addTree(node,addedItemToAdd,pdomTrails)}PDOMTree.reorder(node,pdomTrails);PDOMTree.afterOp(focusedNode);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static pdomContentChange(node){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`pdomContentChange n#${node._id}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();const focusedNode=PDOMTree.beforeOp();let i;const parents=node._pdomParent?[node._pdomParent]:node._parents;const pdomTrailsList=[];for(i=0;i<parents.length;i++){const parent=parents[i];const pdomTrails=PDOMTree.findPDOMTrails(parent);pdomTrailsList.push(pdomTrails);PDOMTree.removeTree(parent,node,pdomTrails)}for(i=0;i<parents.length;i++){PDOMTree.addTree(parents[i],node,pdomTrailsList[i])}for(i=0;i<node._rootedDisplays.length;i++){const display=node._rootedDisplays[i];if(display._accessible){PDOMTree.rebuildInstanceTree(display._rootPDOMInstance)}}PDOMTree.afterOp(focusedNode);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static rebuildInstanceTree(rootInstance){const rootNode=rootInstance.display.rootNode;assert&&assert(rootNode);rootInstance.removeAllChildren();rootInstance.addConsecutiveInstances(PDOMTree.createTree(new Trail(rootNode),rootInstance.display,rootInstance))}static addTree(parent,child,pdomTrails){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`addTree parent:n#${parent._id}, child:n#${child._id}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();assert&&PDOMTree.auditNodeForPDOMCycles(parent);pdomTrails=pdomTrails||PDOMTree.findPDOMTrails(parent);for(let i=0;i<pdomTrails.length;i++){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`trail: ${pdomTrails[i].trail.toString()} full:${pdomTrails[i].fullTrail.toString()} for ${pdomTrails[i].pdomInstance.toString()} root:${pdomTrails[i].isRoot}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();const partialTrail=pdomTrails[i];const parentInstance=partialTrail.pdomInstance;partialTrail.fullTrail.addDescendant(child);const childInstances=PDOMTree.createTree(partialTrail.fullTrail,parentInstance.display,parentInstance);partialTrail.fullTrail.removeDescendant();parentInstance.addConsecutiveInstances(childInstances);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static removeTree(parent,child,pdomTrails){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`removeTree parent:n#${parent._id}, child:n#${child._id}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();pdomTrails=pdomTrails||PDOMTree.findPDOMTrails(parent);for(let i=0;i<pdomTrails.length;i++){const partialTrail=pdomTrails[i];partialTrail.fullTrail.addDescendant(child);partialTrail.pdomInstance.removeInstancesForTrail(partialTrail.fullTrail);partialTrail.fullTrail.removeDescendant()}sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static reorder(node,pdomTrails){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`reorder n#${node._id}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();pdomTrails=pdomTrails||PDOMTree.findPDOMTrails(node);for(let i=0;i<pdomTrails.length;i++){const partialTrail=pdomTrails[i];partialTrail.pdomInstance.sortChildren()}sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop()}static createTree(trail,display,parentInstance){sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`createTree ${trail.toString()} parent:${parentInstance?parentInstance.toString():"null"}`);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.push();const node=trail.lastNode();const effectiveChildren=node.getEffectiveChildren();sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.PDOMTree(`effectiveChildren: ${PDOMTree.debugOrder(effectiveChildren)}`);assert&&assert(!node._lockedPDOMInstanceCreation,"You are recursively creating another PDOMInstance for the same Node! Probably because a child instance is triggering a createTree on a parent.");let instance;let existed=false;if(node.hasPDOMContent){instance=parentInstance.findChildWithTrail(trail);if(instance){existed=true}else{node._lockedPDOMInstanceCreation=true;instance=PDOMInstance.pool.create(parentInstance,display,trail.copy());node._lockedPDOMInstanceCreation=false}parentInstance=instance}const childInstances=[];for(let i=0;i<effectiveChildren.length;i++){trail.addDescendant(effectiveChildren[i],i);Array.prototype.push.apply(childInstances,PDOMTree.createTree(trail,display,parentInstance));trail.removeDescendant()}if(instance){instance.addConsecutiveInstances(childInstances);sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop();return existed?[]:[instance]}else{sceneryLog&&sceneryLog.PDOMTree&&sceneryLog.pop();return childInstances}}static beforeOp(){BrowserEvents.blockFocusCallbacks=true;return getPDOMFocusedNode()}static afterOp(focusedNode){if(!BrowserEvents.dispatchingFocusEvents){focusedNode&&focusedNode.focusable&&focusedNode.focus()}BrowserEvents.blockFocusCallbacks=false}static findPDOMTrails(node){const trails=[];PDOMTree.recursivePDOMTrailSearch(trails,new Trail(node));return trails}static recursivePDOMTrailSearch(trailResults,trail){const root=trail.rootNode();let i;if(root.hasPDOMContent){const instances=root.pdomInstances;for(i=0;i<instances.length;i++){trailResults.push(new PartialPDOMTrail(instances[i],trail.copy(),false))}return}else{const rootedDisplays=root.rootedDisplays;for(i=0;i<rootedDisplays.length;i++){const display=rootedDisplays[i];if(display._accessible){trailResults.push(new PartialPDOMTrail(display._rootPDOMInstance,trail.copy(),true))}}}const parents=root._pdomParent?[root._pdomParent]:root._parents;const parentCount=parents.length;for(i=0;i<parentCount;i++){const parent=parents[i];trail.addAncestor(parent);PDOMTree.recursivePDOMTrailSearch(trailResults,trail);trail.removeAncestor()}}static auditPDOMDisplays(node){if(assertSlow){if(node._pdomDisplaysInfo.canHavePDOMDisplays()){let i;const displays=[];for(i=0;i<node._parents.length;i++){Array.prototype.push.apply(displays,node._parents[i]._pdomDisplaysInfo.pdomDisplays)}for(i=0;i<node._rootedDisplays.length;i++){const display=node._rootedDisplays[i];if(display._accessible){displays.push(display)}}const actualArray=node._pdomDisplaysInfo.pdomDisplays.slice();const expectedArray=displays.slice();assertSlow(actualArray.length===expectedArray.length);for(i=0;i<expectedArray.length;i++){for(let j=0;j<actualArray.length;j++){if(expectedArray[i]===actualArray[j]){expectedArray.splice(i,1);actualArray.splice(j,1);i--;break}}}assertSlow(actualArray.length===0&&expectedArray.length===0,"Mismatch with accessible pdom displays")}else{assertSlow(node._pdomDisplaysInfo.pdomDisplays.length===0,"Invisible/nonaccessible things should have no displays")}}}static auditNodeForPDOMCycles(node){if(assert){const trail=new Trail(node);(function recursiveSearch(){const root=trail.rootNode();assert(trail.length<=1||root!==node,`${"Accessible PDOM graph cycle detected. The combined scene-graph DAG with pdomOrder defining additional "+"parent-child relationships should still be a DAG. Cycle detected with the trail: "}${trail.toString()} path: ${trail.toPathString()}`);const parentCount=root._parents.length;for(let i=0;i<parentCount;i++){const parent=root._parents[i];trail.addAncestor(parent);recursiveSearch();trail.removeAncestor()}if(root._pdomParent&&!root._pdomParent.hasChild(root)){trail.addAncestor(root._pdomParent);recursiveSearch();trail.removeAncestor()}})()}}static debugOrder(pdomOrder){if(pdomOrder===null){return"null"}return`[${pdomOrder.map(nodeOrNull=>nodeOrNull===null?"null":nodeOrNull._id).join(",")}]`}}scenery.register("PDOMTree",PDOMTree);