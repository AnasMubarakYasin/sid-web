import Range from"../../../dot/js/Range.js";import optionize from"../../../phet-core/js/optionize.js";import scenery from"../scenery.js";const getLineBreakRanges=(str,providedOptions)=>{const options=optionize()({requiredOnly:false},providedOptions);const ranges=[];const lineBreaker=new LineBreaker(str);lineBreaker[Symbol.iterator]=()=>{return{next(){const value=lineBreaker.nextBreak();if(value!==null){return{value:value,done:false}}else{return{done:true}}}}};let lastIndex=0;for(const brk of lineBreaker){const index=brk.position;if(options.requiredOnly&&!brk.required){continue}if(lastIndex!==index){ranges.push(new Range(lastIndex,index))}lastIndex=brk.position}if(lastIndex<str.length){ranges.push(new Range(lastIndex,str.length))}return ranges};scenery.register("getLineBreakRanges",getLineBreakRanges);export default getLineBreakRanges;