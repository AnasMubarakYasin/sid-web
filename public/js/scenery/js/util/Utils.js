import Bounds2 from"../../../dot/js/Bounds2.js";import Matrix3 from"../../../dot/js/Matrix3.js";import Transform3 from"../../../dot/js/Transform3.js";import Vector2 from"../../../dot/js/Vector2.js";import platform from"../../../phet-core/js/platform.js";import Features from"../util/Features.js";import scenery from"../scenery.js";import{scratchContext}from"./scratches.js";function p(x,y){return new Vector2(x,y)}const debugChromeBoundsScanning=false;const transformProperty=Features.transform;const transformOriginProperty=Features.transformOrigin||"transformOrigin";let webglEnabled=true;let _extensionlessWebGLSupport;const Utils={prepareForTransform(element){element.style[transformOriginProperty]="top left"},applyPreparedTransform(matrix,element){element.style[transformProperty]=matrix.getCSSTransform()},setTransform(transformString,element){element.style[transformProperty]=transformString},unsetTransform(element){element.style[transformProperty]=""},polyfillRequestAnimationFrame(){if(!window.requestAnimationFrame||!window.cancelAnimationFrame){if(!Features.requestAnimationFrame||!Features.cancelAnimationFrame){window.requestAnimationFrame=callback=>{const timeAtStart=Date.now();return window.setTimeout(()=>{callback(Date.now()-timeAtStart)},16)};window.cancelAnimationFrame=clearTimeout}else{window.requestAnimationFrame=window[Features.requestAnimationFrame];window.cancelAnimationFrame=window[Features.cancelAnimationFrame]}}},backingStorePixelRatio(context){return context.webkitBackingStorePixelRatio||context.mozBackingStorePixelRatio||context.msBackingStorePixelRatio||context.oBackingStorePixelRatio||context.backingStorePixelRatio||1},backingScale(context){if("devicePixelRatio"in window){const backingStoreRatio=Utils.backingStorePixelRatio(context);return window.devicePixelRatio/backingStoreRatio}return 1},supportsNativeCanvasFilter(){return!!Features.canvasFilter},supportsImageDataCanvasFilter(){return Utils.backingStorePixelRatio(scratchContext)===1},scanBounds(imageData,resolution,transform){const dirtyX=_.map(_.range(resolution),()=>false);const dirtyY=_.map(_.range(resolution),()=>false);for(let x=0;x<resolution;x++){for(let y=0;y<resolution;y++){const offset=4*(y*resolution+x);if(imageData.data[offset]!==0||imageData.data[offset+1]!==0||imageData.data[offset+2]!==0||imageData.data[offset+3]!==0){dirtyX[x]=true;dirtyY[y]=true}}}const minX=_.indexOf(dirtyX,true);const maxX=_.lastIndexOf(dirtyX,true);const minY=_.indexOf(dirtyY,true);const maxY=_.lastIndexOf(dirtyY,true);const extraSpread=resolution/16;return{minBounds:new Bounds2(minX<1||minX>=resolution-1?Number.POSITIVE_INFINITY:transform.inversePosition2(p(minX+1+extraSpread,0)).x,minY<1||minY>=resolution-1?Number.POSITIVE_INFINITY:transform.inversePosition2(p(0,minY+1+extraSpread)).y,maxX<1||maxX>=resolution-1?Number.NEGATIVE_INFINITY:transform.inversePosition2(p(maxX-extraSpread,0)).x,maxY<1||maxY>=resolution-1?Number.NEGATIVE_INFINITY:transform.inversePosition2(p(0,maxY-extraSpread)).y),maxBounds:new Bounds2(minX<1||minX>=resolution-1?Number.NEGATIVE_INFINITY:transform.inversePosition2(p(minX-1-extraSpread,0)).x,minY<1||minY>=resolution-1?Number.NEGATIVE_INFINITY:transform.inversePosition2(p(0,minY-1-extraSpread)).y,maxX<1||maxX>=resolution-1?Number.POSITIVE_INFINITY:transform.inversePosition2(p(maxX+2+extraSpread,0)).x,maxY<1||maxY>=resolution-1?Number.POSITIVE_INFINITY:transform.inversePosition2(p(0,maxY+2+extraSpread)).y)}},canvasAccurateBounds(renderToContext,options){const precision=options&&options.precision?options.precision:.001;const resolution=options&&options.resolution?options.resolution:128;const initialScale=options&&options.initialScale?options.initialScale:1/16;let minBounds=Bounds2.NOTHING;let maxBounds=Bounds2.EVERYTHING;const canvas=document.createElement("canvas");canvas.width=resolution;canvas.height=resolution;const context=canvas.getContext("2d");if(debugChromeBoundsScanning){const header=document.createElement("h2");header.textContent="Bounds Scan";document.getElementById("display").appendChild(header)}function scan(transform){context.save();transform.matrix.canvasSetTransform(context);renderToContext(context);context.restore();const data=context.getImageData(0,0,resolution,resolution);const minMaxBounds=Utils.scanBounds(data,resolution,transform);if(debugChromeBoundsScanning){function snapshotToCanvas(snapshot){const canvas=document.createElement("canvas");canvas.width=resolution;canvas.height=resolution;const context=canvas.getContext("2d");context.putImageData(snapshot,0,0);canvas.style.border="1px solid black";document.getElementById("display").appendChild(canvas)}snapshotToCanvas(data)}context.clearRect(0,0,resolution,resolution);return minMaxBounds}function idealTransform(bounds){const borderSize=2;const scaleX=(resolution-borderSize*2)/(bounds.maxX-bounds.minX);const scaleY=(resolution-borderSize*2)/(bounds.maxY-bounds.minY);const translationX=-scaleX*bounds.minX+borderSize;const translationY=-scaleY*bounds.minY+borderSize;return new Transform3(Matrix3.translation(translationX,translationY).timesMatrix(Matrix3.scaling(scaleX,scaleY)))}const initialTransform=new Transform3;initialTransform.append(Matrix3.translation(resolution/2,resolution/2));initialTransform.append(Matrix3.scaling(initialScale));const coarseBounds=scan(initialTransform);minBounds=minBounds.union(coarseBounds.minBounds);maxBounds=maxBounds.intersection(coarseBounds.maxBounds);let tempMin;let tempMax;let refinedBounds;tempMin=maxBounds.minY;tempMax=maxBounds.maxY;while(isFinite(minBounds.minX)&&isFinite(maxBounds.minX)&&Math.abs(minBounds.minX-maxBounds.minX)>precision){refinedBounds=scan(idealTransform(new Bounds2(maxBounds.minX,tempMin,minBounds.minX,tempMax)));if(minBounds.minX<=refinedBounds.minBounds.minX&&maxBounds.minX>=refinedBounds.maxBounds.minX){if(debugChromeBoundsScanning){console.log("warning, exiting infinite loop!");console.log(`transformed "min" minX: ${idealTransform(new Bounds2(maxBounds.minX,maxBounds.minY,minBounds.minX,maxBounds.maxY)).transformPosition2(p(minBounds.minX,0))}`);console.log(`transformed "max" minX: ${idealTransform(new Bounds2(maxBounds.minX,maxBounds.minY,minBounds.minX,maxBounds.maxY)).transformPosition2(p(maxBounds.minX,0))}`)}break}minBounds=minBounds.withMinX(Math.min(minBounds.minX,refinedBounds.minBounds.minX));maxBounds=maxBounds.withMinX(Math.max(maxBounds.minX,refinedBounds.maxBounds.minX));tempMin=Math.max(tempMin,refinedBounds.maxBounds.minY);tempMax=Math.min(tempMax,refinedBounds.maxBounds.maxY)}tempMin=maxBounds.minY;tempMax=maxBounds.maxY;while(isFinite(minBounds.maxX)&&isFinite(maxBounds.maxX)&&Math.abs(minBounds.maxX-maxBounds.maxX)>precision){refinedBounds=scan(idealTransform(new Bounds2(minBounds.maxX,tempMin,maxBounds.maxX,tempMax)));if(minBounds.maxX>=refinedBounds.minBounds.maxX&&maxBounds.maxX<=refinedBounds.maxBounds.maxX){if(debugChromeBoundsScanning){console.log("warning, exiting infinite loop!")}break}minBounds=minBounds.withMaxX(Math.max(minBounds.maxX,refinedBounds.minBounds.maxX));maxBounds=maxBounds.withMaxX(Math.min(maxBounds.maxX,refinedBounds.maxBounds.maxX));tempMin=Math.max(tempMin,refinedBounds.maxBounds.minY);tempMax=Math.min(tempMax,refinedBounds.maxBounds.maxY)}tempMin=maxBounds.minX;tempMax=maxBounds.maxX;while(isFinite(minBounds.minY)&&isFinite(maxBounds.minY)&&Math.abs(minBounds.minY-maxBounds.minY)>precision){refinedBounds=scan(idealTransform(new Bounds2(tempMin,maxBounds.minY,tempMax,minBounds.minY)));if(minBounds.minY<=refinedBounds.minBounds.minY&&maxBounds.minY>=refinedBounds.maxBounds.minY){if(debugChromeBoundsScanning){console.log("warning, exiting infinite loop!")}break}minBounds=minBounds.withMinY(Math.min(minBounds.minY,refinedBounds.minBounds.minY));maxBounds=maxBounds.withMinY(Math.max(maxBounds.minY,refinedBounds.maxBounds.minY));tempMin=Math.max(tempMin,refinedBounds.maxBounds.minX);tempMax=Math.min(tempMax,refinedBounds.maxBounds.maxX)}tempMin=maxBounds.minX;tempMax=maxBounds.maxX;while(isFinite(minBounds.maxY)&&isFinite(maxBounds.maxY)&&Math.abs(minBounds.maxY-maxBounds.maxY)>precision){refinedBounds=scan(idealTransform(new Bounds2(tempMin,minBounds.maxY,tempMax,maxBounds.maxY)));if(minBounds.maxY>=refinedBounds.minBounds.maxY&&maxBounds.maxY<=refinedBounds.maxBounds.maxY){if(debugChromeBoundsScanning){console.log("warning, exiting infinite loop!")}break}minBounds=minBounds.withMaxY(Math.max(minBounds.maxY,refinedBounds.minBounds.maxY));maxBounds=maxBounds.withMaxY(Math.min(maxBounds.maxY,refinedBounds.maxBounds.maxY));tempMin=Math.max(tempMin,refinedBounds.maxBounds.minX);tempMax=Math.min(tempMax,refinedBounds.maxBounds.maxX)}if(debugChromeBoundsScanning){console.log(`minBounds: ${minBounds}`);console.log(`maxBounds: ${maxBounds}`)}const result=new Bounds2(isFinite(minBounds.minX)&&isFinite(maxBounds.minX)?(minBounds.minX+maxBounds.minX)/2:Number.POSITIVE_INFINITY,isFinite(minBounds.minY)&&isFinite(maxBounds.minY)?(minBounds.minY+maxBounds.minY)/2:Number.POSITIVE_INFINITY,isFinite(minBounds.maxX)&&isFinite(maxBounds.maxX)?(minBounds.maxX+maxBounds.maxX)/2:Number.NEGATIVE_INFINITY,isFinite(minBounds.maxY)&&isFinite(maxBounds.maxY)?(minBounds.maxY+maxBounds.maxY)/2:Number.NEGATIVE_INFINITY);result.minBounds=minBounds;result.maxBounds=maxBounds;result.isConsistent=maxBounds.containsBounds(minBounds);result.precision=Math.max(Math.abs(minBounds.minX-maxBounds.minX),Math.abs(minBounds.minY-maxBounds.minY),Math.abs(minBounds.maxX-maxBounds.maxX),Math.abs(minBounds.maxY-maxBounds.maxY));return result},toPowerOf2(n){let result=1;while(result<n){result*=2}return result},createShader(gl,source,type){const shader=gl.createShader(type);gl.shaderSource(shader,source);gl.compileShader(shader);if(!gl.getShaderParameter(shader,gl.COMPILE_STATUS)){console.log("GLSL compile error:");console.log(gl.getShaderInfoLog(shader));console.log(source)}return shader},applyWebGLContextDefaults(gl){gl.clearColor(0,0,0,0);gl.enable(gl.BLEND);gl.blendFunc(gl.ONE,gl.ONE_MINUS_SRC_ALPHA)},setWebGLEnabled(_webglEnabled){webglEnabled=_webglEnabled},checkWebGLSupport(extensions){if(!webglEnabled){return false}const canvas=document.createElement("canvas");const args={failIfMajorPerformanceCaveat:true};try{const gl=!!window.WebGLRenderingContext&&(canvas.getContext("webgl",args)||canvas.getContext("experimental-webgl",args));if(!gl){return false}if(extensions){for(let i=0;i<extensions.length;i++){if(gl.getExtension(extensions[i])===null){return false}}}return true}catch(e){return false}},checkIE11StencilSupport(){const canvas=document.createElement("canvas");try{const gl=!!window.WebGLRenderingContext&&(canvas.getContext("webgl")||canvas.getContext("experimental-webgl"));if(!gl){return false}gl.clearStencil(0);return gl.getError()===0}catch(e){return false}},get isWebGLSupported(){if(_extensionlessWebGLSupport===undefined){_extensionlessWebGLSupport=Utils.checkWebGLSupport()}return _extensionlessWebGLSupport},loseContext(gl){const extension=gl.getExtension("WEBGL_lose_context");if(extension){extension.loseContext();setTimeout(()=>{extension.restoreContext()},1e3)}},safariEmbeddingMarkWorkaround(str){if(platform.safari){const utf16CodeUnits=str.split("");let result="";let lastIsEmbeddingMark=false;for(let i=0;i<utf16CodeUnits.length;i++){const next=utf16CodeUnits[i];const nextIsEmbeddingMark=next==="‪"||next==="‫"||next==="‬";if(lastIsEmbeddingMark&&nextIsEmbeddingMark){result+="​"}result+=next;lastIsEmbeddingMark=nextIsEmbeddingMark}return result}else{return str}}};scenery.register("Utils",Utils);export default Utils;