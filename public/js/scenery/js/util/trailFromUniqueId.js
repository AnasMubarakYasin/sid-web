import{PDOM_UNIQUE_ID_SEPARATOR}from"../accessibility/pdom/PDOM_UNIQUE_ID_SEPARATOR.js";import Trail from"./Trail.js";export const trailFromUniqueId=(rootNode,uniqueId)=>{const trailIds=uniqueId.split(PDOM_UNIQUE_ID_SEPARATOR);const trailIdNumbers=trailIds.map(id=>Number(id));let currentNode=rootNode;const rootId=trailIdNumbers.shift();const nodes=[currentNode];assert&&assert(rootId===rootNode.id);while(trailIdNumbers.length>0){const trailId=trailIdNumbers.shift();const pdomOrder=currentNode.pdomOrder||[];const children=pdomOrder.concat(currentNode.children);for(let j=0;j<children.length;j++){if(children[j]!==null&&children[j].id===trailId){const childAlongTrail=children[j];nodes.push(childAlongTrail);currentNode=childAlongTrail;break}assert&&assert(j!==children.length-1,"unable to find node from unique Trail id")}}return new Trail(nodes)};