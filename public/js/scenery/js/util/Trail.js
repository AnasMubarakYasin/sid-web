import Matrix3 from"../../../dot/js/Matrix3.js";import Transform3 from"../../../dot/js/Transform3.js";import scenery from"../scenery.js";import{PDOM_UNIQUE_ID_SEPARATOR}from"../accessibility/pdom/PDOM_UNIQUE_ID_SEPARATOR.js";const ID_SEPARATOR=PDOM_UNIQUE_ID_SEPARATOR;export default class Trail{constructor(nodes){if(assert){this.immutable=undefined}if(nodes instanceof Trail){const otherTrail=nodes;this.nodes=otherTrail.nodes.slice(0);this.length=otherTrail.length;this.uniqueId=otherTrail.uniqueId;this.indices=otherTrail.indices.slice(0);return}this.nodes=[];this.length=0;this.uniqueId="";this.indices=[];if(nodes){if(nodes&&!Array.isArray(nodes)){const node=nodes;this.addDescendant(node)}else{const len=nodes.length;for(let i=0;i<len;i++){this.addDescendant(nodes[i])}}}}copy(){return new Trail(this)}isPainted(){return this.lastNode().isPainted()}isValid(){this.reindex();const indexLength=this.indices.length;for(let i=0;i<indexLength;i++){if(this.indices[i]<0){return false}}return true}isVisible(){let i=this.nodes.length;while(i--){if(!this.nodes[i].isVisible()){return false}}return true}isAccessibleVisible(){let i=this.nodes.length;while(i--){if(!this.nodes[i].isVisible()||!this.nodes[i].isAccessibleVisible()){return false}}return true}getOpacity(){let opacity=1;let i=this.nodes.length;while(i--){opacity*=this.nodes[i].getOpacity()}return opacity}isPickable(){if(_.some(this.nodes,node=>node.pickable===false||!node.visible)){return false}if(_.some(this.nodes,node=>node._inputListeners.length>0||node.pickableProperty.value===true)){return true}return false}get(index){if(index>=0){return this.nodes[index]}else{return this.nodes[this.nodes.length+index]}}slice(startIndex,endIndex){return new Trail(this.nodes.slice(startIndex,endIndex))}subtrailTo(node,excludeNode=false){return this.slice(0,_.indexOf(this.nodes,node)+(excludeNode?0:1))}isEmpty(){return this.nodes.length===0}getMatrixConcatenation(startingIndex,endingIndex){const matrix=Matrix3.identity();const nodes=this.nodes;for(let i=startingIndex;i<endingIndex;i++){matrix.multiplyMatrix(nodes[i].getMatrix())}return matrix}getMatrix(){return this.getMatrixConcatenation(0,this.nodes.length)}getAncestorMatrix(){return this.getMatrixConcatenation(1,this.nodes.length)}getParentMatrix(){return this.getMatrixConcatenation(0,this.nodes.length-1)}getAncestorParentMatrix(){return this.getMatrixConcatenation(1,this.nodes.length-1)}getTransform(){return new Transform3(this.getMatrix())}getParentTransform(){return new Transform3(this.getParentMatrix())}addAncestor(node,index){assert&&assert(!this.immutable,"cannot modify an immutable Trail with addAncestor");assert&&assert(node,"cannot add falsy value to a Trail");if(this.nodes.length){const oldRoot=this.nodes[0];this.indices.unshift(index===undefined?_.indexOf(node._children,oldRoot):index)}this.nodes.unshift(node);this.length++;this.uniqueId=this.uniqueId?node.id+ID_SEPARATOR+this.uniqueId:`${node.id}`;return this}removeAncestor(){assert&&assert(!this.immutable,"cannot modify an immutable Trail with removeAncestor");assert&&assert(this.length>0,"cannot remove a Node from an empty trail");this.nodes.shift();if(this.indices.length){this.indices.shift()}this.length--;this.updateUniqueId();return this}addDescendant(node,index){assert&&assert(!this.immutable,"cannot modify an immutable Trail with addDescendant");assert&&assert(node,"cannot add falsy value to a Trail");if(this.nodes.length){const parent=this.lastNode();this.indices.push(index===undefined?_.indexOf(parent._children,node):index)}this.nodes.push(node);this.length++;this.uniqueId=this.uniqueId?this.uniqueId+ID_SEPARATOR+node.id:`${node.id}`;return this}removeDescendant(){assert&&assert(!this.immutable,"cannot modify an immutable Trail with removeDescendant");assert&&assert(this.length>0,"cannot remove a Node from an empty trail");this.nodes.pop();if(this.indices.length){this.indices.pop()}this.length--;this.updateUniqueId();return this}addDescendantTrail(trail){const length=trail.length;if(length){this.addDescendant(trail.nodes[0])}for(let i=1;i<length;i++){this.addDescendant(trail.nodes[i],this.indices[i-1])}}removeDescendantTrail(trail){const length=trail.length;for(let i=length-1;i>=0;i--){assert&&assert(this.lastNode()===trail.nodes[i]);this.removeDescendant()}}reindex(){const length=this.length;for(let i=1;i<length;i++){const currentIndex=this.indices[i-1];const baseNode=this.nodes[i-1];if(baseNode._children[currentIndex]!==this.nodes[i]){this.indices[i-1]=_.indexOf(baseNode._children,this.nodes[i])}}}setImmutable(){if(assert){assert(this.immutable!==false,"A trail cannot be made immutable after being flagged as mutable");this.immutable=true}return this}setMutable(){if(assert){assert(this.immutable!==true,"A trail cannot be made mutable after being flagged as immutable");this.immutable=false}return this}areIndicesValid(){for(let i=1;i<this.length;i++){const currentIndex=this.indices[i-1];if(this.nodes[i-1]._children[currentIndex]!==this.nodes[i]){return false}}return true}equals(other){if(this.length!==other.length){return false}for(let i=0;i<this.nodes.length;i++){if(this.nodes[i]!==other.nodes[i]){return false}}return true}upToNode(node){const nodeIndex=_.indexOf(this.nodes,node);assert&&assert(nodeIndex>=0,"Trail does not contain the node");return this.slice(0,_.indexOf(this.nodes,node)+1)}isExtensionOf(other,allowSameTrail){if(this.length<=other.length-(allowSameTrail?1:0)){return false}for(let i=0;i<other.nodes.length;i++){if(this.nodes[i]!==other.nodes[i]){return false}}return true}containsNode(node){return _.includes(this.nodes,node)}getTransformTo(otherTrail){return new Transform3(this.getMatrixTo(otherTrail))}getMatrixTo(otherTrail){this.reindex();otherTrail.reindex();const branchIndex=this.getBranchIndexTo(otherTrail);let idx;let matrix=Matrix3.IDENTITY;for(idx=this.length-1;idx>=branchIndex;idx--){matrix=this.nodes[idx].getMatrix().timesMatrix(matrix)}for(idx=branchIndex;idx<otherTrail.length;idx++){matrix=otherTrail.nodes[idx].getTransform().getInverse().timesMatrix(matrix)}return matrix}getBranchIndexTo(otherTrail){assert&&assert(this.nodes[0]===otherTrail.nodes[0],"To get a branch index, the trails must have the same root");let branchIndex;const min=Math.min(this.length,otherTrail.length);for(branchIndex=0;branchIndex<min;branchIndex++){if(this.nodes[branchIndex]!==otherTrail.nodes[branchIndex]){break}}return branchIndex}getLastInputEnabledIndex(){let trailStartIndex=-1;for(let j=0;j<this.length;j++){if(!this.nodes[j].inputEnabled){break}trailStartIndex=j}return trailStartIndex}getCursorCheckIndex(){return this.getLastInputEnabledIndex()}nodeFromTop(offset){return this.nodes[this.length-1-offset]}lastNode(){return this.nodeFromTop(0)}rootNode(){return this.nodes[0]}previous(){if(this.nodes.length<=1){return null}const top=this.nodeFromTop(0);const parent=this.nodeFromTop(1);const parentIndex=_.indexOf(parent._children,top);assert&&assert(parentIndex!==-1);const arr=this.nodes.slice(0,this.nodes.length-1);if(parentIndex===0){return new Trail(arr)}else{arr.push(parent._children[parentIndex-1]);while(arr[arr.length-1]._children.length!==0){const last=arr[arr.length-1];arr.push(last._children[last._children.length-1])}return new Trail(arr)}}previousPainted(){let result=this.previous();while(result&&!result.isPainted()){result=result.previous()}return result}next(){const arr=this.nodes.slice(0);const top=this.nodeFromTop(0);if(top._children.length>0){arr.push(top._children[0]);return new Trail(arr)}else{let depth=this.nodes.length-1;while(depth>0){const node=this.nodes[depth];const parent=this.nodes[depth-1];arr.pop();const index=_.indexOf(parent._children,node);if(index!==parent._children.length-1){arr.push(parent._children[index+1]);return new Trail(arr)}else{depth--}}return null}}nextPainted(){let result=this.next();while(result&&!result.isPainted()){result=result.next()}return result}compare(other){assert&&assert(!this.isEmpty(),"cannot compare with an empty trail");assert&&assert(!other.isEmpty(),"cannot compare with an empty trail");assert&&assert(this.nodes[0]===other.nodes[0],"for Trail comparison, trails must have the same root node");assertSlow&&assertSlow(this.areIndicesValid(),`Trail.compare this.areIndicesValid() failed on ${this.toString()}`);assertSlow&&assertSlow(other.areIndicesValid(),`Trail.compare other.areIndicesValid() failed on ${other.toString()}`);const minNodeIndex=Math.min(this.nodes.length,other.nodes.length);for(let i=0;i<minNodeIndex;i++){if(this.nodes[i]!==other.nodes[i]){if(this.nodes[i-1].children.indexOf(this.nodes[i])<other.nodes[i-1].children.indexOf(other.nodes[i])){return-1}else{return 1}}}if(this.nodes.length<other.nodes.length){return-1}else if(this.nodes.length>other.nodes.length){return 1}else{return 0}}isBefore(other){return this.compare(other)===-1}isAfter(other){return this.compare(other)===1}localToGlobalPoint(point){return this.getMatrix().timesVector2(point)}localToGlobalBounds(bounds){return bounds.transformed(this.getMatrix())}globalToLocalPoint(point){return this.getTransform().inversePosition2(point)}globalToLocalBounds(bounds){return this.getTransform().inverseBounds2(bounds)}parentToGlobalPoint(point){return this.getParentMatrix().timesVector2(point)}parentToGlobalBounds(bounds){return bounds.transformed(this.getParentMatrix())}globalToParentPoint(point){return this.getParentTransform().inversePosition2(point)}globalToParentBounds(bounds){return this.getParentTransform().inverseBounds2(bounds)}updateUniqueId(){let result="";const len=this.nodes.length;if(len>0){result+=this.nodes[0]._id}for(let i=1;i<len;i++){result+=ID_SEPARATOR+this.nodes[i]._id}this.uniqueId=result}getUniqueId(){if(assert){const oldUniqueId=this.uniqueId;this.updateUniqueId();assert(oldUniqueId===this.uniqueId)}return this.uniqueId}toString(){this.reindex();if(!this.length){return"Empty Trail"}return`[Trail ${this.indices.join(".")} ${this.getUniqueId()}]`}toPathString(){return _.map(this.nodes,n=>{let string=n.constructor.name;if(string==="Node"){string="."}return string}).join("/")}toDebugString(){return`${this.toString()} ${this.toPathString()}`}static branchIndex(a,b){assert&&assert(a.nodes[0]===b.nodes[0],"Branch changes require roots to be the same");let branchIndex;const shortestLength=Math.min(a.length,b.length);for(branchIndex=0;branchIndex<shortestLength;branchIndex++){if(a.nodes[branchIndex]!==b.nodes[branchIndex]){break}}return branchIndex}static sharedTrail(a,b){return a.slice(0,Trail.branchIndex(a,b))}static appendAncestorTrailsWithPredicate(trailResults,trail,predicate){const root=trail.rootNode();if(predicate(root)){trailResults.push(trail.copy())}const parentCount=root._parents.length;for(let i=0;i<parentCount;i++){const parent=root._parents[i];trail.addAncestor(parent);Trail.appendAncestorTrailsWithPredicate(trailResults,trail,predicate);trail.removeAncestor()}}static appendDescendantTrailsWithPredicate(trailResults,trail,predicate){const lastNode=trail.lastNode();if(predicate(lastNode)){trailResults.push(trail.copy())}const childCount=lastNode._children.length;for(let i=0;i<childCount;i++){const child=lastNode._children[i];trail.addDescendant(child,i);Trail.appendDescendantTrailsWithPredicate(trailResults,trail,predicate);trail.removeDescendant()}}static spannedSubtrees(a,b){}}scenery.register("Trail",Trail);