import BinPacker from"../../../dot/js/BinPacker.js";import Bounds2 from"../../../dot/js/Bounds2.js";import Dimension2 from"../../../dot/js/Dimension2.js";import scenery from"../scenery.js";const MAX_DIMENSION=new Dimension2(1024,1024);const GUTTER_SIZE=1;const PADDING=1;export default class SpriteSheet{constructor(useMipmaps){this.useMipmaps=useMipmaps;this.gl=null;this.texture=null;this.bounds=new Bounds2(0,0,MAX_DIMENSION.width,MAX_DIMENSION.height);assert&&assert(this.bounds.minX===0&&this.bounds.minY===0,"Assumed constraint later on for transforms");this.width=this.bounds.width;this.height=this.bounds.height;this.canvas=document.createElement("canvas");this.canvas.width=this.width;this.canvas.height=this.height;this.context=this.canvas.getContext("2d");this.binPacker=new BinPacker(this.bounds);this.dirty=true;this.usedSprites=[];this.unusedSprites=[]}initializeContext(gl){this.gl=gl;this.createTexture()}createTexture(){const gl=this.gl;assert&&assert(gl);this.texture=gl.createTexture();gl.bindTexture(gl.TEXTURE_2D,this.texture);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_S,gl.CLAMP_TO_EDGE);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_T,gl.CLAMP_TO_EDGE);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,this.useMipmaps?gl.LINEAR_MIPMAP_LINEAR:gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.pixelStorei(gl.UNPACK_FLIP_Y_WEBGL,false);gl.pixelStorei(gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL,true);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,gl.RGBA,gl.UNSIGNED_BYTE,this.canvas);if(this.useMipmaps){gl.hint(gl.GENERATE_MIPMAP_HINT,gl.NICEST);gl.generateMipmap(gl.TEXTURE_2D)}gl.bindTexture(gl.TEXTURE_2D,null);this.dirty=false}updateTexture(){assert&&assert(this.gl,"SpriteSheet needs context to updateTexture()");if(this.dirty){this.dirty=false;const gl=this.gl;assert&&assert(gl);gl.bindTexture(gl.TEXTURE_2D,this.texture);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,gl.RGBA,gl.UNSIGNED_BYTE,this.canvas);if(this.useMipmaps){gl.hint(gl.GENERATE_MIPMAP_HINT,gl.NICEST);gl.generateMipmap(gl.TEXTURE_2D)}gl.bindTexture(gl.TEXTURE_2D,null)}}addImage(image,width,height){let i;for(i=0;i<this.usedSprites.length;i++){const usedSprite=this.usedSprites[i];if(usedSprite.image===image){usedSprite.count++;return usedSprite}}for(i=0;i<this.unusedSprites.length;i++){const unusedSprite=this.unusedSprites[i];if(unusedSprite.image===image){unusedSprite.count++;assert&&assert(unusedSprite.count===1,"Count should be exactly 1 after coming back from being unused");this.unusedSprites.splice(i,1);this.usedSprites.push(unusedSprite);return unusedSprite}}let bin;while(!(bin=this.binPacker.allocate(width+2*GUTTER_SIZE+PADDING,height+2*GUTTER_SIZE+PADDING))&&this.unusedSprites.length){const ejectedSprite=this.unusedSprites.shift();this.dirty=true;const ejectedBounds=ejectedSprite.bin.bounds;this.context.clearRect(ejectedBounds.x,ejectedBounds.y,ejectedBounds.width,ejectedBounds.height);this.binPacker.deallocate(ejectedSprite.bin)}if(bin){const uvBounds=new Bounds2((bin.bounds.minX+GUTTER_SIZE)/this.width,(bin.bounds.minY+GUTTER_SIZE)/this.height,(bin.bounds.maxX-GUTTER_SIZE-PADDING)/this.width,(bin.bounds.maxY-GUTTER_SIZE-PADDING)/this.height);const sprite=new Sprite(this,bin,uvBounds,image,1);this.copyImageWithGutter(image,width,height,bin.bounds.x,bin.bounds.y);this.dirty=true;this.usedSprites.push(sprite);return sprite}else{return null}}removeImage(image){let usedSprite;let i;for(i=0;i<this.usedSprites.length;i++){if(this.usedSprites[i].image===image){usedSprite=this.usedSprites[i];break}}assert&&assert(usedSprite,"Sprite not found for removeImage");if(--usedSprite.count<=0){this.usedSprites.splice(i,1);this.unusedSprites.push(usedSprite)}}containsImage(image){let i;for(i=0;i<this.usedSprites.length;i++){if(this.usedSprites[i].image===image){return true}}for(i=0;i<this.unusedSprites.length;i++){if(this.unusedSprites[i].image===image){return true}}return false}copyImageWithGutter(image,width,height,binX,binY){assert&&assert(GUTTER_SIZE===1);this.copyImageRegion(image,1,1,0,0,binX,binY);this.copyImageRegion(image,1,1,width-1,0,binX+1+width,binY);this.copyImageRegion(image,1,1,width-1,height-1,binX+1+width,binY+1+height);this.copyImageRegion(image,1,1,0,height-1,binX,binY+1+height);this.copyImageRegion(image,width,1,0,0,binX+1,binY);this.copyImageRegion(image,width,1,0,height-1,binX+1,binY+1+height);this.copyImageRegion(image,1,height,0,0,binX,binY+1);this.copyImageRegion(image,1,height,width-1,0,binX+1+width,binY+1);this.context.drawImage(image,binX+1,binY+1)}copyImageRegion(image,width,height,sourceX,sourceY,destinationX,destinationY){this.context.drawImage(image,sourceX,sourceY,width,height,destinationX,destinationY,width,height)}static{this.MAX_DIMENSION=MAX_DIMENSION}}scenery.register("SpriteSheet",SpriteSheet);let Sprite=class Sprite{constructor(spriteSheet,bin,uvBounds,image,initialCount){this.spriteSheet=spriteSheet;this.bin=bin;this.uvBounds=uvBounds;this.image=image;this.count=initialCount}};SpriteSheet.Sprite=Sprite;