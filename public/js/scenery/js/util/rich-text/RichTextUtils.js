import scenery from"../../scenery.js";import Text from"../../nodes/Text.js";export const isHimalayaElementNode=node=>node.type.toLowerCase()==="element";export const isHimalayaTextNode=node=>node.type.toLowerCase()==="text";const RichTextUtils={scratchText:new Text(""),himalayaGetAttribute(attribute,element){if(!element){return null}const attributeObject=_.find(element.attributes,x=>x.key===attribute);if(!attributeObject){return null}return attributeObject.value||null},himalayaStyleStringToMap(styleString){const styleElements=styleString.split(";");const styleMap={};styleElements.forEach(styleKeyValue=>{if(styleKeyValue.length>0){const keyValueTuple=styleKeyValue.split(":");assert&&assert(keyValueTuple.length===2,"too many colons");styleMap[keyValueTuple[0].trim()]=keyValueTuple[1].trim()}});return styleMap}};export default RichTextUtils;scenery.register("RichTextUtils",RichTextUtils);