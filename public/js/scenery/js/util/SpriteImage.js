import{isTReadOnlyProperty}from"../../../axon/js/TReadOnlyProperty.js";import Matrix3 from"../../../dot/js/Matrix3.js";import Vector2 from"../../../dot/js/Vector2.js";import Shape from"../../../kite/js/Shape.js";import mutate from"../../../phet-core/js/mutate.js";import optionize from"../../../phet-core/js/optionize.js";import Imageable from"../nodes/Imageable.js";import scenery from"../scenery.js";let globalIdCounter=1;const scratchVector=new Vector2(0,0);export default class SpriteImage extends Imageable(Object){constructor(image,offset,providedOptions){assert&&assert(image instanceof HTMLImageElement||image instanceof HTMLCanvasElement);const initImageableOptions={hitTestPixels:false,pickable:true};if(isTReadOnlyProperty(image)){initImageableOptions.imageProperty=image}else{initImageableOptions.image=image}const options=optionize()(initImageableOptions,providedOptions);super();this.id=globalIdCounter++;this.offset=offset;this.pickable=options.pickable;this.shape=null;this.imageData=null;if(isTReadOnlyProperty(image)){this.imageProperty=image}else{this.image=image}mutate(this,Object.keys(Imageable.DEFAULT_OPTIONS),options)}get width(){return this.imageWidth}get height(){return this.imageHeight}getShape(){if(!this.pickable){return new Shape}if(!this.shape){if(this.hitTestPixels){this.ensureImageData();if(this.imageData){this.shape=Imageable.hitTestDataToShape(this.imageData,this.width,this.height)}else{return new Shape}}else if(this.width&&this.height){this.shape=Shape.rect(0,0,this.width,this.height)}else{return new Shape}this.shape=this.shape.transformed(Matrix3.translation(-this.offset.x,-this.offset.y))}return this.shape}ensureImageData(){if(!this.imageData&&this.width&&this.height){this.imageData=Imageable.getHitTestData(this.image,this.width,this.height)}}containsPoint(point){if(!this.pickable){return false}const width=this.width;const height=this.height;if(!width&&!height){return false}const position=scratchVector.set(point).add(this.offset);if(position.x<0||position.y<0||position.x>width||position.y>height){return false}if(!this.hitTestPixels){return true}else{this.ensureImageData();if(this.imageData){return Imageable.testHitTestData(this.imageData,width,height,position)}else{return false}}}}scenery.register("SpriteImage",SpriteImage);