import Bounds2 from"../../../dot/js/Bounds2.js";import Leaf from"../nodes/Leaf.js";import Node from"../nodes/Node.js";import scenery from"../scenery.js";export default class Spacer extends Leaf(Node){constructor(width,height,options){assert&&assert(isFinite(width),"width should be a finite number");assert&&assert(isFinite(height),"height should be a finite number");super();this.localBounds=new Bounds2(0,0,width,height);this.mutate(options)}}scenery.register("Spacer",Spacer);