import cleanArray from"../../../phet-core/js/cleanArray.js";import CanvasBlock from"../display/CanvasBlock.js";import DOMBlock from"../display/DOMBlock.js";import Drawable from"../display/Drawable.js";import Renderer from"../display/Renderer.js";import scenery from"../scenery.js";import SVGBlock from"../display/SVGBlock.js";import WebGLBlock from"../display/WebGLBlock.js";let Stitcher=class Stitcher{initialize(backbone,firstDrawable,lastDrawable,oldFirstDrawable,oldLastDrawable,firstChangeInterval,lastChangeInterval){assert&&assert(firstChangeInterval&&lastChangeInterval,"We are guaranteed at least one change interval");assert&&assert(!firstDrawable||firstDrawable.previousDrawable===null,"End boundary of drawable linked list should link to null");assert&&assert(!lastDrawable||lastDrawable.nextDrawable===null,"End boundary of drawable linked list should link to null");if(sceneryLog&&sceneryLog.Stitch){sceneryLog.Stitch(`stitch ${backbone.toString()} first:${firstDrawable?firstDrawable.toString():"null"} last:${lastDrawable?lastDrawable.toString():"null"} oldFirst:${oldFirstDrawable?oldFirstDrawable.toString():"null"} oldLast:${oldLastDrawable?oldLastDrawable.toString():"null"}`);sceneryLog.push()}if(sceneryLog&&sceneryLog.StitchDrawables){sceneryLog.StitchDrawables("Before:");sceneryLog.push();Stitcher.debugDrawables(oldFirstDrawable,oldLastDrawable,firstChangeInterval,lastChangeInterval,false);sceneryLog.pop();sceneryLog.StitchDrawables("After:");sceneryLog.push();Stitcher.debugDrawables(firstDrawable,lastDrawable,firstChangeInterval,lastChangeInterval,true);sceneryLog.pop()}this.backbone=backbone;this.firstDrawable=firstDrawable;this.lastDrawable=lastDrawable;this.touchedBlocks=cleanArray(this.touchedBlocks);if(assertSlow){assertSlow(!this.initialized,"We should not be already initialized (clean should be called)");this.initialized=true;this.reindexed=false;this.pendingAdditions=[];this.pendingRemovals=[];this.pendingMoves=[];this.createdBlocks=[];this.disposedBlocks=[];this.intervalsNotified=[];this.boundariesRecorded=false;this.previousBlocks=backbone.blocks.slice(0)}}clean(){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch("clean");sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch("-----------------------------------");if(assertSlow){this.auditStitch();this.initialized=false}this.backbone=null;this.firstDrawable=null;this.lastDrawable=null;sceneryLog&&sceneryLog.Stitch&&sceneryLog.pop()}recordBackboneBoundaries(){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`recording backbone boundaries: ${this.firstDrawable?this.firstDrawable.toString():"null"} to ${this.lastDrawable?this.lastDrawable.toString():"null"}`);this.backbone.previousFirstDrawable=this.firstDrawable;this.backbone.previousLastDrawable=this.lastDrawable;if(assertSlow){this.boundariesRecorded=true}}notePendingAddition(drawable,block){assert&&assert(drawable.renderer===block.renderer);sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`pending add: ${drawable.toString()} to ${block.toString()}`);sceneryLog&&sceneryLog.Stitch&&sceneryLog.push();drawable.notePendingAddition(this.backbone.display,block,this.backbone);if(assertSlow){this.pendingAdditions.push({drawable:drawable,block:block})}sceneryLog&&sceneryLog.Stitch&&sceneryLog.pop()}notePendingMove(drawable,block){assert&&assert(drawable.renderer===block.renderer);sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`pending move: ${drawable.toString()} to ${block.toString()}`);sceneryLog&&sceneryLog.Stitch&&sceneryLog.push();drawable.notePendingMove(this.backbone.display,block);if(assertSlow){this.pendingMoves.push({drawable:drawable,block:block})}sceneryLog&&sceneryLog.Stitch&&sceneryLog.pop()}notePendingRemoval(drawable){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`pending remove: ${drawable.toString()}`);sceneryLog&&sceneryLog.Stitch&&sceneryLog.push();drawable.notePendingRemoval(this.backbone.display);if(assertSlow){this.pendingRemovals.push({drawable:drawable})}sceneryLog&&sceneryLog.Stitch&&sceneryLog.pop()}markBlockForDisposal(block){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`block for disposal: ${block.toString()}`);sceneryLog&&sceneryLog.Stitch&&sceneryLog.push();if(block.domElement.parentNode===this.backbone.domElement){this.backbone.domElement.removeChild(block.domElement)}block.markForDisposal(this.backbone.display);if(assertSlow){this.disposedBlocks.push({block:block})}sceneryLog&&sceneryLog.Stitch&&sceneryLog.pop()}removeAllBlocks(){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`marking all blocks for disposal (count ${this.backbone.blocks.length})`);sceneryLog&&sceneryLog.Stitch&&sceneryLog.push();while(this.backbone.blocks.length){const block=this.backbone.blocks[0];this.removeBlock(block);this.markBlockForDisposal(block)}sceneryLog&&sceneryLog.Stitch&&sceneryLog.pop()}notifyInterval(block,firstDrawable,lastDrawable){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`notify interval: ${block.toString()} ${firstDrawable.toString()} to ${lastDrawable.toString()}`);sceneryLog&&sceneryLog.Stitch&&sceneryLog.push();block.notifyInterval(firstDrawable,lastDrawable);this.backbone.markDirtyDrawable(block);if(assertSlow){this.intervalsNotified.push({block:block,firstDrawable:firstDrawable,lastDrawable:lastDrawable})}sceneryLog&&sceneryLog.Stitch&&sceneryLog.pop()}markBeforeBlock(block,firstDrawable){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`marking block first drawable ${block.toString()} with ${firstDrawable.toString()}`);block.pendingFirstDrawable=firstDrawable;this.touchedBlocks.push(block)}markAfterBlock(block,lastDrawable){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`marking block last drawable ${block.toString()} with ${lastDrawable.toString()}`);block.pendingLastDrawable=lastDrawable;this.touchedBlocks.push(block)}updateBlockIntervals(){while(this.touchedBlocks.length){const block=this.touchedBlocks.pop();if(block.used){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`update interval: ${block.toString()} ${block.pendingFirstDrawable.toString()} to ${block.pendingLastDrawable.toString()}`);block.updateInterval();this.backbone.markDirtyDrawable(block);if(assertSlow){this.intervalsNotified.push({block:block,firstDrawable:block.pendingFirstDrawable,lastDrawable:block.pendingLastDrawable})}}else{sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`skipping update interval: ${block.toString()}, unused`)}}}createBlock(renderer,drawable){const backbone=this.backbone;let block;if(Renderer.isCanvas(renderer)){block=CanvasBlock.createFromPool(backbone.display,renderer,backbone.transformRootInstance,backbone.backboneInstance)}else if(Renderer.isSVG(renderer)){block=SVGBlock.createFromPool(backbone.display,renderer,backbone.transformRootInstance,backbone.backboneInstance)}else if(Renderer.isDOM(renderer)){block=DOMBlock.createFromPool(backbone.display,drawable)}else if(Renderer.isWebGL(renderer)){block=WebGLBlock.createFromPool(backbone.display,renderer,backbone.transformRootInstance,backbone.backboneInstance)}else{throw new Error(`unsupported renderer for createBlock: ${renderer}`)}sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`created block: ${block.toString()} with renderer: ${renderer} for drawable: ${drawable.toString()}`);block.setBlockBackbone(backbone);backbone.domElement.appendChild(block.domElement);if(backbone.isDisplayRoot){block.domElement.setAttribute("aria-hidden",true)}backbone.markDirtyDrawable(block);if(assertSlow){this.createdBlocks.push({block:block,renderer:renderer,drawable:drawable})}return block}appendBlock(block){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`appending block: ${block.toString()}`);this.backbone.blocks.push(block);if(assertSlow){this.reindexed=false}}removeBlock(block){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch(`removing block: ${block.toString()}`);const blockIndex=_.indexOf(this.backbone.blocks,block);assert&&assert(blockIndex>=0,`Cannot remove block, not attached: ${block.toString()}`);this.backbone.blocks.splice(blockIndex,1);if(assertSlow){this.reindexed=false}}useNoBlocks(){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch("using no blocks");cleanArray(this.backbone.blocks)}reindex(){sceneryLog&&sceneryLog.Stitch&&sceneryLog.Stitch("reindexing blocks");this.backbone.reindexBlocks();if(assertSlow){this.reindexed=true}}auditStitch(){if(assertSlow){const blocks=this.backbone.blocks;const previousBlocks=this.previousBlocks;assertSlow(this.initialized,"We seem to have finished a stitch without proper initialization");assertSlow(this.boundariesRecorded,"Our stitch API requires recordBackboneBoundaries() to be called before"+" it is finished.");assertSlow(this.reindexed||blocks.length===0||previousBlocks.length===blocks.length&&_.every(_.zip(previousBlocks,blocks),arr=>arr[0]===arr[1]),"Did not reindex on a block change where we are left with blocks");_.each(this.createdBlocks,blockData=>{assertSlow(_.some(this.intervalsNotified,intervalData=>blockData.block===intervalData.block),`Created block does not seem to have an interval notified: ${blockData.block.toString()}`)});_.each(this.disposedBlocks,blockData=>{assertSlow(!_.some(this.intervalsNotified,intervalData=>blockData.block===intervalData.block),`Removed block seems to have an interval notified: ${blockData.block.toString()}`)});_.each(this.disposedBlocks,blockData=>{const block=blockData.block;_.each(Drawable.oldListToArray(block.firstDrawable,block.lastDrawable),drawable=>{assertSlow(_.some(this.pendingRemovals,removalData=>removalData.drawable===drawable)||_.some(this.pendingMoves,moveData=>moveData.drawable===drawable),`Drawable ${drawable.toString()} originally listed for disposed block ${block.toString()} does not seem to be marked for pending removal or move!`)})});_.each(this.createdBlocks,blockData=>{const block=blockData.block;_.each(Drawable.listToArray(block.pendingFirstDrawable,block.pendingLastDrawable),drawable=>{assertSlow(_.some(this.pendingAdditions,additionData=>additionData.drawable===drawable&&additionData.block===block)||_.some(this.pendingMoves,moveData=>moveData.drawable===drawable&&moveData.block===block),`Drawable ${drawable.toString()} now listed for created block ${block.toString()} does not seem to be marked for pending addition or move!`)})});_.each(this.disposedBlocks,blockData=>{const blockIdx=_.indexOf(blocks,blockData.block);assertSlow(blockIdx<0,`Disposed block ${blockData.block.toString()} still present at index ${blockIdx}`)});_.each(this.createdBlocks,blockData=>{const blockIdx=_.indexOf(blocks,blockData.block);assertSlow(blockIdx>=0,`Created block ${blockData.block.toString()} is not in the blocks array`)});_.each(blocks,block=>{assertSlow(block.used,"All current blocks should be marked as used")});assertSlow(blocks.length-previousBlocks.length===this.createdBlocks.length-this.disposedBlocks.length,`${"The count of unmodified blocks should be constant (equal differences):\n"+"created: "}${_.map(this.createdBlocks,n=>n.block.id).join(",")}
`+`disposed: ${_.map(this.disposedBlocks,n=>n.block.id).join(",")}
`+`before: ${_.map(previousBlocks,n=>n.id).join(",")}
`+`after: ${_.map(blocks,n=>n.id).join(",")}`);assertSlow(this.touchedBlocks.length===0,"If we marked any blocks for changes, we should have called updateBlockIntervals");if(blocks.length){assertSlow(this.backbone.previousFirstDrawable!==null&&this.backbone.previousLastDrawable!==null,"If we are left with at least one block, we must be tracking at least one drawable");assertSlow(blocks[0].pendingFirstDrawable===this.backbone.previousFirstDrawable,"Our first drawable should match the first drawable of our first block");assertSlow(blocks[blocks.length-1].pendingLastDrawable===this.backbone.previousLastDrawable,"Our last drawable should match the last drawable of our last block");for(let i=0;i<blocks.length-1;i++){assertSlow(blocks[i].pendingLastDrawable.nextDrawable===blocks[i+1].pendingFirstDrawable&&blocks[i].pendingLastDrawable===blocks[i+1].pendingFirstDrawable.previousDrawable,"Consecutive blocks should have boundary drawables that are also consecutive in the linked list")}}else{assertSlow(this.backbone.previousFirstDrawable===null&&this.backbone.previousLastDrawable===null,"If we are left with no blocks, it must mean we are tracking precisely zero drawables")}}}static debugIntervals(firstChangeInterval){if(sceneryLog&&sceneryLog.Stitch){for(let debugInterval=firstChangeInterval;debugInterval!==null;debugInterval=debugInterval.nextChangeInterval){sceneryLog.Stitch(`  interval: ${debugInterval.isEmpty()?"(empty) ":""}${debugInterval.drawableBefore?debugInterval.drawableBefore.toString():"-"} to ${debugInterval.drawableAfter?debugInterval.drawableAfter.toString():"-"}`)}}}static debugDrawables(firstDrawable,lastDrawable,firstChangeInterval,lastChangeInterval,useCurrent){if(sceneryLog&&sceneryLog.StitchDrawables){if(firstDrawable===null){sceneryLog.StitchDrawables("nothing","color: #666;");return}let isChanged=firstChangeInterval.drawableBefore===null;let currentInterval=firstChangeInterval;for(let drawable=firstDrawable;;drawable=useCurrent?drawable.nextDrawable:drawable.oldNextDrawable){if(isChanged&&drawable===currentInterval.drawableAfter){isChanged=false;currentInterval=currentInterval.nextChangeInterval}const drawableString=`${drawable.renderer} ${!useCurrent&&drawable.parentDrawable?drawable.parentDrawable.toString():""} ${drawable.toDetailedString()}`;sceneryLog.StitchDrawables(drawableString,isChanged?useCurrent?"color: #0a0;":"color: #a00;":"color: #666");if(!isChanged&&currentInterval&&currentInterval.drawableBefore===drawable){isChanged=true}if(drawable===lastDrawable){break}}}}};scenery.register("Stitcher",Stitcher);export default Stitcher;