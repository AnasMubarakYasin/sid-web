import Renderer from"../display/Renderer.js";import scenery from"../scenery.js";import Stitcher from"../display/Stitcher.js";let RebuildStitcher=class RebuildStitcher extends Stitcher{stitch(backbone,firstDrawable,lastDrawable,oldFirstDrawable,oldLastDrawable,firstChangeInterval,lastChangeInterval){this.initialize(backbone,firstDrawable,lastDrawable,oldFirstDrawable,oldLastDrawable,firstChangeInterval,lastChangeInterval);for(let d=backbone.previousFirstDrawable;d!==null;d=d.oldNextDrawable){this.notePendingRemoval(d);if(d===backbone.previousLastDrawable){break}}this.recordBackboneBoundaries();this.removeAllBlocks();let currentBlock=null;let currentRenderer=0;let firstDrawableForBlock=null;for(let drawable=firstDrawable;drawable!==null;drawable=drawable.nextDrawable){if(!currentBlock||drawable.renderer!==currentRenderer){if(currentBlock){this.notifyInterval(currentBlock,firstDrawableForBlock,drawable.previousDrawable)}currentRenderer=drawable.renderer;currentBlock=this.createBlock(currentRenderer,drawable);if(Renderer.isDOM(currentRenderer)){currentRenderer=0}this.appendBlock(currentBlock);firstDrawableForBlock=drawable}this.notePendingAddition(drawable,currentBlock);if(drawable===lastDrawable){break}}if(currentBlock){this.notifyInterval(currentBlock,firstDrawableForBlock,lastDrawable)}this.reindex();this.clean()}};scenery.register("RebuildStitcher",RebuildStitcher);export default RebuildStitcher;