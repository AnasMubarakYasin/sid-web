import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import cleanArray from"../../../phet-core/js/cleanArray.js";import Poolable from"../../../phet-core/js/Poolable.js";import Drawable from"../display/Drawable.js";import GreedyStitcher from"../display/GreedyStitcher.js";import RebuildStitcher from"../display/RebuildStitcher.js";import scenery from"../scenery.js";import Stitcher from"../display/Stitcher.js";import Utils from"../util/Utils.js";const useGreedyStitcher=true;let BackboneDrawable=class BackboneDrawable extends Drawable{constructor(display,backboneInstance,transformRootInstance,renderer,isDisplayRoot){super();this.initialize(display,backboneInstance,transformRootInstance,renderer,isDisplayRoot)}initialize(display,backboneInstance,transformRootInstance,renderer,isDisplayRoot){super.initialize(renderer);this.display=display;this.backboneInstance=backboneInstance;this.transformRootInstance=transformRootInstance;this.filterRootAncestorInstance=backboneInstance.parent?backboneInstance.parent.getFilterRootInstance():backboneInstance;this.transformRootAncestorInstance=backboneInstance.parent?backboneInstance.parent.getTransformRootInstance():backboneInstance;this.willApplyTransform=this.transformRootAncestorInstance!==this.transformRootInstance;this.willApplyFilters=this.filterRootAncestorInstance!==this.backboneInstance;this.transformListener=this.transformListener||this.markTransformDirty.bind(this);if(this.willApplyTransform){this.backboneInstance.relativeTransform.addListener(this.transformListener);this.backboneInstance.relativeTransform.addPrecompute()}this.backboneVisibilityListener=this.backboneVisibilityListener||this.updateBackboneVisibility.bind(this);this.backboneInstance.relativeVisibleEmitter.addListener(this.backboneVisibilityListener);this.updateBackboneVisibility();this.visibilityDirty=true;this.renderer=renderer;this.domElement=isDisplayRoot?display.domElement:BackboneDrawable.createDivBackbone();this.isDisplayRoot=isDisplayRoot;this.dirtyDrawables=cleanArray(this.dirtyDrawables);Utils.prepareForTransform(this.domElement);this.watchedFilterNodes=cleanArray(this.watchedFilterNodes);this.filterDirty=true;this.clipDirty=true;this.filterDirtyListener=this.filterDirtyListener||this.onFilterDirty.bind(this);this.clipDirtyListener=this.clipDirtyListener||this.onClipDirty.bind(this);if(this.willApplyFilters){assert&&assert(this.filterRootAncestorInstance.trail.nodes.length<this.backboneInstance.trail.nodes.length,"Our backboneInstance should be deeper if we are applying filters");for(let instance=this.backboneInstance;instance!==this.filterRootAncestorInstance;instance=instance.parent){const node=instance.node;this.watchedFilterNodes.push(node);node.filterChangeEmitter.addListener(this.filterDirtyListener);node.clipAreaProperty.lazyLink(this.clipDirtyListener)}}this.lastZIndex=0;this.blocks=this.blocks||[];this.previousFirstDrawable=null;this.previousLastDrawable=null;this.removedDrawables=false;this.stitcher=this.stitcher||(useGreedyStitcher?new GreedyStitcher:new RebuildStitcher);sceneryLog&&sceneryLog.BackboneDrawable&&sceneryLog.BackboneDrawable(`initialized ${this.toString()}`)}dispose(){sceneryLog&&sceneryLog.BackboneDrawable&&sceneryLog.BackboneDrawable(`dispose ${this.toString()}`);sceneryLog&&sceneryLog.BackboneDrawable&&sceneryLog.push();while(this.watchedFilterNodes.length){const node=this.watchedFilterNodes.pop();node.filterChangeEmitter.removeListener(this.filterDirtyListener);node.clipAreaProperty.unlink(this.clipDirtyListener)}this.backboneInstance.relativeVisibleEmitter.removeListener(this.backboneVisibilityListener);if(!this.removedDrawables){for(let d=this.previousFirstDrawable;d!==null;d=d.nextDrawable){d.parentDrawable.removeDrawable(d);if(d===this.previousLastDrawable){break}}}this.markBlocksForDisposal();if(this.willApplyTransform){this.backboneInstance.relativeTransform.removeListener(this.transformListener);this.backboneInstance.relativeTransform.removePrecompute()}this.backboneInstance=null;this.transformRootInstance=null;this.filterRootAncestorInstance=null;this.transformRootAncestorInstance=null;cleanArray(this.dirtyDrawables);cleanArray(this.watchedFilterNodes);this.previousFirstDrawable=null;this.previousLastDrawable=null;super.dispose();sceneryLog&&sceneryLog.BackboneDrawable&&sceneryLog.pop()}markBlocksForDisposal(){while(this.blocks.length){const block=this.blocks.pop();sceneryLog&&sceneryLog.BackboneDrawable&&sceneryLog.BackboneDrawable(`${this.toString()} removing block: ${block.toString()}`);if(block.domElement.parentNode===this.domElement){this.domElement.removeChild(block.domElement)}block.markForDisposal(this.display)}}updateBackboneVisibility(){this.visible=this.backboneInstance.relativeVisible;if(!this.visibilityDirty){this.visibilityDirty=true;this.markDirty()}}markForDisposal(display){for(let d=this.previousFirstDrawable;d!==null;d=d.oldNextDrawable){d.notePendingRemoval(this.display);if(d===this.previousLastDrawable){break}}this.removedDrawables=true;super.markForDisposal(display)}markDirtyDrawable(drawable){if(assert){this.display.ensureNotPainting()}this.dirtyDrawables.push(drawable);this.markDirty()}markTransformDirty(){assert&&assert(this.willApplyTransform,"Sanity check for willApplyTransform");Utils.applyPreparedTransform(this.backboneInstance.relativeTransform.matrix,this.domElement)}onFilterDirty(){if(!this.filterDirty){this.filterDirty=true;this.markDirty()}}onClipDirty(){if(!this.clipDirty){this.clipDirty=true;this.markDirty()}}update(){if(!super.update()){return false}while(this.dirtyDrawables.length){this.dirtyDrawables.pop().update()}if(this.filterDirty){this.filterDirty=false;let filterString="";const len=this.watchedFilterNodes.length;for(let i=0;i<len;i++){const node=this.watchedFilterNodes[i];const opacity=node.getEffectiveOpacity();for(let j=0;j<node._filters.length;j++){filterString+=`${filterString?" ":""}${node._filters[j].getCSSFilterString()}`}if(opacity!==1){filterString+=`${filterString?" ":""}opacity(${toSVGNumber(opacity)})`}}this.domElement.style.filter=filterString}if(this.visibilityDirty){this.visibilityDirty=false;this.domElement.style.display=this.visible?"":"none"}if(this.clipDirty){this.clipDirty=false}return true}getFilterVisibility(){const len=this.watchedFilterNodes.length;for(let i=0;i<len;i++){if(!this.watchedFilterNodes[i].isVisible()){return false}}return true}getFilterClip(){const clip="";return clip}reindexBlocks(){let zIndex=0;for(let k=0;k<this.blocks.length;k++){const block=this.blocks[k];if(block.zIndex<=zIndex){const newIndex=k+1<this.blocks.length&&this.blocks[k+1].zIndex-1>zIndex?Math.ceil((zIndex+this.blocks[k+1].zIndex)/2):zIndex+20;block.domElement.style.zIndex=block.zIndex=newIndex}zIndex=block.zIndex;if(assert){assert(this.blocks[k].zIndex%1===0,"z-indices should be integers");assert(this.blocks[k].zIndex>0,"z-indices should be greater than zero for our needs (see spec)");if(k>0){assert(this.blocks[k-1].zIndex<this.blocks[k].zIndex,"z-indices should be strictly increasing")}}}this.lastZIndex=zIndex+1}stitch(firstDrawable,lastDrawable,firstChangeInterval,lastChangeInterval){if(firstChangeInterval===null||lastChangeInterval===null){assert&&assert(firstChangeInterval===null);assert&&assert(lastChangeInterval===null);return}assert&&assert(lastChangeInterval.nextChangeInterval===null,"This allows us to have less checks in the loop");if(sceneryLog&&sceneryLog.Stitch){sceneryLog.Stitch(`Stitch intervals before constricting: ${this.toString()}`);sceneryLog.push();Stitcher.debugIntervals(firstChangeInterval);sceneryLog.pop()}let lastNonemptyInterval=null;let interval=firstChangeInterval;let intervalsChanged=false;while(interval){intervalsChanged=interval.constrict()||intervalsChanged;if(interval.isEmpty()){assert&&assert(intervalsChanged);if(lastNonemptyInterval){lastNonemptyInterval.nextChangeInterval=interval.nextChangeInterval}}else{if(!lastNonemptyInterval){firstChangeInterval=interval}lastNonemptyInterval=interval}interval=interval.nextChangeInterval}if(!lastNonemptyInterval){return}lastChangeInterval=lastNonemptyInterval;lastChangeInterval.nextChangeInterval=null;if(sceneryLog&&sceneryLog.Stitch&&intervalsChanged){sceneryLog.Stitch(`Stitch intervals after constricting: ${this.toString()}`);sceneryLog.push();Stitcher.debugIntervals(firstChangeInterval);sceneryLog.pop()}if(sceneryLog&&scenery.isLoggingPerformance()){this.display.perfStitchCount++;let dInterval=firstChangeInterval;while(dInterval){this.display.perfIntervalCount++;this.display.perfDrawableOldIntervalCount+=dInterval.getOldInternalDrawableCount(this.previousFirstDrawable,this.previousLastDrawable);this.display.perfDrawableNewIntervalCount+=dInterval.getNewInternalDrawableCount(firstDrawable,lastDrawable);dInterval=dInterval.nextChangeInterval}}this.stitcher.stitch(this,firstDrawable,lastDrawable,this.previousFirstDrawable,this.previousLastDrawable,firstChangeInterval,lastChangeInterval)}audit(allowPendingBlock,allowPendingList,allowDirty){if(assertSlow){super.audit(allowPendingBlock,allowPendingList,allowDirty);assertSlow&&assertSlow(this.backboneInstance.isBackbone,"We should reference an instance that requires a backbone");assertSlow&&assertSlow(this.transformRootInstance.isTransformed,"Transform root should be transformed");for(let i=0;i<this.blocks.length;i++){this.blocks[i].audit(allowPendingBlock,allowPendingList,allowDirty)}}}static createRootDivBackbone(){const div=document.createElement("div");div.style.position="absolute";div.style.left="0";div.style.top="0";div.style.width="0";div.style.height="0";return div}static createDivBackbone(){const div=document.createElement("div");div.style.position="relative";div.style.left="0";div.style.top="0";return div}static repurposeBackboneContainer(element){if(element.style.position!=="relative"||element.style.position!=="absolute"){element.style.position="relative"}element.style.left="0";element.style.top="0";return element}};scenery.register("BackboneDrawable",BackboneDrawable);Poolable.mixInto(BackboneDrawable);export default BackboneDrawable;