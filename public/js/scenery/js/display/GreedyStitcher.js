import cleanArray from"../../../phet-core/js/cleanArray.js";import Block from"../display/Block.js";import ChangeInterval from"../display/ChangeInterval.js";import Drawable from"../display/Drawable.js";import Renderer from"../display/Renderer.js";import scenery from"../scenery.js";import Stitcher from"../display/Stitcher.js";function hasGapBetweenDrawables(a,b){return a.renderer!==b.renderer||Renderer.isDOM(a.renderer)||Renderer.isDOM(b.renderer)}function isOpenBefore(drawable){return drawable.previousDrawable!==null&&!hasGapBetweenDrawables(drawable.previousDrawable,drawable)}function isOpenAfter(drawable){return drawable.nextDrawable!==null&&!hasGapBetweenDrawables(drawable,drawable.nextDrawable)}function intervalHasNewInternalDrawables(interval,firstStitchDrawable,lastStitchDrawable){if(interval.drawableBefore){return interval.drawableBefore.nextDrawable!==interval.drawableAfter}else if(interval.drawableAfter){return interval.drawableAfter.previousDrawable!==interval.drawableBefore}else{return firstStitchDrawable!==null}}function intervalHasOldInternalDrawables(interval,oldFirstStitchDrawable,oldLastStitchDrawable){if(interval.drawableBefore){return interval.drawableBefore.oldNextDrawable!==interval.drawableAfter}else if(interval.drawableAfter){return interval.drawableAfter.oldPreviousDrawable!==interval.drawableBefore}else{return oldFirstStitchDrawable!==null}}function intervalHasOldInternalBlocks(interval,firstStitchBlock,lastStitchBlock){const beforeBlock=interval.drawableBefore?interval.drawableBefore.parentDrawable:null;const afterBlock=interval.drawableAfter?interval.drawableAfter.parentDrawable:null;if(beforeBlock&&afterBlock&&beforeBlock===afterBlock){return false}if(beforeBlock){return beforeBlock.nextBlock!==afterBlock}else if(afterBlock){return afterBlock.previousBlock!==beforeBlock}else{return firstStitchBlock!==null}}function getLastCompatibleExternalDrawable(interval){const firstDrawable=interval.drawableAfter;if(firstDrawable){const renderer=firstDrawable.renderer;const cutoffDrawable=interval.nextChangeInterval?interval.nextChangeInterval.drawableBefore.nextDrawable:null;let drawable=firstDrawable;while(true){const nextDrawable=drawable.nextDrawable;if(nextDrawable!==cutoffDrawable&&nextDrawable.renderer===renderer){drawable=nextDrawable}else{break}}return drawable}else{return null}}let GreedyStitcher=class GreedyStitcher extends Stitcher{stitch(backbone,firstStitchDrawable,lastStitchDrawable,oldFirstStitchDrawable,oldLastStitchDrawable,firstChangeInterval,lastChangeInterval){this.initialize(backbone,firstStitchDrawable,lastStitchDrawable,oldFirstStitchDrawable,oldLastStitchDrawable,firstChangeInterval,lastChangeInterval);this.blockOrderChanged=false;this.reusableBlocks=cleanArray(this.reusableBlocks);this.blockWasAdded=false;let interval;this.recordBackboneBoundaries();sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.GreedyStitcher("phase 1: old linked list");sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.push();if(backbone.blocks.length){const veryFirstBlock=backbone.blocks[0];const veryLastBlock=backbone.blocks[backbone.blocks.length-1];for(interval=firstChangeInterval;interval!==null;interval=interval.nextChangeInterval){assert&&assert(!interval.isEmpty(),"We now guarantee that the intervals are non-empty");if(intervalHasOldInternalDrawables(interval,oldFirstStitchDrawable,oldLastStitchDrawable)){const firstRemoval=interval.drawableBefore?interval.drawableBefore.oldNextDrawable:oldFirstStitchDrawable;const lastRemoval=interval.drawableAfter?interval.drawableAfter.oldPreviousDrawable:oldLastStitchDrawable;for(let removedDrawable=firstRemoval;;removedDrawable=removedDrawable.oldNextDrawable){this.notePendingRemoval(removedDrawable);if(removedDrawable===lastRemoval){break}}}if(intervalHasOldInternalBlocks(interval,veryFirstBlock,veryLastBlock)){const firstBlock=interval.drawableBefore===null?backbone.blocks[0]:interval.drawableBefore.parentDrawable.nextBlock;const lastBlock=interval.drawableAfter===null?backbone.blocks[backbone.blocks.length-1]:interval.drawableAfter.parentDrawable.previousBlock;for(let markedBlock=firstBlock;;markedBlock=markedBlock.nextBlock){this.unuseBlock(markedBlock);if(markedBlock===lastBlock){break}}}}}sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.pop();sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.GreedyStitcher("phase 2: new linked list");sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.push();if(firstStitchDrawable){for(interval=firstChangeInterval;interval!==null;interval=interval.nextChangeInterval){this.processInterval(backbone,interval,firstStitchDrawable,lastStitchDrawable)}}sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.pop();sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.GreedyStitcher("phase 3: cleanup");sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.push();this.removeUnusedBlocks();this.updateBlockIntervals();if(firstStitchDrawable===null){this.useNoBlocks()}else if(this.blockOrderChanged){this.processBlockLinkedList(backbone,firstStitchDrawable.pendingParentDrawable,lastStitchDrawable.pendingParentDrawable);this.reindex()}this.clean();cleanArray(this.reusableBlocks);sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.pop()}processInterval(backbone,interval,firstStitchDrawable,lastStitchDrawable){assert&&assert(interval instanceof ChangeInterval);assert&&assert(firstStitchDrawable instanceof Drawable,"We assume we have a non-null remaining section");assert&&assert(lastStitchDrawable instanceof Drawable,"We assume we have a non-null remaining section");assert&&assert(!interval.isEmpty(),"We now guarantee that the intervals are non-empty");sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`interval: ${interval.drawableBefore?interval.drawableBefore.toString():"null"} to ${interval.drawableAfter?interval.drawableAfter.toString():"null"}`);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();if(!intervalHasNewInternalDrawables(interval,firstStitchDrawable,lastStitchDrawable)){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("no current internal drawables in interval");if(interval.drawableBefore&&interval.drawableAfter){assert&&assert(interval.drawableBefore.nextDrawable===interval.drawableAfter);const isOpen=!hasGapBetweenDrawables(interval.drawableBefore,interval.drawableAfter);this.processEdgeCases(interval,isOpen,isOpen)}if(interval.drawableBefore&&!isOpenAfter(interval.drawableBefore)){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("closed-after collapsed link:");sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();this.linkAfterDrawable(interval.drawableBefore);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}else if(interval.drawableAfter&&!isOpenBefore(interval.drawableAfter)){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("closed-before collapsed link:");sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();this.linkBeforeDrawable(interval.drawableAfter);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}}else{let drawable=interval.drawableBefore?interval.drawableBefore.nextDrawable:firstStitchDrawable;let subBlockFirstDrawable=null;let matchedBlock=null;let isFirst=true;while(true){const nextDrawable=drawable.nextDrawable;const isLast=nextDrawable===interval.drawableAfter;assert&&assert(nextDrawable!==null||isLast,"If our nextDrawable is null, isLast must be true");if(!subBlockFirstDrawable){subBlockFirstDrawable=drawable}if(matchedBlock===null&&drawable.parentDrawable&&!drawable.parentDrawable.used&&drawable.backbone===backbone&&drawable.parentDrawable.parentDrawable===backbone){matchedBlock=drawable.parentDrawable;sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`matching at ${drawable.toString()} with ${matchedBlock}`)}if(isLast||hasGapBetweenDrawables(drawable,nextDrawable)){if(isFirst){this.processEdgeCases(interval,isOpenBefore(subBlockFirstDrawable),isOpenAfter(drawable))}this.processSubBlock(interval,subBlockFirstDrawable,drawable,matchedBlock,isFirst,isLast);subBlockFirstDrawable=null;matchedBlock=null;isFirst=false}if(isLast){break}else{drawable=nextDrawable}}}sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}processSubBlock(interval,firstDrawable,lastDrawable,matchedBlock,isFirst,isLast){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`sub-block: ${firstDrawable.toString()} to ${lastDrawable.toString()} ${matchedBlock?`with matched: ${matchedBlock.toString()}`:"with no match"}`);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();const openBefore=isOpenBefore(firstDrawable);const openAfter=isOpenAfter(lastDrawable);assert&&assert(!openBefore||isFirst,"openBefore implies isFirst");assert&&assert(!openAfter||isLast,"openAfter implies isLast");assert&&assert(!openBefore||!openAfter||firstDrawable.previousDrawable.pendingParentDrawable===lastDrawable.nextDrawable.pendingParentDrawable,"If we would use both the before and after blocks, make sure any gluing ");if(openBefore){matchedBlock=firstDrawable.previousDrawable.pendingParentDrawable;sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`combining into before block: ${matchedBlock.toString()}`)}if(openAfter){matchedBlock=lastDrawable.nextDrawable.pendingParentDrawable;sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`combining into after block: ${matchedBlock.toString()}`)}matchedBlock=this.ensureUsedBlock(matchedBlock,firstDrawable);for(let drawable=firstDrawable;;drawable=drawable.nextDrawable){this.notePendingAddition(drawable,matchedBlock);if(drawable===lastDrawable){break}}if(!openBefore){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("closed-before link:");sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();this.linkBeforeDrawable(firstDrawable);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}if(isLast&&!openAfter){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("last closed-after link:");sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();this.linkAfterDrawable(lastDrawable);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}processEdgeCases(interval,openBefore,openAfter){if(interval.drawableBefore!==null&&interval.drawableAfter!==null){const beforeBlock=interval.drawableBefore.pendingParentDrawable;const afterBlock=interval.drawableAfter.pendingParentDrawable;const nextAfterBlock=interval.nextChangeInterval&&interval.nextChangeInterval.drawableAfter?interval.nextChangeInterval.drawableAfter.pendingParentDrawable:null;if(beforeBlock===afterBlock){this.blockWasAdded=true}sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`edge case: ${openBefore?"open-before ":""}${openAfter?"open-after ":""}${beforeBlock.toString()} to ${afterBlock.toString()}`);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();let newAfterBlock;if(openBefore&&openAfter){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`glue using ${beforeBlock.toString()}`);newAfterBlock=beforeBlock}else{if(this.blockWasAdded||beforeBlock===afterBlock){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("split with fresh block");newAfterBlock=this.createBlock(interval.drawableAfter.renderer,interval.drawableAfter);this.blockOrderChanged=true}else{sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`split with same afterBlock ${afterBlock.toString()}`);newAfterBlock=afterBlock}}if(afterBlock===newAfterBlock){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("no externals change here (blockWasAdded => true)");this.blockWasAdded=true}else{sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("moving externals");sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.push();this.changeExternals(interval,newAfterBlock);sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}if(nextAfterBlock!==afterBlock){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("end of afterBlock stretch");if(!this.blockWasAdded){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`unusing ${afterBlock.toString()}`);this.unuseBlock(afterBlock)}this.blockWasAdded=false}sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.pop()}}changeExternals(interval,newBlock){const lastExternalDrawable=getLastCompatibleExternalDrawable(interval);this.notePendingMoves(newBlock,interval.drawableAfter,lastExternalDrawable);if(!interval.nextChangeInterval||interval.nextChangeInterval.drawableBefore!==lastExternalDrawable){this.linkAfterDrawable(lastExternalDrawable)}}notePendingMoves(newBlock,firstDrawable,lastDrawable){for(let drawable=firstDrawable;;drawable=drawable.nextDrawable){assert&&assert(!drawable.pendingAddition&&!drawable.pendingRemoval,"Moved drawables should be thought of as unchanged, and thus have nothing pending yet");this.notePendingMove(drawable,newBlock);if(drawable===lastDrawable){break}}}ensureUsedBlock(currentBlock,someIncludedDrawable){if(currentBlock){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`using existing block: ${currentBlock.toString()}`);if(!currentBlock.used){this.useBlock(currentBlock)}}else{sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose("searching for block");currentBlock=this.getBlockForRenderer(someIncludedDrawable.renderer,someIncludedDrawable)}return currentBlock}getBlockForRenderer(renderer,drawable){let block;if(!Renderer.isDOM(renderer)){for(let i=this.reusableBlocks.length-1;i>=0;i--){const tmpBlock=this.reusableBlocks[i];assert&&assert(!tmpBlock.used);if(tmpBlock.renderer===renderer){this.useBlockAtIndex(tmpBlock,i);block=tmpBlock;break}}}if(!block){block=this.createBlock(renderer,drawable)}this.blockOrderChanged=true;return block}unuseBlock(block){if(block.used){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`unusing block: ${block.toString()}`);block.used=false;this.reusableBlocks.push(block)}else{sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`not using already-unused block: ${block.toString()}`)}}useBlock(block){this.useBlockAtIndex(block,_.indexOf(this.reusableBlocks,block))}useBlockAtIndex(block,index){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`using reusable block: ${block.toString()} with renderer: ${block.renderer}`);assert&&assert(index>=0&&this.reusableBlocks[index]===block,`bad index for useBlockAtIndex: ${index}`);assert&&assert(!block.used,"Should be called on an unused (reusable) block");this.reusableBlocks.splice(index,1);block.used=true}removeUnusedBlocks(){sceneryLog&&sceneryLog.GreedyStitcher&&this.reusableBlocks.length&&sceneryLog.GreedyStitcher("removeUnusedBlocks");sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.push();while(this.reusableBlocks.length){const block=this.reusableBlocks.pop();this.markBlockForDisposal(block);this.blockOrderChanged=true}sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.pop()}linkBeforeDrawable(drawable){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`link before ${drawable.toString()}`);const beforeDrawable=drawable.previousDrawable;this.linkBlocks(beforeDrawable?beforeDrawable.pendingParentDrawable:null,drawable.pendingParentDrawable,beforeDrawable,drawable)}linkAfterDrawable(drawable){sceneryLog&&sceneryLog.GreedyVerbose&&sceneryLog.GreedyVerbose(`link after ${drawable.toString()}`);const afterDrawable=drawable.nextDrawable;this.linkBlocks(drawable.pendingParentDrawable,afterDrawable?afterDrawable.pendingParentDrawable:null,drawable,afterDrawable)}linkBlocks(beforeBlock,afterBlock,beforeDrawable,afterDrawable){sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.GreedyStitcher(`linking blocks: ${beforeBlock?`${beforeBlock.toString()} (${beforeDrawable.toString()})`:"null"} to ${afterBlock?`${afterBlock.toString()} (${afterDrawable.toString()})`:"null"}`);sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.push();assert&&assert(beforeBlock===null&&beforeDrawable===null||beforeBlock instanceof Block&&beforeDrawable instanceof Drawable);assert&&assert(afterBlock===null&&afterDrawable===null||afterBlock instanceof Block&&afterDrawable instanceof Drawable);if(beforeBlock){if(beforeBlock.nextBlock!==afterBlock){this.blockOrderChanged=true;if(beforeBlock.nextBlock){beforeBlock.nextBlock.previousBlock=null}beforeBlock.nextBlock=afterBlock}this.markAfterBlock(beforeBlock,beforeDrawable)}if(afterBlock){if(afterBlock.previousBlock!==beforeBlock){this.blockOrderChanged=true;if(afterBlock.previousBlock){afterBlock.previousBlock.nextBlock=null}afterBlock.previousBlock=beforeBlock}this.markBeforeBlock(afterBlock,afterDrawable)}sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.pop()}processBlockLinkedList(backbone,firstBlock,lastBlock){while(backbone.blocks.length){backbone.blocks.pop()}sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.GreedyStitcher(`processBlockLinkedList: ${firstBlock.toString()} to ${lastBlock.toString()}`);sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.push();if(firstBlock){for(let block=firstBlock;;block=block.nextBlock){sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.GreedyStitcher(block.toString());backbone.blocks.push(block);if(block===lastBlock){break}}}sceneryLog&&sceneryLog.GreedyStitcher&&sceneryLog.pop()}};scenery.register("GreedyStitcher",GreedyStitcher);export default GreedyStitcher;