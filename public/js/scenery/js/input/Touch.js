import Pointer from"../input/Pointer.js";import scenery from"../scenery.js";export default class Touch extends Pointer{constructor(id,point,event){super(point,"touch");this.id=id;sceneryLog&&sceneryLog.Pointer&&sceneryLog.Pointer(`Created ${this.toString()}`)}move(point){const pointChanged=this.hasPointChanged(point);this.point=point;return pointChanged}toString(){return`Touch#${this.id}`}isTouchLike(){return true}}scenery.register("Touch",Touch);