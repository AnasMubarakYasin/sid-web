import BooleanProperty from"../../../axon/js/BooleanProperty.js";import Vector2 from"../../../dot/js/Vector2.js";import Enumeration from"../../../phet-core/js/Enumeration.js";import EnumerationValue from"../../../phet-core/js/EnumerationValue.js";import IOType from"../../../tandem/js/types/IOType.js";import StringIO from"../../../tandem/js/types/StringIO.js";import scenery from"../scenery.js";export class Intent extends EnumerationValue{static{this.DRAG=new Intent}static{this.KEYBOARD_DRAG=new Intent}static{this.enumeration=new Enumeration(Intent,{phetioDocumentation:"entries when signifying Intent of the pointer"})}}export default class Pointer{static{this.PointerIO=new IOType("PointerIO",{valueType:Pointer,toStateObject:pointer=>{return{point:pointer.point.toStateObject(),type:pointer.type}},stateSchema:{point:Vector2.Vector2IO,type:StringIO}})}constructor(initialPoint,type){assert&&assert(initialPoint===null||initialPoint instanceof Vector2);assert&&assert(Object.getPrototypeOf(this)!==Pointer.prototype,"Pointer is an abstract class");this.point=initialPoint;this.type=type;this.trail=null;this.inputEnabledTrail=null;this.isDownProperty=new BooleanProperty(false);this.attachedProperty=new BooleanProperty(false);this._listeners=[];this._attachedListener=null;this._cursor=null;this.lastEventContext=null;this._intents=[];this._pointerCaptured=false;this._listenerForDragReserve=null;this._listenerForKeyboardDragReserve=null}setCursor(cursor){this._cursor=cursor;return this}set cursor(value){this.setCursor(value)}get cursor(){return this.getCursor()}getCursor(){return this._cursor}getListeners(){return this._listeners.slice()}get listeners(){return this.getListeners()}addInputListener(listener,attach){sceneryLog&&sceneryLog.Pointer&&sceneryLog.Pointer(`addInputListener to ${this.toString()} attach:${attach}`);sceneryLog&&sceneryLog.Pointer&&sceneryLog.push();assert&&assert(listener,"A listener must be provided");assert&&assert(attach===undefined||typeof attach==="boolean","If provided, the attach parameter should be a boolean value");assert&&assert(!_.includes(this._listeners,listener),"Attempted to add an input listener that was already added");this._listeners.push(listener);if(attach){assert&&assert(listener.interrupt,"Interrupt should exist on attached listeners");this.attach(listener)}sceneryLog&&sceneryLog.Pointer&&sceneryLog.pop()}removeInputListener(listener){sceneryLog&&sceneryLog.Pointer&&sceneryLog.Pointer(`removeInputListener to ${this.toString()}`);sceneryLog&&sceneryLog.Pointer&&sceneryLog.push();assert&&assert(listener,"A listener must be provided");const index=_.indexOf(this._listeners,listener);assert&&assert(index!==-1,"Could not find the input listener to remove");if(this.isAttached()&&listener===this._attachedListener){this.detach(listener)}this._listeners.splice(index,1);sceneryLog&&sceneryLog.Pointer&&sceneryLog.pop()}getAttachedListener(){return this._attachedListener}get attachedListener(){return this.getAttachedListener()}isAttached(){return this.attachedProperty.value}isTouchLike(){return false}set isDown(value){this.isDownProperty.value=value}get isDown(){return this.isDownProperty.value}interruptAttached(){if(this.isAttached()){this._attachedListener.interrupt()}}interruptAll(){const listeners=this._listeners.slice();for(let i=0;i<listeners.length;i++){const listener=listeners[i];listener.interrupt&&listener.interrupt()}}attach(listener){sceneryLog&&sceneryLog.Pointer&&sceneryLog.Pointer(`Attaching to ${this.toString()}`);assert&&assert(!this.isAttached(),"Attempted to attach to an already attached pointer");this.attachedProperty.value=true;this._attachedListener=listener}updatePoint(point,eventName="event"){const pointChanged=this.hasPointChanged(point);sceneryLog&&point&&sceneryLog.InputEvent&&sceneryLog.InputEvent(`pointer ${eventName} at ${point.toString()}`);this.point=point;return pointChanged}down(event){this.isDown=true}up(point,event){this.isDown=false;return this.updatePoint(point,"up")}cancel(point){this.isDown=false;return this.updatePoint(point,"cancel")}detach(listener){sceneryLog&&sceneryLog.Pointer&&sceneryLog.Pointer(`Detaching from ${this.toString()}`);assert&&assert(this.isAttached(),"Cannot detach a listener if one is not attached");assert&&assert(this._attachedListener===listener,"Cannot detach a different listener");this.attachedProperty.value=false;this._attachedListener=null}hasPointChanged(point){return this.point!==point&&(!point||!this.point||!this.point.equals(point))}addIntent(intent){assert&&assert(Intent.enumeration.includes(intent),"trying to set unsupported intent for Pointer");if(!this._intents.includes(intent)){this._intents.push(intent)}assert&&assert(this._intents.length<=Intent.enumeration.values.length,"to many Intents saved, memory leak likely")}removeIntent(intent){assert&&assert(Intent.enumeration.includes(intent),"trying to set unsupported intent for Pointer");if(this._intents.includes(intent)){const index=this._intents.indexOf(intent);this._intents.splice(index,1)}}hasIntent(intent){return this._intents.includes(intent)}reserveForDrag(){if(!this._intents.includes(Intent.DRAG)){this.addIntent(Intent.DRAG);const listener={up:event=>{this.removeIntent(Intent.DRAG);this.removeInputListener(this._listenerForDragReserve);this._listenerForDragReserve=null}};assert&&assert(this._listenerForDragReserve===null,"still a listener to reserve pointer, memory leak likely");this._listenerForDragReserve=listener;this.addInputListener(this._listenerForDragReserve)}}reserveForKeyboardDrag(){if(!this._intents.includes(Intent.KEYBOARD_DRAG)){this.addIntent(Intent.KEYBOARD_DRAG);const listener={keyup:event=>clearIntent(),blur:event=>clearIntent()};const clearIntent=()=>{this.removeIntent(Intent.KEYBOARD_DRAG);this.removeInputListener(this._listenerForKeyboardDragReserve);this._listenerForKeyboardDragReserve=null};assert&&assert(this._listenerForDragReserve===null,"still a listener on Pointer for reserve, memory leak likely");this._listenerForKeyboardDragReserve=listener;this.addInputListener(this._listenerForKeyboardDragReserve)}}onGotPointerCapture(){this._pointerCaptured=true}onLostPointerCapture(){if(this._pointerCaptured){this.interruptAll()}this._pointerCaptured=false}hasPointerCaptured(){return this._pointerCaptured}dispose(){sceneryLog&&sceneryLog.Pointer&&sceneryLog.Pointer(`Disposing ${this.toString()}`);if(this._listenerForDragReserve&&this._listeners.includes(this._listenerForDragReserve)){this.removeInputListener(this._listenerForDragReserve)}if(this._listenerForKeyboardDragReserve&&this._listeners.includes(this._listenerForKeyboardDragReserve)){this.removeInputListener(this._listenerForKeyboardDragReserve)}assert&&assert(this._attachedListener===null,"Attached listeners should be cleared before pointer disposal");assert&&assert(this._listeners.length===0,"Should not have listeners when a pointer is disposed")}toString(){return`Pointer#${this.type}_at_${this.point}`}}scenery.register("Pointer",Pointer);