import Pointer from"../input/Pointer.js";import scenery from"../scenery.js";export default class Pen extends Pointer{constructor(id,point,event){super(point,"pen");this.id=id;sceneryLog&&sceneryLog.Pointer&&sceneryLog.Pointer(`Created ${this.toString()}`)}move(point){const pointChanged=this.hasPointChanged(point);this.point=point;return pointChanged}toString(){return`Pen#${this.id}`}isTouchLike(){return true}}scenery.register("Pen",Pen);