import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import Filter from"../filters/Filter.js";import scenery from"../scenery.js";export default class Opacity extends Filter{constructor(amount){assert&&assert(isFinite(amount),"Opacity amount should be finite");assert&&assert(amount>=0,"Opacity amount should be non-negative");assert&&assert(amount<=1,"Opacity amount should be no greater than 1");super();this.amount=amount}getCSSFilterString(){return`opacity(${toSVGNumber(this.amount)})`}isDOMCompatible(){return true}applyCanvasFilter(wrapper){throw new Error("unimplemented")}applySVGFilter(svgFilter,inName,resultName){throw new Error("unimplemented")}}scenery.register("Opacity",Opacity);