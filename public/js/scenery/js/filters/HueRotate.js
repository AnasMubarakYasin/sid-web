import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import ColorMatrixFilter from"../filters/ColorMatrixFilter.js";import scenery from"../scenery.js";import{toDegrees}from"../../../dot/js/util/toDegrees.js";export default class HueRotate extends ColorMatrixFilter{constructor(amount){assert&&assert(isFinite(amount),"HueRotate amount should be finite");assert&&assert(amount>=0,"HueRotate amount should be non-negative");const cos=Math.cos(amount);const sin=Math.sin(amount);super(.213+.787*cos-.213*sin,.715-.715*cos-.715*sin,.072-.072*cos+.928*sin,0,0,.213-.213*cos+.143*sin,.715+.285*cos+.14*sin,.072-.072*cos-.283*sin,0,0,.213-.213*cos-.787*sin,.715-.715*cos+.715*sin,.072+.928*cos+.072*sin,0,0,0,0,0,1,0);this.amount=amount}getCSSFilterString(){return`hue-rotate(${toSVGNumber(toDegrees(this.amount))}deg)`}isDOMCompatible(){return true}}scenery.register("HueRotate",HueRotate);