import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import ColorMatrixFilter from"../filters/ColorMatrixFilter.js";import scenery from"../scenery.js";export default class Sepia extends ColorMatrixFilter{constructor(amount=1){assert&&assert(isFinite(amount),"Sepia amount should be finite");assert&&assert(amount>=0,"Sepia amount should be non-negative");assert&&assert(amount<=1,"Sepia amount should be at most 1");super(.393+.607*(1-amount),.769-.769*(1-amount),.189-.189*(1-amount),0,0,.349-.349*(1-amount),.686+.314*(1-amount),.168-.168*(1-amount),0,0,.272-.272*(1-amount),.534-.534*(1-amount),.131+.869*(1-amount),0,0,0,0,0,1,0);this.amount=amount}getCSSFilterString(){return`sepia(${toSVGNumber(this.amount)})`}isDOMCompatible(){return true}static{this.FULL=new Sepia(1)}}scenery.register("Sepia",Sepia);