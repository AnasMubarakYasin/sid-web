import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import Filter from"../filters/Filter.js";import scenery from"../scenery.js";export default class Invert extends Filter{constructor(amount=1){assert&&assert(isFinite(amount),"Invert amount should be finite");assert&&assert(amount>=0,"Invert amount should be non-negative");assert&&assert(amount<=1,"Invert amount should be no greater than 1");super();this.amount=amount}getCSSFilterString(){return`invert(${toSVGNumber(this.amount)})`}isDOMCompatible(){return true}static{this.FULL=new Invert(1)}applyCanvasFilter(wrapper){throw new Error("unimplemented")}applySVGFilter(svgFilter,inName,resultName){throw new Error("unimplemented")}}scenery.register("Invert",Invert);