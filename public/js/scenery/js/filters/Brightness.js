import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import ColorMatrixFilter from"../filters/ColorMatrixFilter.js";import scenery from"../scenery.js";export default class Brightness extends ColorMatrixFilter{constructor(amount){assert&&assert(isFinite(amount),"Brightness amount should be finite");assert&&assert(amount>=0,"Brightness amount should be non-negative");super(amount,0,0,0,0,0,amount,0,0,0,0,0,amount,0,0,0,0,0,1,0);this.amount=amount}getCSSFilterString(){return`brightness(${toSVGNumber(this.amount)})`}isDOMCompatible(){return true}static{this.BLACKEN=new Brightness(0)}}scenery.register("Brightness",Brightness);