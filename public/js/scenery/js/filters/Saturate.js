import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import ColorMatrixFilter from"../filters/ColorMatrixFilter.js";import scenery from"../scenery.js";export default class Saturate extends ColorMatrixFilter{constructor(amount){assert&&assert(isFinite(amount),"Saturate amount should be finite");assert&&assert(amount>=0,"Saturate amount should be non-negative");super(.213+.787*amount,.715-.715*amount,.072-.072*amount,0,0,.213-.213*amount,.715+.285*amount,.072-.072*amount,0,0,.213-.213*amount,.715-.715*amount,.072+.928*amount,0,0,0,0,0,1,0);this.amount=amount}getCSSFilterString(){return`saturate(${toSVGNumber(this.amount)})`}isDOMCompatible(){return true}}scenery.register("Saturate",Saturate);