import toSVGNumber from"../../../dot/js/util/toSVGNumber.js";import ColorMatrixFilter from"../filters/ColorMatrixFilter.js";import scenery from"../scenery.js";export default class Grayscale extends ColorMatrixFilter{constructor(amount=1){assert&&assert(isFinite(amount),"Grayscale amount should be finite");assert&&assert(amount>=0,"Grayscale amount should be non-negative");assert&&assert(amount<=1,"Grayscale amount should be no greater than 1");const n=1-amount;super(.2126+.7874*n,.7152-.7152*n,.0722-.0722*n,0,0,.2126-.2126*n,.7152+.2848*n,.0722-.0722*n,0,0,.2126-.2126*n,.7152-.7152*n,.0722+.9278*n,0,0,0,0,0,1,0);this.amount=amount}getCSSFilterString(){return`grayscale(${toSVGNumber(this.amount)})`}isDOMCompatible(){return true}static{this.FULL=new Grayscale(1)}}scenery.register("Grayscale",Grayscale);