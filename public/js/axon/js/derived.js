import affirm from"../../perennial-alias/js/browser-and-node/affirm.js";import DerivedProperty from"./DerivedProperty.js";function derived(...args){affirm(args.length>=2,"derived requires at least one dependency and a derivation function");const lastArgument=args[args.length-1];const secondToLastArgument=args[args.length-2];const options=typeof lastArgument==="function"?undefined:lastArgument;const derivation=typeof lastArgument==="function"?lastArgument:secondToLastArgument;const optionOffset=options?2:1;const dependencies=args.slice(0,args.length-optionOffset);affirm(dependencies.length>0,"derived requires at least one dependency");return new DerivedProperty(dependencies,derivation,options)}export default derived;