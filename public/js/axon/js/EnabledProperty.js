import optionize from"../../phet-core/js/optionize.js";import axon from"./axon.js";import BooleanProperty from"./BooleanProperty.js";const TANDEM_NAME="enabledProperty";export default class EnabledProperty extends BooleanProperty{constructor(initialEnabled,providedOptions){const options=optionize()({phetioDocumentation:"Determines whether the element is enabled (true) or disabled (false).",phetioFeatured:true,checkTandemName:true},providedOptions);if(assert&&options&&options.tandem&&options.tandem.supplied&&options.checkTandemName){assert&&assert(options.tandem.name===TANDEM_NAME,`EnabledProperty tandems should be named ${TANDEM_NAME}`)}super(initialEnabled,options)}static get TANDEM_NAME(){return TANDEM_NAME}}axon.register("EnabledProperty",EnabledProperty);