import TinyEmitter from"../../axon/js/TinyEmitter.js";import Bounds2 from"../../dot/js/Bounds2.js";import dotRandom from"../../dot/js/dotRandom.js";import Matrix3 from"../../dot/js/Matrix3.js";import Ray2 from"../../dot/js/Ray2.js";import Vector2 from"../../dot/js/Vector2.js";import optionize,{combineOptions}from"../../phet-core/js/optionize.js";import Subpath from"./util/Subpath.js";import Segment,{Line,Quadratic,Cubic,Arc,EllipticalArc}from"./segments/Segment.js";import svgPath from"./parser/svgPath.js";import svgNumber from"./util/svgNumber.js";import kite from"./kite.js";import Transform3 from"../../dot/js/Transform3.js";import arrayRemove from"../../phet-core/js/arrayRemove.js";import cleanArray from"../../phet-core/js/cleanArray.js";import Boundary from"./ops/Boundary.js";import Edge from"./ops/Edge.js";import EdgeSegmentTree from"./ops/EdgeSegmentTree.js";import Face from"./ops/Face.js";import Loop from"./ops/Loop.js";import Vertex from"./ops/Vertex.js";import VertexSegmentTree from"./ops/VertexSegmentTree.js";import{linear}from"../../dot/js/util/linear.js";const randomSource=Math.random;const v=(x,y)=>new Vector2(x,y);const weightedSplineVector=(beforeVector,currentVector,afterVector,tension)=>{return afterVector.copy().subtract(beforeVector).multiplyScalar((1-tension)/6).add(currentVector)};let Shape=class Shape{constructor(subpaths,bounds){this.subpaths=[];this._invalidatingPoints=false;this._immutable=false;this.invalidatedEmitter=new TinyEmitter;this.lastQuadraticControlPoint=null;this.lastCubicControlPoint=null;this._bounds=bounds?bounds.copy():null;this.resetControlPoints();this._invalidateListener=this.invalidate.bind(this);if(typeof subpaths==="object"){for(let i=0;i<subpaths.length;i++){this.addSubpath(subpaths[i])}}if(subpaths&&typeof subpaths!=="object"){_.each(svgPath.parse(subpaths),item=>{assert&&assert(Shape.prototype[item.cmd]!==undefined,`method ${item.cmd} from parsed SVG does not exist`);this[item.cmd].apply(this,item.args)})}this.invalidate()}resetControlPoints(){this.lastQuadraticControlPoint=null;this.lastCubicControlPoint=null}setQuadraticControlPoint(point){this.lastQuadraticControlPoint=point;this.lastCubicControlPoint=null}setCubicControlPoint(point){this.lastQuadraticControlPoint=null;this.lastCubicControlPoint=point}moveTo(x,y){assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.moveToPoint(v(x,y))}moveToRelative(x,y){assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.moveToPointRelative(v(x,y))}moveToPointRelative(displacement){return this.moveToPoint(this.getRelativePoint().plus(displacement))}moveToPoint(point){this.addSubpath(new Subpath().addPoint(point));this.resetControlPoints();return this}lineTo(x,y){assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.lineToPoint(v(x,y))}lineToRelative(x,y){assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.lineToPointRelative(v(x,y))}lineToPointRelative(displacement){return this.lineToPoint(this.getRelativePoint().plus(displacement))}lineToPoint(point){if(this.hasSubpaths()){const start=this.getLastSubpath().getLastPoint();const end=point;const line=new Line(start,end);this.getLastSubpath().addPoint(end);this.addSegmentAndBounds(line)}else{this.ensure(point)}this.resetControlPoints();return this}horizontalLineTo(x){return this.lineTo(x,this.getRelativePoint().y)}horizontalLineToRelative(x){return this.lineToRelative(x,0)}verticalLineTo(y){return this.lineTo(this.getRelativePoint().x,y)}verticalLineToRelative(y){return this.lineToRelative(0,y)}zigZagTo(endX,endY,amplitude,numberZigZags,symmetrical){return this.zigZagToPoint(new Vector2(endX,endY),amplitude,numberZigZags,symmetrical)}zigZagToPoint(endPoint,amplitude,numberZigZags,symmetrical){assert&&assert(Number.isInteger(numberZigZags),`numberZigZags must be an integer: ${numberZigZags}`);this.ensure(endPoint);const startPoint=this.getLastPoint();const delta=endPoint.minus(startPoint);const directionUnitVector=delta.normalized();const amplitudeNormalVector=directionUnitVector.perpendicular.times(amplitude);let wavelength;if(symmetrical){wavelength=delta.magnitude/(numberZigZags+.5)}else{wavelength=delta.magnitude/numberZigZags}for(let i=0;i<numberZigZags;i++){const waveOrigin=directionUnitVector.times(i*wavelength).plus(startPoint);const topPoint=waveOrigin.plus(directionUnitVector.times(wavelength/4)).plus(amplitudeNormalVector);const bottomPoint=waveOrigin.plus(directionUnitVector.times(3*wavelength/4)).minus(amplitudeNormalVector);this.lineToPoint(topPoint);this.lineToPoint(bottomPoint)}if(symmetrical){const waveOrigin=directionUnitVector.times(numberZigZags*wavelength).plus(startPoint);const topPoint=waveOrigin.plus(directionUnitVector.times(wavelength/4)).plus(amplitudeNormalVector);this.lineToPoint(topPoint)}return this.lineToPoint(endPoint)}quadraticCurveTo(cpx,cpy,x,y){assert&&assert(isFinite(cpx),`cpx must be a finite number: ${cpx}`);assert&&assert(isFinite(cpy),`cpy must be a finite number: ${cpy}`);assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.quadraticCurveToPoint(v(cpx,cpy),v(x,y))}quadraticCurveToRelative(cpx,cpy,x,y){assert&&assert(isFinite(cpx),`cpx must be a finite number: ${cpx}`);assert&&assert(isFinite(cpy),`cpy must be a finite number: ${cpy}`);assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.quadraticCurveToPointRelative(v(cpx,cpy),v(x,y))}quadraticCurveToPointRelative(controlPoint,point){const relativePoint=this.getRelativePoint();return this.quadraticCurveToPoint(relativePoint.plus(controlPoint),relativePoint.plus(point))}smoothQuadraticCurveTo(x,y){assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.quadraticCurveToPoint(this.getSmoothQuadraticControlPoint(),v(x,y))}smoothQuadraticCurveToRelative(x,y){assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.quadraticCurveToPoint(this.getSmoothQuadraticControlPoint(),v(x,y).plus(this.getRelativePoint()))}quadraticCurveToPoint(controlPoint,point){this.ensure(controlPoint);const start=this.getLastSubpath().getLastPoint();const quadratic=new Quadratic(start,controlPoint,point);this.getLastSubpath().addPoint(point);const nondegenerateSegments=quadratic.getNondegenerateSegments();_.each(nondegenerateSegments,segment=>{this.addSegmentAndBounds(segment)});this.setQuadraticControlPoint(controlPoint);return this}cubicCurveTo(cp1x,cp1y,cp2x,cp2y,x,y){assert&&assert(isFinite(cp1x),`cp1x must be a finite number: ${cp1x}`);assert&&assert(isFinite(cp1y),`cp1y must be a finite number: ${cp1y}`);assert&&assert(isFinite(cp2x),`cp2x must be a finite number: ${cp2x}`);assert&&assert(isFinite(cp2y),`cp2y must be a finite number: ${cp2y}`);assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.cubicCurveToPoint(v(cp1x,cp1y),v(cp2x,cp2y),v(x,y))}cubicCurveToRelative(cp1x,cp1y,cp2x,cp2y,x,y){assert&&assert(isFinite(cp1x),`cp1x must be a finite number: ${cp1x}`);assert&&assert(isFinite(cp1y),`cp1y must be a finite number: ${cp1y}`);assert&&assert(isFinite(cp2x),`cp2x must be a finite number: ${cp2x}`);assert&&assert(isFinite(cp2y),`cp2y must be a finite number: ${cp2y}`);assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.cubicCurveToPointRelative(v(cp1x,cp1y),v(cp2x,cp2y),v(x,y))}cubicCurveToPointRelative(control1,control2,point){const relativePoint=this.getRelativePoint();return this.cubicCurveToPoint(relativePoint.plus(control1),relativePoint.plus(control2),relativePoint.plus(point))}smoothCubicCurveTo(cp2x,cp2y,x,y){assert&&assert(isFinite(cp2x),`cp2x must be a finite number: ${cp2x}`);assert&&assert(isFinite(cp2y),`cp2y must be a finite number: ${cp2y}`);assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.cubicCurveToPoint(this.getSmoothCubicControlPoint(),v(cp2x,cp2y),v(x,y))}smoothCubicCurveToRelative(cp2x,cp2y,x,y){assert&&assert(isFinite(cp2x),`cp2x must be a finite number: ${cp2x}`);assert&&assert(isFinite(cp2y),`cp2y must be a finite number: ${cp2y}`);assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);return this.cubicCurveToPoint(this.getSmoothCubicControlPoint(),v(cp2x,cp2y).plus(this.getRelativePoint()),v(x,y).plus(this.getRelativePoint()))}cubicCurveToPoint(control1,control2,point){this.ensure(control1);const start=this.getLastSubpath().getLastPoint();const cubic=new Cubic(start,control1,control2,point);const nondegenerateSegments=cubic.getNondegenerateSegments();_.each(nondegenerateSegments,segment=>{this.addSegmentAndBounds(segment)});this.getLastSubpath().addPoint(point);this.setCubicControlPoint(control2);return this}arc(centerX,centerY,radius,startAngle,endAngle,anticlockwise){assert&&assert(isFinite(centerX),`centerX must be a finite number: ${centerX}`);assert&&assert(isFinite(centerY),`centerY must be a finite number: ${centerY}`);return this.arcPoint(v(centerX,centerY),radius,startAngle,endAngle,anticlockwise)}arcPoint(center,radius,startAngle,endAngle,anticlockwise){if(anticlockwise===undefined){anticlockwise=false}const arc=new Arc(center,radius,startAngle,endAngle,anticlockwise);const startPoint=arc.getStart();const endPoint=arc.getEnd();if(this.hasSubpaths()&&this.getLastSubpath().getLength()>0&&!startPoint.equals(this.getLastSubpath().getLastPoint())){this.addSegmentAndBounds(new Line(this.getLastSubpath().getLastPoint(),startPoint))}if(!this.hasSubpaths()){this.addSubpath(new Subpath)}this.getLastSubpath().addPoint(startPoint);this.getLastSubpath().addPoint(endPoint);this.addSegmentAndBounds(arc);this.resetControlPoints();return this}ellipticalArc(centerX,centerY,radiusX,radiusY,rotation,startAngle,endAngle,anticlockwise){assert&&assert(isFinite(centerX),`centerX must be a finite number: ${centerX}`);assert&&assert(isFinite(centerY),`centerY must be a finite number: ${centerY}`);return this.ellipticalArcPoint(v(centerX,centerY),radiusX,radiusY,rotation,startAngle,endAngle,anticlockwise)}ellipticalArcPoint(center,radiusX,radiusY,rotation,startAngle,endAngle,anticlockwise){if(anticlockwise===undefined){anticlockwise=false}const ellipticalArc=new EllipticalArc(center,radiusX,radiusY,rotation,startAngle,endAngle,anticlockwise);const startPoint=ellipticalArc.start;const endPoint=ellipticalArc.end;if(this.hasSubpaths()&&this.getLastSubpath().getLength()>0&&!startPoint.equals(this.getLastSubpath().getLastPoint())){this.addSegmentAndBounds(new Line(this.getLastSubpath().getLastPoint(),startPoint))}if(!this.hasSubpaths()){this.addSubpath(new Subpath)}this.getLastSubpath().addPoint(startPoint);this.getLastSubpath().addPoint(endPoint);this.addSegmentAndBounds(ellipticalArc);this.resetControlPoints();return this}close(){if(this.hasSubpaths()){const previousPath=this.getLastSubpath();const nextPath=new Subpath;previousPath.close();this.addSubpath(nextPath);nextPath.addPoint(previousPath.getFirstPoint())}this.resetControlPoints();return this}newSubpath(){this.addSubpath(new Subpath);this.resetControlPoints();return this}makeImmutable(){this._immutable=true;this.notifyInvalidationListeners();return this}isImmutable(){return this._immutable}ellipticalArcToRelative(radiusX,radiusY,rotation,largeArc,sweep,x,y){const relativePoint=this.getRelativePoint();return this.ellipticalArcTo(radiusX,radiusY,rotation,largeArc,sweep,x+relativePoint.x,y+relativePoint.y)}ellipticalArcTo(radiusX,radiusY,rotation,largeArc,sweep,x,y){const endPoint=new Vector2(x,y);this.ensure(endPoint);const startPoint=this.getLastSubpath().getLastPoint();this.getLastSubpath().addPoint(endPoint);if(radiusX<0){radiusX*=-1}if(radiusY<0){radiusY*=-1}let rxs=radiusX*radiusX;let rys=radiusY*radiusY;const prime=startPoint.minus(endPoint).dividedScalar(2).rotated(-rotation);const pxs=prime.x*prime.x;const pys=prime.y*prime.y;let centerPrime=new Vector2(radiusX*prime.y/radiusY,-radiusY*prime.x/radiusX);const size=pxs/rxs+pys/rys;if(size>1){radiusX*=Math.sqrt(size);radiusY*=Math.sqrt(size);rxs=radiusX*radiusX;rys=radiusY*radiusY;centerPrime=new Vector2(radiusX*prime.y/radiusY,-radiusY*prime.x/radiusX)}centerPrime.multiplyScalar(Math.sqrt(Math.max(0,(rxs*rys-rxs*pys-rys*pxs)/(rxs*pys+rys*pxs))));if(largeArc===sweep){centerPrime.multiplyScalar(-1)}const center=startPoint.blend(endPoint,.5).plus(centerPrime.rotated(rotation));const signedAngle=(u,v)=>{return(u.x*v.y-u.y*v.x>0?1:-1)*u.angleBetween(v)};const victor=new Vector2((prime.x-centerPrime.x)/radiusX,(prime.y-centerPrime.y)/radiusY);const ross=new Vector2((-prime.x-centerPrime.x)/radiusX,(-prime.y-centerPrime.y)/radiusY);const startAngle=signedAngle(Vector2.X_UNIT,victor);let deltaAngle=signedAngle(victor,ross)%(Math.PI*2);if(!sweep&&deltaAngle>0){deltaAngle-=Math.PI*2}if(sweep&&deltaAngle<0){deltaAngle+=Math.PI*2}const ellipticalArc=new EllipticalArc(center,radiusX,radiusY,rotation,startAngle,startAngle+deltaAngle,!sweep);const nondegenerateSegments=ellipticalArc.getNondegenerateSegments();_.each(nondegenerateSegments,segment=>{this.addSegmentAndBounds(segment)});return this}circle(centerX,centerY,radius){if(typeof centerX==="object"){const center=centerX;radius=centerY;return this.arcPoint(center,radius,0,Math.PI*2,false).close()}else{assert&&assert(isFinite(centerX),`centerX must be a finite number: ${centerX}`);assert&&assert(isFinite(centerY),`centerY must be a finite number: ${centerY}`);return this.arcPoint(v(centerX,centerY),radius,0,Math.PI*2,false).close()}}ellipse(centerX,centerY,radiusX,radiusY,rotation){if(typeof centerX==="object"){const center=centerX;rotation=radiusY;radiusY=radiusX;radiusX=centerY;return this.ellipticalArcPoint(center,radiusX,radiusY,rotation||0,0,Math.PI*2,false).close()}else{assert&&assert(isFinite(centerX),`centerX must be a finite number: ${centerX}`);assert&&assert(isFinite(centerY),`centerY must be a finite number: ${centerY}`);return this.ellipticalArcPoint(v(centerX,centerY),radiusX,radiusY,rotation||0,0,Math.PI*2,false).close()}}rect(x,y,width,height){assert&&assert(isFinite(x),`x must be a finite number: ${x}`);assert&&assert(isFinite(y),`y must be a finite number: ${y}`);assert&&assert(isFinite(width),`width must be a finite number: ${width}`);assert&&assert(isFinite(height),`height must be a finite number: ${height}`);const subpath=new Subpath;this.addSubpath(subpath);subpath.addPoint(v(x,y));subpath.addPoint(v(x+width,y));subpath.addPoint(v(x+width,y+height));subpath.addPoint(v(x,y+height));this.addSegmentAndBounds(new Line(subpath.points[0],subpath.points[1]));this.addSegmentAndBounds(new Line(subpath.points[1],subpath.points[2]));this.addSegmentAndBounds(new Line(subpath.points[2],subpath.points[3]));subpath.close();this.addSubpath(new Subpath);this.getLastSubpath().addPoint(v(x,y));assert&&assert(!isNaN(this.bounds.getX()));this.resetControlPoints();return this}roundRect(x,y,width,height,arcw,arch){const lowX=x+arcw;const highX=x+width-arcw;const lowY=y+arch;const highY=y+height-arch;if(arcw===arch){this.arc(highX,lowY,arcw,-Math.PI/2,0,false).arc(highX,highY,arcw,0,Math.PI/2,false).arc(lowX,highY,arcw,Math.PI/2,Math.PI,false).arc(lowX,lowY,arcw,Math.PI,Math.PI*3/2,false).close()}else{this.ellipticalArc(highX,lowY,arcw,arch,0,-Math.PI/2,0,false).ellipticalArc(highX,highY,arcw,arch,0,0,Math.PI/2,false).ellipticalArc(lowX,highY,arcw,arch,0,Math.PI/2,Math.PI,false).ellipticalArc(lowX,lowY,arcw,arch,0,Math.PI,Math.PI*3/2,false).close()}return this}polygon(vertices){const length=vertices.length;if(length>0){this.moveToPoint(vertices[0]);for(let i=1;i<length;i++){this.lineToPoint(vertices[i])}}return this.close()}cardinalSpline(positions,providedOptions){const options=optionize()({tension:0,isClosedLineSegments:false},providedOptions);assert&&assert(options.tension<1&&options.tension>-1," the tension goes from -1 to 1 ");const pointNumber=positions.length;const segmentNumber=options.isClosedLineSegments?pointNumber:pointNumber-1;for(let i=0;i<segmentNumber;i++){let cardinalPoints;if(i===0&&!options.isClosedLineSegments){cardinalPoints=[positions[0],positions[0],positions[1],positions[2]]}else if(i===segmentNumber-1&&!options.isClosedLineSegments){cardinalPoints=[positions[i-1],positions[i],positions[i+1],positions[i+1]]}else{cardinalPoints=[positions[(i-1+pointNumber)%pointNumber],positions[i%pointNumber],positions[(i+1)%pointNumber],positions[(i+2)%pointNumber]]}const bezierPoints=[cardinalPoints[1],weightedSplineVector(cardinalPoints[0],cardinalPoints[1],cardinalPoints[2],options.tension),weightedSplineVector(cardinalPoints[3],cardinalPoints[2],cardinalPoints[1],options.tension),cardinalPoints[2]];if(i===0){this.ensure(bezierPoints[0]);this.getLastSubpath().addPoint(bezierPoints[0])}this.cubicCurveToPoint(bezierPoints[1],bezierPoints[2],bezierPoints[3])}return this}copy(){return new Shape(_.map(this.subpaths,subpath=>subpath.copy()),this.bounds)}writeToContext(context){const len=this.subpaths.length;for(let i=0;i<len;i++){this.subpaths[i].writeToContext(context)}}getSVGPath(){let string="";const len=this.subpaths.length;for(let i=0;i<len;i++){const subpath=this.subpaths[i];if(subpath.isDrawable()){const startPoint=subpath.segments[0].start;string+=`M ${svgNumber(startPoint.x)} ${svgNumber(startPoint.y)} `;for(let k=0;k<subpath.segments.length;k++){string+=`${subpath.segments[k].getSVGPathFragment()} `}if(subpath.isClosed()){string+="Z "}}}return string}transformed(matrix){const subpaths=_.map(this.subpaths,subpath=>subpath.transformed(matrix));const bounds=_.reduce(subpaths,(bounds,subpath)=>bounds.union(subpath.bounds),Bounds2.NOTHING);return new Shape(subpaths,bounds)}nonlinearTransformed(providedOptions){const options=combineOptions({minLevels:0,maxLevels:7,distanceEpsilon:.16,curveEpsilon:providedOptions&&providedOptions.includeCurvature?.002:null},providedOptions);const subpaths=_.map(this.subpaths,subpath=>subpath.nonlinearTransformed(options));const bounds=_.reduce(subpaths,(bounds,subpath)=>bounds.union(subpath.bounds),Bounds2.NOTHING);return new Shape(subpaths,bounds)}polarToCartesian(options){return this.nonlinearTransformed(combineOptions({pointMap:p=>Vector2.createPolar(p.y,p.x),methodName:"polarToCartesian"},options))}toPiecewiseLinear(options){assert&&assert(!options||!options.pointMap,"No pointMap for toPiecewiseLinear allowed, since it could create non-linear segments");assert&&assert(!options||!options.methodName,"No methodName for toPiecewiseLinear allowed, since it could create non-linear segments");return this.nonlinearTransformed(options)}containsPoint(point){const rayDirection=Vector2.X_UNIT.copy();let count=0;while(count<5){count++;const rayIntersectsSegmentVertex=_.some(this.subpaths,subpath=>{return _.some(subpath.segments,segment=>{const delta=segment.start.minus(point);const magnitude=delta.magnitude;if(magnitude!==0){delta.divideScalar(magnitude);delta.subtract(rayDirection);return delta.magnitudeSquared<1e-9}else{return false}})});if(rayIntersectsSegmentVertex){rayDirection.rotate(dotRandom.nextDouble())}else{break}}return this.windingIntersection(new Ray2(point,rayDirection))!==0}intersection(ray){let hits=[];const numSubpaths=this.subpaths.length;for(let i=0;i<numSubpaths;i++){const subpath=this.subpaths[i];if(subpath.isDrawable()){const numSegments=subpath.segments.length;for(let k=0;k<numSegments;k++){const segment=subpath.segments[k];hits=hits.concat(segment.intersection(ray))}if(subpath.hasClosingSegment()){hits=hits.concat(subpath.getClosingSegment().intersection(ray))}}}return _.sortBy(hits,hit=>hit.distance)}interiorIntersectsLineSegment(startPoint,endPoint){const midpoint=startPoint.blend(endPoint,.5);if(this.containsPoint(midpoint)){return true}const delta=endPoint.minus(startPoint);const length=delta.magnitude;if(length===0){return false}delta.normalize();const hits=this.intersection(new Ray2(startPoint,delta));for(let i=0;i<hits.length;i++){if(hits[i].distance<=length){return true}}return false}windingIntersection(ray){let wind=0;const numSubpaths=this.subpaths.length;for(let i=0;i<numSubpaths;i++){const subpath=this.subpaths[i];if(subpath.isDrawable()){const numSegments=subpath.segments.length;for(let k=0;k<numSegments;k++){wind+=subpath.segments[k].windingIntersection(ray)}if(subpath.hasClosingSegment()){wind+=subpath.getClosingSegment().windingIntersection(ray)}}}return wind}intersectsBounds(bounds){if(this.bounds.intersection(bounds).equals(this.bounds)){return true}const minHorizontalRay=new Ray2(new Vector2(bounds.minX,bounds.minY),new Vector2(1,0));const minVerticalRay=new Ray2(new Vector2(bounds.minX,bounds.minY),new Vector2(0,1));const maxHorizontalRay=new Ray2(new Vector2(bounds.maxX,bounds.maxY),new Vector2(-1,0));const maxVerticalRay=new Ray2(new Vector2(bounds.maxX,bounds.maxY),new Vector2(0,-1));let hitPoint;let i;const horizontalRayIntersections=this.intersection(minHorizontalRay).concat(this.intersection(maxHorizontalRay));for(i=0;i<horizontalRayIntersections.length;i++){hitPoint=horizontalRayIntersections[i].point;if(hitPoint.x>=bounds.minX&&hitPoint.x<=bounds.maxX){return true}}const verticalRayIntersections=this.intersection(minVerticalRay).concat(this.intersection(maxVerticalRay));for(i=0;i<verticalRayIntersections.length;i++){hitPoint=verticalRayIntersections[i].point;if(hitPoint.y>=bounds.minY&&hitPoint.y<=bounds.maxY){return true}}return false}getStrokedShape(lineStyles){let subpaths=[];const bounds=Bounds2.NOTHING.copy();let subLen=this.subpaths.length;for(let i=0;i<subLen;i++){const subpath=this.subpaths[i];const strokedSubpath=subpath.stroked(lineStyles);subpaths=subpaths.concat(strokedSubpath)}subLen=subpaths.length;for(let i=0;i<subLen;i++){bounds.includeBounds(subpaths[i].bounds)}return new Shape(subpaths,bounds)}getOffsetShape(distance){const subpaths=[];const bounds=Bounds2.NOTHING.copy();let subLen=this.subpaths.length;for(let i=0;i<subLen;i++){subpaths.push(this.subpaths[i].offset(distance))}subLen=subpaths.length;for(let i=0;i<subLen;i++){bounds.includeBounds(subpaths[i].bounds)}return new Shape(subpaths,bounds)}getDashedShape(lineDash,lineDashOffset,providedOptions){const options=optionize()({distanceEpsilon:1e-10,curveEpsilon:1e-8},providedOptions);return new Shape(_.flatten(this.subpaths.map(subpath=>subpath.dashed(lineDash,lineDashOffset,options.distanceEpsilon,options.curveEpsilon))))}getBounds(){if(this._bounds===null){const bounds=Bounds2.NOTHING.copy();_.each(this.subpaths,subpath=>{bounds.includeBounds(subpath.getBounds())});this._bounds=bounds}return this._bounds}get bounds(){return this.getBounds()}getStrokedBounds(lineStyles){let areStrokedBoundsDilated=true;for(let i=0;i<this.subpaths.length;i++){const subpath=this.subpaths[i];if(subpath.isDrawable()&&!subpath.isClosed()){areStrokedBoundsDilated=false;break}for(let j=0;j<subpath.segments.length;j++){const segment=subpath.segments[j];if(!segment.areStrokedBoundsDilated()){areStrokedBoundsDilated=false;break}}}if(areStrokedBoundsDilated){return this.bounds.dilated(lineStyles.lineWidth/2)}else{const bounds=this.bounds.copy();for(let i=0;i<this.subpaths.length;i++){const subpaths=this.subpaths[i].stroked(lineStyles);for(let j=0;j<subpaths.length;j++){bounds.includeBounds(subpaths[j].bounds)}}return bounds}}getSimplifiedAreaShape(){return Graph.simplifyNonZero(this)}getBoundsWithTransform(matrix,lineStyles){const bounds=Bounds2.NOTHING.copy();const numSubpaths=this.subpaths.length;for(let i=0;i<numSubpaths;i++){const subpath=this.subpaths[i];bounds.includeBounds(subpath.getBoundsWithTransform(matrix))}if(lineStyles){bounds.includeBounds(this.getStrokedShape(lineStyles).getBoundsWithTransform(matrix))}return bounds}getApproximateArea(numSamples){const x=this.bounds.minX;const y=this.bounds.minY;const width=this.bounds.width;const height=this.bounds.height;const rectangleArea=width*height;let count=0;const point=new Vector2(0,0);for(let i=0;i<numSamples;i++){point.x=x+randomSource()*width;point.y=y+randomSource()*height;if(this.containsPoint(point)){count++}}return rectangleArea*count/numSamples}getNonoverlappingArea(){return Math.abs(_.sum(this.subpaths.map(subpath=>_.sum(subpath.getFillSegments().map(segment=>segment.getSignedAreaFragment())))))}getArea(){return this.getSimplifiedAreaShape().getNonoverlappingArea()}getApproximateCentroid(numSamples){const x=this.bounds.minX;const y=this.bounds.minY;const width=this.bounds.width;const height=this.bounds.height;let count=0;const sum=new Vector2(0,0);const point=new Vector2(0,0);for(let i=0;i<numSamples;i++){point.x=x+randomSource()*width;point.y=y+randomSource()*height;if(this.containsPoint(point)){sum.add(point);count++}}return sum.dividedScalar(count)}getClosestPoints(point){return Segment.filterClosestToPointResult(_.flatten(this.subpaths.map(subpath=>subpath.getClosestPoints(point))))}getClosestPoint(point){return this.getClosestPoints(point)[0].closestPoint}invalidatePoints(){this._invalidatingPoints=true;const numSubpaths=this.subpaths.length;for(let i=0;i<numSubpaths;i++){this.subpaths[i].invalidatePoints()}this._invalidatingPoints=false;this.invalidate()}toString(){return`new phet.kite.Shape( '${this.getSVGPath().trim()}' )`}invalidate(){assert&&assert(!this._immutable,"Attempt to modify an immutable Shape");if(!this._invalidatingPoints){this._bounds=null;this.notifyInvalidationListeners()}}notifyInvalidationListeners(){this.invalidatedEmitter.emit()}addSegmentAndBounds(segment){this.getLastSubpath().addSegment(segment);this.invalidate()}ensure(point){if(!this.hasSubpaths()){this.addSubpath(new Subpath);this.getLastSubpath().addPoint(point)}}addSubpath(subpath){this.subpaths.push(subpath);subpath.invalidatedEmitter.addListener(this._invalidateListener);this.invalidate();return this}hasSubpaths(){return this.subpaths.length>0}getLastSubpath(){assert&&assert(this.hasSubpaths(),"We should have a subpath if this is called");return _.last(this.subpaths)}getLastPoint(){assert&&assert(this.hasSubpaths(),"We should have a subpath if this is called");assert&&assert(this.getLastSubpath().getLastPoint(),"We should have a last point");return this.getLastSubpath().getLastPoint()}getLastSegment(){if(!this.hasSubpaths()){return null}const subpath=this.getLastSubpath();if(!subpath.isDrawable()){return null}return subpath.getLastSegment()}getSmoothQuadraticControlPoint(){const lastPoint=this.getLastPoint();if(this.lastQuadraticControlPoint){return lastPoint.plus(lastPoint.minus(this.lastQuadraticControlPoint))}else{return lastPoint}}getSmoothCubicControlPoint(){const lastPoint=this.getLastPoint();if(this.lastCubicControlPoint){return lastPoint.plus(lastPoint.minus(this.lastCubicControlPoint))}else{return lastPoint}}getRelativePoint(){let result=Vector2.ZERO;if(this.hasSubpaths()){const subpath=this.getLastSubpath();if(subpath.points.length){result=subpath.getLastPoint()}}return result}shapeUnion(shape){return Graph.binaryResult(this,shape,Graph.BINARY_NONZERO_UNION)}shapeIntersection(shape){return Graph.binaryResult(this,shape,Graph.BINARY_NONZERO_INTERSECTION)}shapeDifference(shape){return Graph.binaryResult(this,shape,Graph.BINARY_NONZERO_DIFFERENCE)}shapeXor(shape){return Graph.binaryResult(this,shape,Graph.BINARY_NONZERO_XOR)}shapeClip(shape,options){return Graph.clipShape(shape,this,options)}getArcLength(distanceEpsilon,curveEpsilon,maxLevels){let length=0;for(let i=0;i<this.subpaths.length;i++){length+=this.subpaths[i].getArcLength(distanceEpsilon,curveEpsilon,maxLevels)}return length}serialize(){return{type:"Shape",subpaths:this.subpaths.map(subpath=>subpath.serialize())}}static deserialize(obj){assert&&assert(obj.type==="Shape");return new Shape(obj.subpaths.map(Subpath.deserialize))}static rectangle(x,y,width,height){return new Shape().rect(x,y,width,height)}static{this.rect=Shape.rectangle}static roundRect(x,y,width,height,arcw,arch){return new Shape().roundRect(x,y,width,height,arcw,arch)}static{this.roundRectangle=Shape.roundRect}static roundedRectangleWithRadii(x,y,width,height,cornerRadii){let topLeftRadius=cornerRadii&&cornerRadii.topLeft||0;let topRightRadius=cornerRadii&&cornerRadii.topRight||0;let bottomLeftRadius=cornerRadii&&cornerRadii.bottomLeft||0;let bottomRightRadius=cornerRadii&&cornerRadii.bottomRight||0;assert&&assert(isFinite(x),"Non-finite x");assert&&assert(isFinite(y),"Non-finite y");assert&&assert(width>=0&&isFinite(width),"Negative or non-finite width");assert&&assert(height>=0&&isFinite(height),"Negative or non-finite height");assert&&assert(topLeftRadius>=0&&isFinite(topLeftRadius),"Invalid topLeft");assert&&assert(topRightRadius>=0&&isFinite(topRightRadius),"Invalid topRight");assert&&assert(bottomLeftRadius>=0&&isFinite(bottomLeftRadius),"Invalid bottomLeft");assert&&assert(bottomRightRadius>=0&&isFinite(bottomRightRadius),"Invalid bottomRight");const topSum=topLeftRadius+topRightRadius;if(topSum>width&&topSum>0){topLeftRadius=topLeftRadius/topSum*width;topRightRadius=topRightRadius/topSum*width}const bottomSum=bottomLeftRadius+bottomRightRadius;if(bottomSum>width&&bottomSum>0){bottomLeftRadius=bottomLeftRadius/bottomSum*width;bottomRightRadius=bottomRightRadius/bottomSum*width}const leftSum=topLeftRadius+bottomLeftRadius;if(leftSum>height&&leftSum>0){topLeftRadius=topLeftRadius/leftSum*height;bottomLeftRadius=bottomLeftRadius/leftSum*height}const rightSum=topRightRadius+bottomRightRadius;if(rightSum>height&&rightSum>0){topRightRadius=topRightRadius/rightSum*height;bottomRightRadius=bottomRightRadius/rightSum*height}assert&&assert(topLeftRadius+topRightRadius<=width,"Corner overlap on top edge");assert&&assert(bottomLeftRadius+bottomRightRadius<=width,"Corner overlap on bottom edge");assert&&assert(topLeftRadius+bottomLeftRadius<=height,"Corner overlap on left edge");assert&&assert(topRightRadius+bottomRightRadius<=height,"Corner overlap on right edge");const shape=new Shape;const right=x+width;const bottom=y+height;if(bottomRightRadius>0){shape.arc(right-bottomRightRadius,bottom-bottomRightRadius,bottomRightRadius,0,Math.PI/2,false)}else{shape.moveTo(right,bottom)}if(bottomLeftRadius>0){shape.arc(x+bottomLeftRadius,bottom-bottomLeftRadius,bottomLeftRadius,Math.PI/2,Math.PI,false)}else{shape.lineTo(x,bottom)}if(topLeftRadius>0){shape.arc(x+topLeftRadius,y+topLeftRadius,topLeftRadius,Math.PI,3*Math.PI/2,false)}else{shape.lineTo(x,y)}if(topRightRadius>0){shape.arc(right-topRightRadius,y+topRightRadius,topRightRadius,3*Math.PI/2,2*Math.PI,false)}else{shape.lineTo(right,y)}shape.close();return shape}static boundsOffsetWithRadii(bounds,offsets,radii){const offsetBounds=bounds.withOffsets(offsets.left,offsets.top,offsets.right,offsets.bottom);return Shape.roundedRectangleWithRadii(offsetBounds.minX,offsetBounds.minY,offsetBounds.width,offsetBounds.height,radii)}static polygon(vertices){return new Shape().polygon(vertices)}static bounds(bounds){return new Shape().rect(bounds.minX,bounds.minY,bounds.maxX-bounds.minX,bounds.maxY-bounds.minY)}static lineSegment(a,b,c,d){if(typeof a==="number"){return new Shape().moveTo(a,b).lineTo(c,d)}else{return new Shape().moveToPoint(a).lineToPoint(b)}}static regularPolygon(sides,radius){const shape=new Shape;_.each(_.range(sides),k=>{const point=Vector2.createPolar(radius,2*Math.PI*k/sides);k===0?shape.moveToPoint(point):shape.lineToPoint(point)});return shape.close()}static circle(a,b,c){if(b===undefined){return new Shape().circle(0,0,a)}return new Shape().circle(a,b,c)}static ellipse(a,b,c,d,e){if(d===undefined){return new Shape().ellipse(0,0,a,b,c)}return new Shape().ellipse(a,b,c,d,e)}static arc(a,b,c,d,e,f){return new Shape().arc(a,b,c,d,e,f)}static union(shapes){return Graph.unionNonZero(shapes)}static intersection(shapes){return Graph.intersectionNonZero(shapes)}static xor(shapes){return Graph.xorNonZero(shapes)}static segments(segments,closed){if(assert){for(let i=1;i<segments.length;i++){assert(segments[i-1].end.equalsEpsilon(segments[i].start,1e-6),"Mismatched start/end")}}return new Shape([new Subpath(segments,undefined,!!closed)])}static fromGraph(graph){const subpaths=[];for(let i=0;i<graph.faces.length;i++){const face=graph.faces[i];if(face.filled){subpaths.push(Subpath.fromBoundary(face.boundary));for(let j=0;j<face.holes.length;j++){subpaths.push(Subpath.fromBoundary(face.holes[j]))}}}return new Shape(subpaths)}static fromSegment(segment){return new Shape([new Subpath([segment])])}};kite.register("Shape",Shape);export default Shape;let bridgeId=0;let globalId=0;const VERTEX_COLLAPSE_THRESHOLD_DISTANCE=1e-5;const INTERSECTION_ENDPOINT_THRESHOLD_DISTANCE=.1*VERTEX_COLLAPSE_THRESHOLD_DISTANCE;const SPLIT_ENDPOINT_THRESHOLD_DISTANCE=.01*VERTEX_COLLAPSE_THRESHOLD_DISTANCE;const T_THRESHOLD=1e-6;export class Graph{constructor(){this.vertices=[];this.edges=[];this.innerBoundaries=[];this.outerBoundaries=[];this.boundaries=[];this.shapeIds=[];this.loops=[];this.unboundedFace=Face.pool.create(null);this.faces=[this.unboundedFace]}serialize(){return{type:"Graph",vertices:this.vertices.map(vertex=>vertex.serialize()),edges:this.edges.map(edge=>edge.serialize()),boundaries:this.boundaries.map(boundary=>boundary.serialize()),innerBoundaries:this.innerBoundaries.map(boundary=>boundary.id),outerBoundaries:this.outerBoundaries.map(boundary=>boundary.id),shapeIds:this.shapeIds,loops:this.loops.map(loop=>loop.serialize()),unboundedFace:this.unboundedFace.id,faces:this.faces.map(face=>face.serialize())}}static deserialize(obj){const graph=new Graph;const vertexMap={};const edgeMap={};const halfEdgeMap={};const boundaryMap={};const loopMap={};const faceMap={};graph.vertices=obj.vertices.map(data=>{const vertex=new Vertex(Vector2.Vector2IO.fromStateObject(data.point));vertexMap[data.id]=vertex;vertex.visited=data.visited;vertex.visitIndex=data.visitIndex;vertex.lowIndex=data.lowIndex;return vertex});graph.edges=obj.edges.map(data=>{const edge=new Edge(Segment.deserialize(data.segment),vertexMap[data.startVertex],vertexMap[data.endVertex]);edgeMap[data.id]=edge;edge.signedAreaFragment=data.signedAreaFragment;const deserializeHalfEdge=(halfEdge,halfEdgeData)=>{halfEdgeMap[halfEdgeData.id]=halfEdge;halfEdge.isReversed=halfEdgeData.isReversed;halfEdge.signedAreaFragment=halfEdgeData.signedAreaFragment;halfEdge.startVertex=vertexMap[halfEdgeData.startVertex.id];halfEdge.endVertex=vertexMap[halfEdgeData.endVertex.id];halfEdge.sortVector=Vector2.Vector2IO.fromStateObject(halfEdgeData.sortVector);halfEdge.data=halfEdgeData.data};deserializeHalfEdge(edge.forwardHalf,data.forwardHalf);deserializeHalfEdge(edge.reversedHalf,data.reversedHalf);edge.visited=data.visited;edge.data=data.data;return edge});obj.vertices.forEach((data,i)=>{const vertex=graph.vertices[i];vertex.incidentHalfEdges=data.incidentHalfEdges.map(id=>halfEdgeMap[id])});graph.boundaries=obj.boundaries.map(data=>{const boundary=Boundary.pool.create(data.halfEdges.map(id=>halfEdgeMap[id]));boundaryMap[data.id]=boundary;boundary.signedArea=data.signedArea;boundary.bounds=Bounds2.Bounds2IO.fromStateObject(data.bounds);return boundary});obj.boundaries.forEach((data,i)=>{const boundary=graph.boundaries[i];boundary.childBoundaries=data.childBoundaries.map(id=>boundaryMap[id])});graph.innerBoundaries=obj.innerBoundaries.map(id=>boundaryMap[id]);graph.outerBoundaries=obj.outerBoundaries.map(id=>boundaryMap[id]);graph.shapeIds=obj.shapeIds;graph.loops=obj.loops.map(data=>{const loop=new Loop(data.shapeId,data.closed);loopMap[data.id]=loop;loop.halfEdges=data.halfEdges.map(id=>halfEdgeMap[id]);return loop});graph.faces=obj.faces.map((data,i)=>{const face=i===0?graph.unboundedFace:new Face(boundaryMap[data.boundary]);faceMap[data.id]=face;face.holes=data.holes.map(id=>boundaryMap[id]);face.windingMap=data.windingMap;face.filled=data.filled;return face});obj.edges.forEach((data,i)=>{const edge=graph.edges[i];edge.forwardHalf.face=data.forwardHalf.face===null?null:faceMap[data.forwardHalf.face];edge.reversedHalf.face=data.reversedHalf.face===null?null:faceMap[data.reversedHalf.face]});return graph}addShape(shapeId,shape,options){for(let i=0;i<shape.subpaths.length;i++){this.addSubpath(shapeId,shape.subpaths[i],options)}}addSubpath(shapeId,subpath,providedOptions){const options=optionize()({ensureClosed:true},providedOptions);if(!this.shapeIds.includes(shapeId)){this.shapeIds.push(shapeId)}if(subpath.segments.length===0){return}const closed=subpath.closed||options.ensureClosed;const segments=options.ensureClosed?subpath.getFillSegments():subpath.segments;let index;const vertices=[];for(index=0;index<segments.length;index++){let previousIndex=index-1;if(previousIndex<0){previousIndex=segments.length-1}let end=segments[previousIndex].end;const start=segments[index].start;if(!closed&&index===0){end=start}if(start.equals(end)){vertices.push(Vertex.pool.create(start))}else{assert&&assert(start.distance(end)<1e-5,"Inaccurate start/end points");vertices.push(Vertex.pool.create(start.average(end)))}}if(!closed){vertices.push(Vertex.pool.create(segments[segments.length-1].end))}const loop=Loop.pool.create(shapeId,closed);for(index=0;index<segments.length;index++){let nextIndex=index+1;if(closed&&nextIndex===segments.length){nextIndex=0}const edge=Edge.pool.create(segments[index],vertices[index],vertices[nextIndex]);loop.halfEdges.push(edge.forwardHalf);this.addEdge(edge)}this.loops.push(loop);this.vertices.push(...vertices)}computeSimplifiedFaces(){this.eliminateOverlap();this.eliminateSelfIntersection();this.eliminateIntersection();this.collapseVertices();this.removeBridges();this.removeLowOrderVertices();this.orderVertexEdges();this.extractFaces();this.computeBoundaryTree();this.computeWindingMap()}computeFaceInclusion(windingMapFilter){for(let i=0;i<this.faces.length;i++){const face=this.faces[i];face.filled=windingMapFilter(face.windingMap)}}createFilledSubGraph(){const graph=new Graph;const vertexMap={};for(let i=0;i<this.edges.length;i++){const edge=this.edges[i];if(edge.forwardHalf.face.filled!==edge.reversedHalf.face.filled){if(!vertexMap[edge.startVertex.id]){const newStartVertex=Vertex.pool.create(edge.startVertex.point);graph.vertices.push(newStartVertex);vertexMap[edge.startVertex.id]=newStartVertex}if(!vertexMap[edge.endVertex.id]){const newEndVertex=Vertex.pool.create(edge.endVertex.point);graph.vertices.push(newEndVertex);vertexMap[edge.endVertex.id]=newEndVertex}const startVertex=vertexMap[edge.startVertex.id];const endVertex=vertexMap[edge.endVertex.id];graph.addEdge(Edge.pool.create(edge.segment,startVertex,endVertex))}}graph.collapseAdjacentEdges();graph.orderVertexEdges();graph.extractFaces();graph.computeBoundaryTree();graph.fillAlternatingFaces();return graph}facesToShape(){const subpaths=[];for(let i=0;i<this.faces.length;i++){const face=this.faces[i];if(face.filled){subpaths.push(Subpath.fromBoundary(face.boundary));for(let j=0;j<face.holes.length;j++){subpaths.push(Subpath.fromBoundary(face.holes[j]))}}}return new Shape(subpaths)}dispose(){while(this.boundaries.length){this.boundaries.pop().dispose()}cleanArray(this.innerBoundaries);cleanArray(this.outerBoundaries);while(this.loops.length){this.loops.pop().dispose()}while(this.faces.length){this.faces.pop().dispose()}while(this.vertices.length){this.vertices.pop().dispose()}while(this.edges.length){this.edges.pop().dispose()}}addEdge(edge){assert&&assert(!_.includes(edge.endVertex.incidentHalfEdges,edge.forwardHalf),"Should not already be connected");this.edges.push(edge);edge.startVertex.incidentHalfEdges.push(edge.reversedHalf);edge.endVertex.incidentHalfEdges.push(edge.forwardHalf)}removeEdge(edge){arrayRemove(this.edges,edge);arrayRemove(edge.startVertex.incidentHalfEdges,edge.reversedHalf);arrayRemove(edge.endVertex.incidentHalfEdges,edge.forwardHalf)}replaceEdgeInLoops(edge,forwardHalfEdges){const reversedHalfEdges=[];for(let i=0;i<forwardHalfEdges.length;i++){reversedHalfEdges.push(forwardHalfEdges[forwardHalfEdges.length-1-i].getReversed())}for(let i=0;i<this.loops.length;i++){const loop=this.loops[i];for(let j=loop.halfEdges.length-1;j>=0;j--){const halfEdge=loop.halfEdges[j];if(halfEdge.edge===edge){const replacementHalfEdges=halfEdge===edge.forwardHalf?forwardHalfEdges:reversedHalfEdges;loop.halfEdges.splice(j,1,...replacementHalfEdges)}}}}collapseAdjacentEdges(){let needsLoop=true;while(needsLoop){needsLoop=false;for(let i=0;i<this.vertices.length;i++){const vertex=this.vertices[i];if(vertex.incidentHalfEdges.length===2){const aEdge=vertex.incidentHalfEdges[0].edge;const bEdge=vertex.incidentHalfEdges[1].edge;let aSegment=aEdge.segment;let bSegment=bEdge.segment;const aVertex=aEdge.getOtherVertex(vertex);const bVertex=bEdge.getOtherVertex(vertex);assert&&assert(this.loops.length===0);if(aEdge.startVertex===vertex){aSegment=aSegment.reversed()}if(bEdge.endVertex===vertex){bSegment=bSegment.reversed()}if(aSegment instanceof Line&&bSegment instanceof Line){if(aSegment.tangentAt(0).normalized().distance(bSegment.tangentAt(0).normalized())<1e-6){this.removeEdge(aEdge);this.removeEdge(bEdge);aEdge.dispose();bEdge.dispose();arrayRemove(this.vertices,vertex);vertex.dispose();const newSegment=new Line(aVertex.point,bVertex.point);this.addEdge(new Edge(newSegment,aVertex,bVertex));needsLoop=true;break}}}}}}eliminateOverlap(){const epsilon=1e-4;const queue=new window.FlatQueue;const segmentTree=new EdgeSegmentTree(epsilon);const nextId=globalId++;const addToQueue=edge=>{const bounds=edge.segment.bounds;queue.push({start:true,edge:edge},bounds.minY-epsilon);queue.push({start:false,edge:edge},bounds.maxY+epsilon)};const removeFromQueue=edge=>{edge.internalData.removedId=nextId};for(let i=0;i<this.edges.length;i++){addToQueue(this.edges[i])}const edgesToDispose=[];while(queue.length){const entry=queue.pop();const edge=entry.edge;if(edge.internalData.removedId===nextId){continue}if(entry.start){let found=false;let overlappedEdge;let addedEdges;segmentTree.query(edge,otherEdge=>{const overlaps=edge.segment.getOverlaps(otherEdge.segment);if(overlaps!==null&&overlaps.length){for(let k=0;k<overlaps.length;k++){const overlap=overlaps[k];if(Math.abs(overlap.t1-overlap.t0)>1e-5&&Math.abs(overlap.qt1-overlap.qt0)>1e-5){addedEdges=this.splitOverlap(edge,otherEdge,overlap);found=true;overlappedEdge=otherEdge;return true}}}return false});if(found){segmentTree.removeItem(overlappedEdge);removeFromQueue(overlappedEdge);removeFromQueue(edge);for(let i=0;i<addedEdges.length;i++){addToQueue(addedEdges[i])}edgesToDispose.push(edge);edgesToDispose.push(overlappedEdge)}else{segmentTree.addItem(edge)}}else{segmentTree.removeItem(edge)}}for(let i=0;i<edgesToDispose.length;i++){edgesToDispose[i].dispose()}}splitOverlap(aEdge,bEdge,overlap){const newEdges=[];const aSegment=aEdge.segment;const bSegment=bEdge.segment;this.removeEdge(aEdge);this.removeEdge(bEdge);let t0=overlap.t0;let t1=overlap.t1;let qt0=overlap.qt0;let qt1=overlap.qt1;if(t0<1e-5){t0=0}if(t1>1-1e-5){t1=1}if(qt0<1e-5){qt0=0}if(qt1>1-1e-5){qt1=1}const aBefore=t0>0?aSegment.subdivided(t0)[0]:null;const bBefore=qt0>0?bSegment.subdivided(qt0)[0]:null;const aAfter=t1<1?aSegment.subdivided(t1)[1]:null;const bAfter=qt1<1?bSegment.subdivided(qt1)[1]:null;let middle=aSegment;if(t0>0){middle=middle.subdivided(t0)[1]}if(t1<1){middle=middle.subdivided(linear(t0,1,0,1,t1))[0]}let beforeVertex;if(aBefore&&bBefore){beforeVertex=Vertex.pool.create(middle.start);this.vertices.push(beforeVertex)}else if(aBefore){beforeVertex=overlap.a>0?bEdge.startVertex:bEdge.endVertex}else{beforeVertex=aEdge.startVertex}let afterVertex;if(aAfter&&bAfter){afterVertex=Vertex.pool.create(middle.end);this.vertices.push(afterVertex)}else if(aAfter){afterVertex=overlap.a>0?bEdge.endVertex:bEdge.startVertex}else{afterVertex=aEdge.endVertex}const middleEdge=Edge.pool.create(middle,beforeVertex,afterVertex);newEdges.push(middleEdge);let aBeforeEdge;let aAfterEdge;let bBeforeEdge;let bAfterEdge;if(aBefore){aBeforeEdge=Edge.pool.create(aBefore,aEdge.startVertex,beforeVertex);newEdges.push(aBeforeEdge)}if(aAfter){aAfterEdge=Edge.pool.create(aAfter,afterVertex,aEdge.endVertex);newEdges.push(aAfterEdge)}if(bBefore){bBeforeEdge=Edge.pool.create(bBefore,bEdge.startVertex,overlap.a>0?beforeVertex:afterVertex);newEdges.push(bBeforeEdge)}if(bAfter){bAfterEdge=Edge.pool.create(bAfter,overlap.a>0?afterVertex:beforeVertex,bEdge.endVertex);newEdges.push(bAfterEdge)}for(let i=0;i<newEdges.length;i++){this.addEdge(newEdges[i])}const aEdges=(aBefore?[aBeforeEdge]:[]).concat([middleEdge]).concat(aAfter?[aAfterEdge]:[]);const bEdges=(bBefore?[bBeforeEdge]:[]).concat([middleEdge]).concat(bAfter?[bAfterEdge]:[]);const aForwardHalfEdges=[];const bForwardHalfEdges=[];for(let i=0;i<aEdges.length;i++){aForwardHalfEdges.push(aEdges[i].forwardHalf)}for(let i=0;i<bEdges.length;i++){const isForward=bEdges[i]!==middleEdge||overlap.a>0;bForwardHalfEdges.push(isForward?bEdges[i].forwardHalf:bEdges[i].reversedHalf)}this.replaceEdgeInLoops(aEdge,aForwardHalfEdges);this.replaceEdgeInLoops(bEdge,bForwardHalfEdges);return newEdges}eliminateSelfIntersection(){assert&&assert(this.boundaries.length===0,"Only handles simpler level primitive splitting right now");for(let i=this.edges.length-1;i>=0;i--){const edge=this.edges[i];const segment=edge.segment;if(segment instanceof Cubic){const selfIntersection=segment.getSelfIntersection();if(selfIntersection){assert&&assert(selfIntersection.aT<selfIntersection.bT);const segments=segment.subdivisions([selfIntersection.aT,selfIntersection.bT]);const vertex=Vertex.pool.create(selfIntersection.point);this.vertices.push(vertex);const startEdge=Edge.pool.create(segments[0],edge.startVertex,vertex);const middleEdge=Edge.pool.create(segments[1],vertex,vertex);const endEdge=Edge.pool.create(segments[2],vertex,edge.endVertex);this.removeEdge(edge);this.addEdge(startEdge);this.addEdge(middleEdge);this.addEdge(endEdge);this.replaceEdgeInLoops(edge,[startEdge.forwardHalf,middleEdge.forwardHalf,endEdge.forwardHalf]);edge.dispose()}}}}eliminateIntersection(){const epsilon=1e-4;const queue=new window.FlatQueue;const segmentTree=new EdgeSegmentTree(epsilon);const nextId=globalId++;const addToQueue=edge=>{const bounds=edge.segment.bounds;queue.push({start:true,edge:edge},bounds.minY-epsilon);queue.push({start:false,edge:edge},bounds.maxY+epsilon)};const removeFromQueue=edge=>{edge.internalData.removedId=nextId};for(let i=0;i<this.edges.length;i++){addToQueue(this.edges[i])}const edgesToDispose=[];while(queue.length){const entry=queue.pop();const edge=entry.edge;if(edge.internalData.removedId===nextId){continue}if(entry.start){let found=false;let overlappedEdge;let addedEdges;let removedEdges;segmentTree.query(edge,otherEdge=>{const aSegment=edge.segment;const bSegment=otherEdge.segment;let intersections=Segment.intersect(aSegment,bSegment);intersections=intersections.filter(intersection=>{const point=intersection.point;return Graph.isInternal(point,intersection.aT,aSegment,INTERSECTION_ENDPOINT_THRESHOLD_DISTANCE,T_THRESHOLD)||Graph.isInternal(point,intersection.bT,bSegment,INTERSECTION_ENDPOINT_THRESHOLD_DISTANCE,T_THRESHOLD)});if(intersections.length){const intersection=intersections[0];const result=this.simpleSplit(edge,otherEdge,intersection.aT,intersection.bT,intersection.point);if(result){found=true;overlappedEdge=otherEdge;addedEdges=result.addedEdges;removedEdges=result.removedEdges;return true}}return false});if(found){if(removedEdges.includes(edge)){removeFromQueue(edge);edgesToDispose.push(edge)}else{segmentTree.addItem(edge)}if(removedEdges.includes(overlappedEdge)){segmentTree.removeItem(overlappedEdge);removeFromQueue(overlappedEdge);edgesToDispose.push(overlappedEdge)}for(let i=0;i<addedEdges.length;i++){addToQueue(addedEdges[i])}}else{segmentTree.addItem(edge)}}else{segmentTree.removeItem(edge)}}for(let i=0;i<edgesToDispose.length;i++){edgesToDispose[i].dispose()}}simpleSplit(aEdge,bEdge,aT,bT,point){const aInternal=Graph.isInternal(point,aT,aEdge.segment,SPLIT_ENDPOINT_THRESHOLD_DISTANCE,T_THRESHOLD);const bInternal=Graph.isInternal(point,bT,bEdge.segment,SPLIT_ENDPOINT_THRESHOLD_DISTANCE,T_THRESHOLD);let vertex=null;if(!aInternal){vertex=aT<.5?aEdge.startVertex:aEdge.endVertex}else if(!bInternal){vertex=bT<.5?bEdge.startVertex:bEdge.endVertex}else{vertex=Vertex.pool.create(point);this.vertices.push(vertex)}let changed=false;const addedEdges=[];const removedEdges=[];if(aInternal&&vertex!==aEdge.startVertex&&vertex!==aEdge.endVertex){addedEdges.push(...this.splitEdge(aEdge,aT,vertex));removedEdges.push(aEdge);changed=true}if(bInternal&&vertex!==bEdge.startVertex&&vertex!==bEdge.endVertex){addedEdges.push(...this.splitEdge(bEdge,bT,vertex));removedEdges.push(bEdge);changed=true}return changed?{addedEdges:addedEdges,removedEdges:removedEdges}:null}splitEdge(edge,t,vertex){assert&&assert(this.boundaries.length===0,"Only handles simpler level primitive splitting right now");assert&&assert(edge.startVertex!==vertex);assert&&assert(edge.endVertex!==vertex);const segments=edge.segment.subdivided(t);assert&&assert(segments.length===2);const firstEdge=Edge.pool.create(segments[0],edge.startVertex,vertex);const secondEdge=Edge.pool.create(segments[1],vertex,edge.endVertex);this.removeEdge(edge);this.addEdge(firstEdge);this.addEdge(secondEdge);this.replaceEdgeInLoops(edge,[firstEdge.forwardHalf,secondEdge.forwardHalf]);return[firstEdge,secondEdge]}collapseVertices(){assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.startVertex)));assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.endVertex)));const epsilon=10*VERTEX_COLLAPSE_THRESHOLD_DISTANCE;const queue=new window.FlatQueue;const segmentTree=new VertexSegmentTree(epsilon);const nextId=globalId++;const addToQueue=vertex=>{queue.push({start:true,vertex:vertex},vertex.point.y-epsilon);queue.push({start:false,vertex:vertex},vertex.point.y+epsilon)};const removeFromQueue=vertex=>{vertex.internalData.removedId=nextId};for(let i=0;i<this.vertices.length;i++){addToQueue(this.vertices[i])}const verticesToDispose=[];while(queue.length){const entry=queue.pop();const vertex=entry.vertex;if(vertex.internalData.removedId===nextId){continue}if(entry.start){let found=false;let overlappedVertex;let addedVertices;segmentTree.query(vertex,otherVertex=>{const distance=vertex.point.distance(otherVertex.point);if(distance<VERTEX_COLLAPSE_THRESHOLD_DISTANCE){const newVertex=Vertex.pool.create(distance===0?vertex.point:vertex.point.average(otherVertex.point));this.vertices.push(newVertex);arrayRemove(this.vertices,vertex);arrayRemove(this.vertices,otherVertex);for(let k=this.edges.length-1;k>=0;k--){const edge=this.edges[k];const startMatches=edge.startVertex===vertex||edge.startVertex===otherVertex;const endMatches=edge.endVertex===vertex||edge.endVertex===otherVertex;if(startMatches&&endMatches){if((edge.segment.bounds.width>1e-5||edge.segment.bounds.height>1e-5)&&(edge.segment instanceof Cubic||edge.segment instanceof Arc||edge.segment instanceof EllipticalArc)){const replacementEdge=Edge.pool.create(edge.segment,newVertex,newVertex);this.addEdge(replacementEdge);this.replaceEdgeInLoops(edge,[replacementEdge.forwardHalf])}else{this.replaceEdgeInLoops(edge,[])}this.removeEdge(edge);edge.dispose()}else if(startMatches){edge.startVertex=newVertex;newVertex.incidentHalfEdges.push(edge.reversedHalf);edge.updateReferences()}else if(endMatches){edge.endVertex=newVertex;newVertex.incidentHalfEdges.push(edge.forwardHalf);edge.updateReferences()}}addedVertices=[newVertex];found=true;overlappedVertex=otherVertex;return true}return false});if(found){segmentTree.removeItem(overlappedVertex);removeFromQueue(overlappedVertex);removeFromQueue(vertex);for(let i=0;i<addedVertices.length;i++){addToQueue(addedVertices[i])}verticesToDispose.push(vertex);verticesToDispose.push(overlappedVertex)}else{segmentTree.addItem(vertex)}}else{segmentTree.removeItem(vertex)}}for(let i=0;i<verticesToDispose.length;i++){verticesToDispose[i].dispose()}assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.startVertex)));assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.endVertex)))}markBridges(bridges,vertex){vertex.visited=true;vertex.visitIndex=vertex.lowIndex=bridgeId++;for(let i=0;i<vertex.incidentHalfEdges.length;i++){const edge=vertex.incidentHalfEdges[i].edge;const childVertex=vertex.incidentHalfEdges[i].startVertex;if(!childVertex.visited){edge.visited=true;this.markBridges(bridges,childVertex);vertex.lowIndex=Math.min(vertex.lowIndex,childVertex.lowIndex);if(childVertex.lowIndex>vertex.visitIndex){bridges.push(edge)}}else if(!edge.visited){vertex.lowIndex=Math.min(vertex.lowIndex,childVertex.visitIndex)}}}removeBridges(){const bridges=[];for(let i=0;i<this.vertices.length;i++){const vertex=this.vertices[i];if(!vertex.visited){this.markBridges(bridges,vertex)}}for(let i=0;i<bridges.length;i++){const bridgeEdge=bridges[i];this.removeEdge(bridgeEdge);this.replaceEdgeInLoops(bridgeEdge,[]);bridgeEdge.dispose()}}removeLowOrderVertices(){assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.startVertex)));assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.endVertex)));let needsLoop=true;while(needsLoop){needsLoop=false;for(let i=this.vertices.length-1;i>=0;i--){const vertex=this.vertices[i];if(vertex.incidentHalfEdges.length<2){for(let j=0;j<vertex.incidentHalfEdges.length;j++){const edge=vertex.incidentHalfEdges[j].edge;this.removeEdge(edge);this.replaceEdgeInLoops(edge,[]);edge.dispose()}this.vertices.splice(i,1);vertex.dispose();needsLoop=true;break}}}assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.startVertex)));assert&&assert(_.every(this.edges,edge=>_.includes(this.vertices,edge.endVertex)))}orderVertexEdges(){for(let i=0;i<this.vertices.length;i++){this.vertices[i].sortEdges()}}extractFaces(){const halfEdges=[];for(let i=0;i<this.edges.length;i++){halfEdges.push(this.edges[i].forwardHalf);halfEdges.push(this.edges[i].reversedHalf)}while(halfEdges.length){const boundaryHalfEdges=[];let halfEdge=halfEdges[0];const startingHalfEdge=halfEdge;while(halfEdge){arrayRemove(halfEdges,halfEdge);boundaryHalfEdges.push(halfEdge);halfEdge=halfEdge.getNext();if(halfEdge===startingHalfEdge){break}}const boundary=Boundary.pool.create(boundaryHalfEdges);(boundary.signedArea>0?this.innerBoundaries:this.outerBoundaries).push(boundary);this.boundaries.push(boundary)}for(let i=0;i<this.innerBoundaries.length;i++){this.faces.push(Face.pool.create(this.innerBoundaries[i]))}}computeBoundaryTree(){const unboundedHoles=[];const transform=new Transform3(Matrix3.rotation2(1.5729657));for(let i=0;i<this.outerBoundaries.length;i++){const outerBoundary=this.outerBoundaries[i];const ray=outerBoundary.computeExtremeRay(transform);let closestEdge=null;let closestDistance=Number.POSITIVE_INFINITY;let closestWind;for(let j=0;j<this.edges.length;j++){const edge=this.edges[j];const intersections=edge.segment.intersection(ray);for(let k=0;k<intersections.length;k++){const intersection=intersections[k];if(intersection.distance<closestDistance){closestEdge=edge;closestDistance=intersection.distance;closestWind=intersection.wind}}}if(closestEdge===null){unboundedHoles.push(outerBoundary)}else{const reversed=closestWind<0;const closestHalfEdge=reversed?closestEdge.reversedHalf:closestEdge.forwardHalf;const closestBoundary=this.getBoundaryOfHalfEdge(closestHalfEdge);closestBoundary.childBoundaries.push(outerBoundary)}}unboundedHoles.forEach(this.unboundedFace.recursivelyAddHoles.bind(this.unboundedFace));for(let i=0;i<this.faces.length;i++){const face=this.faces[i];if(face.boundary!==null){face.boundary.childBoundaries.forEach(face.recursivelyAddHoles.bind(face))}}}computeWindingMap(){const edges=this.edges.slice();const outsideMap={};for(let i=0;i<this.shapeIds.length;i++){outsideMap[this.shapeIds[i]]=0}this.unboundedFace.windingMap=outsideMap;while(edges.length){for(let j=edges.length-1;j>=0;j--){const edge=edges[j];const forwardHalf=edge.forwardHalf;const reversedHalf=edge.reversedHalf;const forwardFace=forwardHalf.face;const reversedFace=reversedHalf.face;assert&&assert(forwardFace!==reversedFace);const solvedForward=forwardFace.windingMap!==null;const solvedReversed=reversedFace.windingMap!==null;if(solvedForward&&solvedReversed){edges.splice(j,1);if(assert){for(let m=0;m<this.shapeIds.length;m++){const id=this.shapeIds[m];assert(forwardFace.windingMap[id]-reversedFace.windingMap[id]===this.computeDifferential(edge,id))}}}else if(!solvedForward&&!solvedReversed){continue}else{const solvedFace=solvedForward?forwardFace:reversedFace;const unsolvedFace=solvedForward?reversedFace:forwardFace;const windingMap={};for(let k=0;k<this.shapeIds.length;k++){const shapeId=this.shapeIds[k];const differential=this.computeDifferential(edge,shapeId);windingMap[shapeId]=solvedFace.windingMap[shapeId]+differential*(solvedForward?-1:1)}unsolvedFace.windingMap=windingMap}}}}computeDifferential(edge,shapeId){let differential=0;for(let m=0;m<this.loops.length;m++){const loop=this.loops[m];assert&&assert(loop.closed,"This is only defined to work for closed loops");if(loop.shapeId!==shapeId){continue}for(let n=0;n<loop.halfEdges.length;n++){const loopHalfEdge=loop.halfEdges[n];if(loopHalfEdge===edge.forwardHalf){differential++}else if(loopHalfEdge===edge.reversedHalf){differential--}}}return differential}fillAlternatingFaces(){let nullFaceFilledCount=0;for(let i=0;i<this.faces.length;i++){this.faces[i].filled=null;nullFaceFilledCount++}this.unboundedFace.filled=false;nullFaceFilledCount--;while(nullFaceFilledCount){for(let i=0;i<this.edges.length;i++){const edge=this.edges[i];const forwardFace=edge.forwardHalf.face;const reversedFace=edge.reversedHalf.face;const forwardNull=forwardFace.filled===null;const reversedNull=reversedFace.filled===null;if(forwardNull&&!reversedNull){forwardFace.filled=!reversedFace.filled;nullFaceFilledCount--}else if(!forwardNull&&reversedNull){reversedFace.filled=!forwardFace.filled;nullFaceFilledCount--}}}}getBoundaryOfHalfEdge(halfEdge){for(let i=0;i<this.boundaries.length;i++){const boundary=this.boundaries[i];if(boundary.hasHalfEdge(halfEdge)){return boundary}}throw new Error("Could not find boundary")}static isInternal(point,t,segment,distanceThreshold,tThreshold){return t>tThreshold&&t<1-tThreshold&&point.distance(segment.start)>distanceThreshold&&point.distance(segment.end)>distanceThreshold}static BINARY_NONZERO_UNION(windingMap){return windingMap["0"]!==0||windingMap["1"]!==0}static BINARY_NONZERO_INTERSECTION(windingMap){return windingMap["0"]!==0&&windingMap["1"]!==0}static BINARY_NONZERO_DIFFERENCE(windingMap){return windingMap["0"]!==0&&windingMap["1"]===0}static BINARY_NONZERO_XOR(windingMap){return windingMap["0"]!==0!==(windingMap["1"]!==0)}static binaryResult(shapeA,shapeB,windingMapFilter){const graph=new Graph;graph.addShape(0,shapeA);graph.addShape(1,shapeB);graph.computeSimplifiedFaces();graph.computeFaceInclusion(windingMapFilter);const subgraph=graph.createFilledSubGraph();const shape=subgraph.facesToShape();graph.dispose();subgraph.dispose();return shape}static unionNonZero(shapes){const graph=new Graph;for(let i=0;i<shapes.length;i++){graph.addShape(i,shapes[i])}graph.computeSimplifiedFaces();graph.computeFaceInclusion(windingMap=>{for(let j=0;j<shapes.length;j++){if(windingMap[j]!==0){return true}}return false});const subgraph=graph.createFilledSubGraph();const shape=subgraph.facesToShape();graph.dispose();subgraph.dispose();return shape}static intersectionNonZero(shapes){const graph=new Graph;for(let i=0;i<shapes.length;i++){graph.addShape(i,shapes[i])}graph.computeSimplifiedFaces();graph.computeFaceInclusion(windingMap=>{for(let j=0;j<shapes.length;j++){if(windingMap[j]===0){return false}}return true});const subgraph=graph.createFilledSubGraph();const shape=subgraph.facesToShape();graph.dispose();subgraph.dispose();return shape}static xorNonZero(shapes){const graph=new Graph;for(let i=0;i<shapes.length;i++){graph.addShape(i,shapes[i])}graph.computeSimplifiedFaces();graph.computeFaceInclusion(windingMap=>{let included=false;for(let j=0;j<shapes.length;j++){if(windingMap[j]!==0){included=!included}}return included});const subgraph=graph.createFilledSubGraph();const shape=subgraph.facesToShape();graph.dispose();subgraph.dispose();return shape}static simplifyNonZero(shape){const graph=new Graph;graph.addShape(0,shape);graph.computeSimplifiedFaces();graph.computeFaceInclusion(map=>map["0"]!==0);const subgraph=graph.createFilledSubGraph();const resultShape=subgraph.facesToShape();graph.dispose();subgraph.dispose();return resultShape}static clipShape(clipAreaShape,shape,providedOptions){let i;let j;let loop;const SHAPE_ID=0;const CLIP_SHAPE_ID=1;const options=optionize()({includeExterior:false,includeBoundary:true,includeInterior:true},providedOptions);const simplifiedClipAreaShape=Graph.simplifyNonZero(clipAreaShape);const graph=new Graph;graph.addShape(SHAPE_ID,shape,{ensureClosed:false});graph.addShape(CLIP_SHAPE_ID,simplifiedClipAreaShape);graph.eliminateOverlap();graph.eliminateSelfIntersection();graph.eliminateIntersection();graph.collapseVertices();for(i=0;i<graph.loops.length;i++){loop=graph.loops[i];if(loop.shapeId===CLIP_SHAPE_ID){for(j=0;j<loop.halfEdges.length;j++){loop.halfEdges[j].edge.data=true}}}const subpaths=[];for(i=0;i<graph.loops.length;i++){loop=graph.loops[i];if(loop.shapeId===SHAPE_ID){let segments=[];for(j=0;j<loop.halfEdges.length;j++){const halfEdge=loop.halfEdges[j];const included=halfEdge.edge.data?options.includeBoundary:simplifiedClipAreaShape.containsPoint(halfEdge.edge.segment.positionAt(.5))?options.includeInterior:options.includeExterior;if(included){segments.push(halfEdge.getDirectionalSegment())}else if(segments.length){subpaths.push(new Subpath(segments,undefined,loop.closed));segments=[]}}if(segments.length){subpaths.push(new Subpath(segments,undefined,loop.closed))}}}graph.dispose();return new Shape(subpaths)}}kite.register("Graph",Graph);