import kite from"../kite.js";import{clamp}from"../../../dot/js/util/clamp.js";export default class RayIntersection{constructor(distance,point,normal,wind,t){assert&&assert(isFinite(distance)&&distance>=0,"invalid distance");assert&&assert(Math.abs(normal.magnitude-1)<1e-7,"invalid normal");assert&&assert(t>=-1e-10&&t<=1+1e-10,`t out of range: ${t}`);this.point=point;this.normal=normal;this.distance=distance;this.wind=wind;this.t=clamp(t,0,1)}}kite.register("RayIntersection",RayIntersection);