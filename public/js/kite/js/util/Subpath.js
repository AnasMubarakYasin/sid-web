import TinyEmitter from"../../../axon/js/TinyEmitter.js";import Bounds2 from"../../../dot/js/Bounds2.js";import Vector2 from"../../../dot/js/Vector2.js";import Segment,{Line,Arc}from"../segments/Segment.js";import LineStyles from"./LineStyles.js";import kite from"../kite.js";let Subpath=class Subpath{constructor(segments,points,closed){this.segments=[];this.invalidatedEmitter=new TinyEmitter;this._bounds=null;this._strokedSubpaths=null;this._strokedSubpathsComputed=false;this._strokedStyles=null;this._invalidatingPoints=false;this.points=points||(segments&&segments.length?_.map(segments,segment=>segment.start).concat(segments[segments.length-1].end):[]);this.closed=!!closed;this._invalidateListener=this.invalidate.bind(this);if(segments){for(let i=0;i<segments.length;i++){_.each(segments[i].getNondegenerateSegments(),segment=>{this.addSegmentDirectly(segment)})}}}getBounds(){if(this._bounds===null){const bounds=Bounds2.NOTHING.copy();_.each(this.segments,segment=>{bounds.includeBounds(segment.getBounds())});this._bounds=bounds}return this._bounds}get bounds(){return this.getBounds()}getArcLength(distanceEpsilon,curveEpsilon,maxLevels){let length=0;for(let i=0;i<this.segments.length;i++){length+=this.segments[i].getArcLength(distanceEpsilon,curveEpsilon,maxLevels)}return length}copy(){return new Subpath(this.segments.slice(0),this.points.slice(0),this.closed)}invalidatePoints(){this._invalidatingPoints=true;const numSegments=this.segments.length;for(let i=0;i<numSegments;i++){this.segments[i].invalidate()}this._invalidatingPoints=false;this.invalidate()}invalidate(){if(!this._invalidatingPoints){this._bounds=null;this._strokedSubpathsComputed=false;this.invalidatedEmitter.emit()}}addPoint(point){this.points.push(point);return this}addSegmentDirectly(segment){assert&&assert(segment.start.isFinite(),"Segment start is infinite");assert&&assert(segment.end.isFinite(),"Segment end is infinite");assert&&assert(segment.startTangent.isFinite(),"Segment startTangent is infinite");assert&&assert(segment.endTangent.isFinite(),"Segment endTangent is infinite");assert&&assert(segment.bounds.isEmpty()||segment.bounds.isFinite(),"Segment bounds is infinite and non-empty");this.segments.push(segment);segment.invalidationEmitter.addListener(this._invalidateListener);return this}addSegment(segment){const nondegenerateSegments=segment.getNondegenerateSegments();const numNondegenerateSegments=nondegenerateSegments.length;for(let i=0;i<numNondegenerateSegments;i++){this.addSegmentDirectly(segment)}this.invalidate();return this}addClosingSegment(){if(this.hasClosingSegment()){const closingSegment=this.getClosingSegment();this.addSegmentDirectly(closingSegment);this.invalidate();this.addPoint(this.getFirstPoint());this.closed=true}}close(){this.closed=true;this.addClosingSegment()}getLength(){return this.points.length}getFirstPoint(){assert&&assert(this.points.length);return _.first(this.points)}getLastPoint(){assert&&assert(this.points.length);return _.last(this.points)}getFirstSegment(){assert&&assert(this.segments.length);return _.first(this.segments)}getLastSegment(){assert&&assert(this.segments.length);return _.last(this.segments)}getFillSegments(){const segments=this.segments.slice();if(this.hasClosingSegment()){segments.push(this.getClosingSegment())}return segments}isDrawable(){return this.segments.length>0}isClosed(){return this.closed}hasClosingSegment(){return!this.getFirstPoint().equalsEpsilon(this.getLastPoint(),1e-9)}getClosingSegment(){assert&&assert(this.hasClosingSegment(),"Implicit closing segment unnecessary on a fully closed path");return new Line(this.getLastPoint(),this.getFirstPoint())}getClosestPoints(point){return Segment.filterClosestToPointResult(_.flatten(this.segments.map(segment=>segment.getClosestPoints(point))))}writeToContext(context){if(this.isDrawable()){const startPoint=this.getFirstSegment().start;context.moveTo(startPoint.x,startPoint.y);let len=this.segments.length;if(this.closed&&len>=2&&this.segments[len-1]instanceof Line){len--}for(let i=0;i<len;i++){this.segments[i].writeToContext(context)}if(this.closed){context.closePath()}}}toPiecewiseLinear(options){assert&&assert(!options.pointMap,"For use with pointMap, please use nonlinearTransformed");return new Subpath(_.flatten(_.map(this.segments,segment=>segment.toPiecewiseLinearSegments(options))),undefined,this.closed)}transformed(matrix){return new Subpath(_.map(this.segments,segment=>segment.transformed(matrix)),_.map(this.points,point=>matrix.timesVector2(point)),this.closed)}nonlinearTransformed(options){return new Subpath(_.flatten(_.map(this.segments,segment=>{if(options.methodName&&segment[options.methodName]){return segment[options.methodName](options)}else{return segment.toPiecewiseLinearSegments(options)}})),undefined,this.closed)}getBoundsWithTransform(matrix){const bounds=Bounds2.NOTHING.copy();const numSegments=this.segments.length;for(let i=0;i<numSegments;i++){bounds.includeBounds(this.segments[i].getBoundsWithTransform(matrix))}return bounds}offset(distance){if(!this.isDrawable()){return new Subpath([],undefined,this.closed)}if(distance===0){return new Subpath(this.segments.slice(),undefined,this.closed)}let i;const regularSegments=this.segments.slice();const offsets=[];for(i=0;i<regularSegments.length;i++){offsets.push(regularSegments[i].strokeLeft(2*distance))}let segments=[];for(i=0;i<regularSegments.length;i++){if(this.closed||i>0){const previousI=(i>0?i:regularSegments.length)-1;const center=regularSegments[i].start;const fromTangent=regularSegments[previousI].endTangent;const toTangent=regularSegments[i].startTangent;const startAngle=fromTangent.perpendicular.negated().times(distance).angle;const endAngle=toTangent.perpendicular.negated().times(distance).angle;const anticlockwise=fromTangent.perpendicular.dot(toTangent)>0;segments.push(new Arc(center,Math.abs(distance),startAngle,endAngle,anticlockwise))}segments=segments.concat(offsets[i])}return new Subpath(segments,undefined,this.closed)}stroked(lineStyles){if(!this.isDrawable()){return[]}if(lineStyles===undefined){lineStyles=new LineStyles}assert&&assert(!this._strokedSubpathsComputed||this._strokedStyles&&this._strokedSubpaths);if(this._strokedSubpathsComputed&&this._strokedStyles.equals(lineStyles)){return this._strokedSubpaths}const lineWidth=lineStyles.lineWidth;let i;let leftSegments=[];let rightSegments=[];const firstSegment=this.getFirstSegment();const lastSegment=this.getLastSegment();const appendLeftSegments=segments=>{leftSegments=leftSegments.concat(segments)};const appendRightSegments=segments=>{rightSegments=rightSegments.concat(segments)};const alreadyClosed=lastSegment.end.equals(firstSegment.start);const closingSegment=alreadyClosed?null:new Line(this.segments[this.segments.length-1].end,this.segments[0].start);for(i=0;i<this.segments.length;i++){if(i>0){appendLeftSegments(lineStyles.leftJoin(this.segments[i].start,this.segments[i-1].endTangent,this.segments[i].startTangent))}appendLeftSegments(this.segments[i].strokeLeft(lineWidth))}for(i=this.segments.length-1;i>=0;i--){if(i<this.segments.length-1){appendRightSegments(lineStyles.rightJoin(this.segments[i].end,this.segments[i].endTangent,this.segments[i+1].startTangent))}appendRightSegments(this.segments[i].strokeRight(lineWidth))}let subpaths;if(this.closed){if(alreadyClosed){appendLeftSegments(lineStyles.leftJoin(lastSegment.end,lastSegment.endTangent,firstSegment.startTangent));appendRightSegments(lineStyles.rightJoin(lastSegment.end,lastSegment.endTangent,firstSegment.startTangent))}else{appendLeftSegments(lineStyles.leftJoin(closingSegment.start,lastSegment.endTangent,closingSegment.startTangent));appendLeftSegments(closingSegment.strokeLeft(lineWidth));appendLeftSegments(lineStyles.leftJoin(closingSegment.end,closingSegment.endTangent,firstSegment.startTangent));appendRightSegments(lineStyles.rightJoin(closingSegment.end,closingSegment.endTangent,firstSegment.startTangent));appendRightSegments(closingSegment.strokeRight(lineWidth));appendRightSegments(lineStyles.rightJoin(closingSegment.start,lastSegment.endTangent,closingSegment.startTangent))}subpaths=[new Subpath(leftSegments,undefined,true),new Subpath(rightSegments,undefined,true)]}else{subpaths=[new Subpath(leftSegments.concat(lineStyles.cap(lastSegment.end,lastSegment.endTangent)).concat(rightSegments).concat(lineStyles.cap(firstSegment.start,firstSegment.startTangent.negated())),undefined,true)]}this._strokedSubpaths=subpaths;this._strokedSubpathsComputed=true;this._strokedStyles=lineStyles.copy();return subpaths}dashed(lineDash,lineDashOffset,distanceEpsilon,curveEpsilon){const combineSegmentArrays=(left,right)=>{const combined=left[left.length-1].concat(right[0]);const result=left.slice(0,left.length-1).concat([combined]).concat(right.slice(1));assert&&assert(result.length===left.length+right.length-1);return result};const canBeCombined=(leftItem,rightItem)=>{if(!leftItem.hasRightFilled||!rightItem.hasLeftFilled){return false}const leftSegment=_.last(_.last(leftItem.segmentArrays));const rightSegment=rightItem.segmentArrays[0][0];return leftSegment.end.distance(rightSegment.start)<1e-5};const dashItems=[];for(let i=0;i<this.segments.length;i++){const segment=this.segments[i];const dashItem=segment.getDashValues(lineDash,lineDashOffset,distanceEpsilon,curveEpsilon);dashItems.push(dashItem);lineDashOffset+=dashItem.arcLength;const values=[0].concat(dashItem.values).concat([1]);const initiallyInside=dashItem.initiallyInside;dashItem.hasLeftFilled=initiallyInside;dashItem.hasRightFilled=values.length%2===0?initiallyInside:!initiallyInside;dashItem.segmentArrays=[];for(let j=initiallyInside?0:1;j<values.length-1;j+=2){if(values[j]!==values[j+1]){dashItem.segmentArrays.push([segment.slice(values[j],values[j+1])])}}}for(let i=dashItems.length-1;i>=1;i--){const leftItem=dashItems[i-1];const rightItem=dashItems[i];if(canBeCombined(leftItem,rightItem)){dashItems.splice(i-1,2,{segmentArrays:combineSegmentArrays(leftItem.segmentArrays,rightItem.segmentArrays),hasLeftFilled:leftItem.hasLeftFilled,hasRightFilled:rightItem.hasRightFilled})}}if(dashItems.length>1&&canBeCombined(dashItems[dashItems.length-1],dashItems[0])){const leftItem=dashItems.pop();const rightItem=dashItems.shift();dashItems.push({segmentArrays:combineSegmentArrays(leftItem.segmentArrays,rightItem.segmentArrays),hasLeftFilled:leftItem.hasLeftFilled,hasRightFilled:rightItem.hasRightFilled})}if(this.closed&&dashItems.length===1&&dashItems[0].segmentArrays.length===1&&dashItems[0].hasLeftFilled&&dashItems[0].hasRightFilled){return[new Subpath(dashItems[0].segmentArrays[0],undefined,true)]}return _.flatten(dashItems.map(dashItem=>dashItem.segmentArrays)).map(segments=>new Subpath(segments))}static fromBoundary(boundary){const segments=[];for(let i=0;i<boundary.halfEdges.length;i++){segments.push(boundary.halfEdges[i].getDirectionalSegment())}return new Subpath(segments,undefined,true)}static fromLoop(loop){const segments=[];for(let i=0;i<loop.halfEdges.length;i++){segments.push(loop.halfEdges[i].getDirectionalSegment())}return new Subpath(segments,undefined,loop.closed)}serialize(){return{type:"Subpath",segments:this.segments.map(segment=>segment.serialize()),points:this.points.map(point=>({x:point.x,y:point.y})),closed:this.closed}}static deserialize(obj){assert&&assert(obj.type==="Subpath");return new Subpath(obj.segments.map(Segment.deserialize),obj.points.map(pt=>new Vector2(pt.x,pt.y)),obj.closed)}};kite.register("Subpath",Subpath);export default Subpath;