import Pool from"../../../phet-core/js/Pool.js";import kite from"../kite.js";import SegmentIntersection from"../util/SegmentIntersection.js";export default class BoundsIntersection{constructor(a,b,atMin,atMax,btMin,btMax,aMin,aMax,bMin,bMax){this.initialize(a,b,atMin,atMax,btMin,btMax,aMin,aMax,bMin,bMax)}initialize(a,b,atMin,atMax,btMin,btMax,aMin,aMax,bMin,bMax){this.a=a;this.b=b;this.atMin=atMin;this.atMax=atMax;this.btMin=btMin;this.btMax=btMax;this.aMin=aMin;this.aMax=aMax;this.bMin=bMin;this.bMax=bMax;return this}pushSubdivisions(intersections){const thisActive=this;const atMid=(thisActive.atMax+thisActive.atMin)/2;const btMid=(thisActive.btMax+thisActive.btMin)/2;if(atMid===this.atMin||atMid===this.atMax||btMid===this.btMin||btMid===this.btMax){intersections.push(this);return}const aMid=thisActive.a.positionAt(atMid);const bMid=thisActive.b.positionAt(btMid);if(BoundsIntersection.boxIntersects(thisActive.aMin,aMid,thisActive.bMin,bMid)){intersections.push(BoundsIntersection.pool.create(thisActive.a,thisActive.b,thisActive.atMin,atMid,thisActive.btMin,btMid,thisActive.aMin,aMid,thisActive.bMin,bMid))}if(BoundsIntersection.boxIntersects(aMid,thisActive.aMax,thisActive.bMin,bMid)){intersections.push(BoundsIntersection.pool.create(thisActive.a,thisActive.b,atMid,thisActive.atMax,thisActive.btMin,btMid,aMid,thisActive.aMax,thisActive.bMin,bMid))}if(BoundsIntersection.boxIntersects(thisActive.aMin,aMid,bMid,thisActive.bMax)){intersections.push(BoundsIntersection.pool.create(thisActive.a,thisActive.b,thisActive.atMin,atMid,btMid,thisActive.btMax,thisActive.aMin,aMid,bMid,thisActive.bMax))}if(BoundsIntersection.boxIntersects(aMid,thisActive.aMax,bMid,thisActive.bMax)){intersections.push(BoundsIntersection.pool.create(thisActive.a,thisActive.b,atMid,thisActive.atMax,btMid,thisActive.btMax,aMid,thisActive.aMax,bMid,thisActive.bMax))}this.freeToPool()}distance(otherIntersection){const daMin=this.atMin-otherIntersection.atMin;const daMax=this.atMax-otherIntersection.atMax;const dbMin=this.btMin-otherIntersection.btMin;const dbMax=this.btMax-otherIntersection.btMax;return daMin*daMin+daMax*daMax+dbMin*dbMin+dbMax*dbMax}clean(){this.a=null;this.b=null;this.aMin=null;this.aMax=null;this.bMin=null;this.bMax=null}static intersect(a,b){if(!a.bounds.intersectsBounds(b.bounds)){return[]}const intersections=BoundsIntersection.getIntersectionRanges(a,b);const groups=[];for(let i=0;i<intersections.length;i++){const intersection=intersections[i];let wasAdded=false;nextComparison:for(let j=0;j<groups.length;j++){const group=groups[j];for(let k=0;k<group.length;k++){const otherIntersection=group[k];if(intersection.distance(otherIntersection)<1e-13){group.push(intersection);wasAdded=true;break nextComparison}}}if(!wasAdded){groups.push([intersection])}}const results=[];for(let i=0;i<groups.length;i++){const group=groups[i];let aT=0;let bT=0;for(let j=0;j<group.length;j++){aT+=group[j].atMin+group[j].atMax;bT+=group[j].btMin+group[j].btMax}aT/=2*group.length;bT/=2*group.length;const positionA=a.positionAt(aT);const positionB=b.positionAt(bT);assert&&assert(positionA.distance(positionB)<1e-10);results.push(new SegmentIntersection(positionA.average(positionB),aT,bT))}for(let i=0;i<intersections.length;i++){intersections[i].freeToPool()}BoundsIntersection.cleanPool();return results}static getIntersectionRanges(a,b){const aExtrema=a.getInteriorExtremaTs();const bExtrema=b.getInteriorExtremaTs();const aInternals=_.zip([0].concat(aExtrema),aExtrema.concat([1]));const bInternals=_.zip([0].concat(bExtrema),bExtrema.concat([1]));let intersections=[];for(let i=0;i<aInternals.length;i++){for(let j=0;j<bInternals.length;j++){const atMin=aInternals[i][0];const atMax=aInternals[i][1];const btMin=bInternals[j][0];const btMax=bInternals[j][1];const aMin=a.positionAt(atMin);const aMax=a.positionAt(atMax);const bMin=b.positionAt(btMin);const bMax=b.positionAt(btMax);if(BoundsIntersection.boxIntersects(aMin,aMax,bMin,bMax)){intersections.push(BoundsIntersection.pool.create(a,b,atMin,atMax,btMin,btMax,aMin,aMax,bMin,bMax))}}}for(let i=0;i<50;i++){const newIntersections=[];for(let j=intersections.length-1;j>=0;j--){intersections[j].pushSubdivisions(newIntersections)}intersections=newIntersections}return intersections}static boxIntersects(aMin,aMax,bMin,bMax){const minX=Math.max(Math.min(aMin.x,aMax.x),Math.min(bMin.x,bMax.x));const minY=Math.max(Math.min(aMin.y,aMax.y),Math.min(bMin.y,bMax.y));const maxX=Math.min(Math.max(aMin.x,aMax.x),Math.max(bMin.x,bMax.x));const maxY=Math.min(Math.max(aMin.y,aMax.y),Math.max(bMin.y,bMax.y));return maxX-minX>=0&&maxY-minY>=0}static cleanPool(){BoundsIntersection.pool.forEach(intersection=>intersection.clean())}freeToPool(){BoundsIntersection.pool.freeToPool(this)}static{this.pool=new Pool(BoundsIntersection)}}kite.register("BoundsIntersection",BoundsIntersection);