import Vector2 from"../../../dot/js/Vector2.js";import cleanArray from"../../../phet-core/js/cleanArray.js";import Pool from"../../../phet-core/js/Pool.js";import kite from"../kite.js";import{Line}from"../segments/Segment.js";let globalId=0;export default class Vertex{constructor(point){this.id=++globalId;this.point=Vector2.ZERO;this.incidentHalfEdges=[];this.visited=false;this.visitIndex=0;this.lowIndex=0;this.data=null;this.internalData=null;this.initialize(point)}initialize(point){this.point=point;cleanArray(this.incidentHalfEdges);this.visited=false;this.visitIndex=0;this.lowIndex=0;this.data=null;this.internalData={};return this}serialize(){return{type:"Vertex",id:this.id,point:Vector2.Vector2IO.toStateObject(this.point),incidentHalfEdges:this.incidentHalfEdges.map(halfEdge=>halfEdge.id),visited:this.visited,visitIndex:this.visitIndex,lowIndex:this.lowIndex}}dispose(){this.point=Vector2.ZERO;cleanArray(this.incidentHalfEdges);this.freeToPool()}sortEdges(){const vectors=[];for(let i=0;i<this.incidentHalfEdges.length;i++){const halfEdge=this.incidentHalfEdges[i];vectors.push(halfEdge.sortVector.setXY(halfEdge.getEndTangent().angle,halfEdge.getEndCurvature()))}const cutoff=-Math.PI+1e-4;let atCutAngle=false;while(!atCutAngle){atCutAngle=true;for(let i=0;i<vectors.length;i++){if(vectors[i].x<cutoff){atCutAngle=false}}if(!atCutAngle){for(let i=0;i<vectors.length;i++){const vector=vectors[i];vector.x-=1.62594024516;if(vector.x<-Math.PI-1e-4){vector.x+=Math.PI*2}}}}this.incidentHalfEdges.sort(Vertex.edgeComparison)}static edgeComparison(halfEdgeA,halfEdgeB){const angleA=halfEdgeA.sortVector.x;const angleB=halfEdgeB.sortVector.x;if(Math.abs(angleA-angleB)>1e-5||angleA!==angleB&&halfEdgeA.edge.segment instanceof Line&&halfEdgeB.edge.segment instanceof Line){return angleA<angleB?-1:1}else{const curvatureA=halfEdgeA.sortVector.y;const curvatureB=halfEdgeB.sortVector.y;if(Math.abs(curvatureA-curvatureB)>1e-5){return curvatureA<curvatureB?1:-1}else{const t=1-.001;const curvatureAX=halfEdgeA.getDirectionalSegment().subdivided(t)[1].curvatureAt(0);const curvatureBX=halfEdgeB.getDirectionalSegment().subdivided(t)[1].curvatureAt(0);return curvatureAX<curvatureBX?1:-1}}}freeToPool(){Vertex.pool.freeToPool(this)}static{this.pool=new Pool(Vertex,{initialize:Vertex.prototype.initialize})}}kite.register("Vertex",Vertex);