export function appendQueryFragments(baseURL,queryFragments){const nonEmptyFragments=queryFragments.map(fragment=>fragment.trim().replace(/^[?&]+/,"").replace(/[?&]+$/,"")).filter(fragment=>fragment.length>0);if(nonEmptyFragments.length===0){return baseURL}const separator=baseURL.includes("?")?baseURL.endsWith("?")||baseURL.endsWith("&")?"":"&":"?";return`${baseURL}${separator}${nonEmptyFragments.join("&")}`}