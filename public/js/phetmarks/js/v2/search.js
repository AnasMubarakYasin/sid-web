const NO_MATCH=Number.NEGATIVE_INFINITY;export function parseSearch(rawSearch){const repoTokens=[];const modeTokens=[];rawSearch.toLowerCase().split(/\s+/).map(token=>token.trim()).filter(token=>token.length>0).forEach(token=>{if(token.startsWith("-")&&token.length>1){modeTokens.push(token.slice(1))}else{repoTokens.push(token)}});return{repoTokens:repoTokens,modeTokens:modeTokens}}function getRepoWords(repo){return repo.toLowerCase().split(/[-_]+/)}function scoreTokenAgainstRepo(token,repo){const lowerRepo=repo.toLowerCase();const words=getRepoWords(repo);if(lowerRepo===token){return 100}if(words.includes(token)){return 80}if(lowerRepo.includes(token)){return 58-Math.min(20,lowerRepo.indexOf(token))}const startingWord=words.find(word=>word.startsWith(token));if(startingWord){return 64-Math.min(15,startingWord.length-token.length)}let tokenIndex=0;for(let i=0;i<lowerRepo.length&&tokenIndex<token.length;i++){if(lowerRepo[i]===token[tokenIndex]){tokenIndex++}}return tokenIndex===token.length?18:NO_MATCH}function modeMatchesToken(mode,token){const searchableText=[mode.name,mode.text,mode.group,mode.description,...mode.tags||[]].join(" ").toLowerCase();return searchableText.includes(token)}export function modeMatchesSearch(mode,modeTokens){return modeTokens.every(token=>modeMatchesToken(mode,token))}export function selectModeForSearch(modes,modeTokens,savedModeName){const savedMode=savedModeName?modes.find(mode=>mode.name===savedModeName):null;if(savedMode&&modeMatchesSearch(savedMode,modeTokens)){return savedMode}if(modeTokens.length>0){const matchingMode=modes.find(mode=>modeMatchesSearch(mode,modeTokens));if(matchingMode){return matchingMode}}return modes[0]||null}export function rankRepos(catalog,search,pinnedRepos){return catalog.repos.map(repo=>{const modes=catalog.modesByRepo[repo]||[];if(search.modeTokens.length>0&&!modes.some(mode=>modeMatchesSearch(mode,search.modeTokens))){return{repo:repo,score:NO_MATCH}}let score=pinnedRepos.has(repo)?8:0;for(const token of search.repoTokens){const tokenScore=scoreTokenAgainstRepo(token,repo);if(tokenScore===NO_MATCH){return{repo:repo,score:NO_MATCH}}score+=tokenScore}return{repo:repo,score:score}}).filter(rankedRepo=>rankedRepo.score!==NO_MATCH).sort((a,b)=>b.score-a.score||a.repo.localeCompare(b.repo))}export function resolveSearch(catalog,rawSearch,pinnedRepos){const strictSearch=parseSearch(rawSearch);const strictRankedRepos=rankRepos(catalog,strictSearch,pinnedRepos);if(strictRankedRepos.length>0||strictSearch.repoTokens.length===0){return{search:strictSearch,rankedRepos:strictRankedRepos}}const repoTokens=strictSearch.repoTokens;const candidateSearches=[];const addCandidateSearches=(index,candidateRepoTokens,candidateModeTokens)=>{if(index===repoTokens.length){if(candidateModeTokens.length>strictSearch.modeTokens.length){candidateSearches.push({repoTokens:candidateRepoTokens,modeTokens:candidateModeTokens})}return}const token=repoTokens[index];addCandidateSearches(index+1,[...candidateRepoTokens,token],candidateModeTokens);addCandidateSearches(index+1,candidateRepoTokens,[...candidateModeTokens,token])};addCandidateSearches(0,[],[...strictSearch.modeTokens]);candidateSearches.sort((a,b)=>{return b.repoTokens.length-a.repoTokens.length||a.modeTokens.length-b.modeTokens.length});for(const candidateSearch of candidateSearches){const rankedRepos=rankRepos(catalog,candidateSearch,pinnedRepos);if(rankedRepos.length>0){return{search:candidateSearch,rankedRepos:rankedRepos}}}return{search:strictSearch,rankedRepos:[]}}