const DATA_DIRECTORY="../../perennial-alias/data/";async function fetchText(url){const response=await fetch(url,{credentials:"same-origin"});if(!response.ok){throw new Error(`Failed to fetch ${url}: ${response.status} ${response.statusText}`)}return response.text()}async function fetchOptionalText(url){try{return await fetchText(url)}catch(e){console.warn(e);return""}}async function fetchOptionalJSON(url,fallback){try{const response=await fetch(url,{credentials:"same-origin"});if(!response.ok){throw new Error(`Failed to fetch ${url}: ${response.status} ${response.statusText}`)}return response.json()}catch(e){console.warn(e);return fallback}}function splitLines(text){return text.split(/\r?\n/).map(line=>line.trim()).filter(line=>line.length>0).sort()}async function loadPackageJSONs(repos){const entries=await Promise.allSettled(repos.map(async repo=>{const packageJSON=await fetchOptionalJSON(`../../${repo}/package.json`,{});return[repo,packageJSON]}));const packageJSONs={};entries.forEach(result=>{if(result.status==="fulfilled"){packageJSONs[result.value[0]]=result.value[1]}});return packageJSONs}export async function loadDataSources(){const[activeReposText,activeRunnablesText,phetioSimsText,interactiveDescriptionSimsText,unitTestReposText,wrappersText]=await Promise.all([fetchText(`${DATA_DIRECTORY}active-repos`),fetchText(`${DATA_DIRECTORY}active-runnables`),fetchText(`${DATA_DIRECTORY}phet-io`),fetchText(`${DATA_DIRECTORY}interactive-description`),fetchText(`${DATA_DIRECTORY}unit-tests`),fetchOptionalText(`${DATA_DIRECTORY}wrappers`)]);const phetioSims=splitLines(phetioSimsText);return{activeRepos:splitLines(activeReposText),activeRunnables:splitLines(activeRunnablesText),phetioSims:phetioSims,interactiveDescriptionSims:splitLines(interactiveDescriptionSimsText),unitTestRepos:splitLines(unitTestReposText),wrappers:splitLines(wrappersText),packageJSONs:await loadPackageJSONs(phetioSims)}}