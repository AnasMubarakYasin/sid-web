import{appendQueryFragments}from"./url.js";import{modeMatchesSearch,parseSearch,rankRepos,resolveSearch,selectModeForSearch}from"./search.js";const STORAGE_PREFIX="phetmarks-v2";const MAX_RECENT_LAUNCHES=8;const MODE_GROUPS=["Run","Test","PhET-iO","Web"];function storageKey(key){return`${STORAGE_PREFIX}:${key}`}function getElement(id){const element=document.getElementById(id);if(!element){throw new Error(`Missing element: ${id}`)}return element}function loadStringArray(key){try{const rawValue=localStorage.getItem(storageKey(key));return rawValue?JSON.parse(rawValue):[]}catch{return[]}}function saveStringArray(key,value){localStorage.setItem(storageKey(key),JSON.stringify(value))}function loadRecentLaunches(){try{const rawValue=localStorage.getItem(storageKey("recentLaunches"));return rawValue?JSON.parse(rawValue):[]}catch{return[]}}function saveRecentLaunches(recentLaunches){localStorage.setItem(storageKey("recentLaunches"),JSON.stringify(recentLaunches.slice(0,MAX_RECENT_LAUNCHES)))}function clearChildren(element){while(element.firstChild){element.removeChild(element.firstChild)}}function setButtonPressed(button,pressed){button.setAttribute("aria-pressed",`${pressed}`)}function createTextElement(tagName,className,text){const element=document.createElement(tagName);element.className=className;element.textContent=text;return element}export function renderApp(catalog){const searchInput=getElement("search");const targetSelect=getElement("targetSelect");const launchButton=getElement("launchButton");const repoCount=getElement("repoCount");const modeCount=getElement("modeCount");const repoList=getElement("repoList");const modeList=getElement("modeList");const detailPane=getElement("detailPane");const pinButton=getElement("pinButton");const pinnedRepos=new Set(loadStringArray("pinnedRepos"));let recentLaunches=loadRecentLaunches();let currentSearch=parseSearch("");let rankedRepos=rankRepos(catalog,currentSearch,pinnedRepos);let selectedRepo=localStorage.getItem(storageKey("lastRepo"))||rankedRepos[0]?.repo||"";let selectedMode=selectModeForSearch(catalog.modesByRepo[selectedRepo]||[],[],localStorage.getItem(storageKey(`mode:${selectedRepo}`)));let selectedIndex=Math.max(0,rankedRepos.findIndex(rankedRepo=>rankedRepo.repo===selectedRepo));const toggleValues=new Map;const selectValues=new Map;const customQueryInput=document.createElement("input");customQueryInput.className="customQueryInput";customQueryInput.type="text";customQueryInput.spellcheck=false;customQueryInput.placeholder="custom=params&go=here";targetSelect.value=localStorage.getItem(storageKey("target"))||"sameTab";function getCurrentModeControls(){return selectedMode?.queryControls||[]}function resetQueryState(){toggleValues.clear();selectValues.clear();getCurrentModeControls().forEach(control=>{if(control.type==="toggle"){toggleValues.set(control.key,!!control.defaultEnabled)}else{selectValues.set(control.key,control.defaultValue)}});customQueryInput.value=""}function getQueryFragments(){const fragments=[];getCurrentModeControls().forEach(control=>{if(control.type==="toggle"){if(toggleValues.get(control.key)){fragments.push(control.parameter)}}else{const value=selectValues.get(control.key)||control.defaultValue;if(!(control.omitDefault&&value===control.defaultValue)&&value!=="default"){fragments.push(`${control.parameter}=${value}`)}}});fragments.push(customQueryInput.value);return fragments}function getCurrentURL(){if(!selectedMode){return""}return appendQueryFragments(selectedMode.url,getQueryFragments())}function persistSelection(){if(selectedRepo&&selectedMode){localStorage.setItem(storageKey("lastRepo"),selectedRepo);localStorage.setItem(storageKey(`mode:${selectedRepo}`),selectedMode.name)}}function updateSelectionFromSearch(keepRepo){const resolvedSearch=resolveSearch(catalog,searchInput.value,pinnedRepos);currentSearch=resolvedSearch.search;rankedRepos=resolvedSearch.rankedRepos;const repoStillVisible=rankedRepos.some(rankedRepo=>rankedRepo.repo===selectedRepo);if(!keepRepo||!repoStillVisible){selectedIndex=0;selectedRepo=rankedRepos[0]?.repo||""}else{selectedIndex=Math.max(0,rankedRepos.findIndex(rankedRepo=>rankedRepo.repo===selectedRepo))}const modes=catalog.modesByRepo[selectedRepo]||[];selectedMode=selectModeForSearch(modes,currentSearch.modeTokens,localStorage.getItem(storageKey(`mode:${selectedRepo}`)));resetQueryState()}function selectRepo(repo){selectedRepo=repo;selectedIndex=Math.max(0,rankedRepos.findIndex(rankedRepo=>rankedRepo.repo===repo));const modes=catalog.modesByRepo[selectedRepo]||[];selectedMode=selectModeForSearch(modes,currentSearch.modeTokens,localStorage.getItem(storageKey(`mode:${selectedRepo}`)));resetQueryState();persistSelection();render()}function selectMode(mode){selectedMode=mode;resetQueryState();persistSelection();render()}function addRecentLaunch(url){if(!selectedMode){return}const nextRecentLaunch={repo:selectedRepo,modeName:selectedMode.name,modeText:selectedMode.text,url:url,time:Date.now()};recentLaunches=[nextRecentLaunch,...recentLaunches.filter(launchInfo=>launchInfo.repo!==nextRecentLaunch.repo||launchInfo.modeName!==nextRecentLaunch.modeName||launchInfo.url!==nextRecentLaunch.url)].slice(0,MAX_RECENT_LAUNCHES);saveRecentLaunches(recentLaunches)}function openURL(url,forceNewTab){addRecentLaunch(url);openRawURL(url,forceNewTab)}function openRawURL(url,forceNewTab){if(forceNewTab||targetSelect.value==="newTab"){window.open(url,"_blank")}else{window.location.href=url}}function launch(forceNewTab){if(selectedMode){openURL(getCurrentURL(),forceNewTab)}}function launchRecent(recentIndex){const launchInfo=recentLaunches[recentIndex];if(!launchInfo){return}recentLaunches=[launchInfo,...recentLaunches.filter(recentLaunch=>recentLaunch.repo!==launchInfo.repo||recentLaunch.modeName!==launchInfo.modeName||recentLaunch.url!==launchInfo.url)].slice(0,MAX_RECENT_LAUNCHES);saveRecentLaunches(recentLaunches);openRawURL(launchInfo.url,false);render()}function getRecentShortcutIndex(event){if(!event.metaKey&&!event.ctrlKey&&!event.shiftKey&&!event.altKey){return null}const match=event.code.match(/^(Digit|Numpad)([1-8])$/);return match?parseInt(match[2],10)-1:null}function renderRepos(){clearChildren(repoList);repoCount.textContent=`${rankedRepos.length}/${catalog.repos.length}`;if(rankedRepos.length===0){repoList.appendChild(createTextElement("div","emptyText","No matching repositories."));return}rankedRepos.forEach(rankedRepo=>{const repo=rankedRepo.repo;const button=document.createElement("button");button.type="button";button.className=`repoButton${repo===selectedRepo?" selected":""}`;button.addEventListener("click",()=>selectRepo(repo));const row=document.createElement("span");row.className="repoRowText";row.appendChild(createTextElement("span","repoName",repo));if(pinnedRepos.has(repo)){row.appendChild(createTextElement("span","pinMark","Pinned"))}row.appendChild(createTextElement("span","repoMeta",`${(catalog.modesByRepo[repo]||[]).length}`));button.appendChild(row);repoList.appendChild(button);if(repo===selectedRepo){setTimeout(()=>button.scrollIntoView({block:"nearest"}),0)}})}function renderModes(){clearChildren(modeList);const modes=(catalog.modesByRepo[selectedRepo]||[]).filter(mode=>modeMatchesSearch(mode,currentSearch.modeTokens));modeCount.textContent=`${modes.length}/${(catalog.modesByRepo[selectedRepo]||[]).length}`;if(modes.length===0){modeList.appendChild(createTextElement("div","emptyText","No matching modes."));return}MODE_GROUPS.forEach(group=>{const groupModes=modes.filter(mode=>mode.group===group);if(groupModes.length===0){return}modeList.appendChild(createTextElement("div","modeGroup",group));groupModes.forEach(mode=>{const button=document.createElement("button");button.type="button";button.className=`modeButton${mode.name===selectedMode?.name?" selected":""}`;button.addEventListener("click",()=>selectMode(mode));button.appendChild(createTextElement("div","modeName",mode.text));button.appendChild(createTextElement("div","modeDescription",mode.description));modeList.appendChild(button);if(mode.name===selectedMode?.name){setTimeout(()=>button.scrollIntoView({block:"nearest"}),0)}})})}function renderControls(container){const controls=getCurrentModeControls();if(controls.length>0){container.appendChild(createTextElement("div","sectionTitle","Common Params"));const chipGrid=document.createElement("div");chipGrid.className="chipGrid";controls.filter(control=>control.type==="toggle").forEach(control=>{const button=document.createElement("button");button.type="button";button.className="chip";button.textContent=control.label;setButtonPressed(button,!!toggleValues.get(control.key));button.addEventListener("click",()=>{toggleValues.set(control.key,!toggleValues.get(control.key));render()});chipGrid.appendChild(button)});container.appendChild(chipGrid);const selectControls=controls.filter(control=>control.type==="select");if(selectControls.length>0){const fieldGrid=document.createElement("div");fieldGrid.className="fieldGrid";selectControls.forEach(control=>{const label=document.createElement("label");label.className="fieldLabel";label.textContent=control.label;const select=document.createElement("select");select.value=selectValues.get(control.key)||control.defaultValue;control.values.forEach(value=>{const option=document.createElement("option");option.value=value;option.textContent=value;select.appendChild(option)});select.addEventListener("change",()=>{selectValues.set(control.key,select.value);render()});label.appendChild(select);fieldGrid.appendChild(label)});container.appendChild(fieldGrid)}}container.appendChild(createTextElement("div","sectionTitle","Custom Query"));container.appendChild(customQueryInput);const actions=document.createElement("div");actions.className="smallActions";const resetButton=document.createElement("button");resetButton.className="quietButton";resetButton.type="button";resetButton.textContent="Reset Params";resetButton.addEventListener("click",()=>{resetQueryState();render()});actions.appendChild(resetButton);container.appendChild(actions)}function renderRecentAndPinned(container){const pinnedList=[...pinnedRepos].filter(repo=>catalog.modesByRepo[repo]);if(pinnedList.length>0){container.appendChild(createTextElement("div","sectionTitle","Pinned"));pinnedList.forEach(repo=>{const button=document.createElement("button");button.type="button";button.className="pinListButton";button.textContent=repo;button.addEventListener("click",()=>selectRepo(repo));container.appendChild(button)})}if(recentLaunches.length>0){container.appendChild(createTextElement("div","sectionTitle recentSectionTitle","Recent"));recentLaunches.forEach((launchInfo,index)=>{const button=document.createElement("button");button.type="button";button.className="recentButton";button.appendChild(createTextElement("span","recentShortcut",`[${index+1}]`));const content=document.createElement("span");content.className="recentContent";content.appendChild(createTextElement("div","repoName",`${launchInfo.repo} / ${launchInfo.modeText}`));content.appendChild(createTextElement("div","recentMeta",launchInfo.url));button.appendChild(content);button.addEventListener("click",()=>launchRecent(index));container.appendChild(button)})}}function renderDetails(){clearChildren(detailPane);if(!selectedRepo||!selectedMode){detailPane.appendChild(createTextElement("div","emptyText","Select a repository."));return}const preview=document.createElement("code");preview.className="urlPreview";preview.textContent=getCurrentURL();detailPane.appendChild(preview);renderControls(detailPane);renderRecentAndPinned(detailPane);pinButton.textContent=pinnedRepos.has(selectedRepo)?"Unpin":"Pin"}function render(){renderRepos();renderModes();renderDetails()}searchInput.addEventListener("input",()=>{updateSelectionFromSearch(false);persistSelection();render()});customQueryInput.addEventListener("input",render);searchInput.addEventListener("keydown",event=>{const recentShortcutIndex=getRecentShortcutIndex(event);if(recentShortcutIndex!==null){event.preventDefault();event.stopPropagation();launchRecent(recentShortcutIndex)}else if(event.key==="ArrowDown"||event.key==="ArrowUp"){event.preventDefault();if(rankedRepos.length>0){selectedIndex=Math.max(0,Math.min(rankedRepos.length-1,selectedIndex+(event.key==="ArrowDown"?1:-1)));selectRepo(rankedRepos[selectedIndex].repo)}}else if(event.key==="Enter"){event.preventDefault();launch(event.shiftKey)}});launchButton.addEventListener("click",()=>launch(false));targetSelect.addEventListener("change",()=>{const target=targetSelect.value;localStorage.setItem(storageKey("target"),target)});pinButton.addEventListener("click",()=>{if(pinnedRepos.has(selectedRepo)){pinnedRepos.delete(selectedRepo)}else{pinnedRepos.add(selectedRepo)}saveStringArray("pinnedRepos",[...pinnedRepos]);render()});updateSelectionFromSearch(true);resetQueryState();render();searchInput.focus()}