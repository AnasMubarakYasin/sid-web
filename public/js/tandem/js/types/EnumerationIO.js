import IOTypeCache from"../IOTypeCache.js";import tandemNamespace from"../tandemNamespace.js";import IOType from"./IOType.js";import StateSchema from"./StateSchema.js";const cache=new IOTypeCache;const getKeyList=keys=>[...keys].sort();const joinKeys=keys=>getKeyList(keys).join("|");const EnumerationIO=enumerationContainer=>{const enumeration=enumerationContainer.enumeration;if(!cache.has(enumeration)){const additionalDocs=enumeration.phetioDocumentation?` ${enumeration.phetioDocumentation}`:"";const keys=enumeration.keys;const values=enumeration.values;const ioTypeName=`EnumerationIO(${joinKeys(keys)})`;assert&&assert(!Array.from(cache.values()).find(ioType=>ioType.typeName===ioTypeName),"There was already another IOType with the same name: "+ioTypeName);cache.set(enumeration,new IOType(ioTypeName,{validValues:values,documentation:`Possible values: ${getKeyList(keys).join(", ")}.${additionalDocs}`,toStateObject:value=>enumeration.getKey(value),fromStateObject:stateObject=>{assert&&assert(typeof stateObject==="string","unsupported EnumerationIO value type, expected string");assert&&assert(keys.includes(stateObject),`Unrecognized value: ${stateObject}`);return enumeration.getValue(stateObject)},stateSchema:StateSchema.asValue(`${joinKeys(keys)}`,{isValidValue:key=>keys.includes(key)})}))}return cache.get(enumeration)};tandemNamespace.register("EnumerationIO",EnumerationIO);export default EnumerationIO;