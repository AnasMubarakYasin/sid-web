import optionize from"../../phet-core/js/optionize.js";import Tandem from"./Tandem.js";import TandemConstants from"./TandemConstants.js";import tandemNamespace from"./tandemNamespace.js";let DynamicTandem=class DynamicTandem extends Tandem{constructor(parentTandem,name,providedOptions){assert&&assert(parentTandem,"DynamicTandem must have a parentTandem");const options=optionize()({isValidTandemName:name=>Tandem.getRegexFromCharacterClass(TandemConstants.BASE_DYNAMIC_TANDEM_CHARACTER_CLASS).test(name)},providedOptions);super(parentTandem,name,options)}};tandemNamespace.register("DynamicTandem",DynamicTandem);export default DynamicTandem;