import validate from"../../axon/js/validate.js";import Validation from"../../axon/js/Validation.js";import assertMutuallyExclusiveOptions from"../../phet-core/js/assertMutuallyExclusiveOptions.js";import optionize from"../../phet-core/js/optionize.js";import PhetioObject from"./PhetioObject.js";import Tandem from"./Tandem.js";import tandemNamespace from"./tandemNamespace.js";const VALIDATE_OPTIONS_FALSE={validateValidator:false};const EMPTY_ARRAY=[];assert&&Object.freeze(EMPTY_ARRAY);const PARAMETER_KEYS=["name","phetioType","phetioDocumentation","phetioPrivate"].concat(Validation.VALIDATOR_KEYS);const paramToPhetioType=param=>param.phetioType;const paramToName=param=>param.name;let PhetioDataHandler=class PhetioDataHandler extends PhetioObject{constructor(providedOptions){const options=optionize()({parameters:EMPTY_ARRAY,hasListenerOrderDependencies:false,phetioPlayback:PhetioObject.DEFAULT_OPTIONS.phetioPlayback,phetioEventMetadata:PhetioObject.DEFAULT_OPTIONS.phetioEventMetadata,phetioDocumentation:""},providedOptions);assert&&PhetioDataHandler.validateParameters(options.parameters,!!options.tandem?.supplied);assert&&assert(options.phetioType===undefined,"PhetioDataHandler sets its own phetioType. Instead provide parameter phetioTypes through `options.parameters` with a phetioOuterType");const phetioPublicParameters=options.parameters.filter(paramToPhetioType);options.phetioType=options.phetioOuterType(phetioPublicParameters.map(paramToPhetioType));if(options.phetioPlayback){options.phetioEventMetadata=options.phetioEventMetadata||{};assert&&assert(!options.phetioEventMetadata.hasOwnProperty("dataKeys"),"dataKeys should be supplied by PhetioDataHandler, not elsewhere");options.phetioEventMetadata.dataKeys=options.parameters.map(paramToName)}options.phetioDocumentation=PhetioDataHandler.getPhetioDocumentation(options.phetioDocumentation,phetioPublicParameters);super(options);this.parameters=options.parameters}static validateParameters(parameters,tandemSupplied){validate(parameters,{valueType:Array});let reachedPhetioPrivate=false;for(let i=0;i<parameters.length;i++){const parameter=parameters[i];assert&&assert(Object.getPrototypeOf(parameter)===Object.prototype,"Extra prototype on parameter object is a code smell");reachedPhetioPrivate=reachedPhetioPrivate||parameter.phetioPrivate;assert&&reachedPhetioPrivate&&assert(parameter.phetioPrivate,"after first phetioPrivate parameter, all subsequent parameters must be phetioPrivate");assert&&tandemSupplied&&Tandem.VALIDATION&&assert(parameter.phetioType||parameter.phetioPrivate,"instrumented Emitters must include phetioType for each parameter or be marked as `phetioPrivate`.");assert&&parameter.phetioType&&assert(parameter.name,"`name` is a required parameter for phet-io instrumented parameters.");assert&&assertMutuallyExclusiveOptions(parameter,["phetioPrivate"],["name","phetioType","phetioDocumentation"]);assert&&assert(_.intersection(Object.keys(parameter),Validation.VALIDATOR_KEYS).length>0,`validator must be specified for parameter ${i}`);for(const key in parameter){assert&&assert(PARAMETER_KEYS.includes(key),`unrecognized parameter key: ${key}`)}assert&&Object.freeze(parameters[i]);Validation.validateValidator(parameter)}assert&&Object.freeze(parameters)}validateArguments(...args){assert&&assert(args.length===this.parameters.length,`Emitted unexpected number of args. Expected: ${this.parameters.length} and received ${args.length}`);for(let i=0;i<this.parameters.length;i++){const parameter=this.parameters[i];assert&&validate(args[i],parameter,VALIDATE_OPTIONS_FALSE);if(parameter.phetioType&&!parameter.valueType){assert&&validate(args[i],parameter.phetioType.validator,VALIDATE_OPTIONS_FALSE)}}}getValidationErrors(...args){assert&&assert(args.length===this.parameters.length,`Emitted unexpected number of args. Expected: ${this.parameters.length} and received ${args.length}`);return this.parameters.map((parameter,index)=>{return Validation.getValidationError(args[index],parameter,VALIDATE_OPTIONS_FALSE)})}getPhetioData(...args){assert&&assert(Tandem.PHET_IO_ENABLED,"should only get phet-io data in phet-io brand");let data=null;if(this.parameters.length>0){data={};for(let i=0;i<this.parameters.length;i++){const element=this.parameters[i];if(!element.phetioPrivate){assert&&assert(element.name,"name required");data[element.name]=element.phetioType.toStateObject(args[i])}}}return data}static getPhetioDocumentation(currentPhetioDocumentation,parameters){const paramToDocString=param=>{const docText=param.phetioDocumentation?` - ${param.phetioDocumentation}`:"";return`<li>${param.name}: ${_.escape(param.phetioType.typeName)}${docText}</li>`};return currentPhetioDocumentation+(parameters.length===0?"<br>No parameters.":`${"<br>The parameters are:<br/>"+"<ol>"}${parameters.map(paramToDocString).join("<br/>")}</ol>`)}};tandemNamespace.register("PhetioDataHandler",PhetioDataHandler);export default PhetioDataHandler;