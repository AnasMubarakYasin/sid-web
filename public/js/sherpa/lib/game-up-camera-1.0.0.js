const logging=window.phet.chipper.queryParameters.gameUpLogging;const isGameUp=window.phet.chipper.queryParameters.gameUp;const isGameUpTestHarness=window.phet.chipper.queryParameters.gameUpTestHarness;const log=text=>logging&&console.log(text);if(isGameUp){log("Enabled Game Up Camera");const suffix=".brainpop.com";let gameUpCaptureReady=false;let numberOfChecks=0;const checkInitialization=()=>{if(!gameUpCaptureReady&&numberOfChecks<10){parent.postMessage("captureReady","*");numberOfChecks++;log("Posted captureReady, number of checks: "+numberOfChecks);setTimeout(checkInitialization,1e3)}};const receiver=event=>{if(event.origin.indexOf(suffix,event.origin.length-suffix.length)!==-1||isGameUpTestHarness){if(event.data==="captureImage"){const dataURL=window.phet.joist.ScreenshotGenerator.generateScreenshot(window.phet.joist.sim,"image/jpeg");sendImage(dataURL,event.origin,event.source);log("Sent image")}else if(event.data==="GameUpCaptureReady"){log("GameUpCaptureReady");gameUpCaptureReady=true}}};const sendImage=(imageString,origin,source)=>{imageString=imageString.replace("data:image/jpeg;base64,","");source.postMessage(imageString,origin)};if(window.addEventListener){window.addEventListener("message",receiver,false)}else if(window.attachEvent){window.attachEvent("onmessage",receiver)}checkInitialization()}