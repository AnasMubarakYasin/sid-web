import{FluentParser}from"../../browser-and-node/FluentLibrary.js";export function getFluentInternalReferences(fluentFileFTL,key){const parser=new FluentParser;const resourceAst=parser.parse(fluentFileFTL);const entryIndex=new Map;for(const entry of resourceAst.body){if("id"in entry){const id=entry.id.name;entryIndex.set(entry.type==="Term"?`-${id}`:id,entry)}}const rootEntry=entryIndex.get(key);if(!rootEntry){return[]}const collected=new Set;const collect=(entry,seen=new Set)=>{if(seen.has(entry)){return}seen.add(entry);const walkPattern=pattern=>{if(!pattern){return}for(const element of pattern.elements){if(element.type==="Placeable"){visitExpression(element.expression)}}};const visitExpression=expr=>{switch(expr.type){case"MessageReference":case"TermReference":{const refName=expr.id.type==="Identifier"?expr.id.name:String(expr.id.value);const refKey=expr.type==="TermReference"?`-${refName}`:refName;if(refKey!==key){collected.add(refKey)}const refEntry=entryIndex.get(refKey);if(refEntry){collect(refEntry,seen)}break}case"SelectExpression":{visitExpression(expr.selector);for(const variant of expr.variants){walkPattern(variant.value)}break}case"CallExpression":{for(const positional of expr.positional){visitExpression(positional)}for(const named of expr.named){visitExpression(named.value)}break}case"AttributeExpression":case"VariantExpression":visitExpression(expr.id);break;default:break}};if(entry.value){walkPattern(entry.value)}if(entry.attributes?.length){for(const attr of entry.attributes){walkPattern(attr.value)}}};collect(rootEntry);return Array.from(collected).sort()}