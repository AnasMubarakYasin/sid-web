import _ from"lodash";import grunt from"../../../perennial-alias/js/npm-dependencies/grunt.js";const MAX_SUPPORTED_LENGTH=29;export default function gruntTimingLog(message,time,bytes){const tabsNeeded=Math.ceil(Math.max(MAX_SUPPORTED_LENGTH-message.length,0)/8)+1;const tabs=_.times(tabsNeeded).map(()=>"	").join("");const bytesString=bytes?` (${bytes} bytes)`:"";grunt.log.ok(`${message}:${tabs}${time}ms${bytesString}`)}