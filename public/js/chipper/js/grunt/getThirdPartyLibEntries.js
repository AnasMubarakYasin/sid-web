import assert from"assert";import{readFileSync}from"fs";import _ from"lodash";import grunt from"../../../perennial-alias/js/npm-dependencies/grunt.js";import getLicenseKeys from"./getLicenseKeys.js";const THIRD_PARTY_LICENSES_FILENAME="../sherpa/lib/license.json";const LICENSES_DIRECTORY="../sherpa/licenses/";export default function getThirdPartyLibEntries(repo,brand){const licenseInfo=JSON.parse(readFileSync(THIRD_PARTY_LICENSES_FILENAME,"utf8"));let licenseKeys=getLicenseKeys(repo,brand);for(let i=0;i<licenseKeys.length;i++){const license=licenseInfo[licenseKeys[i]];assert(license,`${THIRD_PARTY_LICENSES_FILENAME}: no entry for key = ${licenseKeys[i]}`);const dependencies=license.dependencies;if(typeof dependencies==="object"){licenseKeys=licenseKeys.concat(dependencies)}}licenseKeys=_.uniq(_.sortBy(licenseKeys,key=>key.toUpperCase()));grunt.log.verbose.writeln(`licenseKeys = ${licenseKeys.toString()}`);const libEntries={};licenseKeys.forEach(key=>{const license=licenseInfo[key];assert(license,`${THIRD_PARTY_LICENSES_FILENAME}: no entry for key = ${key}`);assert(license.text,`${THIRD_PARTY_LICENSES_FILENAME}: no text field for key = ${key}`);assert(license.license,`${THIRD_PARTY_LICENSES_FILENAME}: no license field for key = ${key}`);assert(license.projectURL,`${THIRD_PARTY_LICENSES_FILENAME}: no projectURL field for key = ${key}`);assert(license.notes,`${THIRD_PARTY_LICENSES_FILENAME}: no notes field for key = ${key}`);const licenseText=grunt.file.read(`${LICENSES_DIRECTORY+key}.txt`);license.licenseText=licenseText.split(/\r?\n/);libEntries[key]=license});return libEntries}