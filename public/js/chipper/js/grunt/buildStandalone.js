import assert from"assert";import fs,{readFileSync}from"fs";import _ from"lodash";import optionize from"../../../phet-core/js/optionize.js";import ChipperConstants from"../common/ChipperConstants.js";import getLocalesFromRepository from"./getLocalesFromRepository.js";import getPhetLibs from"./getPhetLibs.js";import getStringMap from"./getStringMap.js";import minify from"./minify.js";import webpackBuild from"./webpackBuild.js";export default async function buildStandalone(repo,providedOptions){const options=optionize()({isDebug:false,omitPreloads:null,tempOutputDir:repo,brand:"phet",profileFileSize:false},providedOptions);const packageObject=JSON.parse(readFileSync(`../${repo}/package.json`,"utf8"));assert(packageObject.phet,"`phet` object expected in package.json");const webpackResult=await webpackBuild(repo,options.brand,{outputDir:options.tempOutputDir,profileFileSize:options.profileFileSize});const webpackJS=webpackResult.js;let includedSources=["../assert/js/assert.js"];if(packageObject.phet.preload){assert(Array.isArray(packageObject.phet.preload),"preload should be an array");includedSources=includedSources.concat(packageObject.phet.preload);includedSources.forEach((source,index)=>{if(source.includes("sherpa/lib/lodash-")){includedSources.splice(index,1);includedSources.unshift(source)}});includedSources.forEach((source,index)=>{if(source.includes("sherpa/lib/jquery-")){includedSources.splice(index,1);includedSources.unshift(source)}})}if(options.omitPreloads){includedSources=includedSources.filter(source=>!options.omitPreloads.includes(source))}let includedJS=includedSources.map(file=>fs.readFileSync(file,"utf8")).join("\n");const testLodash="  if ( !window.hasOwnProperty( '_' ) ) {\n"+"    throw new Error( 'Underscore/Lodash not found: _' );\n"+"  }\n";const debugJS="\nwindow.assertions.enableAssert();\n";if(options.isDebug){includedJS+=debugJS}let fullSource=`${includedJS}
${webpackJS}`;if(packageObject.phet.requiresLodash){fullSource=testLodash+fullSource}let globals="window.phet=window.phet||{}\n;";if(packageObject.name==="phet-lib"){globals+=`phet.chipper=phet.chipper||{};
phet.chipper.packageObject=${JSON.stringify(packageObject)};
`;const subRepos=["scenery","sun","scenery-phet","twixt","mobius"];const phetLibs=_.uniq(_.flatten(subRepos.map(subRepo=>{return getPhetLibs(subRepo)})).sort());const locales=[ChipperConstants.FALLBACK_LOCALE,..._.flatten(subRepos.map(subRepo=>getLocalesFromRepository(subRepo)))];const{stringMap,stringMetadata}=getStringMap(repo,locales,phetLibs,webpackResult.usedModules);const localeData=JSON.parse(fs.readFileSync("../babel/localeData.json","utf8"));globals+="phet.chipper.stringPath = '../';\n";globals+="phet.chipper.locale = 'en';\n";globals+="phet.chipper.loadModules = () => {};\n";globals+=`phet.chipper.strings = ${JSON.stringify(stringMap,null,options.isDebug?2:"")};
`;globals+=`phet.chipper.localeData = ${JSON.stringify(localeData,null,options.isDebug?2:"")};
`;globals+=`phet.chipper.stringMetadata = ${JSON.stringify(stringMetadata,null,options.isDebug?2:"")};
`}fullSource=`
${globals}
${fullSource}`;fullSource=`(function() {
${fullSource}
}());`;fullSource=minify(fullSource,options);return fullSource}