import fs from"fs";import _ from"lodash";import optionize from"../../../phet-core/js/optionize.js";import phetioCompareAPIs from"../browser-and-node/phetioCompareAPIs.js";const jsondiffpatch=require("../../../sherpa/lib/jsondiffpatch-v0.3.11.umd").create({});export default(async(repos,proposedAPIs,providedOptions)=>{const options=optionize()({delta:false,compareBreakingAPIChanges:true},providedOptions);let ok=true;repos.forEach(repo=>{const packageObject=JSON.parse(fs.readFileSync(`../${repo}/package.json`,"utf8"));const phetioSection=packageObject.phet["phet-io"]||{};const referenceAPI=JSON.parse(fs.readFileSync(`../phet-io-sim-specific/repos/${repo}/${repo}-phet-io-api.json`,"utf8"));const proposedAPI=proposedAPIs[repo];if(!proposedAPI){throw new Error(`No proposedAPI for repo: ${repo}`)}const comparisonData=phetioCompareAPIs(referenceAPI,proposedAPI,_,{compareBreakingAPIChanges:options.compareBreakingAPIChanges,compareDesignedAPIChanges:!!phetioSection.compareDesignedAPIChanges});if(comparisonData.breakingProblems.length){ok=false;console.log(`${repo} BREAKING PROBLEMS`);console.log(comparisonData.breakingProblems.join("\n"));console.log("\n")}if(comparisonData.designedProblems.length){ok=false;console.log(`${repo} DESIGN PROBLEMS`);console.log(comparisonData.designedProblems.join("\n"));console.log("\n")}if(options.delta){const delta=jsondiffpatch.diff(referenceAPI,proposedAPI);if(delta){console.log(JSON.stringify(delta,null,2))}}});return ok});