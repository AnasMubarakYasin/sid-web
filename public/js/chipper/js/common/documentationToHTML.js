let typeURLs={};function escapeHTML(str){return str.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#x27;").replace(/\//g,"&#x2F;")}function descriptionHTML(string){let result="";const lines=string.split("\n");let inParagraph=false;function insideParagraph(){if(!inParagraph){result+="<p>\n";inParagraph=true}}function outsideParagraph(){if(inParagraph){result+="</p>\n";inParagraph=false}}for(let i=0;i<lines.length;i++){const line=lines[i];if(line.startsWith("#begin")){const initialLine=lines[i];const runLines=[];const displayLines=[];let isDisplayed=false;for(i++;i<lines.length;i++){if(lines[i].startsWith("#end")){break}else if(lines[i].startsWith("#on")){isDisplayed=true}else if(lines[i].startsWith("#off")){isDisplayed=false}else{runLines.push(lines[i]);if(isDisplayed){displayLines.push(lines[i])}}}const runString=runLines.join("\n");const displayString=displayLines.join("\n");const canvasExampleMatch=initialLine.match(/^#begin canvasExample ([^ ]+) ([^x]+)x([^x]+)$/);if(canvasExampleMatch){outsideParagraph();const name=canvasExampleMatch[1];const width=canvasExampleMatch[2];const height=canvasExampleMatch[3];const exampleName=`example-${name}`;result+=`<canvas id="${exampleName}" class="exampleScene"></canvas>`;result+="<script>(function(){";result+=`var canvas = document.getElementById( "${exampleName}" );`;result+=`canvas.width = ${width};`;result+=`canvas.height = ${height};`;result+='var context = canvas.getContext( "2d" );';result+=runString;result+="})();</"+"script>";result+='<pre class="brush: js">\n';result+=displayString;result+="</pre>"}}else{if(line.length===0){outsideParagraph()}else{insideParagraph();result+=`${line}
`}}}outsideParagraph();return result}function typeString(type){const url=typeURLs[type];if(url){return` <a href="${url}" class="type">${escapeHTML(type)}</a>`}else{return` <span class="type">${escapeHTML(type)}</span>`}}function inlineParameterList(object){let result="";if(object.parameters){result+=`( ${object.parameters.map(parameter=>{let name=parameter.name;if(parameter.optional){name=`<span class="optional">${name}</span>`}return`<span class="args">${typeString(parameter.type)} ${name}</span>`}).join(", ")} )`}else if(object.type==="function"){result+="()"}return result}function parameterDetailsList(object){let result="";const parametersWithDescriptions=object.parameters?object.parameters.filter(parameter=>{return!!parameter.description}):[];if(parametersWithDescriptions.length){result+='<table class="params">\n';parametersWithDescriptions.forEach(parameter=>{let name=parameter.name;const description=parameter.description||"";if(parameter.optional){name=`<span class="optional">${name}</span>`}result+=`<tr class="param"><td>${typeString(parameter.type)}</td><td>${name}</td><td> - </td><td>${description}</td></tr>
`});result+="</table>\n"}return result}function returnOrConstant(object){let result="";if(object.returns||object.constant){const type=(object.returns||object.constant).type;if(object.returns){result+=" :"}result+=`<span class="return">${typeString(type)}</span>`}return result}function nameLookup(array,name){for(let i=0;i<array.length;i++){if(array[i].name===name){return array[i]}}return null}function documentationToHTML(doc,baseName,typeNames,localTypeIds,externalTypeURLs){let indexHTML="";let contentHTML="";typeURLs={};Object.keys(externalTypeURLs).forEach(typeId=>{typeURLs[typeId]=externalTypeURLs[typeId]});Object.keys(localTypeIds).forEach(typeId=>{typeURLs[typeId]=`#${localTypeIds[typeId]}`});const baseURL=typeURLs[baseName];indexHTML+=`<a class="navlink" href="${baseURL}" data-toggle="collapse" data-target="#collapse-${baseName}" onclick="$( '.collapse.in' ).collapse( 'toggle' ); return true;">${baseName}</a><br>
`;indexHTML+=`<div id="collapse-${baseName}" class="collapse">
`;contentHTML+=`<h3 id="${baseURL.slice(1)}" class="section">${baseName}</h3>
`;contentHTML+=descriptionHTML(doc.topLevelComment.description);typeNames.forEach(typeName=>{const baseObject=doc[typeName];const baseURLPrefix=`${localTypeIds[typeName]}-`;if(baseObject.type==="type"){if(typeName!==baseName){contentHTML+=`<div id="${baseURLPrefix.slice(0,baseURLPrefix.length-1)}"></div>`}let constructorLine=typeName+inlineParameterList(baseObject.comment);if(baseObject.supertype){constructorLine+=` <span class="inherit">extends ${typeString(baseObject.supertype)}</span>`}contentHTML+=`<h4 id="${baseURLPrefix}constructor" class="section">${constructorLine}</h4>`;contentHTML+=descriptionHTML(baseObject.comment.description);contentHTML+=parameterDetailsList(baseObject.comment)}const staticProperties=baseObject.staticProperties||baseObject.properties||[];const staticNames=staticProperties.map(prop=>prop.name).sort();staticNames.forEach(name=>{const object=nameLookup(staticProperties,name);indexHTML+=`<a class="sublink" href="#${baseURLPrefix}${object.name}">${object.name}</a><br>`;let typeLine=`<span class="entryName">${typeName}.${object.name}</span>`;typeLine+=inlineParameterList(object);typeLine+=returnOrConstant(object);contentHTML+=`<h5 id="${baseURLPrefix}${object.name}" class="section">${typeLine}</h5>`;if(object.description){contentHTML+=descriptionHTML(object.description)}contentHTML+=parameterDetailsList(object)});if(baseObject.type==="type"){const constructorNames=baseObject.constructorProperties.map(prop=>prop.name).sort();constructorNames.forEach(name=>{const object=nameLookup(baseObject.constructorProperties,name);indexHTML+=`<a class="sublink" href="#${baseURLPrefix}${object.name}">${object.name}</a><br>`;let typeLine=`<span class="entryName">${object.name}</span>`;typeLine+=` <span class="property">${typeString(object.type)}</span>`;contentHTML+=`<h5 id="${baseURLPrefix}${object.name}" class="section">${typeLine}</h5>`;if(object.description){contentHTML+=descriptionHTML(object.description)}})}if(baseObject.type==="type"){const instanceNames=baseObject.instanceProperties.map(prop=>prop.name).sort();instanceNames.forEach(name=>{const object=nameLookup(baseObject.instanceProperties,name);indexHTML+=`<a class="sublink" href="#${baseURLPrefix}${object.name}">${object.name}</a><br>`;let typeLine=`<span class="entryName">${object.name}</span>`;if(object.explicitGetName){typeLine+=` <span class="property">${typeString(object.returns.type)}</span>`;typeLine+=` <span class="entryName explicitSetterGetter">${object.explicitGetName}`}if(object.explicitSetName){typeLine+=` <span class="property">${typeString(object.returns.type)}</span>`;typeLine+=` <span class="entryName explicitSetterGetter">${object.explicitSetName}`}typeLine+=inlineParameterList(object);typeLine+=returnOrConstant(object);if(object.explicitSetName||object.explicitGetName){typeLine+="</span>"}contentHTML+=`<h5 id="${baseURLPrefix}${object.name}" class="section">${typeLine}</h5>`;contentHTML+=descriptionHTML(object.description);contentHTML+=parameterDetailsList(object)})}});indexHTML+="</div>";return{indexHTML:indexHTML,contentHTML:contentHTML}}export default documentationToHTML;