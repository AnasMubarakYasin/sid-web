import fs from"fs";export default function getSimList(){const args=process.argv.slice(2);const processKey=(key,callback)=>{const prefix=`--${key}`;const values=args.filter(arg=>arg.startsWith(prefix));if(values.length===1){if(values[0].startsWith(`${prefix}=`)){callback(values[0].substring(prefix.length+1))}else{callback(null)}}else if(values.length>1){console.log(`Too many --${prefix}... specified`);process.exit(1)}};let repos=[];processKey("simList",value=>{const contents=fs.readFileSync(value,"utf8").trim();repos=contents.split("\n").map(sim=>sim.trim())});processKey("stable",()=>{const contents=fs.readFileSync("../perennial-alias/data/phet-io-api-stable","utf8").trim();repos=contents.split("\n").map(sim=>sim.trim())});processKey("sims",value=>{repos=value.split(",")});return repos}