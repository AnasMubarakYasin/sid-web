import optionize from"../../../phet-core/js/optionize.js";export default function showCommandLineProgress(progress,newline,providedOptions){const options=optionize()({progressBarLength:40},providedOptions);const dots=".".repeat(Math.round(progress*options.progressBarLength));const empty=" ".repeat(Math.round((1-progress)*options.progressBarLength));const newlineString=newline?"\n":"";process.stdout.write(`\r[${dots}${empty}] ${(progress*100).toFixed(2)}%${newlineString}`)}