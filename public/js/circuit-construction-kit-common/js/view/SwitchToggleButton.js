import optionize from"../../../phet-core/js/optionize.js";import Circle from"../../../scenery/js/nodes/Circle.js";import Line from"../../../scenery/js/nodes/Line.js";import Node from"../../../scenery/js/nodes/Node.js";import CircuitConstructionKitCommonFluent from"../CircuitConstructionKitCommonFluent.js";import Switch from"../model/Switch.js";import CCKCRoundPushButton from"./CCKCRoundPushButton.js";const ICON_SCALE=.4;const SWITCH_LENGTH=112*ICON_SCALE;const SWITCH_START=1/3;const SWITCH_END=2/3;const LINE_WIDTH=3;const CIRCLE_RADIUS=3;const OPEN_ANGLE=-Math.PI/4;const STRUT_HALF_HEIGHT=12;export default class SwitchToggleButton extends CCKCRoundPushButton{constructor(circuit,providedOptions){const pivotX=SWITCH_LENGTH*SWITCH_START;const contactX=SWITCH_LENGTH*SWITCH_END;const leftSegment=new Line(0,0,pivotX,0,{stroke:"black",lineWidth:LINE_WIDTH,lineCap:"round"});const rightSegment=new Line(contactX,0,SWITCH_LENGTH,0,{stroke:"black",lineWidth:LINE_WIDTH,lineCap:"round"});const pivotCircle=new Circle(CIRCLE_RADIUS,{fill:"black",centerX:pivotX,centerY:0});const contactCircle=new Circle(CIRCLE_RADIUS,{fill:"black",centerX:contactX,centerY:0});const rotatingSegment=new Line(pivotX,0,contactX,0,{stroke:"black",lineWidth:LINE_WIDTH,lineCap:"round"});const centerX=SWITCH_LENGTH/2;const leftStrut=new Line(centerX,-STRUT_HALF_HEIGHT,centerX,STRUT_HALF_HEIGHT,{stroke:"transparent"});const icon=new Node({children:[leftStrut,leftSegment,rightSegment,rotatingSegment,pivotCircle,contactCircle]});const options=optionize()({touchAreaDilation:5,content:icon,listener:()=>{const switchElement=circuit.selectionProperty.value;if(switchElement instanceof Switch){switchElement.isClosedProperty.value=!switchElement.isClosedProperty.value;circuit.componentEditedEmitter.emit()}},isDisposable:false},providedOptions);super(options);const updateIcon=isClosed=>{if(isClosed){const segmentLength=contactX-pivotX;const endX=pivotX+segmentLength*Math.cos(OPEN_ANGLE);const endY=segmentLength*Math.sin(OPEN_ANGLE);rotatingSegment.setLine(pivotX,0,endX,endY)}else{rotatingSegment.setLine(pivotX,0,contactX,0)}};const isClosedListener=isClosed=>{updateIcon(isClosed);this.accessibleName=isClosed?CircuitConstructionKitCommonFluent.a11y.switchToggleButton.openSwitchStringProperty:CircuitConstructionKitCommonFluent.a11y.switchToggleButton.closeSwitchStringProperty};circuit.selectionProperty.link((newCircuitElement,oldCircuitElement)=>{if(oldCircuitElement instanceof Switch){oldCircuitElement.isClosedProperty.unlink(isClosedListener)}if(newCircuitElement instanceof Switch){newCircuitElement.isClosedProperty.link(isClosedListener)}})}}