import affirm from"../../../../../perennial-alias/js/browser-and-node/affirm.js";export default class MNASolution{constructor(nodeVoltages,elements){this.nodeVoltages=nodeVoltages;this.elements=elements}getSolvedCurrent(circuitElement){affirm(this.elements.has(circuitElement));return this.elements.get(circuitElement)}approxEquals(modifiedNodalAnalysisSolution,qassert){const keys=Array.from(this.nodeVoltages.keys());const otherKeys=Array.from(modifiedNodalAnalysisSolution.nodeVoltages.keys());const keyDifference=_.difference(keys,otherKeys);affirm(keyDifference.length===0,"wrong keys in compared solution");for(let i=0;i<keys.length;i++){const key=keys[i];const closeEnough=approximatelyEquals(this.getNodeVoltage(key),modifiedNodalAnalysisSolution.getNodeVoltage(key));qassert&&qassert.equal(closeEnough,true,`node voltages[${i}] should match. ${this.getNodeVoltage(key)}!==${modifiedNodalAnalysisSolution.getNodeVoltage(key)}`);if(!closeEnough){return false}}if(!this.hasAllCurrents(modifiedNodalAnalysisSolution)){return false}if(!modifiedNodalAnalysisSolution.hasAllCurrents(this)){return false}return true}hasAllCurrents(modifiedNodalAnalysisSolution){const keys=Array.from(modifiedNodalAnalysisSolution.elements.keys());for(let i=0;i<keys.length;i++){const element=keys[i];if(!this.hasMatchingElement(element)){return false}}return true}hasMatchingElement(element){const elements=Array.from(this.elements.keys());for(let i=0;i<elements.length;i++){const proposedElement=elements[i];if(proposedElement.nodeId0===element.nodeId0&&proposedElement.nodeId1===element.nodeId1&&approximatelyEquals(this.elements.get(proposedElement),this.elements.get(element))){return true}}return false}getCurrentForResistor(resistor){affirm(resistor.resistance>0,"resistor must have resistance to use Ohms Law");return-this.getVoltage(resistor)/resistor.resistance}getNodeVoltage(nodeIndex){return this.nodeVoltages.get(nodeIndex)}getVoltage(element){const voltage=this.nodeVoltages.get(element.nodeId1)-this.nodeVoltages.get(element.nodeId0);affirm(!isNaN(voltage));return voltage}}const approximatelyEquals=(a,b)=>Math.abs(a-b)<1e-4;