import affirm from"../../../../perennial-alias/js/browser-and-node/affirm.js";import CCKCConstants from"../../CCKCConstants.js";import CCKCQueryParameters from"../../CCKCQueryParameters.js";const MIN_DT=CCKCQueryParameters.minDT;const ERROR_THRESHOLD=1e-5;const searchTimeStep=CCKCQueryParameters.searchTimeStep;export default class TimestepSubdivisions{stepInTimeWithHistory(originalState,steppable,totalTime){let state=originalState;let elapsedTime=0;const states=[];let attemptedDT=totalTime;while(elapsedTime<totalTime){const result=this.search(state,steppable,attemptedDT,null);state=result.state;states.push(result);elapsedTime=elapsedTime+result.dt;if(searchTimeStep){attemptedDT=result.dt*2;if(attemptedDT>totalTime-elapsedTime){attemptedDT=totalTime-elapsedTime}}}if(phet.log){console.log("states per frame: "+states.length)}return states}search(state,steppable,dt,halfStepState){if(dt===CCKCConstants.PAUSED_DT){return{dt:CCKCConstants.PAUSED_DT,state:steppable.update(state,CCKCConstants.PAUSED_DT)}}else if(dt<=MIN_DT){return{dt:MIN_DT,state:steppable.update(state,MIN_DT)}}else{if(searchTimeStep){const a=steppable.update(state,dt);const b1=halfStepState||steppable.update(state,dt/2);const b2=steppable.update(b1,dt/2);const distance=steppable.distance(a,b2);affirm(!isNaN(distance),"distance should be numeric");const errorAcceptable=distance<ERROR_THRESHOLD;if(errorAcceptable){return{dt:dt,state:b2}}else{return this.search(state,steppable,dt/2,b1)}}else{return{dt:MIN_DT,state:steppable.update(state,MIN_DT)}}}}}