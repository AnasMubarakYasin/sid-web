import affirm from"../../../../perennial-alias/js/browser-and-node/affirm.js";export default class LTAStateSet{constructor(resultSet){this.resultSet=resultSet}getTimeAverageCurrent(element){let weightedSum=0;let totalTime=0;this.resultSet.forEach(resultElement=>{weightedSum+=resultElement.state.ltaSolution.getCurrent(element)*resultElement.dt;totalTime+=resultElement.dt});const number=weightedSum/totalTime;affirm(!isNaN(number));return number}getTimeAverageCurrentForCoreModel(element){let weightedSum=0;let totalTime=0;this.resultSet.forEach(stateObject=>{weightedSum+=stateObject.state.ltaSolution.getCurrentForCompanion(element)*stateObject.dt;totalTime+=stateObject.dt});const number=weightedSum/totalTime;affirm(!isNaN(number));return number}getInstantaneousCurrent(element){return this.getFinalState().ltaSolution.getCurrent(element)}getInstantaneousVoltage(element){return this.getFinalState().ltaSolution.getVoltage(element.nodeId0,element.nodeId1)}getInstantaneousVoltageForCoreModel(coreModel){return this.getFinalState().ltaSolution.getVoltage(coreModel.node0,coreModel.node1)}getInstantaneousCurrentForCoreModel(coreModel){return this.getFinalState().ltaSolution.getCurrentForCompanion(coreModel)}getFinalState(){return _.last(this.resultSet).state}}