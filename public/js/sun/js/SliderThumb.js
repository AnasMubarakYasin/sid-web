import Dimension2 from"../../dot/js/Dimension2.js";import Shape from"../../kite/js/Shape.js";import optionize from"../../phet-core/js/optionize.js";import PressListener from"../../scenery/js/listeners/PressListener.js";import Path from"../../scenery/js/nodes/Path.js";import Rectangle from"../../scenery/js/nodes/Rectangle.js";import Tandem from"../../tandem/js/Tandem.js";export const DEFAULT_FILL="rgb( 50, 145, 184 )";export const DEFAULT_FILL_HIGHLIGHTED="rgb( 71, 207, 255 )";export default class SliderThumb extends Rectangle{constructor(providedOptions){const options=optionize()({size:new Dimension2(22,45),fillHighlighted:DEFAULT_FILL_HIGHLIGHTED,centerLineStroke:"white",fill:DEFAULT_FILL,stroke:"black",lineWidth:1,tandem:Tandem.REQUIRED,tandemNameSuffix:"ThumbNode"},providedOptions);if(options.cornerRadius===undefined){options.cornerRadius=.25*options.size.width}options.cachedPaints=[options.fill,options.fillHighlighted];super(0,0,options.size.width,options.size.height,options);const paintLayer=Rectangle.bounds(this.bounds.dilated(5),{fill:"transparent",localBounds:this.bounds,pickable:false});this.addChild(paintLayer);const centerLineYMargin=3;this.addChild(new Path(Shape.lineSegment(options.size.width/2,centerLineYMargin,options.size.width/2,options.size.height-centerLineYMargin),{stroke:options.centerLineStroke}));const pressListener=new PressListener({attach:false,tandem:Tandem.OPT_OUT});pressListener.isHighlightedProperty.link(isHighlighted=>{this.fill=isHighlighted?options.fillHighlighted:options.fill});this.addInputListener(pressListener)}}