import BooleanProperty from"../../axon/js/BooleanProperty.js";import qunitStart from"../../chipper/js/browser/sim-tests/qunitStart.js";import merge from"../../phet-core/js/merge.js";import Node from"../../scenery/js/nodes/Node.js";import SceneryConstants from"../../scenery/js/SceneryConstants.js";import"./SliderTests.js";QUnit.module("sun");QUnit.test("enabled/disabled appearance",assert=>{let SunComponentNode=class SunComponentNode extends Node{constructor(options){options=merge({disabledOpacity:SceneryConstants.DISABLED_OPACITY},options);super(options)}};let node=new SunComponentNode;const disabledOpacity=.2;node=new SunComponentNode({disabledOpacity:disabledOpacity});assert.ok(node.effectiveOpacity===new SunComponentNode().opacity,"opacity should default to Node default");node.enabled=false;assert.ok(node.effectiveOpacity===disabledOpacity,"test disabled opacity");node.dispose();window.assert&&assert.ok(node.enabledProperty.isDisposed,"should be disposed");const myEnabledProperty=new BooleanProperty(true);const defaultListenerCount=myEnabledProperty.getListenerCount();const node2=new SunComponentNode({enabledProperty:myEnabledProperty});assert.ok(myEnabledProperty.getListenerCount()>defaultListenerCount,"listener count should be more since passing in enabledProperty");node2.dispose();assert.ok(myEnabledProperty.getListenerCount()===defaultListenerCount,"listener count should match original")});qunitStart();