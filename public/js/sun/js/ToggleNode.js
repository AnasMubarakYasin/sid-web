import InstanceRegistry from"../../phet-core/js/documentation/InstanceRegistry.js";import optionize from"../../phet-core/js/optionize.js";import ManualConstraint from"../../scenery/js/layout/constraints/ManualConstraint.js";import Node from"../../scenery/js/nodes/Node.js";import{getGroupItemNodes}from"./GroupItemOptions.js";export default class ToggleNode extends Node{constructor(valueProperty,elements,providedOptions){const options=optionize()({alignChildren:ToggleNode.CENTER,unselectedChildrenSceneGraphStrategy:"included"},providedOptions);const nodes=getGroupItemNodes(elements,options.tandem);options.children=nodes;options.alignChildren(options.children);super(options);const alignmentConstraint=new ManualConstraint(this,options.children,(...x)=>{options.alignChildren(x)});const valueListener=value=>{const matches=[];for(let i=0;i<elements.length;i++){const element=elements[i];const visible=element.value===value;nodes[i].visible=visible;if(visible){matches.push(nodes[i])}}assert&&assert(matches.length===1,`Wrong number of matches: ${matches.length}`);if(options.unselectedChildrenSceneGraphStrategy==="excluded"){this.children=matches}};valueProperty.link(valueListener);this.nodes=nodes;this.disposeToggleNode=function(){valueProperty.unlink(valueListener);alignmentConstraint.dispose();nodes.forEach(node=>node.dispose())};assert&&window.phet?.chipper?.queryParameters?.binder&&InstanceRegistry.registerDataURL("sun","ToggleNode",this)}dispose(){this.disposeToggleNode();super.dispose()}static CENTER(children){for(let i=1;i<children.length;i++){children[i].center=children[0].center}}static CENTER_X(children){for(let i=1;i<children.length;i++){children[i].centerX=children[0].centerX}}static CENTER_Y(children){for(let i=1;i<children.length;i++){children[i].centerY=children[0].centerY}}static LEFT(children){for(let i=1;i<children.length;i++){children[i].left=children[0].left}}static BOTTOM(children){for(let i=1;i<children.length;i++){children[i].bottom=children[0].bottom}}static CENTER_BOTTOM(children){for(let i=1;i<children.length;i++){children[i].centerBottom=children[0].centerBottom}}static RIGHT(children){for(let i=1;i<children.length;i++){children[i].right=children[0].right}}static NONE(children){}}