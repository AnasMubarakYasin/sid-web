import Mouse from"../../scenery/js/input/Mouse.js";export default class ClosestDragForwardingListener{constructor(touchThreshold,mouseThreshold){this.touchThreshold=touchThreshold;this.mouseThreshold=mouseThreshold;this.items=[]}addDraggableItem(item){this.items.push(item)}removeDraggableItem(item){const index=_.indexOf(this.items,item);assert&&assert(index>=0);this.items.splice(index,1)}down(event){if(event.target===event.currentTarget){let threshold=0;if(event.pointer.isTouchLike()){threshold=this.touchThreshold}if(event.pointer instanceof Mouse){threshold=this.mouseThreshold}if(threshold){let currentItem=null;let currentDistance=Number.POSITIVE_INFINITY;const globalPoint=event.pointer.point;const numItems=this.items.length;for(let i=0;i<numItems;i++){const item=this.items[i];const distance=item.computeDistance(globalPoint);if(distance<currentDistance){currentDistance=distance;currentItem=item}}if(currentItem&&currentDistance<threshold){currentItem.startDrag(event)}}}}}