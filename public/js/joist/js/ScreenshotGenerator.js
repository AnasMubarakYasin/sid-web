import Matrix3 from"../../dot/js/Matrix3.js";import CanvasContextWrapper from"../../scenery/js/util/CanvasContextWrapper.js";import Utils from"../../scenery/js/util/Utils.js";import joist from"./joist.js";let ScreenshotGenerator=class ScreenshotGenerator{static generateScreenshotAtIncreasedResolution(sim,scale){const canvas=document.createElement("canvas");const context=canvas.getContext("2d");assert&&assert(context);const backingScale=Utils.backingScale(context)*scale;canvas.width=sim.display.width*backingScale;canvas.height=sim.display.height*backingScale;context.scale(backingScale,backingScale);context.fillStyle=sim.display.domElement.style.backgroundColor;context.fillRect(0,0,canvas.width,canvas.height);const wrapper=new CanvasContextWrapper(canvas,context);sim.rootNode.renderToCanvasSubtree(wrapper,Matrix3.scaling(backingScale));return canvas}static renderAtScale(raster,scale){const canvas=document.createElement("canvas");const context=canvas.getContext("2d");canvas.width=raster.width*scale;canvas.height=raster.height*scale;context.scale(scale,scale);context.drawImage(raster,0,0);return canvas}static generateScreenshot(sim,mimeType="image/png"){const res2x=ScreenshotGenerator.generateScreenshotAtIncreasedResolution(sim,2);const res1x=ScreenshotGenerator.renderAtScale(res2x,1/2);return res1x.toDataURL(mimeType)}};joist.register("ScreenshotGenerator",ScreenshotGenerator);export default ScreenshotGenerator;