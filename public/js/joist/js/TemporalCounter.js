import joist from"./joist.js";let TemporalCounter=class TemporalCounter{constructor(binSize){this.previousTime=null;this.binSize=binSize;this.lastBinIndex=null;this.counts=0}onEvent(time){assert&&this.previousTime&&assert(time>=this.previousTime,"time must increase each event");assert&&assert(Number.isInteger(time),"time must be an integer");const currentBinIndex=Math.floor(time/this.binSize);if(currentBinIndex!==this.lastBinIndex){this.counts++;this.lastBinIndex=currentBinIndex}this.previousTime=time}};joist.register("TemporalCounter",TemporalCounter);export default TemporalCounter;