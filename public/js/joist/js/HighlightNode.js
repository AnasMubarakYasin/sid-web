import optionize from"../../phet-core/js/optionize.js";import HBox from"../../scenery/js/layout/nodes/HBox.js";import Rectangle from"../../scenery/js/nodes/Rectangle.js";import Color from"../../scenery/js/util/Color.js";import LinearGradient from"../../scenery/js/util/LinearGradient.js";import joist from"./joist.js";let HighlightNode=class HighlightNode extends HBox{constructor(width,height,providedOptions){const options=optionize()({fill:"white",highlightWidth:1,pickable:false},providedOptions);assert&&assert(options.spacing===undefined,"HighlightNode sets spacing");options.spacing=width;const innerColor=options.fill;const outerColor=Color.toColor(innerColor).withAlpha(0);const barOptions={fill:new LinearGradient(0,0,0,height).addColorStop(0,outerColor).addColorStop(.5,innerColor).addColorStop(1,outerColor)};const leftBar=new Rectangle(0,0,options.highlightWidth,height,barOptions);const rightBar=new Rectangle(0,0,options.highlightWidth,height,barOptions);assert&&assert(!options.children,"HighlightNode sets children");options.children=[leftBar,rightBar];super(options)}};joist.register("HighlightNode",HighlightNode);export default HighlightNode;