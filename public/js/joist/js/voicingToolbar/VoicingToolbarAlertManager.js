import joist from"../joist.js";let VoicingToolbarAlertManager=class VoicingToolbarAlertManager{constructor(screenProperty){this.screenProperty=screenProperty}createOverviewContent(){const screenView=this.screenProperty.value.view;assert&&assert(screenView,"view needs to be inititalized for VoicingToolbar content");return screenView.getVoicingOverviewContent()}createDetailsContent(){const screenView=this.screenProperty.value.view;assert&&assert(screenView,"view needs to be inititalized for VoicingToolbar content");return screenView.getVoicingDetailsContent()}createHintContent(){const screenView=this.screenProperty.value.view;assert&&assert(screenView,"view needs to be inititalized for VoicingToolbar content");return this.screenProperty.value.view.getVoicingHintContent()}};joist.register("VoicingToolbarAlertManager",VoicingToolbarAlertManager);export default VoicingToolbarAlertManager;